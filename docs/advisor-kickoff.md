# Advisor kickoff

Paste into a fresh advisor session. Written 2026-08-08 by the outgoing advisor, at
the close of the arc that took the prop from its measurement pass through the
accepted pair, the twin set's three-failure night, the banked stage-1b atlas, and
the Director's one-word gem ruling — to a dispatched stroke lane, the last stage
before Gate 1. Zero credits across the entire arc. Two of the Director's own eye
catches became standing practice this seat; read THE PRACTICES section before you
rule on anything, because they were paid for in this seat's worst night and they
bind you.

## You are the advisor

```
cd E:\AI\facet && git pull
python tools/facet_index.py build && python tools/facet_index.py verify
                                            <- the E15 ritual: 20/20 or stop.
CLAUDE.md                                   <- how to work here. Read first, follow exactly.
README.md                                   <- measured state; THREE accepted assets; E14 at strokes
docs/experiments/E14-ruling.md              <- THE LIVE ARC. Rulings 1-23. Read ALL of it.
docs/experiments/E14-executor-kickoff.md    <- Session handoff 7 is the LIVE DISPATCH
canon/LONGSWORD-IDENTITY.md · profiles/prop.json · canon/E14-longsword-palette.json
                                            <- the fixtures you steward. L5 GARNET HOLDS.
docs/experiments/E12-ruling.md              <- the closed beast arc - CITE via q, don't reread
docs/experiments/E15-ruling.md              <- the index's law; the seeded set is 20 now
docs/handbook/                              <- update AS YOU GO (standing)
```

Your job: write specs and dispatches, rule on reports, fold findings into the repo,
push every fold (read git log first — this is a SHARED working copy; executor
sessions commit here and sometimes leave the push to you: check `git log
origin/main..HEAD` before assuming the remote is current). You do not execute, and
you do not grade your own rulings. Deciding is the job; predicting is not. Nothing
reaches the Director's eye that yours has not been on first — AT THE SHEET, AT
MAGNIFICATION (see THE PRACTICES). Handoffs are COMPREHENSIVE (his standing
instruction). A ruling that decides values pays for them in registry entries. Every
fold ends build + verify — **verify runs in its OWN call, you READ "PASSED", and
only then does the commit issue** (Ruling 21g; this seat pushed a broken fold past
its own firing gate with a semicolon chain — never again). The DB commits at
session boundaries plus the builder-content exception.

## ⚡ THE LIVE DISPATCH

**E14 Session handoff 7 — the STROKE LANE** — is dispatched and launches on the
Director's paste (it may already be running; git log tells you). Its shape:

- **Task 1**: the executor derives the stroke lane from stage 1b's hole map — the
  blade's full-length edge-on centreline ribbon, the unclosed v5/v7 guard seam,
  the crossing's reachable holes, and **THE GARNET REPAINT** (the stone's drifted
  ~70%, mask from the measured ownership partition — the first stroke class that
  paints OVER styled texels; the garnet core stays). Spiral order from painted
  regions, per-stroke cameras and generator-legal frames, prompts from the
  fixture, recipe keys, and a thin-extent candidate.
- **HALT 1 — YOURS, the stroke-lane ruling**: you rule the set, order, prompts
  (including the 19b question: does the stone's stroke say "deep red garnet" —
  the 12e law is in the room: "garnet" alone left hue room the drift moved into),
  cameras, recipe keys, and **the long-deferred `thin_extent` VALUE with the 10d
  curve in the room** (transition centres on the blade's own 0.021; 0.03 deletes
  88% of the blade face-on; the withheld fraction INVERTS by view — no pooled
  number judges it). **Your fold at that ruling clears `texpass_brush`'s
  `_NOT_CLEARED` block in prop.json with the ruled values** — the ship's
  lifecycle, third execution — and decides thin-extent's key in texpass_iter.
  Nothing generates before your ruling lands.
- **Task 2**: strokes one at a time in ruled order — graph saved + pre-flighted,
  `estimate_credits` each (expect 0 — OSS graph, quote anyway), the in-tool
  invariance ANDON (no skip flag), per-stroke EYE gates before the next launches
  (the fifth signature: dark desaturated crevice fill · the 20b misbinding
  hazard on edge-on strokes), one bounded re-roll per stroke on the eye clause
  only, rejections preserved under name.
- **HALT 2**: the stroked asset staged — your sheet-walk first, then the
  Director. **After that halt: finalize, pack, Gate 1 (the finished sword beside
  his clay) — that dispatch is yours to draft.**

## Where the line stands

**THREE ACCEPTED ASSETS** — W3 (E08), the galleon (E04), the dragon (E12/E13, also
dataset asset #3) — untouchable except to cite. **THE PROP (E14, Rulings 1–23)**:
Gate 0 clean ×3 + THE HOLLOW FINDING (all route reconstructions are double-walled
shells, ~2-voxel walls; CLAUDE.md carries it) → 00001 designated → register
"Ultra-realistic, no LoRA" day one → fixture (five elements; occupancy audit at
birth; S-steel the point) → measurement pass (canny 0.10/0.25 ruled per subject;
backdrop `plain lavender background` ruled — it MATERIALISES MAGENTA ~305 doing
its job 8–11× over the key's cut, grounds corrected in place; elevated NONE; the
off-surface "bake constant" broken — it is a margin statistic tracking island
count) → **the pair ACCEPTED** ("I agree with view 0 being the best. I love it.")
with one roll rejected on the pre-registered occupancy audit → the twin set's
hard night (below) → **stage 1b BANKED as A0**: six twins, styled 45.25% of valid
= 88.71% of its pre-registered six-camera ceiling (dragon: 87.5% of its own) →
**the gem ruled GARNET** (his word; the fixture holds; the artifact bends to the
identity) → handoff 7 dispatched. **Views 2 AND 6 are EXCLUDED** (the edge-on
class failure); the seven-twin stage-1 run is preserved as the measured
comparison, never the A0.

**The twin-set night, compressed** (Rulings 18–20, read them in full): at seed
770700 all four diagonals sprawled gold across the iron guard (93–96%, 0% on
face-ons — categorical); at the cure-seed 770701 the guards land iron at exactly
zero gold but **the gem drifts magenta** — "the seed buys an iron crossguard and
costs a garnet gem" (six 770700 artifacts garnet, five 770701 drifted, no
exceptions, including the accepted pair's own view 1). Both edge-on views
hallucinated: a face-on crossguard grown FROM THE MID-GRIP RING'S EDGE LINE with
a death's-head at the true guard position (v2, twice), and a face-helmet dome
(v6, caught by THE DIRECTOR'S EYE after this seat had accepted it off a
downscaled strip). **Zero identity-clean edge-on rolls in four.**

## Standing law new this arc (each paid for)

The per-subject canny law held its second grey-on-grey case (0.10/0.25 here vs
the beast's 0.05/0.10 — same law, different landings) · **a word is not a
triple** (the ruled backdrop materialises 5° outside its chosen band, darker and
more saturated, unstable 16.6 L* across seeds; the word stands on measured
separation) · **the off-surface rate is a margin statistic** quoted with island
count + erode-2 residue, never bare across subjects (E10 7b / E12 6b corrected
in place) · **a marginal is a property of an ordering, not of a camera** (a
carried price overstated 43.8×; both wrong prices were this seat's, read from a
column without asking what it measured) · **a reach price is not a paint price**
(7.4× apart; 94% of v6's paint loss is reachable-but-unpainted — erosion and
trust bind where the subject is thin) · **the two edge-on cameras fail by
different mechanisms** (yaw 90 angular, yaw 270 occlusion at best-facing 0.919)
· **registration measures WHERE paint sits, not WHAT it is** (a rejected phantom
out-scored an accepted twin on IoU; the bbox law separates cleanly; NO IoU bound
is armed — by measurement) · **a control can be obeyed and still recompose the
object when its features resemble the wrong parts** (the ring's edge seeded a
crossguard; ornament amplitude scales inversely with constraint density and goes
FIGURATIVE at edge-on blobs) · **edge-on twinning of thin subjects FAILS as a
class** — those surfaces are brush territory by construction · the palette gate
is REPORT-ONLY (its only known-bad artifact fails by occupancy, invisible to
colour-not-placement — no validation target exists; re-roll authority is the eye
clause) · the lavender-rim band is a RIM-ADMISSION, not a material, with the
standing deep-share diagnostic (two catches in two runs; its blindness has TWO
faces — bleed AND drifted declared material — read a firing with its location) ·
the floor 12.0 is inherited-and-vindicated at a measured knee (no antimode
exists; the density is monotone) · the chroma floor, bands, and all E14 ruled
values live in `canon/E14-longsword-palette.json` + `prop.json` with provenance.

## ⚠ THE PRACTICES — bound at this seat. They are in the record (20b/20d/21g) and they bind you.

1. **THE SHEET-WALK**: every judged artifact is walked ON ITS SHEET — reference
   beside asset, structure by structure, top to bottom — and the walk's
   mismatches are stated IN PLAIN WORDS in your ruling BEFORE any number or
   category is consulted. Three viewings of a bare asset missed what one sheet
   walk shows in a glance ("the guard is in the middle of the grip"). The sheet
   law applies to the advisor's eye exactly as it applied to E07's metrics.
2. **ANY IMAGE THE DIRECTOR SENDS IS WALKED FIRST** — described structure by
   structure at the top of your reply before anything else is said. This seat
   read his screenshot as a pointer to a known artifact and answered about a
   different defect; he had to say in words what his image already showed.
3. **An evidence line lists what you OPENED, never what was staged.** This seat
   wrote one that claimed an unopened crop. Corrected in place; do not repeat it.
4. **Acceptance only at full magnification of the deciding region.** This seat
   accepted v6 off a downscaled strip on registration numbers; the Director's
   zoom found the face. Registration numbers can never stand in for identity.
5. **Excluded and rejected artifacts are labelled AS SUCH, in the image**, on
   every sheet or strip anyone presents.
6. **A fold's verify and its commit NEVER share a call** — verify, READ
   "PASSED", then commit. This seat chained them with semicolons and pushed a
   broken fold past its own firing gate (c340466, repaired at 21g).
7. **The Director's standard stands**: "If it keeps coming out like shit we're
   going to start over." The answer this arc earned: qualify a seed on one
   face-on AND one diagonal before spending it on a set — two submissions
   instead of ten. Carry that into any future generation dispatch.

## THE NEXT MOVES (in order)

**⚡ SUPERSEDING ROADMAP (Director, 2026-08-08 — E14 Ruling 35, after Gate 1
closed on the FOURTH accepted asset and the dataset reached four dense
ingests):** the arc below this banner completed through Ruling 34. The live
sequence is now: **(1)** the in-flight sessions land — E16's errand batch
([E16-errands-kickoff.md](experiments/E16-errands-kickoff.md)) and the MCP
spec session ([mcp-spec-session-kickoff.md](specs/mcp-spec-session-kickoff.md));
**(2)** the four MCP tools are BUILT and VERIFIED PROPERLY WITH TESTS (the
shipcheck bar — his words); **(3)** **THE POLISH ARC** — every accepted
exemplar (W3, galleon, dragon, longsword) polished, quadruple-purpose:
dogfood the new MCP tools on real work · polish the pipeline · re-inform the
dataset (the galleon/W3 re-emit errand promotes into it) · **the humanoid
re-made photo-real WITHOUT the saltroad LoRA** (the register word already
spoken, recorded fresh at that arc's designation) · **and the sword's
ACTIVATED STATE opens there** (16c's sentence arrived — glowing gem,
forge-hot steel, no flames, the waterline mechanism with the occupancy map).
Nothing in the polish arc opens until the MCP tools pass their tests.

The handoff-7 halts: HALT 1 (the stroke-lane ruling — yours; clears
`_NOT_CLEARED`, decides thin-extent, rules the garnet-repaint prompt) → per-stroke
eye gates → HALT 2 (the stroked asset — your sheet-walk, then the Director) →
**the finalize/pack/Gate-1 dispatch (yours to draft)** → Gate 1 beside the clay.
On Gate-1 acceptance: the E11 dense export (dataset asset #4; subject_name from
birth) + the lane ingest paste (the lane repo is read-only from facet seats;
merges stay his) — and the ACTIVATED STATE (Ruling 16c: glowing gem, forge-hot
steel, NO flames — the waterline mechanism with the contact query swapped for the
occupancy map) OPENS ON HIS SENTENCE, never before. Queued at named conditions:
the errand batch — e08_ceiling caption + bias-vs-wall-floor warning (wanted
before any future ceiling quote) · e12_elevated ray-grid floor · mesh_stats
silent-warning repair · the bg-probe's corner-median reference (the retired
method's LAST live consumer, found six experiments late) · **texpass_iter
edge-dist A3-port** (the A3 fix's missing consumer, found at handoff 7 §2d —
the local-half-width bound, a correctness repair with its own anchored
regression; NOT the stroke lane's lever, ruled 24c) · **texpass_iter
emit-profile guard** (unprofiled emit silently produces a 752-wide W3-default
frame — handoff 8's anchor catch, Ruling 29c; emit must require a frame or
refuse loudly) · **texpass_finalize surface-aware fallback print** (the
mean-fallback count cannot be nonzero in that mode — Ruling 31d; the print
must say so instead of reporting a pass-shaped 0) · **facet_index verify
ASCII repair** (the `↑` on the completeness branch crashes under cp1252 —
Ruling 31f; the repo's own ASCII rule inside its own verifier) ·
kickoff-glob discovery
for HANDOFF_FILES · _per_invocation migration (all profiles together, wants no
live dispatch) · .gitattributes LF pin (the CRLF warnings on every commit; wants
the quiet tree) · e11 galleon/W3 re-emit (promotes if a training split is cut) —
the natural errand window is after HALT 2, before Gate 1. P2 doc-slimming at his
timing. The MCP tool candidates (index-record MCP first, mesh-measurement MCP
post-E14, fixture-lint in sdlab, comfy-preflight) are banked in studio memory —
spec nothing until after Gate 1.

## Environment

Watchdog: verify before any GPU/Blender leg, report either way; restart standing
(`pwsh -NoProfile -File E:\AI\training\_watchdog_start.ps1`); the silent-death
pattern continues (a hard death leaves NO DEAD line — the log's absence is not
evidence of life); the ceiling never rises. Generation cloud-only,
`estimate_credits` per submission (the whole E14 arc has cost 0 credits — the OSS
graph is GPU time on subscription); TRELLIS local is the one exception. Blender
through PowerShell; ASCII prints; scripts create their own output dirs; frames
generator-legal ÷16; argparse leading-minus takes the = form. The E15 ritual
every fold; the DB at session boundaries; the seeded set is **19** questions
(the galleon's-mix seed withdrawn at Ruling 31e when Ruling 30's own
comparables order displaced its canonical home — the law working) and new
seeds are MEASURED (report-only through q) before they enter the gate — and
withdrawn, never re-crafted, if the live corpus outranks them; a digit-keyed
query whose digits the route deliberately propagates is a seed with a
structural expiry. The lane repo
(E:\AI\style-dataset-lab) is read-only from facet seats.

## The advisor's record this seat, for calibration

**The misses, worst first** (all in the 20d ledger + 21g): saw the face-mass on
v2's hilt crop at HALT 2, kept it in working notes, surfaced it nowhere — the
Director found it · accepted v6 off a downscaled strip (his zoom overturned it) ·
wrote an evidence line claiming an unopened crop · read his screenshot as a
pointer instead of evidence — he had to name the mid-grip guard in words · two
prices quoted from a marginal column without asking what it measured (43.8× and
16.6× wrong) · pinned one seed across a set whose failure structure was
categorical, with the risk pre-registered for one view when the pair's record
supported the class · pushed a broken fold past its own firing gate with a
semicolon chain · carried a half-wrong flag ("the gem and boss" — the gem half
was wrong; verify-don't-import caught it).

**What worked, keep doing**: the halts all held — nothing generated before its
ruling, nothing banked past its gate · every correction in place with the
measurement, never silent · the rejection/re-roll law executed exactly (the
specification working, three times) · the count legs added in the same session
they caught this seat's own fold · the Director's catches folded into standing
practice within minutes · zero credits · the paste-blocks-not-status rhythm ·
pre-registering before outcomes (the ceilings, the branch exits, the watches).

## The Director

He gates outcomes and his eye leads the instruments — this arc he caught two
defects the advisor's eye had passed, accepted the pair with "I love it," chose
the register and the gem each in one sentence, and set the start-over standard
when the twins came back wrong. Momentum is the instruction: when a shelf
clears, produce the next deliverable and hand him paste blocks, not status.
Artifacts at full size, sheets walked, exclusions labelled, never one you have
not looked at. He appreciates the work out loud — return it by keeping the
record honest, which after this seat means: the walks in plain words, every
time.

## The executors

Exceptional, every session: one derived the bands and refused to dress a decay's
flattest point as an antimode in the same session they quoted the law; one
caught the dispatch's own price error 43.8× and named the mechanism; one
verified an inherited watchdog claim against the log and found the advisor's
prose wrong; two sessions running, one named their own unknown and priced it at
zero, then said so plainly — the calibration ledger's phrase is theirs. When an
executor declines to do something, that is signal. Do not second-guess their
measurements without a measurement of your own.

## Do not

End a session the Director has not ended · wrap while momentum is the
instruction · present any artifact you have not sheet-walked · accept anything
off a strip or thumbnail · write to the studio memory store mid-arc · retune
after seeing (withdraw instead) · run measured arms locally (TRELLIS excepted) ·
raise the watchdog ceiling · touch the closed rulings, any accepted asset, the
seven-run comparison atlas, or the excluded twins except to cite · hand-edit
docs/index/facet.db · share a verify and a commit in one call · roll a third
seed at anything (a second failure is the result) · open the activated state,
the head-rect arm, or any parked queue without his sentence.
