# Advisor kickoff

Paste into a fresh advisor session. Written 2026-08-06 by the outgoing advisor, at the
close of the night that flipped the standing family to on-surface, closed the off-surface
mechanism line, registered both dense trees in the dataset lane, designated the dragon AND
its logo, survived one rejected pair, and learned — from the Director directly — that
handoffs must never compress. The executors' judgement has been consistently better than
mine; read their reports rather than re-deriving them. My error forms are at the bottom;
the newest one is the reason this document is long.

## You are the advisor

```
cd E:\AI\facet && git pull
CLAUDE.md                                   <- how to work here. Read first, follow exactly.
README.md                                   <- measured state; TWO accepted assets; E12 in flight
docs/experiments/E12-ruling.md              <- THE LIVE ARC. Rulings 1-11. Read all of it.
docs/experiments/E12-executor-kickoff.md    <- handoffs 1-5; FIVE is the live dispatch, comprehensive
docs/experiments/E10-offsurface-ruling.md   <- Rulings 1-7; the standing-family flip (R7)
docs/experiments/E11-ruling.md              <- rulings + two post-ingest addenda; the dataset lane state
docs/style-registers.md                     <- the Director's style directive, made a plan
canon/DRAGON-IDENTITY.md · profiles/beast.json       <- the subject, as ruled through Ruling 11
canon/LOGO-IDENTITY.md                      <- the logo: 00001 designated, bake contract ready
docs/handbook/                              <- index + profiles + subjects; update AS YOU GO (standing)
```

Your job: write specs and dispatches, rule on reports, fold findings into the repo — and
push every fold (standing, Director 2026-08-06: "push as needed"; read `git log` first in
this shared working copy, merges stay his). You do not execute, and you do not grade your
own rulings. **Deciding is the job; predicting is not.** Nothing reaches the Director's
eye that yours has not been on first. **And handoffs are COMPREHENSIVE — his instruction,
verbatim: "make it comprehensive so that there's no room for misunderstanding."** A fresh
executor session has no shared context; the paste stays short only because the handoff it
points to is complete. Handoff 5 is the template now, not handoff 2.

## ⚡ THE LIVE SESSION — read this before anything else

**A handoff-5 executor session may be RUNNING right now** (the Director pastes it when
ready; it may already be mid-flight in this working copy). Its dispatch
([E12-executor-kickoff.md](experiments/E12-executor-kickoff.md), Session handoff 5,
`fa92d4f`): three tasks — **(1)** the tongue geometry check on `dragon_00003_raw.glb`
(present? reachable? — Gate 0 confirmed a tongue on 00001/00002, NOT on 00003, and 7 of 8
satellite shells are interior geometry), **(2)** the view-5 bounded re-roll (seed the only
delta; pale-tan haunch on D1's surface and ivory membranes on D3's are the spec
violations), **(3)** the head-crop companion at yaw 0 (the RESOLUTION rung of the
allocation ladder — frame from `head_00003.json` padded 1.12, ÷16 both axes, ruled canny,
deletion-construction stem, sidecar: "head-region spec source and definition gate; never a
projection reference"). **It HALTS with everything staged, to YOUR eye first, then his.**
Do not collide with its lane (dragon trees, the handoff-4 output tree, the twin-prompts
file); its committed work is final and travels with your next push.

## Where the line stands

**TWO ACCEPTED ASSETS**, untouchable except to cite: W3 (E08, 35 amendments) and the
galleon (E04, 28 rulings + close). **Their headline family FLIPPED 2026-08-06**
([E10-offsurface-ruling](experiments/E10-offsurface-ruling.md) Ruling 7): the ON-SURFACE
numbers are the standing cross-asset family (galleon 42.25 / 36.68 / 86.8 / 56.44 / 6.89;
W3 74.30 reach / 69.28 styled / 26.43 dilation), as-recorded preserved beside them
everywhere. The rate is a bake constant (2.50 / 2.58 / 2.64 across three subjects); **the
composition inverts between subjects — carry the rate, never the split.** The mechanism
line is CLOSED measured-and-bounded (90.85% of the population is the mask's outermost
texel ring, largest blob 33 texels; no fix; re-open = a consumer reading exact rim
positions).

**THE DATASET LANE IS LIVE**: both dense trees registered in sdlab (`e04_galleon_dense`
28/28, `w3_warrior_dense` 26/26; staged W3 curated `rejected`), lane schema **1.1.0**
authored lane-side (861 tests; the 1.1-not-2.0 call corrected my own "2.x" shorthand),
branch `feat/asset-lane-skeleton` pushed through `9c8bc26` — published, NOT merged.
⚠ **Durability, told to the Director and recorded twice**: the lane holds texture-space
channels as `materialized:false` sha-pointers into
`E:\AI\training\facet_next\E04_stroke\export\turnaround\` and
`E:\AI\training\facet_E08\ARMB\export\turnaround\` — those trees must not move and belong
in any backup covering the dataset.

## THE BEAST ARC — exact position and next moves

Designated **00003** ("3 is the winner"). The arc's spine so far: Gate 0 (9 shells,
membranes = closed slabs that pinch, poses not preserved) → allocation NONE (Ruling 2, on
commissioned head evidence; eye clause later struck at 7× — Ruling 4c) → ceiling banked
**50.46% of 3,240,510 valid** (prep bake EXISTS) → elevated NONE (Ruling 7; re-open at the
stroke derivation, hole map in hand) → thin_extent curve banked, **value deliberately
undecided until the stroke-lane ruling** (no global value separates membranes on a subject
where most detail is thin; registry's 1 UNDECIDED is the expected state; the pair was
explicitly ungated by it) → backdrop `plain lavender-grey` (Ruling 8a; W3's grey scores
UNDER the key's own cut here, bound by the membranes) → per-view stems ruled and
render-verified (9d: mouth family off {3,4,5}, horn family off {3,5}) → **the first pair
REJECTED** ("a dragon should look ultra realistic and scary") → **Ruling 10: the style
register is SUBJECT DATA** — beast runs ultra-realistic, **NO LoRA** (`lora-w 0.0`, no
loader node in the graph, enforced by an inverted pre-flight) → canny derived per-subject
and RULED **0.05/0.10** (control 2.2× richer; on grey-on-grey clay HIGH is the structure
lever at ~10× low's effect) → **the re-pair: REGISTER CONFIRMED ("a lot better"), pair NOT
yet accepted** — Ruling 11's three items are handoff 5 (above).

**After handoff 5 halts, your sequence:** look at everything first · the Director gates
three questions (does view 5 wear its declared materials; does the face define at
resolution; what may the mouth hold — the D9/D11 canon question needs his window with the
geometry answer in hand) · **on acceptance**: handoff-4 Task 3 runs (bands re-derived
against the ACCEPTED pair — the suspended Task-5 bands died with the rejected one,
non-circularity holds — plus D8 closure; Ruling 10g holds it passed-as-mechanism) · then
the route proper: **twins** (eight, per-view stems v3, registration halt suspended at 0.0,
measure-report-rule) · **stage 1** (projection against the banked 50.46% ceiling; the
subject's numbers are born on-surface) · **the sweep gate** (0 UNDECIDED before arms —
thin_extent lands at the **stroke-lane ruling** with the wing boxes and the ship's
artifact-judged criterion both in the room) · strokes → finalize → pack → **Gate 1,
five-column sheet, his zoom.**

**The allocation ladder** (Ruling 11b — his question reopened Ruling 2 by its own
clause): the companion is the resolution rung; if the face defines at bust resolution,
the asset-time answer is the **band machinery** (bake `head-scale`, looser head bands —
the additive arm, yours to arm at texture stage); if it stays soft, **the bust-crop
re-reconstruction replaces the designated mesh — a Ruling 1 re-open, HIS sentence, never
an arm you run.**

## Repo-wide queues (none opens unbidden)

1. **The standing errand batch** — one session, when the working copy is quiet:
   `_per_invocation` migration (sweep recognizer + all three profiles — Ruling 6d) ·
   `e08_ceiling` repairs (three-blocks/two-false-captions + the overlap line that cannot
   be non-zero — Ruling 6e) · **`.gitattributes` LF pin** (no attrs + autocrlf=true means
   every predictions hash reproduces HERE and not in a fresh clone; verify by
   scratch-clone reproduction of `cf16bb55…` — Ruling 7f of the offsurface record) ·
   **`e11_manifest.py` `identity.subject_name`** (`galleon` / `w3`), re-emit both dense
   manifests, lane re-ingest — **PROMOTES TO IMMEDIATE if the Director cuts a training
   split first** (the stem-guess leakage lives in the split; E11 addenda 2).
2. **The logo bake** — 00001 designated and frozen by hash (`4c9685b9e901a69d`); the bake
   contract in [LOGO-IDENTITY.md](../canon/LOGO-IDENTITY.md) is complete (canny-lock,
   per-letter masks, colors from the table). Runs whenever he asks.
3. **The style consolidation pass** ([style-registers.md](style-registers.md)) —
   inventory every LoRA card against the **browser Model Library (ground truth, never the
   API)**, one registry page per card, retire explicitly; the training roadmap is
   earned-on-drift, trained on accepted assets through the E11 lane. Studio-side,
   sdlab-adjacent, his timing.
4. **Parked, untouched**: the boot-top coat (E10 R11), both polish queues, bottom
   strokes, E03 (no live subject — Ruling 2's evidence pattern covers it), the W3/galleon
   files except to cite.

## Standing law new since the last relief (with where each was paid for)

The register is subject data — a style arriving by inheritance is the profile system's
founding accident one layer up (R10, one rejected pair) · **the structural channel at a
style gate is the eye** — every colour instrument scored the rejected pair 10/11 (R10d) ·
a value cited as SPENT inherits its anchor only if every sibling of the anchored run is
pinned to the anchored mode (R4a) · a conclusion carried to a new subject without the
premise it states for itself is the same error (R9d — the ship's one-string rested on "no
view-specific anatomy words") · an anchor is computed with the source's own arithmetic —
the float class is now TWICE-seen (9a; handoff 4's bool→float64) · **a first-hit coverage
quoted without its ray density is not a number** (R7b) · carry the rate, never the split
(offsurface R7) · validated-before-used for any instrument crossing subjects (R6c) · a
union re-issue must not be triggered by a ray-density artifact (R7a) · no third rolls,
ever · **README + handbook update at every fold; translations deferred until treatment**
(his instruction, 2026-08-05) · **handoffs comprehensive, always** (his instruction,
2026-08-06).

## Environment

Watchdog: alive at relief (pid 22324, up since 20:21 on 08-05); its third hard death was
a NEW mechanism (a file lock on its own heartbeat) — verify before any GPU leg and report
either way; restart is standing authorization; **the ceiling is never raised.** TRELLIS
local is the one local-GPU exception; generation is cloud, zero credits across this
entire arc so far, `estimate_credits` before every submission anyway. Blender through
PowerShell; ASCII prints; scripts create their own output dirs; generation frames
generator-legal (÷8 floor, ÷16 preferred — now on BOTH axes when neither is 1024).

## The advisor's record this arc, for calibration

Four errors, each caught by an executor or the Director, each now a named form: the
**SPENT-sibling** anchor break (fit-axis pinned beside a margin labeled SPENT) · the
**eye clause** written at 2× that died at 7× (observations cited as decision legs get
looked at at the zoom where they would fail) · the **imported conclusion without its
premise** (byte-equal stems) · the **compressed handoff** (his correction; this
document's length is the fix). What worked, keep doing: pre-stating both branches of
every ruling so the executor never waits · correcting in place with the overturning
measurement, including in my own rulings · deciding instead of escalating on input-side
calls, with his overrule window always named · the looking rule, without exception ·
owning errors in commit messages · celebrating falsified predictions as the content.

## The Director

He gates outcomes, delegates generously, and rules instantly and well on artifacts at
his zoom — "3 is the winner," the register verdict, and the handoff correction were each
one sentence that redirected real work. **Momentum is the instruction**: when a shelf
clears, produce the next deliverable and hand him paste blocks, not status. Show him
artifacts at full size, never contact sheets, never one you have not looked at. He
appreciates the work out loud — return it by keeping the record honest.

## The executors

Exceptional, every session, and better than the advisor at the advisor's own error
forms: they re-validated instruments before first use, fixed operands instead of
tolerances, self-tested guards against real payloads and found defects in their own
tools, refused to propose bounds while looking at results, asked "shouldn't you be
working in the Beast profile?" when I compared across subjects, and reported their own
statistics' failures with the artifact that proved the eye right. **When one declines to
do something, that is signal.** Do not second-guess their measurements without a
measurement of your own.

## Do not

End a session the Director has not ended · wrap up while momentum is the instruction ·
present any artifact you have not looked at · write to the studio memory store mid-arc ·
retune after seeing (withdraw instead) · compress a handoff · run measured arms locally
(TRELLIS excepted) · raise the watchdog ceiling · touch the closed rulings
(E04/E08/E10/E11's closed parts), either accepted asset, or the designated logo clay
except to cite · open any parked queue unbidden · run the geometry rung of the
allocation ladder without his sentence.
