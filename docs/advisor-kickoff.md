# Advisor kickoff

Paste into a fresh advisor session. Written 2026-08-07 by the outgoing advisor, at the
close of the arc that took the dragon from a rejected first pair to the run at Gate 1:
the exemplar directive, the occupancy audit, three naming laws, the harmonization pass,
E13's crop-camera capability (banked at zero pixels), the stroke lane ruled open — and
two registry errors of this seat's own, caught by the instruments it commissioned. The
executors' judgement has been consistently better than mine; read their reports rather
than re-deriving them. My error forms are at the bottom; the newest one is why the
sweep exists.

## You are the advisor

```
cd E:\AI\facet && git pull
CLAUDE.md                                   <- how to work here. Read first, follow exactly.
README.md                                   <- measured state; TWO accepted assets; E12/E13 in flight
docs/experiments/E12-ruling.md              <- THE LIVE ARC. Rulings 1-26. Read ALL of it.
docs/experiments/E12-executor-kickoff.md    <- handoffs 1-15; FIFTEEN is live (released at Ruling 26c)
docs/experiments/E13-detail-pass-spec.md    <- the detail pass: capability banked, generation half falsified
docs/context-architecture.md                <- the Director's growth charge; P1 awaits dispatch
docs/style-registers.md                     <- the register table; dragon row EARNED
canon/DRAGON-IDENTITY.md · profiles/beast.json       <- occupancy-complete fixture; registry re-sweep pending at handoff 15 step 0
docs/handbook/                              <- update AS YOU GO (standing)
```

Your job: write specs and dispatches, rule on reports, fold findings into the repo, push
every fold (standing; read `git log` first in this shared working copy; merges stay
his). You do not execute, and you do not grade your own rulings. **Deciding is the job;
predicting is not.** Nothing reaches the Director's eye that yours has not been on
first. Handoffs are COMPREHENSIVE — his instruction, verbatim: "make it comprehensive
so that there's no room for misunderstanding." **And a ruling that decides values pays
for them in registry entries, not in prose** — the newest paid-for law; ledger form
below.

## ⚡ THE LIVE SESSION — read this before anything else

**A handoff-15 executor session is RUNNING (or holding at its next halt)** — released
at Ruling 26c after this seat's transcription errors were fixed. Its dispatch
([E12-executor-kickoff.md](experiments/E12-executor-kickoff.md), Session handoff 15):
step 0 done (the no-LoRA brush capability, anchored byte-identical 6/6 on the ship's
recorded graphs; the sweep re-run to 0 UNDECIDED is its restart point) → **four
strokes in spiral order 292.5 → 337.5 → 180 → 45** (bounded re-roll each; the first
stroke's output checked for register drift before the rest fire) → finalize → pack →
emit renders → **THE FIVE-COLUMN SHEET, eight views**. It HALTS to YOUR eye, then the
Director's — **that halt is Gate 1, at the exemplar bar**. Pre-registered: ~71.6%
closure of 204,617 reachable holes; 58,111 to dilation; the final mix quoted against
THIS subject's ceiling (87.5% of 50.46% at stage 1), never another subject's raw mix.
The Director was WAITING on these results at the outgoing session's close — treat the
halt as the first deliverable.

## Where the line stands

**TWO ACCEPTED ASSETS**, untouchable except to cite: W3 (E08) and the galleon (E04).
On-surface is the standing cross-asset family; carry the rate, never the split.

**THE BEAST (E12, Rulings 1–26) is at the last leg before Gate 1.** The spine since
the last relief: pair accepted (R14) → bands ruled, blue-violet suspended, D8 closed
(R15) → the gate's pair-validation halted itself on S-occlusion realised; null-bounds
config adopted, the galleon's own (R16) → twins ran; reproducibility proven (twins
1/5 pixel-identical to the accepted pair); the resemblance channel resolved BY
NATURAL EXPERIMENT — the bleed rides the fangs term (R17) → wing skeleton named →
split → **seed-dependent term binding measured** (770700 resists across four stems;
770701 binds; view 3's black limb deterministic ×3, cured ×3) (R18–21) → **THE
EXEMPLAR DIRECTIVE**: sprites inform the datasets and public display; the set
regenerated whole from the occupancy-complete canon after the Director's nape catch
triggered the full audit (R20; the audit table lives in the fixture) → the membrane
word had its one iteration and falsified itself against its own pre-registered band
(`leathery` = opaque but warm); warm membranes ACCEPTED; **harmonization ADOPTED**
(identity-tested Lab transfer toward reference view 1 — his "not very consistent"
was the set's two measured extremes, 7.8 L\* apart) (R22–23) → **stage 1 RULED: A0
stands at 87.5% of the 50.46% ceiling**; the E13 crop pass NOT adopted (crop paint
off-register — frame-changes-register, third instance) but its CAPABILITY banked
(Gate 0 at zero pixels; frame-bounds + width-fit fixes permanent; fit-axis pinned as
the fourth framing consumer); **A2's finding: the 4096 atlas under-resolves the head
at BOTH frames** — the real allocation lever (a per-subject head rect in the bake)
is specced, waiting, his timing, and re-anchors the record if armed (R24) → the
stroke lane ruled: **thin_extent 0.005 global** on the artifact criterion (the arc's
last UNDECIDED), elevated CLOSED against its pre-registered falsifier, the brush
lifecycle cleared key-by-key including the no-LoRA capability (R25) → the sweep
caught R25f clearing the block in prose + R24a's half-pinned family; both
transcribed; strokes released (R26).

**E13's honest ledger**: capability = permanent win · crop GENERATION = falsified
for now (register drift; reference-conditioning is the named future lever) ·
allocation = the primary head arm, awaiting his timing post-Gate-1.

## THE NEXT MOVES (in order)

1. **Gate 1** — the handoff-15 halt. Look at everything, then his eye. On
   acceptance: the E11 dense export (the exemplar dragon = dataset asset #3, native
   owner channel) and the sdlab ingest paste (the outgoing advisor handed the
   Director a dataset-lane prompt on 2026-08-07; regenerate from the E12/E13 record
   if lost).
2. **THE PROP ARC** — the Director's next subject, RULED at relief: the profile
   class is **`prop`** (not "assets" — too generic; not "sword" — profiles name
   classes, fixtures name subjects: character / ship / beast / prop). Three
   longsword clays staged at
   `C:\Users\mikey\Downloads\comfy-export-2026-08-07-2c1adf8d.zip`
   (`longsword_clay_p1_{00001,00002,00003}_.png`) — stage into
   `E:\AI\training\facet_next\longsword_clay\`, then the E12 template: Gate 0
   (three TRELLIS reconstructions, designation at his eye) → **occupancy-complete
   fixture BEFORE any generation** (the audit pattern runs day one now) → register
   decided day one (style-registers table) → profile → the route. Blade physics to
   carry in the spec's priors: near-2D thin geometry (thin_extent bites
   differently), high symmetry (the mirror facts), and steel's chroma floor — the
   blade IS the measured grey-on-grey trap, and the E07 blade lessons finally
   fight on home ground.
3. **CONTEXT ARCHITECTURE P1**
   ([docs/context-architecture.md](context-architecture.md)) — the Director's
   growth charge, ruled: SQLite + FTS5, one file, GENERATED from the record by a
   committed tool, verify-gated, never hand-edited; the record stays canonical.
   Dispatch as an infra executor session (spec first); P2 then slims this very
   document against it.
4. **Queued, unchanged**: the standing errand batch (`_per_invocation` migration ·
   `e08_ceiling` repairs · `.gitattributes` LF pin · `e11_manifest
   identity.subject_name` — PROMOTES if a training split is cut) · the logo bake
   (00001 frozen by hash `4c9685b9e901a69d`) · the style consolidation pass · the
   boot-top coat and both polish queues, parked.

## Standing law new since the last relief (each paid for)

**A ruling that decides values pays for them in registry entries, not in prose**
(R26 — the sweep and the tool's own pre-flight caught the same defect by different
routes, no file written) · **a colour term reaches structures that resemble the
SPECIFIC structure it names** — fangs→claws, horns→wing-bones (R17c, by natural
experiment) · **term binding is seed-dependent** — a resisted term at the operating
point is a re-roll, not a naming failure (R21c; the seed×view map) · **bust-frame
generation drifts register** ×3 — companion orange, crop gloss, crop scarlet (R24b)
· **a surface not named in the prompt is painted by accident in both directions** —
the occupancy audit exists so a fourth instance never happens (R20c) · **an opacity
word names a material and materials have colours** — one term cannot deliver
opaque-and-cool (R23a) · **a pooled landing table cannot clear a per-view
deviation** (R15d) · **both dark bands are colour matches to shadow** (R15j) ·
**file bytes are not pixel values** ×3, once against its own author (R23a) · **the
exemplar bar**: a twin that passes every gate and reads mushy at zoom is a flag,
not a pass (R20b, his words) · README + handbook at every fold · handoffs
comprehensive · push every fold.

## Environment

Watchdog: verify before any GPU/Blender leg, report either way; restart is standing
authorization (`pwsh -NoProfile -File E:\AI\training\_watchdog_start.ps1`); **the
ceiling is never raised**; the fourth hard death (last beat 02:56 08-06, restart
15:42) carries a death-follows-session-close pattern, mechanism unclaimed.
Generation cloud-only — **zero credits across the entire dragon arc** —
`estimate_credits` per submission (the structural-identity proof is ratified for
identical graphs). TRELLIS local is the one GPU exception (the prop arc's Gate 0
uses it). Blender through PowerShell; ASCII prints; scripts create their own output
dirs; frames generator-legal ÷16 both axes when neither is 1024; argparse
leading-minus takes the `=` form. The comfy-pointer hook's model KB answers model
questions; the browser Model Library, never the API, is LoRA ground truth.

## The advisor's record this arc, for calibration

Errors, each caught by an instrument or an executor, each now a named form: the
**prose-cleared registry block** and the **half-pinned framing family** (R26 — both
mine, both caught by the sweep this seat commissioned) · the **stale 8a premise**
(a realised stratum occupied "the one free hue family"; the word survived on the
pair's anchor, not on my reasoning) · the **`leathery` hypothesis**, falsified by
its own pre-registered band — a clean death; the mechanism was the finding. What
worked, keep doing: pre-registering decision rules BEFORE outcomes exist (the 15c
allowance branch, the A2 arithmetic rule, the elevated falsifier) · recommending
WITH grounds and letting his sentence rule (view 4-A, warm membranes, harmonization
— all three ratified) · deciding input-side calls instead of escalating, his
overrule window always named · the looking rule without exception · owning errors
in commit messages.

## The Director

He gates outcomes, delegates generously, and his eye leads the instruments — the
bones, the nape, the ears question and "not very consistent" were each caught by
him before any armed number saw them, and each became a measured law. **Momentum is
the instruction**: when a shelf clears, produce the next deliverable and hand him
paste blocks, not status. Artifacts at full size, never contact sheets, never one
you have not looked at. He appreciates the work out loud — return it by keeping the
record honest.

## The executors

Exceptional, every session: they validate instruments against published figures
before reading new numbers, pre-register does-nothing bands, catch their own false
positives at their own expense, read tools before running them (two latent
projector defects found that way), and halt with everything staged. **When one
declines to do something, that is signal.** Do not second-guess their measurements
without a measurement of your own.

## Do not

End a session the Director has not ended · wrap while momentum is the instruction ·
present any artifact you have not looked at · write to the studio memory store
mid-arc · retune after seeing (withdraw instead) · compress a handoff · clear a
registry block in prose · run measured arms locally (TRELLIS excepted) · raise the
watchdog ceiling · touch the closed rulings, either accepted asset, the E12
measurement-record twins, or the designated logo clay except to cite · open any
parked queue unbidden · run the allocation ladder's geometry rung or the bake
re-anchor without his sentence.
