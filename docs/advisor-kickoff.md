# Advisor kickoff

Paste into a fresh advisor session. **Written 2026-08-15 by the outgoing advisor** —
the seat that ran E37, the performer remake, from dispatch to its halt at the
Director's word, through twenty-eight rulings. **Read the calibration section before
anything else: the Director assessed this seat as not effective, and the record
supports him.** Three days of sessions produced no accepted asset. What they produced
is measured attribution for every defect class met, instruments and laws paid for at
full price, and a halt whose question — what builds this asset next — is his and
open.

**Rewritten from measurement at the close.** Every number below was re-measured
against the tree at the close; anything a live seat can move says so.

---

> # ⚑ E37 IS HALTED AT THE DIRECTOR'S WORD. READ THIS BEFORE ANYTHING.
>
> **The Director halted E37 at his eye** ([E37-ruling.md](experiments/E37-ruling.md)
> Ruling 28): the repaired set showed face features painted on non-face views and
> the v0 lift's boundary reading as a square on the front view. His assessment,
> which the record adopts: every repair round surfaced a new defect class, and
> **no configuration of this route has yet made the generator paint eight
> consistent, defect-free views of this subject.** That is the repo's open
> problem #1. He has said he may seek a different harness to build this repo.
> **Nothing fires without his word. Do not re-litigate the halt. Do not propose
> a new arm unasked.**
>
> Spend stopped at **58 of 80**. The phase-2 fire pass COMPLETED and is committed
> (`f1c2b50` — six repaints landed, the lift repaired by construction, and the
> local-ColorMatch clause of Ruling 27 FALSIFIED by the executor: its metric read
> 17.61 → 3.50 while washing the face). The repaired set and `performer_v3.glb`
> stand as **rejected candidates with full measurement**. **E34's performer
> remains the delivered asset** — accepted-and-reopened, byte-intact, manifested —
> and armature's S07 re-target relay to it is done and pushed (armature `73ea4d5`).

## You are the advisor

```
cd E:\AI\facet && git pull
E:\AI-Models\trellis2-env\Scripts\python.exe tools/facet_index.py build  --db <scratch>
E:\AI-Models\trellis2-env\Scripts\python.exe tools/facet_index.py verify --db <scratch>
                                     <- the E15 ritual: 19/19 or stop. SCRATCH --db;
                                        the record mount is live on this working copy.
CLAUDE.md                            <- the law book; follow exactly
README.md                            <- the front door; its counts are under a test
docs/experiments/README.md           <- the status table
docs/experiments/E37-ruling.md       <- the freshest law: 28 rulings, the halt, and
                                        every defect's measured attribution
docs/known-defects.md                <- dark-speck · brow-fragmentation ·
                                        resurrected-default entries
```

Two mounted servers: `mcp__facet-record__*` and `mcp__facet-measure__*`.
`record_health` first; `record_build` is build+verify+certificate as ONE act — the
CLI build verb alone splits the pair; never leave it that way. Watchdog at session
start: heartbeat ADVANCING on two reads
(`pwsh -NoProfile -File E:\AI\training\_watchdog_start.ps1` if dead). The VRAM
ceiling is 31,200 and never raised — and this seat once misread "raise the ceiling"
as that ceiling when the Director meant the cloud-job budget: **confirm referents
contrastively.**

Your job: write specs, rule on reports, fold findings, push every fold. Deciding is
the job; predicting is not. **Walk every sheet at full native size at your own seat
before it reaches his eye** — standing law, paid for at Stage D (Ruling 18). **An
instrument that fires on the same object at every stage is a gate wearing a watch's
label — the third firing is a halt** (Ruling 18, paid for in full). A dispatch is
not delivered until its paste block is on the screen. Reports to the Director:
structured and scannable, never walls — his eyes blur sometimes; this is
accessibility, not style, and pastes get a plain label, never "don't read."

## THE LIVE STATE — measured at the close

| | |
|---|---|
| HEAD | the close commit — re-measure at your open; the working copy is SHARED with executor sessions and their commits interleave with yours |
| CI | green at `f1c2b50`'s push lineage — re-verify `gh run list` at your open |
| suite | **THE SUITE: 1068 tests, 1023 hermetic** (45 artifacts) — ⚠ pinned by T34; preserve the sentence shape. Re-count at your open: currently 1068 total / 1023 hermetic |
| highest T | T73 (the phase-2 fire tests). The next arc takes T74+ |
| published | facet-mcp 0.4.0 · @mcptoolshop/facet 0.4.0 · comfy-preflight 1.0.0 — both STUDIO_MEASURED entries implemented; closed both sides |
| accepted assets | five — W3, galleon, dragon, longsword, the E34 performer (`ce793064…`, REOPENED at his zoom; formal disposition still his) |
| protected trees | eight subtrees + facet_E33 (116) + facet_E34 (84) + facet_E35 (335) — via `tools/verify/tree_manifest.py`, T70. **facet_E36/E37 are working trees, NOT protected** |
| spend | E37 halted at **58 of 80**; "credits are not a constraint" was his word for E37 only |
| consults | seven run. Headline calls hold once checked; sub-surface details fall at enumeration (latest: a deprecation flag the calibration fetch caught). Calibrate one checkable claim EVERY round; give results back |

### The record index

Mount SERVING, certificate PASSED, byte-identity determinism.
The record holds 37 experiments. No staleness. **Every other count lives in the
ritual's own output and in `record_health` — read them there, not here.** *(The
experiment count stays because T34's fourth leg pins this exact sentence. Preserve
the sentence shape when you edit this file.)*

## What E37 measured — the map the next attempt inherits; let nobody re-derive it

1. **The mesh is solved.** Generator fingerprint-swirls were on the SOURCE PLATE
   itself; the plate was edited clean locally (Qwen-Image-Edit-2511, Apache-2.0);
   TRELLIS seed 987654 on the clean plate is the Director's picked mesh
   (`884abe04…`, 32 shells, 0 boundary edges). The seed moves geometry (raw-face
   spread 10.98%); every reconstruction before E37 ran at the hidden default
   seed 42; `tools/reconstruct_mesh.py` (T71) is the seed-capable runner.
2. **The wood register is his-approved** (Gate-R form, views 0/4; prompt v-w1 via
   the register fixture, whole-term deletion, never typed). R3's terracotta
   prompt NAMED "sculpted thumbprint hatching" — the hatching was asked for;
   v-w1 drops the term.
3. **Mixed seeds = four woods = the banded rejection. Per-view seed mixing is
   permanently dead** by his word and its measured product. One seed across
   eight views is tone-consistent (set A, seed 2026081511, spread 8.33).
4. **Per-view content defects are seed lottery**, and the masked-repair road —
   mechanically perfected through Rulings 20–27 (region protocol, corrected
   init recipe, probe-first law, repaint proof) — still failed his eye on the
   composed result. **Per-view generation + composite repair did not converge
   to an acceptable asset. That is the wall, stated without spin.**
5. **Measured levers**: cn 1.0 suppresses invention at one seed (72→50) but not
   as a set-level property (73% seed variance, n=2); the negative backdrop
   clause enforces the field (151→181); no documented sampler-level speck
   suppressant exists (consult #7's honest answer). The local ColorMatch on a
   repaint region WASHES the face while its metric improves — falsified at
   `f1c2b50`; metrics on this subject keep grading the wrong thing.
6. **Schema-verified and live**: ColorMatchV2 · SetLatentNoiseMask (restores
   unmasked regions to the INIT — the init must be the image under repair,
   Ruling 27) · the QwenImageDiffsynthControlnet reference-anchor patch —
   verified, **UNMEASURED on this subject, the one untested generation-side
   consistency lever**.
7. **The consult channel's landscape verdict**: cross-view coherence from
   independent per-view samples is structural, not a missing config. If any
   next attempt stays on this route, that sentence prices it.

## THE QUEUE — nothing fires without his word

1. **The E37 disposition** — halted, awaiting his direction on what builds the
   performer next (possibly a different harness entirely; support whatever he
   picks — the record travels).
2. **Translations sweep** — the seven translated READMEs carry the tool count
   stale ("thirty-four", six spelled in-language); a translation act, the
   advisor's, batched for the next natural sweep.
3. **tree-manifest guard spec** — written and ready
   ([tree-manifest-guard-spec.md](specs/tree-manifest-guard-spec.md)); never
   concurrent with another seat's test-adding commit.
4. **The resurrected-default fix** (`project_twins.py:93` bg-max-pct 2.0 against
   the E16 R4e withdrawal) — TWO live firings; wants its spec.
5. **E34 candidate anchors** (E34 Ruling 9) — still owed.
6. Errands: `silhouette_masks.py:89` hardcoded basename · the `LABEL=PATH`
   parser splitting on `=` · an append-only write-guard for receipts ·
   `anchor_check` silent without PIL (E31 R5) · the identity envelope's
   dependency set (E31 R3b) · the archive-to-D: spec (never written).

## The Director's open decisions — surface these; never re-decide them

1. **What builds the performer next** — his, in progress.
2. The E34 formal disposition (accepted-with-known-classes or held reopened).
3. The pure-black dot routing — mechanism OPEN (task 0a: UNMEASURED, UV-gap
   candidate live; E37 proved the pit screen measures facet shading, not that
   class).
4. Hosted-tier revalidation (priced, unspent — armature's board).

## This seat's record — read before trusting this seat's rulings

The Director's assessment: not effective. The misses, each in the record where it
happened: **the Stage-D sheets reached his eye unwalked** (the worst one — the
law existed and was broken at the gate that mattered); **the wash guard fired
three times on one object and was demoted to advisory three times** (Rulings
9/15/17 — the four-woods asset it warned against got built); **"v0 is best" was
folded as acceptance before he finished reading**; **Ruling 26's repair recipe
pinned the clay as init** (the executor's one-job probe caught it at a sixth of
the cost); **Ruling 10's chain omitted smart_decimate** (the prep ANDON caught
it); **the E36 pit-premise mis-citation**; **the two-seat collision mis-call**
(pattern-matched, not measured); **the VRAM-ceiling referent misread**; and
**the seat's own last commits scooped sibling staged work in a shared index —
twice, in both directions** (check `git status` for staged sibling files BEFORE
every commit; pathspec-adds do not protect you from a pre-staged index). What
held: every fold pushed, every correction in place beside its measurement, the
mount servable and the suite green at the close.

## The executor seats — exceptional, uniformly

Every catch that saved this arc was an executor's: the seed-42 discovery, the
citation-read that broke the pit premise, the one-job probe, the
census-would-erase-the-face halt, the native-resolution mask walks, the
truncation self-catches, the backdrop-invention refusal, the ColorMatch
falsification, the clean stand-downs at context end. When an executor declines
to do something, that is signal.

## Environment

```
python    E:\AI-Models\trellis2-env\Scripts\python.exe      <- ABSOLUTE, always
blender   "C:\Program Files\Blender Foundation\Blender 5.2\blender.exe"   -b -P only
assets    E:\AI\training\facet_E3*\
```

Blender through PowerShell. Scripts create their own output dirs. argparse eats
leading minus signs. ASCII in tool/test output. PowerShell `Add-Content` writes
CRLF into the worktree — the committed blobs normalize via `.gitattributes`;
T06 fires on the worktree; normalize and verify `git diff` empty. Generation is
cloud-only. Derived-artifact order: census (after ALL documents final) → the
pair via `record_build` in a terminal commit; scratch-gate 19/19 first, every
time.

## Do not

End a session the Director has not ended · present any surface you have not
walked at native size at your own seat · carry a twice-fired instrument as
advisory a third time · do work he did not ask for · `git add -A`, or commit
over a shared staged index (read `git status` first, every time) · chain a
status check to a commit · push over a sibling's local commits without the
deferral reasoning · run the suite or the mount on bare python · leave CI red ·
run translations outside the advisor's own commit · fire any job, tag, release,
or metadata change before his word · touch closed rulings, accepted assets,
protected trees, or the seeded set except to cite · hand-edit facet.db or its
certificate · split the DB/cert pair · decide an executor's findings in the
executor's seat · quote any consult claim as fact before resolving it live ·
re-litigate the halt.
