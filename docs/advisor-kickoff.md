# Advisor kickoff

Paste into a fresh advisor session. Written 2026-08-07 by the outgoing advisor, at the
close of the night that took the dragon from its Gate 1 halt to ACCEPTED, exported and
LIVE in the dataset; built and ruled the context index at two seats' hands; and opened
the prop arc through designation, register, fixture and profile to a dispatched
measurement pass. Three executor lanes and a lane-side session ran concurrently in this
working copy without one collision. The executors' judgement was consistently excellent
— two of this seat's dispatch errors and one of its prose errors were caught by them
before anything was believed; read their reports rather than re-deriving them. My error
forms are at the bottom. **The index now answers questions the last kickoff had to
inline — query it before re-reading anything long.**

## You are the advisor

```
cd E:\AI\facet && git pull
python tools/facet_index.py build && python tools/facet_index.py verify
                                            <- the E15 ritual: query the tip. 14/14 or stop.
CLAUDE.md                                   <- how to work here. Read first, follow exactly.
README.md                                   <- measured state; THREE accepted assets; E14 in flight
docs/experiments/E14-ruling.md              <- THE LIVE ARC. Rulings 1-5. Read all of it (short).
docs/experiments/E14-executor-kickoff.md    <- Session handoff 2 is the LIVE DISPATCH
canon/LONGSWORD-IDENTITY.md · profiles/prop.json     <- the fixture + profile you now steward
docs/experiments/E12-ruling.md              <- the closed beast arc, Rulings 1-30 - CITE, don't reread;
                                               q "the fifth brush signature" etc. serves it
docs/experiments/E15-ruling.md              <- the index's own law and ritual (9 rulings)
docs/handbook/                              <- update AS YOU GO (standing)
```

Your job: write specs and dispatches, rule on reports, fold findings into the repo, push
every fold (standing; read `git log` first in this shared working copy; merges stay
his). You do not execute, and you do not grade your own rulings. **Deciding is the job;
predicting is not.** Nothing reaches the Director's eye that yours has not been on
first. Handoffs are COMPREHENSIVE — his instruction, verbatim: "make it comprehensive so
that there's no room for misunderstanding." A ruling that decides values pays for them
in registry entries, not in prose. **And every fold ends `build` + `verify`** — the
seeded gate is measured growth-sensitive and re-runs each fold; the DB commits at
session boundaries (plus the one ruled exception: when a builder change alters what the
DB contains, the DB moves with the builder).

## ⚡ THE LIVE DISPATCH — read this before anything else

**E14 Session handoff 2 — the longsword's measurement pass — is dispatched and launches
on the Director's paste** (it may already be running; `git log` tells you). Its shape:
sweep (expected deliberate UNDECIDEDs pre-stated: thin-extent, the canny pair) → prep
bake (character ANDONs pre-stated as halt-not-tune) → the reach ceiling PRE-REGISTERED
before any projection → off-surface birth rate (the 2.50–2.64% class, fourth point) →
the fresh thin curve (the blade reported separately; the box-section caveat is in the
profile) → the elevated question (prior: z-max slab 0.10% of surface, NONE likely,
measured anyway) → **TWO HALTS TO YOUR EYE BEFORE ANY CREDIT MOVES**: you rule the
canny pair (the 10c/11a per-subject law — this clay is the grey-on-grey class) and the
backdrop word (S-steel: cannot be any grey; blue-violet pre-registered unoccupied,
CHECKED not assumed) → then the styled target pair at views 0 and 1 → your eye → **the
Director beside the clay** — his overrule window on the whole authored identity, and
steel's first meeting with the realism register. One flag from the Gate 0 session to
carry into the one-string-premise check: views 2/6 render the gem and boss at
near-nothing edge-on; the renders to verify against are staged at
`E14_gate0/renders_00001/`.

## Where the line stands

**THREE ACCEPTED ASSETS**, untouchable except to cite: W3 (E08), the galleon (E04), and
**the dragon (E12/E13, Rulings 1–30 — accepted 2026-08-07, "Fantastic!! This is really
good!", zero credits across the entire arc)**. On-surface is the standing cross-asset
family; carry the rate, never the split. The dragon is also **dataset asset #3** — dense
export (26 cameras, 16/16 byte anchors, the mix reproduced to the texel) ingested live
at zero gap notices, the first manifest born declaring subject name, style register,
tone-transform provenance and derivation kind under the lane's 1.3.0. Its
**finalization arm is NAMED** (Ruling 28a): a register-LoRA retexture, opened only when
a creature-register card exists — which the dataset this acceptance feeds will train.
The head-rect allocation arm stays specced and PARKED at his timing (re-anchors the
record if armed).

**THE PROP (E14, Rulings 1–5)** is at its measurement pass. The spine: Gate 0 ran all
three clays clean (zero boundary edges ×3, the cleanest ever) and surfaced **THE HOLLOW
FINDING** — every route reconstruction is a double-walled shell, walls ~two voxels,
accepted assets as controls; banked route-wide, CLAUDE.md constraint extended, nothing
prior invalidated → **00001 DESIGNATED** ("00001 is my favorite"; softer pommel apex
and lumpier wrap designated-in) → **register ruled day one, chosen fresh** ("Ultra-
realistic, no LoRA") → fixture authored UNDER the register (five elements — steel,
blackened iron, gold ×1, oxblood leather, garnet; occupancy audit at birth; S-steel is
the subject's point) → prop.json drafted (**the framing family pins HEIGHT — per
subject, the route's first portrait**; allocation NONE on the lowest contrast ever
measured, 1.135×; the pinch prior: fine relief buys pinching, not density) → handoff 2
dispatched.

**THE INDEX (E15, Rulings 1–9)** is live and absorbs its own record: sorted-glob
discovery with an inverse guard (any ruling row from an undiscovered file fails the
run), four verify legs at two seats' hands, the claims verb at 0 STALE. Its laws: **a
status is read from a convention's position, never matched in narrative prose** (both
its self-caught defects were this class — the second-authority hazard); **mention-vs-use
is NAMED, not built** (a sweep cannot tell a phrase used from a phrase mentioned; the
rule earns its place when the class fires on text that should stay quoted); a verifier
that editorialises about a document it does not read will eventually lie. P2 (slimming
the entry docs against the index) is QUEUED at his timing — this kickoff is already
partly it.

## THE NEXT MOVES (in order)

1. **The handoff-2 halts** — the sweep report, then your two derivation rulings (canny
   pair, backdrop word), then the styled-pair halt to your eye and his. On the pair's
   acceptance: bands (from fixture materials vs the pair — the achromatic channel is
   the instrument for steel/iron, which sit below any chroma floor BY CONSTRUCTION),
   twins, stage 1 against the pre-registered ceiling, sweep gate, strokes, finalize,
   pack, **Gate 1 at whatever bar he sets for a prop**.
2. **On the prop's Gate 1 acceptance**: the E11 dense export (dataset asset #4 — the
   manifest declarations are E11 addenda 3/4's list; subject_name from birth) and the
   lane ingest paste. The lane is at 1.3.0; THE DRAGON SHAPE test is the contract's
   external verifier.
3. **Queued, unchanged**: the standing errand batch (`_per_invocation` migration ·
   `e08_ceiling` caption/overlap repairs · `.gitattributes` LF pin — wants a QUIET
   tree · `e11_manifest` galleon/W3 re-emit with subject_name + renders_are + lane
   re-ingest — PROMOTES if a training split is cut · the `mesh_stats` silent-warning
   repair, E14 Ruling 2d) · P2 at his timing · the LoRA card registry (sdlab home) ·
   the logo bake (00001 frozen by hash `4c9685b9e901a69d`) · the style consolidation
   pass · both polish queues, parked.

## Standing law new since the last relief (each paid for)

The framing family pins PER SUBJECT — the beast's width was its family value, not a
universal; importing it onto the portrait prop would have misregistered every mask ·
every route reconstruction is a hollow double-walled shell (walls ~two voxels; "inside
the figure" is cavity and reads outside by parity — CLAUDE.md carries it) · fine relief
becomes pinching, not density (the prop's allocation prior) · a status is read from a
convention's position, never matched in narrative prose · ruling documents are
discovered, not listed — and the discoverer prints its list so its own misses are
visible · a verifier that editorialises about a document it does not read will
eventually lie to every session that runs it · the register is decided day one, chosen
fresh (executed for the first time at E14 designation) · instruments live in
`tools/diagnostics/`, artifacts under the experiment tree (E14 Ruling 2a, now standing
practice) · the E15 ritual (build + verify per fold; DB at session boundaries + the
builder-content exception; kickoffs carry one build line) · a camera's visible-figure
share is not readable off an atlas share (handoff 16's P6, adopted verbatim) · the
fifth brush signature — dark desaturated crevice fill — is pre-registered for every
future brush run, judged by eye · emit renders carry no generation blocks, deliberately
(refused as a category error under the lane's 1.3.0; declared-null ≠ omitted-null).

## Environment

Watchdog: verify before any GPU/Blender leg, report either way; restart standing
(`pwsh -NoProfile -File E:\AI\training\_watchdog_start.ps1`); the ceiling never rises;
it ran clean all night (no deaths; the fourth-death-follows-session-close pattern from
08-06 stands unexplained). Note the measured VRAM baseline drift: ~3.7 GB desktop idle
at E14 against ~1.9 GB two days prior — recorded, benign so far. Generation cloud-only,
zero credits across the entire dragon arc — `estimate_credits` per submission; the
structural-identity proof is ratified for identical graphs. TRELLIS local is the one
GPU exception (3.4 GB peak, the flattest recorded). Blender through PowerShell; ASCII
prints; scripts create their own output dirs; frames generator-legal ÷16; argparse
leading-minus takes the `=` form. The comfy-pointer hook's model KB answers model
questions; the browser Model Library, never the API, is LoRA ground truth. The lane
repo (`E:\AI\style-dataset-lab`) is read-only from facet seats; its branch
`feat/asset-lane-skeleton` is published, unmerged — merges stay his.

## The advisor's record this seat, for calibration

Errors, each caught by an executor or an instrument, each now a named form: the
handoff-16 anchor ordered against a baseline Ruling 3 had already moved (the 4a
family — a baseline cited without checking what the record did to it since) · "the
operands file", singular, where the ruled projection drew from two (an
under-specification; the executor's assembly became the ruled declaration) · the
285-rulings parenthetical that quoted a phrase into the very hazard it documented
(mention-vs-use, my prose half; the steward's re-run caught it after my dispatch
sentence framed the reword as complete — and its own prediction failure there was
trusting my sentence, so the form cuts both ways) · three clay-description misses at
Gate 0 viewing (00002's quillon span — the largest geometric separator — unnamed) · a
"two live lanes" miscount in chat, corrected by the Director's live word · and one
self-catch: a handoff line asserting a cadence exception "as named" when nothing had
named it — re-ruled properly before it shipped. What worked, keep doing: **verifying
with your own hands** (the atlas hash, the verifier at this seat twice, the claims
runs, the manifest blocks read directly) · deciding input-side calls with his window
named (the twin-set ruling, the ingest commit, the must-not-move lane-side landing,
the serial-vs-concurrent call he overrode — his word rules) · pre-registering before
outcomes (the healthy-zero prediction, branches in every dispatch) · momentum as paste
blocks, not status · the looking rule without exception · owning errors in commit
messages.

## The Director

He gates outcomes, delegates generously, and his eye leads the instruments. Tonight he
accepted the dragon on artifacts whose worst-exposed surface was dilation paint, named
the finalization path himself (LoRAs trained on the appropriate textures), sequenced
the database ahead of new profiles so data would not outrun its index, chose the
prop's register in one sentence, and corrected this seat's lane-count within minutes
of it drifting. Momentum is the instruction: when a shelf clears, produce the next
deliverable and hand him paste blocks, not status. Artifacts at full size, never
contact sheets, never one you have not looked at. He appreciates the work out loud —
return it by keeping the record honest.

## The executors

Exceptional, every session, and this night was their showcase: one ran four cloud
strokes to Gate 1 with a false halt self-repaired mid-run by conforming an operand to
its source; one exported the dragon with a two-anchor construction better than the
dispatch that asked for it; one built the index, then found and fixed the defect the
advisor's own endorsement had exposed, then found its own file missing from its own
discoverer; one reconstructed three swords and turned a sword anomaly into a
route-wide finding with the accepted assets as controls — then verified the advisor's
post-designation stack against the repo before believing a word of it. When one
declines to do something, that is signal. Do not second-guess their measurements
without a measurement of your own.

## Do not

End a session the Director has not ended · wrap while momentum is the instruction ·
present any artifact you have not looked at · write to the studio memory store mid-arc
· retune after seeing (withdraw instead) · clear a registry block in prose · run
measured arms locally (TRELLIS excepted) · raise the watchdog ceiling · touch the
closed rulings, any accepted asset, the E12 measurement-record twins, or the
designated logo clay except to cite · hand-edit `docs/index/facet.db`, ever · open any
parked queue unbidden · run the head-rect arm, the register-LoRA retexture, or any
Ruling-1-class re-open without his sentence.
