# Advisor kickoff

Paste into a fresh advisor session. **Written 2026-08-15 by the outgoing advisor** — the
seat that closed the comfy-preflight treatment (v1.0.0 live on both registries), ruled
E35 through Gate R2 to its close (eleven rulings, five directions shut by measurement, no
candidate produced), ran five Comfy-channel consults with the calibration ritual holding
on every one, dispatched **E36** with its execution-spec amendment, and owned seven of its
own defects in the record with what caught each.

**Rewritten from measurement at the close.** Every number below was re-measured against
the tree at `1e293ed`; anything a live seat can move says so beside the number.

---

> # ⚑ ONE ARC IS DISPATCHED AND NOT YET STARTED. READ THIS BEFORE ANYTHING.
>
> **E36 — the route arms** ([E36-route-arms-kickoff.md](experiments/E36-route-arms-kickoff.md)
> **plus its amendment, which supersedes the thin arm text above it — read both**).
> Two one-job arms on the recorded base in their correct form for the first time:
> dual-control depth-early(0.80/0.0/0.45)+canny(0.45), and the flat-lit init. The
> arc's FIRST job is the structural anchor (0d: the rewired graph at depth-strength
> 0.0 must reproduce the recorded twin pixel-identically — platform determinism is
> E35-measured, ΔE 0.0000). Ceiling **15**, spend **0**. Grounding:
> [repaint-route-consult5-grounding.md](research/repaint-route-consult5-grounding.md).
> The executor paste block ships from your seat WITH the Director's go — rule 5:
> same message, always. The corrector-contract track (local, above the 36 px² cap,
> validated against Director-rejected artifacts) is E36's second front and is
> **unspecced — you spec it.**
>
> **E35 is CLOSED** ([E35-ruling.md](experiments/E35-ruling.md), Rulings 1–11).
> 47 of 60 spent, thirteen returned. Five directions closed by measurement. Four
> instruments committed with fixtures. **No repaint candidate exists** — R2-b (the
> best-measured configuration) stands rejected at the Director's eye; E34 remains
> the accepted-and-reopened asset.
>
> **comfy-preflight is CLOSED and LIVE** — v1.0.0 on PyPI and npm, OIDC-published
> (facet `docs/specs/comfy-preflight-spec.md`, Amendments 4–5 + postscript). ⚠ One
> verification you owe early: **whether the two ruled-in `STUDIO_MEASURED` entries
> were ever implemented** in that repo — the ruling-in is done (Amendment 5;
> E35-ruling Ruling 11), the code may not be.

## THE DIRECTOR'S OPEN DECISIONS — surface these, do not re-decide them

1. **E36's go** — the kickoff is dispatched; his word starts it.
2. **The r3L prompt term** — "flat even lighting" edits a RULED register term
   ("soft studio light", R3, his 2026-08-11 ruling) and is **his to veto or
   ratify**; the prior seat's Ruling 1 adopted it overstepping that. Its evidence:
   pale 226/4.09 vs 278/4.97, dark flat, the man kept.
3. **The armature relay re-target** — its held target ("E35's candidate") does not
   exist. The recommendation on record: E34's recorded GLB — the open classes are
   texture-side, the geometry is the accepted repair, and the named successor work
   is texture-side on the same mesh, so an armature survey cannot be invalidated
   by it. His word.
4. **The E34 disposition** — accepted-with-known-classes or held reopened.
5. **The three F1 sheet calls** — identity, register 26.99 vs 23.77, the
   crazed-hatching read — now scoping only whether 2509 keeps any successor place.
6. **The mesh-pit routing** (inherited unresolved from the prior banner): the
   pure-black RENDER class is geometry — in no texel of atlas or GLB (E35 task
   0a) — so it either goes to a mesh arc or is accepted as geometry. His call,
   never yet put to him plainly.

## You are the advisor

```
cd E:\AI\facet && git pull
```

```
E:\AI-Models\trellis2-env\Scripts\python.exe tools/facet_index.py build  --db <scratch>
E:\AI-Models\trellis2-env\Scripts\python.exe tools/facet_index.py verify --db <scratch>
                                     <- the E15 ritual: 19/19 or stop. SCRATCH --db;
                                        the record mount is live on this working copy.

CLAUDE.md                            <- the law book; follow exactly
README.md                            <- the front door; its counts are under a test
docs/experiments/README.md           <- the status table
docs/experiments/E35-ruling.md       <- the freshest law: eleven rulings, the measured map
docs/known-defects.md                <- the dark-speck entry; the pale class joins at
                                        E36's close (two signatures, one mechanism open)
```

**Two mounted servers**: `mcp__facet-record__*` and `mcp__facet-measure__*`.
`record_health` tells you whether to trust the first. `record_build` is build+verify
as ONE act and writes the certificate — the CLI `build` verb alone writes a db with
NO cert and **splits the pair**; never leave it that way.

Your job: write specs, rule on reports, fold findings, push every fold. **Deciding is
the job; predicting is not.** Nothing reaches the Director's eye that yours has not
walked at full size — except the pale class, which **no eye reads at working zoom**
(four measured misses including a perceptual one; the chroma-split instrument is the
detector; your eye keeps identity and register). **A dispatch is not delivered until
its paste block is on the screen, and the kickoff DOC carries the execution spec —
the block only points.** The Director ruled a thin block the weakest handoff this
seat made; the E36 amendment is the repair and the standard.

## THE LIVE STATE — measured at `1e293ed`

| | |
|---|---|
| HEAD | `1e293ed`, tree **clean**, 0/0 with origin — re-measure at your open |
| CI | **green** at the E35 close (`694a9e3`, run `31864729310`, resolved live); commits after it are docs-only |
| suite | **THE SUITE: 1002 tests, 957 hermetic** (45 artifacts) — ⚠ pinned by T34; preserve the sentence shape. E36 moves it; reconcile per the corrected order below |
| highest T | **T70** (`tree_manifest`). E36 takes **T71+** |
| published | `facet-mcp` 0.4.0 · `@mcptoolshop/facet` 0.4.0 · **comfy-preflight 1.0.0 on both registries** (OIDC, `trustedPublisher` verified from registry metadata) |
| accepted assets | five — W3, galleon, dragon, longsword, the E34 performer (`ce793064…`) **REOPENED at his zoom**; E33's pre-repair GLB recorded byte-unchanged |
| protected trees | eight subtrees (7,312 / 17,072,807,610 bytes) + `facet_E33` (116/116 — the self-reference now REPORTED, not counted; same tree as the old 117) + `facet_E34` (84) + **`facet_E35` (335, self-excluded)** — all via `tools/verify/tree_manifest.py`, both encodings, T70 |
| spend | E35 **closed at 47/60**. E36: **0 of 15**; the anchor is job one |
| consults | five run; the ledger: headline calls **4-for-4** once checked, sub-mechanisms **0-for-3** at enumeration. Calibrate one checkable claim EVERY consult; enumerate anything load-bearing; give results back |

### The record index

Mount SERVING, certificate PASSED, byte-identity determinism.
The record holds 36 experiments. No staleness. **Every other count lives in the
ritual's own output and in `record_health` — read them there, not here.** *(The
experiment count stays because T34's fourth leg pins this exact sentence. Preserve
the sentence shape when you edit this file.)*

## The measured map you rule against — E35's product; let nobody re-derive it

The recorded route (qwen-image img2img + canny 0.9, denoise 0.92, R3 prompt, no
LoRA) is the class-best configuration and the only served route where identity is a
first-class input (the partner texture tier is identity-blind BY SCHEMA — three
vendors, zero reference-image inputs; consult #5, calibrated). **Closed by
measurement**: seeds (a 1-D exposure frontier — dark↔pale Spearman −1.000 over seed
means, +0.018 over twins); cn weakening (RAISES pale +235/+339% — anchoring
SUPPRESSES the class); denoise < 0.92 (register C\* 23.77 → 1.89 before either
class dies); the slate (euler_ancestral and single-depth broke identity;
flat-lighting kept the man, pale −19%, dark 0); 2509 at both configurations
(classes ~2.9× worse, identity lost — and reg-IoU **0.9611 with no ControlNet**:
the edit reference out-registers canny, recorded for whatever future wants it).
**The pale class is TWO signatures**: chroma-collapsing init-bleed (mechanism
ESTABLISHED; anchoring suppresses it) and chroma-preserved lightening (the class he
rejected; mechanism OPEN). **The dark class** is baked-shadow painting, 57
components to 377 px², below no lever and above the corrector's 36 px² cap. **The
2509 quantisation trigger**: ≲2,620-unique-colour flat renders corrupt its encode
deterministically; ~5,000 clean; one lanczos round-trip repairs; the mechanism is a
bounded black box (consult #5 Q4 — not a schema-visible knob).

## The queue behind the arc

1. **comfy-preflight `STUDIO_MEASURED` implementation** — verify, then dispatch the
   written blocks if absent (Amendment 5 · Ruling 11).
2. **The corrector-contract spec** — E36's second front, yours to write.
3. **E34 candidate anchors** (E34 Ruling 9) — still owed.
4. **The premise-reconciliation paragraph** (inherited banner item a, never
   formally laid out): the atlas census (61–79% twin-painted) and the render-dot
   co-occurrence (14.3%, 6.13× chance) measure DIFFERENT operands and both stand —
   one paragraph in a fold, so the next reader stops re-deriving it.
5. Inherited, still unspent: `anchor_check` PASSES SILENTLY WITHOUT PIL (E31 R5) ·
   the identity envelope's dependency set (E31 R3b) · archive-to-`D:` (spec never
   written) · E30's W3 era-flags re-run · `texel_provenance` empty-stroke refusal
   (E34 R7b).

## The release sequence, when he fires one — the order is law

As the prior seat recorded it (re-count off `T34.PINS` · FIVE version declarations ·
CHANGELOG · release-notes BY TAG NAME · translations ONCE after content is final ·
derived artifacts census-first then the pair · tag AT HIS WORD BY SHA · read-back a
real VERB from outside), with one law added this seat and paid for the same day it
was written: **the advisor's own commit carries the translations — that commit IS
the sweep receipt** (comfy-preflight Amendment 4; the tag outran the sweep once and
only luck shipped the repair). Digits-only on the seven translated READMEs.

## ⚠ WHAT WILL COST YOU — the standing list, plus this seat's tuition

1. **The status check, its read, and the commit are separate calls, always.** This
   seat chained them once and committed the executor's staged files under its own
   message (`63cff76`) — the third shared-copy scoop in one day.
2. **Every structural or named-model change anchors first, IN THE DISPATCH.** The
   2509 pilot burned two jobs because the dispatch ordered five deltas and no
   `run_template` baseline (Ruling 6). E36's 0d is the pattern.
3. **Scope a mechanism closure to the signature you measured.** "The pale is the
   clay init surviving" was written over a class that was two classes (Ruling 2's
   correction). Name the signature, not only the magnitude.
4. **A fork over a signed quantity has at least three branches.** Three seats
   shared one sign assumption; R2-c broke it. The UP branch is live even when
   nobody believes it.
5. **Ruling headings are `## Ruling N — Title`** or the parser serves your rulings
   as prose (this seat found its own ten un-extracted by reading the scratch
   build's counts against the document).
6. **The kickoff doc carries the execution spec; the paste block points at it** —
   exact invocations, baselines, floors, anchors, homes, gates. A block that makes
   the executor re-derive the record is a defect; the Director said so in as many
   words.
7. **A config read is not an artifact read.** npm's `files` field said the tarball
   excluded translations; the pack listing showed all seven inside. The PyPI sdist
   was cleared by DOWNLOADING it. Read the artifact.
8. **The truncation family stands at FIVE instances** and gained a member: a
   wrapper's exit code over the tool's own summary. Complete captures,
   totals-reporting commands, nothing capped that decides.
9. **Consult claims are testimony until enumerated.** Calibrate one checkable claim
   every round. Sub-mechanisms have fallen at enumeration three times; headline
   calls have held four times — both facts are the calibration.
10. Standing and still true: verify alone, read complete, then the commit · the
    two-seat push discipline (sibling LOCAL COMMITS defer yours; the reason goes in
    the message) · the E(n) span bump, thrice-proven (E37 → `E3[0-7]`, in the
    creating commit) · the corrected count order: **pins → FULL suite → collect →
    surfaces → census last** · `pathlib.write_text()` writes CRLF on Windows · run
    a verb, never `--help` · check nightly/devel channels before declaring a
    dependency unavailable.

## The advisor's record, this seat — for calibration

Seven misses, each caught by an instrument, the executor, the Director, or a
re-read — none by this seat's own first reading: the chained-guard scoop; the
anchorless 2509 dispatch; the over-scoped mechanism closure; the heading form; the
thin E36 handoff (the Director's catch, in plain words); the npm files-field read
(falsified by the tool's own pack listing); the alpha hypothesis (died by its own
falsifier — the right kind of wrong). It also answered a disposition question from
recital twice before measuring — he had to ask three times, and the law is **when
he asks about a claim, re-measure the claim: the first ask, not the third.** What
worked, keep doing: calibration before crediting any consult; discriminators
before fixes; zero-job archive measurements before any spend; the sheet-walk
before every ruling; enumeration before commissioning (the flat-render
implementation existed rig-side; the served relight was never needed); owning
errors in the fold that finds them, with the measurement beside them.

## The executor seats — exceptional, in the record's tradition

E35's seat: the anchor discipline rebuilt mid-arc after a shared error and never
dropped again; the refusal of A2's number with nothing else to show; B2's
pale-direction hit made by refusing to look; guards of its own construction fired
on its own work and every one was honored. comfy-preflight's seat: predicted its
own release's exact failure mode in the report that preceded it. When an executor
declines to do something, that is signal.

## The Director

He gates outcomes; his eye leads the instruments — except the pale class, where
the instrument leads and his zoom is the discovery tool, not the working-zoom
check. **When he asks about a claim, re-measure the claim.** When he says a
handoff is weak, it is weak — the fix is the document, not a defense. His mandate
built E35's instruments; his allotment words are the ceiling authority; his R3
ruling owns the prompt's material terms (see open decision 2). Confirm
product-defining frames contrastively; ship every paste block in the same message
as the go; do not end a session he has not ended.

## Environment

⚠ **CHECK THE VRAM WATCHDOG AT SESSION START** — heartbeat ADVANCING (two reads),
never the starter's exit code.

```
pwsh -NoProfile -File E:\AI\training\_watchdog_start.ps1
python    E:\AI-Models\trellis2-env\Scripts\python.exe      <- ABSOLUTE, always
blender   "C:\Program Files\Blender Foundation\Blender 5.2\blender.exe"   -b -P only
```

Bare `python` lacks `open3d` AND `mcp`. Generation is cloud-only; the local VRAM
ceiling is never raised. Blender through PowerShell. Scripts create their own
output dirs. `argparse` eats leading minus signs. ASCII prints. Derived-artifact
order: census (after ALL documents are final) → the pair via **`record_build`**
(build+verify+cert as one act) in a terminal commit; scratch-gate 19/19 first,
every time. E33's manifest self-reference is handled by `tree_manifest` — reported,
not counted.

## Do not

End a session the Director has not ended · present any surface you have not walked
at full size (the pale class exempted to the instrument, by measurement) · do work
he did not ask for · `git add -A` in a shared copy · chain a status check to a
commit · push over a sibling's local commits without the deferral reasoning · run
the suite or the mount on bare `python` · leave CI red · run translations outside
the advisor's own commit · fire a tag, release, or metadata change before his
word · touch the closed rulings, accepted assets, protected trees, or the seeded
set except to cite · hand-edit `facet.db` or its certificate · split the DB/cert
pair (the CLI `build` verb alone does exactly this — use `record_build`) · decide
an executor's findings in the executor's seat · quote any consult or research
citation as fact before resolving it live.
