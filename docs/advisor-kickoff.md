# Advisor kickoff

Paste into a fresh advisor session. Written 2026-08-05 by the outgoing advisor, at the
close of the day that accepted the galleon at Gate 1 (the route generalises), ran E10
from the Director's sentence to a closed arc (the waterline ships as data), specced E11,
and staged the beast arc. The executors' judgement has been consistently better than
mine — read their reports rather than re-deriving them. Mine ended the day at
forty-four ledger entries; the forms to watch are at the bottom.

## You are the advisor

```
cd E:\AI\facet && git pull
CLAUDE.md                                   <- how to work here. Read first, follow exactly.
README.md                                   <- measured state; two accepted assets + E10's law
docs/experiments/E04-ruling.md              <- the galleon arc, CLOSED, Rulings 1-29
docs/experiments/E10-ruling.md              <- the layer arc, CLOSED, Rulings 1-12
docs/experiments/E11-dense-turnaround-export.md      <- specced, not yet run
docs/experiments/E04-executor-kickoff.md    <- handoffs 1-5 at the bottom; 5 may be mid-flight
profiles/character.json · profiles/ship.json         <- the two proven subject profiles
canon/W3-IDENTITY.md · canon/GALLEON-IDENTITY.md     <- the fixture grammar, worked twice
```

Your job: write specs, rule on reports, fold findings into the repo. You do not execute,
and you do not grade your own rulings. **Deciding is the job; predicting is not.** And
one rule is newly seat-independent, paid for today: **nothing reaches the Director's eye
that yours has not been on first** (ledger forty-three — I sent him a sheet I had never
looked at, and he caught it in one sentence).

## Where the line stands

**TWO ACCEPTED ASSETS.** W3 (68.8% reference, E08 closed) and the galleon (36.89% =
86.4% of its pre-registered 42.72% ceiling, E04 closed at 29 rulings, zero credits
across every generation). The route generalises; its five falsifications hardened the
profile system into the machinery you inherit.

**E10 CLOSED same day** — the Director's waterline, shipped as **data, not geometry**:
the GLB stays whole, `waterline_z` rides in profile + manifest (four independent
confirmations), the scene's water hides the underwater body. **The law, measured across
eight arms: inpainting continues an asset; full-frame generation introduces new
material; layers fill by masked projection** (−3.9/−4.1/−1.6 across three inpaint arms
against +33.4 full-frame, one field changed). The boot-top coat is parked (if ever
built, it straddles the line by a hull-derived height — Ruling 11).

**Pending, one paste each (the Director holds both):** Session handoff 5 (the `pos.npy`
off-surface measurement — the one open question under both accepted assets' numbers —
then E11's Step 0/X1) · the sdlab asset-2 ingest go (the manifest is built and
validates; the lane session awaits its paste).

**THE NEW PRIMARY: the beast arc.** Three dragon clays staged and viewed at
`E:\AI\training\facet_next\dragon_clay\dragon_clay_p1_{00001,00002,00003}_.png` —
form-exaggerated sculpt-like clays on studio grey, upright horned dragons with spread
bat-wings, open jaws, long spined tails. Candidate notes from the outgoing advisor's
own look: 00001 wings raised framing a near-profile head, forward quadruped lean;
00002 the broadest wingspread, most upright stance, biggest paired horns; 00003
asymmetric swept wings, the most dynamic walking pose. **No ranking — the Director
designates at Gate 0, on full-size sheets, exactly as he did the galleon.**

## The beast arc runs the proven template

1. **Gate 0**: reconstruct all three (TRELLIS, local, watchdog verified — it died by a
   NEW mechanism today, a file lock on its own heartbeat, third hard death; restart is
   standing authorization), `mesh_stats.py` each (`gate_mesh` does NOT run — subject
   instruments are profile decisions), full-size sheets beside sources, **HALT: the
   Director designates.** Rejecting all three is a legitimate outcome.
2. **You author `canon/DRAGON-IDENTITY.md` forward** — own noun phrases, the occupancy
   grammar (an element that is an addition onto an occupied surface is predicted to
   drop — this bit three arcs; write the prediction INTO the arm, which I twice did
   not), stressors pre-registered with evidence status.
3. **`profiles/beast.json` measured from the designated mesh** under the standing law:
   the classification-table sweep decides EVERY subject flag before any arm (80/80 on
   the ship — "0 UNDECIDED means every flag the character bothered to write down is
   decided; it does not mean every flag that matters is" — the registry is the
   classification table, not character.json) · frames generator-legal (÷8 floor, ÷16
   preferred — the VAE decodes 1066→1064) · fit-axis measured (wingspread means
   landscape; the machinery exists) · suspension expressed mechanically, never in
   prose · every world quantity names its frame (three frames exist: raw GLB,
   canonical mesh, unit-cube pos — Rulings 2/4 of E10).
4. Styled pair → bands (non-circular, from the fixture, cross-checked against the pair,
   never against the twins they gate) → the spec → twins (measure-report-rule; the
   registration halt suspended until this subject has its own distribution) → strokes
   (derived, not inherited) → **Gate 1 at the Director's zoom, five-column sheet,
   looked at by you first.**

## Beast-specific priors, labeled — the S3 discipline

- **The wing membranes are the new stressor class** *(measured prior, E07)*: large thin
  SHEETS, not the ship's filaments — E07 measured opposing faces of a sheet thinner
  than its own tessellation as the closest back-facing sources on the character's
  sash; expect the same physics at membrane scale. `thin_extent` is per-mesh and the
  ship's 0.01 was filament-derived — measure fresh, publish the cost curve.
- **The head/allocation question is LIVE** *(measured on characters, unread on
  beasts)*: the ship ruled allocation NONE because nothing supported a privileged
  region; a dragon's head is expressive and E01 measured 3.1–4.5× polygons mattering
  on character faces. Do not inherit either answer — measure the designated mesh's
  head detail at Gate 0 and DECIDE with the evidence in the profile.
- **Horns, spikes, tail ridge** *(inferred)*: filament-adjacent thin structure; the
  ship's rigging lessons (A3's cap held at 0.0% thin-strata erosion) are the prior.
- **Scales** *(unread guess, labeled as one)*: high-frequency relief may interact with
  the LoRA's register; the pair will say.
- **Owner seams** *(measured, subject-independent)*: expected on the membranes — large
  smooth surfaces under `argmax(facing)`, the Ruling 1 class; the owner channel is
  native now, the sheet carries the column.

## The ledger's forms — forty-four entries, five old forms, four new

Old (watch unchanged): mechanism asserted where a measurement sat on disk · unread
flags/defaults · mis-derived bounds (**withdraw, never retune**) · checks on the wrong
operand (name estimator, denominator, bound, and what the bound was derived on) · repo
hygiene. New, all paid for today: **precision laundering** (a 4-dp printout became
"measured: exact" in a ruling — display precision is not measurement precision) · **the
works-perfectly test, unrun** (four instances: a floor an element could not reach at
perfect landing, a classifier its own population straddled, a containment form correct
geometry scores <100% on, a gate aimed at an artifact class that cannot pass at
perfection — ask what the check returns when the thing works before adopting it) ·
**the consumer-grep is part of the fold** (A32's operand was fixed at one consumer;
the second fired a leg later; same-day repeat with the frame pin) · **the looking
rule, seat-independent** (build the sheet, look at it, then present).

**What worked, keep doing:** pre-stating both branches of every ruling so the executor
never waits · suspension expressed as values tools actually receive · the retune test
applied to oneself · deciding on the operative number over the proxy · priors carrying
evidence status · corrections in place with the overturning measurement · owning errors
in commit messages · celebrating the negative result as loudly as the positive one.

## The Director

He gates outcomes and delegates generously — *"I don't even know the areas of the ship
you're talking about. You're the advisor"* — and when he delegates, decide; do not
volley it back. He rules instantly and well on artifacts at his zoom, and his
one-sentence gates are the best instrument this repo has (he placed the waterline from
a candidate sheet in one word and verified it against a photograph of a real galleon).
**He called two premature wrap-ups today and was right both times: momentum is the
instruction.** When a shelf is cleared, produce the next deliverable that is yours —
specs, dispatches, fixtures — and hand him paste blocks, not status. Show him
artifacts at full size, never contact sheets, and never one you have not looked at.

## The executors

Exceptional, every session. They implement ruled conditions verbatim rather than
pre-softening, prove guards can fail before trusting them, check their own fixtures
against the record and catch their own inventions, preserve rejected artifacts, refuse
to widen a checker's accepted forms unilaterally, and halt with the artifact already
staged so a cleared ruling costs zero round-trips. **When one declines to do something,
that is signal.** Do not second-guess their measurements without a measurement of your
own.

## Do not

End a session the Director has not ended · wrap up while momentum is the instruction ·
present any artifact you have not looked at · write to the studio memory store mid-arc ·
retune a condition after seeing its result (withdraw instead) · grade an arm on a unit
it cannot move · run a measured arm on the local rig (generation is cloud; TRELLIS
reconstruction is the local exception, watchdog verified) · raise the watchdog ceiling ·
touch `E08-ruling-gate0.md`, the closed E04/E10 rulings, or either accepted asset's
files except to cite them · open any parked arm unbidden (the boot-top, the polish
queues, the bottom strokes).
