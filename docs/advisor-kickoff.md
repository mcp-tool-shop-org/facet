# Advisor kickoff

Paste into a fresh advisor session. Written 2026-08-08 by the outgoing advisor,
at the close of the arc that took the prop from its stroke-lane ruling through
the misbind that put the stone back on arithmetic, eight clean strokes, Gate 1
acceptance ("Fantastic! I accept"), the dense export at zero gap notices, the
ingest that made the dataset four assets deep, and the Director's polish-arc
charter. **Zero credits across the entire E14 arc, designation to acceptance.**
⚠ **TWO SESSIONS ARE IN FLIGHT IN THIS WORKING COPY** — your first section below
is about them; read it before touching anything.

## You are the advisor

```
cd E:\AI\facet && git pull
python tools/facet_index.py build && python tools/facet_index.py verify
                                            <- the E15 ritual: 19/19 or stop.
                                               (The set is 19 — one seed withdrawn
                                               under its own law, Ruling 31e.)
CLAUDE.md                                   <- how to work here. Read first, follow exactly.
                                               TWO NEW LAWS since E14 mid-arc: a statistic
                                               of angles must be circular; the chroma-floor
                                               law's third-firing sibling.
README.md                                   <- measured state; FOUR accepted assets; E14 CLOSED
docs/experiments/E14-ruling.md              <- Rulings 24-35 are THIS seat's arc. READ ALL OF
                                               THEM. Rulings 1-23 are the prior seat's - cite
                                               via q, don't reread.
docs/experiments/E16-errands-kickoff.md     <- LIVE DISPATCH 1 (in flight)
docs/specs/mcp-spec-session-kickoff.md      <- LIVE DISPATCH 2 (in flight)
profiles/prop.json                          <- ACCEPTED; every value spent; citable-only
docs/experiments/E15-ruling.md              <- the index's law; seeds enter measured,
                                               leave withdrawn-never-recrafted
docs/handbook/                              <- update AS YOU GO (standing)
```

Your job: write specs and dispatches, rule on reports, fold findings into the
repo, push every fold. Deciding is the job; predicting is not. Nothing reaches
the Director's eye that yours has not been on first — AT THE SHEET, AT
MAGNIFICATION. Handoffs are COMPREHENSIVE (his standing instruction). A ruling
that decides values pays for them in registry entries — **and ships the
predicate that produced its counts, or the indices** (Ruling 28b; this seat
paid for that one live). Every fold ends build + verify — **verify runs in its
OWN call, you READ "PASSED", and only then does the commit issue.** The DB
commits at session boundaries plus the builder-content exception.

## ⚡ THE TWO LIVE SESSIONS — your first stewardship, before any new work

**This is a SHARED working copy.** Check `git log origin/main..HEAD` and
`git status --short` before every fold; executor commits ride with your
pushes; **never `git add -A`** — file-specific adds only; if you meet an index
lock or staged files you did not stage, stop and report rather than improvise.
One measured hazard, hit live at this seat's last fold: **concurrent verifies
race on the fixed determinism scratch path (`facet.db.det_a`)** — a
PermissionError there is the other session's verify; retry once, flag it,
never force.

1. **E16 — the errand batch** (`E16-errands-kickoff.md`): eleven queued
   repairs, one commit + one anchor each. Its blind predictions are committed
   (`30d60c5`); **E16-1 (the verifier's ASCII repair) has already landed in
   the working copy** — the PASSED line prints ASCII now. It halts at
   `E16-errands-report.md`; **your ruling lands at `E16-ruling.md`** (a new
   arc file — the glob discovers it). Watch items: the one behavior change
   (E16-10, the edge-dist A3-port) must land as an OPT-IN flag with the
   default byte-identical; the galleon/W3 re-emit is explicitly NOT in the
   batch (promoted into the polish arc, Ruling 35b).
2. **The MCP spec session** (`docs/specs/mcp-spec-session-kickoff.md`): four
   specs + the placement decision memo. **Mid-write in the copy now**
   (`docs/specs/index-mcp-spec.md` and `placement-memo.md` are untracked). It
   halts with everything staged for YOUR eye, then the Director's word on
   placement (standalone vs repo-knowledge) and build order. It specs only —
   builds nothing, creates no repos.

**Ruling those two halts is your first work.** Neither session ends until the
Director ends it.

## Where the line stands

**FOUR ACCEPTED ASSETS — all four subject classes**: W3 (E08, character), the
galleon (E04, ship), the dragon (E12/E13, beast), **the longsword (E14, prop —
Ruling 32, "Fantastic! I accept", ruled on `longsword_hero.glb` at his own
zoom)**. All citable-only. **The dataset holds four dense assets** — 114
records across five ingests — and **sdlab's asset lane is MERGED TO MAIN and
green** (pushed at the Director's explicit word, 2026-08-08; the lane stays
read-only from facet seats by default — his live word is the override, and
that push is the precedent).

**The E14 tail this seat ruled (24–35), compressed**: the stroke-lane ruling
(eight merged strokes, thin_extent DECIDED 0.0, spiral order = risk cadence) →
stroke 1 misbound twice (the stone recomposed as a cabochon at both seeds —
20b's class at a second structure; **the term worked; the failure was form**) →
**Ruling 25 took the stone OFF the generation path**: the garnet re-projection
— a per-view HUE ROTATION of the four drifted twins' stone regions (L and C\*
preserved exactly; the drifted paint already carried the correct facets),
re-projected through the spine after a works-perfectly gate that came back
whole-atlas byte-identical → the collar repair on an asserted-count union
(1,436; the count-assert FIRED on this seat's own predicate first — the law:
**a set's description is not its generator**) → seven strokes clean, both
edge-ons 20b-clean, zero re-rolls → **the probe falsified as a bound by its
own pre-registered falsifier** (the lane committed 109.6% of the "achievable
set"; **a static probe of a sequential process is a snapshot** — each stroke's
commit enlarges the next stroke's keyed context) → HALT 2 accepted ("I love
it") → finalize (median 2.04 edges = **the dragon's 0.92 in ABSOLUTE
distance** — the median triangle edge IS the voxel pitch; quote both units) →
Gate 1 ACCEPTED → the dense export (six-class provenance palette, the tone
transform declared as a masked hue rotation, zero gap notices first run) →
INGESTED, live digits = dry digits line for line.

## ⚡ THE ROADMAP (Ruling 35 — the Director's charter, his words in the ruling)

**(1)** The two in-flight sessions land and are ruled. **(2)** The four MCP
tools are **BUILT and VERIFIED PROPERLY WITH TESTS** — the shipcheck bar
(context-index MCP first; measurement MCP; fixture-lint → sdlab; comfy-
preflight; the banked studio memory `facet-mcp-tool-candidates.md` carries the
plan). **(3) THE POLISH ARC** — every accepted exemplar polished,
quadruple-purpose: **dogfood the new MCP tools on real work · polish the
pipeline · re-inform the dataset** (the galleon/W3 re-emit promotes into it) ·
**the humanoid re-made PHOTO-REAL without the saltroad LoRA** (his register
word, recorded fresh at that arc's designation per the register-is-subject-data
law) · **the sword's ACTIVATED STATE opens there** (16c — glowing gem,
forge-hot steel, NO flames; the waterline mechanism with the occupancy map;
its sentence has arrived and its time is named). Nothing in the polish arc
opens until the tools pass their tests. You draft the polish arc as its own
experiment when they do.

## Standing law new this seat (each paid for; pointers into Rulings 24–35)

**A set's description is not its generator** — a ruling names the predicate
that produced its count, or ships the indices (28b) · **a restricted write
needs a restricted sidecar** (33c — the re-projection's owner sidecar carried
the unrestricted run's drift) · **an image edit feeding a keyed consumer
changes the key** (27b — +1,374 texels a colour-only edit would have smuggled
in) · **a static probe of a sequential process is a snapshot, not a bound**
(29f) · **the achievable set is a write-head number and a territory is a reach
number** (24b — the price law's third instance) · **a statistic of angles must
be circular** (26b, CLAUDE.md — +49.1° reported where the truth was −8.4°, on
the family that straddles the wrap) · **a check that cannot fail is not a
check, at the route's tail too** (31d — the surface-aware mean-fallback zero
three subjects quoted was structural) · **quote a dilation distance in both
units** (31d — same absolute distance, 2.35× different triangles) · **a
cross-session render column is anchored by re-emission or it is not a column**
(31d — every pixel differed; the render path, not the asset) · **the pair's
twin side is the projection's actual input set; a transform's outputs ride
with its declaration** (33d) · **reason about which texels were reachable,
not which elements are visible** (33e) · **owner ids are positional — verify
the id→view mapping against legend counts** (33c) · **"islands" names its
object** (31d — mesh unwelded shells 46,496 vs atlas components 31,887) ·
the seeded gate: **a digit-keyed query whose digits the route deliberately
propagates is a seed with a structural expiry** (31e — withdrawn under its
own law, never re-crafted).

## ⚠ THE PRACTICES — they bind you. The prior seat's five, plus this seat's.

1. **THE SHEET-WALK**: every judged artifact walked ON ITS SHEET — reference
   beside asset, structure by structure — mismatches stated IN PLAIN WORDS
   before any number. 2. **Any image the Director sends is walked FIRST**,
   described structure by structure at the top of the reply. 3. **An evidence
   line lists what you OPENED, never what was staged.** 4. **Acceptance only
   at full magnification of the deciding region.** 5. **Excluded and rejected
   artifacts labelled AS SUCH, in the image.** 6. **A fold's verify and its
   commit NEVER share a call.** 7. **Seed qualification before any set.**
   — And from this seat: 8. **DISPATCH TEXT IS HYPOTHESIS** — four dispatch
   errors this arc (a count attached to a predicate that never generated it; a
   stale path sketch that would have dropped a stroke; a stale tool caveat the
   record itself had flagged; an anchor path that would have re-stamped a
   citable tree), every one caught by an executor verifying dispatch text
   against source. Write dispatches expecting that check; honour it when it
   lands. 9. **Masks and mappings travel as indices with asserted counts**
   (the npz practice, 28b). 10. **Watches are eye-class unless a ruling arms
   a number** — this seat's "less-or-equal is clean" wording pointed a numeric
   reading at an eye watch and forced a mid-lane escalation (29e); the
   relational grid REPORTS, the eye DECIDES.

## THE NEXT MOVES (in order)

Rule the E16 halt → rule the MCP-specs halt (the placement memo goes to the
Director's word) → coordinate the MCP builds studio-side (shipcheck bar;
built-with-a-consumer-waiting — the polish arc is named as their first
consumer, Ruling 35b) → **draft the polish arc** when the tools pass. Parked
at his sentence: the activated state (opens AT the polish arc — the time is
named, the fixture authors from scratch when opened, per-state derivations
re-run) · P2 doc-slimming · the fifth subject class. The errand batch's
galleon/W3 re-emit is promoted into the polish arc. MCP-tool placement:
the memo's question (standalone vs repo-knowledge) is the Director's word
after your eye.

## Environment

Watchdog: verify before any GPU/Blender leg, report either way; restart
standing (`pwsh -NoProfile -File E:\AI\training\_watchdog_start.ps1`); a hard
death leaves NO DEAD line; the ceiling never rises. Generation cloud-only
(the whole E14 arc cost 0 credits; the OSS graph is GPU time on subscription);
TRELLIS local is the exception. Blender through PowerShell; ASCII prints;
scripts create their own output dirs; frames generator-legal ÷16;
argparse leading-minus takes the = form; **emit ALWAYS carries `--profile`**
(unprofiled emit silently produces a 752-wide W3 frame — Ruling 29c; the
guard is E16-4). The E15 ritual every fold; verify in its OWN call; the DB at
session boundaries; the seeded set is 19, new seeds enter MEASURED and leave
WITHDRAWN, never re-crafted. The lane repo (`E:\AI\style-dataset-lab`) is
read-only from facet seats — the Director's live word overrides (the
2026-08-08 push-to-main is the precedent; merges stay his). The must-not-move
trees: E04's, E08's, E13's and now **E14's export trees** — the lane holds
sha-pinned pointers into all four.

## The advisor's record this seat, for calibration

**The misses, worst first**: the registry payment one row short at the
stroke-lane ruling (the guard fired before stroke 1 — 24l) · the count-
predicate conflation (the assert fired on my own mask — 28b) · the stale
path sketch that would have dropped stroke 1's texels (28b-ii) · the stale
hardcode caveat (31c) · the anchor path into a citable tree (33b) · the
crop-frame dispatch item with no consumer (24j) · the eye-watch wording that
forced a mid-lane escalation (29e). **What worked, keep doing**: the walks in
plain words before every number, every time · the halts all held — nothing
generated before its ruling, nothing banked past its gate · corrections in
place with the measurement, never silent · the seed withdrawn under its own
law instead of re-crafted · the misbind answered with arithmetic instead of
a third roll (prefer eliminating a risk to gating it — the arc's best call) ·
zero credits · paste blocks, not status · pre-registering before outcomes.

## The executors

Exceptional without exception. This arc's sessions: ran the write-head against
itself rather than modelling it (46/46 ANDON passes); disclosed a structural
conflict as found-before in a predictions commit; exercised a compensator
BEFORE the operation it protects; honoured a fired count-assert with no second
reading tried; caught four of the advisor's dispatch errors against source;
self-caught two instrument errors per session by checking figures that moved
a direction the mechanism forbids; graded 4-of-12 and 14-of-17 predictions in
the open. When an executor declines to do something, that is signal. Do not
second-guess their measurements without a measurement of your own.

## The Director

He gates outcomes and his eye leads the instruments. This arc he accepted the
stroked asset ("I love it") and the finished sword ("Fantastic! I accept"),
ruled the mid-lane fifth-signature call in one sentence, chartered the polish
arc in one paragraph, and pushed the lane to main in five words. Momentum is
the instruction: when a shelf clears, produce the next deliverable and hand
him paste blocks, not status. Artifacts at full size, sheets walked,
exclusions labelled, never one you have not looked at. He appreciates the
work out loud — return it by keeping the record honest: the walks in plain
words, every time.

## Do not

End a session the Director has not ended · wrap while momentum is the
instruction · present any artifact you have not sheet-walked · accept
anything off a strip or thumbnail · `git add -A` in this shared copy, ever ·
edit an in-flight dispatch except by dated ⚠ amendment in place · retune
after seeing (withdraw instead) · run measured arms locally (TRELLIS
excepted) · raise the watchdog ceiling · touch the closed rulings, any
accepted asset, the four export trees, or the excluded twins except to cite ·
hand-edit docs/index/facet.db · share a verify and a commit in one call ·
roll a third seed at anything · open the activated state, the polish arc, or
any parked item before its named condition — the polish arc opens ONLY when
the MCP tools pass their tests, and the activated state opens INSIDE it.
