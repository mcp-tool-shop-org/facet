# The MCP spec session — four instruments from the facet record become product specs

**Written by the advisor, 2026-08-08, at Gate 1's close on the fourth asset.**
The spec-after-Gate-1 condition (banked in studio memory,
`facet-mcp-tool-candidates.md`) is met. This session SPECS; it builds nothing,
creates no repo, publishes nothing — the studio's repo-first rule governs when
building starts, and that is a later session.

## You are the spec author

```
cd E:\AI\facet && git pull
python tools/facet_index.py build            <- the E15 ritual; query the record via q
CLAUDE.md                                    <- the repo's discipline; your specs cite it
C:\Users\mikey\.claude\projects\F--AI\memory\memory\facet-mcp-tool-candidates.md
                                             <- the banked candidates + the opening decision
docs/experiments/E15-ruling.md               <- what the index IS (the four-leg gate, the law)
tools/facet_index.py                         <- the proven CLI the first spec wraps
E:\AI\ollama-intern-mcp\                     <- the studio's reference MCP form (42 job-shaped
                                                tools, error shapes, README standard) — read
                                                its README + one tool's implementation
E:\AI\repo-knowledge\                        <- the placement question's other half — read its
                                                README + MCP surface before deciding anything
```

Output: FOUR spec documents under `docs/specs/` in the facet repo (versioned,
in the record) — `index-mcp-spec.md` comprehensive; the other three at charter
depth. Plus one decision memo. The studio's workflow standards apply: each spec
carries a Standards-compliance block scoring the six standards, and every
workflow it defines names its compensators.

## Spec 1 — the context-index MCP (comprehensive; the others follow its pattern)

The facet index as a server: `build` / `verify` / `q` / `claims` as MCP tools
over any repo that adopts the record's conventions. What the spec must decide
and carry:

1. **THE PLACEMENT DECISION — the opening question, flagged in the banked
   memory**: standalone `@mcptoolshop/facet-index-mcp` vs a capability inside
   `repo-knowledge`. Decide it in a decision memo with the trade named both
   ways (repo-knowledge already owns "relational knowledge about repos" and has
   an MCP surface; standalone owns its release cadence and keeps facet's legal
   record discipline undiluted). A recommendation with grounds, Director
   overrulable in a sentence.
2. **The contract is the four-leg verify** — determinism, counts-vs-grep,
   pointer integrity, the seeded gate — as the server's health surface, not an
   optional extra. A `verify`-failing index refuses queries loudly (the gate is
   the product; this is the differentiator).
3. **The conventions boundary**: what a repo must LOOK like to be indexable
   (ruling documents by glob, the status-position law, the seeded-question
   process including measured-entry and withdrawn-never-recrafted). The spec
   states them as the adoption contract, citing the rulings that paid for each.
4. **Read-only by construction** — the server never writes the corpus; `build`
   writes only the DB artifact; the seeded set is repo-owned config, never
   server state.
5. The E15/E16 lessons as requirements: ASCII prints; a status read from a
   convention's position, never matched in prose; discovery by glob with the
   inverse guard; mention-vs-use named as a boundary.

## Specs 2–4 (charter depth: the job, the tools, the home, what it does NOT do)

- **Spec 2 — the mesh/texture measurement MCP**: `mesh_stats`, the reach
  ceilings, thin curves, off-surface classification, the anchored-regression
  pattern — facet's instruments as a service. Carries the instrument laws
  (quote both units; a statistic of angles is circular; denominators named).
- **Spec 3 — fixture-lint** (natural home: sdlab — say so and why): the
  identity-fixture grammar (noun-phrase elements, comma-free terms, occupancy
  audit completeness, colour-words-name-materials) as a lint. The E12/E14
  occupancy catches are its test corpus.
- **Spec 4 — comfy-preflight**: the inverted no-LoRA scan, link-topology check
  (self-links, dangling targets — the E04 G7 trap), `estimate_credits`
  discipline, graph-saved-is-graph-submitted (A30) — the cloud-submission
  pre-flight as a standalone gate.

## The study-swarm question

Spec 1 wraps a proven CLI — architecture is settled by the record; the
research-grounded protocol is NOT required for it. If any spec grows a
genuinely new product layer (e.g., the placement memo turns into a
knowledge-server architecture question), the global study-swarm trigger
applies as written — invoke it rather than improvising.

## Do not

Build anything · create repos or publish · edit facet tools, fixtures,
profiles, or the seeded set · write to the memory store (the advisor folds
the banked memory's update after ruling) · end a session the Director has
not ended.

## Then HALT

The four specs + the placement memo staged, to the advisor's eye, then the
Director's word on placement and build order.
