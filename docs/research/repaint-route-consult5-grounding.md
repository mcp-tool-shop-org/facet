# Repaint-route grounding — consult #5, calibrated and ruled (advisor, 2026-08-15)

Successor-arc grounding. E35 is closed (its ruling document carries the measured map);
this document folds the fifth Comfy-channel consult, its calibration at this seat, and
the advisor's ruling on the pilot ranking. The successor arc's kickoff cites this file;
nothing here spends a job.

## Calibration — PASSED, and it generalized

The consult's nominated checkable claim: *the served mesh-native texture node takes no
image input — identity for a specific person rides prompt text alone.* Verified at this
seat by direct schema fetch: **`Tencent3DTextureEditNode`** ("Hunyuan3D: 3D Texture
Edit") inputs are `model_3d` (FILE_3D_FBX/FILE_3D) + `prompt` (STRING) + `seed` (INT) —
no IMAGE input. **Exact.** The same fetch generalized the finding beyond the claim:
**every served partner texture path is identity-blind by schema** — `MeshyTextureNode`
conditions on `text_style_prompt` only; `TripoTextureNode` takes a `model_task_id`
whose texturing rides a text prompt at import. Three vendors, zero reference-image
inputs. For a route whose founding constraint is *the man stays the man*, the partner
texture tier is **CLOSED by schema**, independent of its licence bucket (which the
consult correctly refused to read and we did not resolve).

## The consult's landscape verdict, adopted

No local-weights atlas-native painter is served; no paint-the-atlas template exists
(both catalog filters returned zero, consult-verified). The recorded per-view
qwen-image base remains **the only route where identity is a first-class conditioned
input** — which keeps it the baseline to beat, exactly as E35 closed it. The UV-space
idea (unwrap, then img2img on the atlas) is recorded as fragile-by-evidence: qwen-image
has never seen UV layouts, and the E35 quantisation finding is the measured warning for
off-distribution input character. Not piloted.

## The ruled ranking — two one-job arms for the successor, in order

**Arm 1 — RELIGHT THE INIT (first; lowest identity risk, targets the dark class at its
source).** The consult's mechanism: the dark class is amplification of shading already
latent in the init; flatten the init's lighting and the prior has nothing to amplify.
Its identity-risk read is low (the init contributes ~0.5% reverted-at-0.92; identity
rides prompt + conditioning). **The implementation is ours, not the consult's**: the
init is our own Blender render — render the clay under **FLAT light** rig-side (the
repo's own `--flat` convention) instead of any served relight model. Zero new tools,
zero cloud prep. One correction to the consult's ceiling caveat, scoped at this seat:
it assumed the atlas's pre-baked dark texels ride the init — they do not; a repaint's
init is the clay render, and the dark class arrives via the model's painting, so the
ceiling does not apply to a full repaint. **Guard that binds**: a flat render is a NEW
input character for the recorded base — count unique colours before submission; below
the clean family (~5,000), one lanczos round-trip per the E35-measured repair. The E01
family law applies to every new consumer, including this one.

**Arm 2 — DUAL CONTROL, depth-early + canny (second; the strongest
mechanism-consistent pale prediction).** Consult #1's schedule, never tested on this
subject: depth preprocessor → union apply #1 (strength ~0.8, end ~0.45) → canny
preprocessor → union apply #2 (~0.4–0.5, full schedule) → the recorded sampler at
denoise 0.92. Type follows the preprocessor (the C-i enumeration); same union loaded
twice. The pale prediction is OUR measured direction (anchoring suppresses the class —
R2-c), which makes it the consult's least speculative claim; the dark prediction
(depth explains concavities, removing the excuse to paint AO) is mechanism-supported
and untested. **The failure signal to watch is register C\*, not identity** — two
anchors can starve chroma the way low denoise did. The slate's failed (c) was a
different topology (single depth at full strength) and does not pre-judge this.

**Both arms**: view 1, seed 770700, one job each, the recorded configuration as the
anchor (one variable per arm), blind bands with the register floor and the
chroma-split column, sheets beside the recorded twin, the Director's eye at each. If
both help independently, their combination is the third job. All at the Director's
word inside the successor arc's frame and budget.

## Q4, recorded as bounded

The quantisation-corruption mechanism stays UNCLAIMED. The consult's schema-level
negative is adopted: it is not a served parameter — the behaviour lives below the
schema surface. The studio holds trigger and repair; the mechanism is a measured
black box, and no arm depends on opening it.

## Consult ledger

Headline calls now 4-for-4 once checked (this consult's landscape verdict verified at
its own nominated claim and beyond it). Sub-mechanisms remain 0-for-3 from consults
#3–#4; consult #5 offered no unverified sub-mechanisms — its guesses were labelled as
guesses throughout, which is the shape the channel's round-2 rules were written to
produce. Every claim remains testimony until enumerated; the enumeration habit has
now upgraded or falsified something in every consult since #1.
