# Comfy consult #7 — the channel's answer, calibrated

**Relayed by the Director, 2026-08-15**, answering
[comfy-consult-7-brief.md](comfy-consult-7-brief.md). Answer held verbatim in the
relay; this fold records the calibration and the ledger.

## Calibration — the enum verified verbatim; the fetch caught a stale flag

The nominated claim (`ColorMatch`'s method enum `["mkl","hm","reinhard","mvgd",
"hm-mvgd-hm","hm-mkl-hm"]`, inputs and single IMAGE output) **verified exactly**
by live `get_node` fetch at this seat — and the same fetch surfaced what the
channel's read missed: **the node is flagged `deprecated` in the live catalog.**
Its successor **`ColorMatchV2`** is live, same pack, the full six-method family
plus `reinhard_lab_gpu`, not deprecated. Two further live alternatives carry
masks and blur scoping (`ImageColorMatchAdobe+` with `reference_mask`;
`ColorMatchImage`). **`QwenImageDiffsynthControlnet` verified EXACT** — core
pack, `model/patch/qwen`, model+model_patch+vae+image+strength with optional
MASK, returns MODEL, not deprecated.

Ledger: headline calls hold again (the harmonization road, the batching read,
the honest Q3 "nothing documented"); a sub-surface detail fell at enumeration
again, in the mildest form yet (a deprecation flag on a correctly-described
schema). The channel's own stop-and-tell-me clause did its job — reported back
with this fold.

## What the round establishes for the remake

1. **Cross-view coherence is structural, not a missing config** — Q4 answered
   plainly, matching our measurement. Consistency lives post-generation.
2. **The harmonization road is served and deterministic**: ColorMatchV2, one
   approved view as `image_ref`, byte-reproducible, no sampler.
3. **Per-region repair at the same seed is served** (masked conditioning +
   `noise_mask`), with its two named costs — mask-edge seams (feather) and
   in-mask tone drift (harmonize after every repaint).
4. **No documented sampler-level speck suppressant exists** — our measured
   cn 1.0 lever and census-driven masked repaint are the honest tools.
5. **The reference-anchor model patch exists and is verified** — held as a
   single-variable later test, not the critical path; its coupling strength on
   this subject is the channel's own flagged hypothesis.
