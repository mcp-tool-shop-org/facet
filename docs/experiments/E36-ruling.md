# E36 — ruling (advisor, 2026-08-15)

Opened and closed at one seat, same day. The arc dispatched, ran task 0's zero-cloud
work to completion, was ruled at its halt (the kickoff's Amendment 2), and was then
**superseded before its first cloud job by the Director's directive, relayed from the
armature seat 2026-08-15: remake the performer from source; no more repair arcs.**
The evidence base: [E36-open-manifest-halt.md](E36-open-manifest-halt.md),
[E36-task0-report.md](E36-task0-report.md) (committed `e53a695`), and the kickoff's
Amendment 2 (`9cf4ce7`).

## Ruling 1 — The arc CLOSES SUPERSEDED, at spend 0 of 15, with zero loose state

The directive's ground is the record's own: both remaining defect classes on the
performer are baked in at generation time — the dark specks are generator-painted
(E35 measured them to their floor; no lever removes them while keeping the man) and
the pure-black dots are mesh-pit shading, in no texel of atlas or GLB (E35 task 0a).
E36's two arms acted on the texture route of the old mesh; a remake supersedes both.
The close is clean by construction: **no cloud job ever fired, the blind bands were
never sealed, and nothing about the arms was seen or set** — there is no partial
result to dispose of. The successor is **E37, the remake arc**; its kickoff carries
the directive as its frame.

## Ruling 2 — What the arc banked is permanent, and none of it dies with the arms

1. **The open manifest halt and its lesson** — the E35 close's own verify receipt,
   written into the tree eleven seconds after the seal, the E28 self-reference family
   at the receipt layer. The Director ruled the receipt deleted; the seal was made
   true rather than rewritten; every receipt since lands outside every protected
   tree. The tool debt it named is specced at
   [tree-manifest-guard-spec.md](../specs/tree-manifest-guard-spec.md) and runs at
   the Director's word — the supersession does not touch it.
2. **The recovered clay invocation** — proven at 0 of 360,448 px before use, recorded
   where two arcs never wrote it (task 0 §2a). The byte-vs-pixel law's third live
   firing. E37 inherits the invocation as a recorded fact.
3. **The flat-control route fact** (Amendment 2, A2-1) — under flat light the canny
   term contributes nothing; a flat-lit Workbench render cannot feed a canny control
   on this route. Permanent, and it prices any future flat-lit variant.
4. **The withdrawn colour guard** (A2-2) — the ~5,000 bound was the 2509 encode's,
   not this route's; the quantisation-character CHECK stands for every new consumer,
   the number as a reported diagnostic, lanczos as a named mechanical repair.
5. **The instrument anchors** — pale and register instruments reproduce every
   published row bit-identically at full float precision (task 0 §3). E37's task 0
   re-runs the same anchor form.
6. **Premise 5's dead gate, owned** (A2-3) — the advisor specced a check that could
   not fail; the `LABEL=PATH` parser paper-cut rides the errand list.

## Ruling 3 — The corrector-contract track closes with the arc

The track's purpose was the 377-texel dark components on the OLD asset's texture,
above the 36 px² cap by design. A remade texture selected clean at generation makes
that repair target moot. The track was never specced and nothing is withdrawn; its
one durable design constraint — **any future corrector validates against
Director-rejected artifacts before any A/B** (the E07 metric law applied to
correctors) — is recorded here so a future texture arc does not re-derive it.

## Ruling 4 — Close mechanics

Docs-only close: E36 added no tests, so no count surface moves with this document —
the suite stands at its recorded counts and T71+ passes to E37. The status table
row carries the supersession; the E37 row is created in the same commit as its
kickoff; the span bump to `E3[0-7]` rides that commit under the thrice-proven rule.
Manifest state at close is the state task 0 measured: all four gates HELD after the
Director's receipt ruling, every receipt outside every protected tree. Spend ledger:
**0 of 15 cloud jobs; nothing was submitted in this arc.**
