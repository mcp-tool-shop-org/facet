# E16 — advisor ruling on the errand batch (2026-08-08)

Evidence — what this seat OPENED: the report in full
([E16-errands-report.md](E16-errands-report.md), closed at `abe62a9`), the blind
predictions (`30d60c5`, committed before the first tool was read into), the eleven
errand commits' stats, and this seat's own hands on the deliverable: the registry
sweep re-run here (beast and ship both **85/85 decided, exit 0, `_per_invocation 3`**
— E16-11's claim reproduced), the claims sweep at **0 STALE**, and build + verify
re-run below this fold **on a default cp1252 console with no encoding override** —
which is E16-1's repair exercised at the ruling's own hands.

## Ruling 1 — the batch is RATIFIED: ten anchors held, one honest halt (2026-08-08)

**1a — the ten landed errands are ACCEPTED as reported**, each anchor verified in
the form the dispatch asked or sharper: E16-1's repair went past the dispatch on the
executor's own blind prediction (the literal fix alone was insufficient — 17
cp1252-fatal DB cells; the stdout errors-handler repair keeps the record's own
characters as data and bends the console instead, which is the right boundary);
E16-2 isolated the line-ending effect on the DB **before** pinning (byte-identical
across the conversion); E16-3 audited every write path before touching a
citable-only tree (Ruling 33's ledger honoured) and tested the branch by running
the flood path, not by reading it; E16-4 repaired the two unprofiled callers in the
same commit and measured **why** the silent default was invisible (`W/h_ext = H/v_ext`
— a wrong frame renders identical logs, which is the reason the guard REFUSES
rather than warns); E16-6's consumer catch is the caption-proxy defect one tool
over, repaired by selecting on the floors — the gate-on-the-quantity law applied;
E16-7 preserved recorded reproducibility (`--exact-grid`) while repairing the
default; E16-8 factored to ONE background-model implementation at proven
bit-exactness; E16-9's synthetic miss was injected without writing a decoy into a
shared tree; E16-10's first-cut defect was caught by a printed median that could
not be right, and the per-structure decomposition is the global-constant law's
signature measured (0 of 89 and 0 of 159 admitted in the two thinnest strata,
+0 at 16–32 px); E16-11 enumerated before renaming and repaid E16-10's own
registry debt.

**1b — the executor's session, accounted.** The E16-1 byproduct finding was
**withdrawn in place under controlled re-test** — three reproductions and still
invalid, the *reproducibility-is-not-validity* law applied by an executor to their
own claim, which is the correction discipline at its strongest form this repo has
recorded. The stash misfire is owned with its lesson (adopted at Ruling 5d). The
predictions scored 9 held / 1 split / 1 falsified, with both misses the
informative ones — E16-8's rising view and E16-9's incomplete list — and neither
tuned around. **The standard holds.**

**1c — four dispatch corrections enter the ledger of the dispatch's author** (the
outgoing seat): `--frame` for `--aspect`; two conflated "N6"s; the missing
`--fit-axis height`; "report-only" for a probe carrying an armed ANDON. Dispatch
text is hypothesis — the arc's line, paid four more times, every catch by an
executor verifying text against source.

**1d — a cadence note, no action.** E16-11's mid-batch DB commit sat outside the
cadence's letter (neither a session boundary nor builder content); the close's DB
commit IS a session boundary and correct. Harmless — the DB is derived,
deterministic and verify-gated — noted so the next batch does not read the
mid-batch commit as the cadence.

## Ruling 2 — E16-5 is RULED: the honest condition ALONE; the galleon/beast silences are CORRECT; the dispatch's "unchanged" clause was the dispatch's error (2026-08-08)

The halt is RATIFIED — the executor was right not to resolve it, and the union
form's exact-anchor fit is precisely what convicts it (chosen after seeing what
each candidate produces). This ruling decides from what the warning is FOR, and
would be the same whatever the four measured numbers had been — the
pre-registration test, stated.

**2a — the decision: candidate 1, replace.** `mesh_stats` warns on
`rect_frac_of_figure > 1` and on nothing else. Grounds: (i) Ruling 2d's defect
class is a **geometric impossibility going unreported** — a face rect larger than
the figure — and `rect_frac > 1` states that impossibility directly; it is the
quantity, not a proxy (*gate on the failure itself*). (ii) The proxy's firings on
the galleon and beast answered a DIFFERENT question — *is this subject
character-shaped* — which the profiles already answer authoritatively and with
provenance (`mesh_gate: none`, `head_rect_metrics: false`, the ship's own
`_tools_not_on_route` text: "the character instrument correctly noticing it is
not looking at a character"). A per-run warning re-litigating a decided profile
value is a second authority in miniature. **The subject-class question lives in
the profile; the instrument's warning covers geometric degeneracy, which no
profile value carries.** (iii) The executor's prior question is thereby answered:
the warning does not need a subject class — the route already HAS one, and it is
the profile. One mask cannot answer two questions; one warning cannot either.

**2b — the repair's anchor, pre-stated from the report's own measured table**:
every VALUE unchanged; W3 silent (0.680787); galleon silent (0.327707); beast
silent (0.568773); the sword WARNS (1.902512). The warning text names the
condition ("face rect exceeds the figure"), not the proxy. **Executes in E17 as
T12** (the kickoff is amended in place this fold), with the warning's test in the
same commit under the tests law.

**2c — the "unchanged" clause joins 1c's ledger**: it inherited the proxy's
incidental behaviour as if it were specified behaviour. The executor's prediction
5 pre-named exactly this doubt and held.

## Ruling 3 — the twelfth errand is RULED IN: leg 1's fixed temp paths gain per-process uniqueness (2026-08-08)

The report's diagnosis is adopted: `facet.db.det_a`/`.det_b` are fixed names
(`facet_index.py:1681–1682`), so two verifies in one working copy write the same
files — the race this seat and E16-1 both hit live, from opposite sides,
independently. The repair: per-process-unique temp names in the same directory.
The anchor: leg 1 byte-identity unchanged on a quiet tree, and the collision made
**impossible by construction** rather than retried — *prefer eliminating a risk to
gating it*. **Executes in E17 as T13**, test in the same commit.

## Ruling 4 — the queue disposed (2026-08-08)

**4a — `edge-frac` is CODE-CLASS; `edge-mode` is SUBJECT-DATA.** The fraction is
A3's derived law (erosion never exceeds a third of a structure's own half-width)
— the classification `project_twins` already carries in the sweep's CODE_ROWS,
verbatim. The mode is a per-subject adoption decision and stays declared (a mode
must never arrive by silence; E16-11's entries stand, and the profile `edge-frac`
entries stand as protective transcription). The CODE_ROWS row addition for
`texpass_iter` is **E17 T14** — a sweep edit wants the sweep's own before/after
anchor.

**4b — `finalize.json`'s structural zero: QUEUED for the next errand window**,
with the repair form ruled now so it is not re-derived: a sibling boolean
(`mean_fallback_structural: true`) emitted in surface-aware mode, the numeric
field unchanged for existing consumers; anchor: `atlas_final.png` byte-identical,
the JSON delta reported. Not E17's — the harness anchors on today's bytes first.

**4c — the greedy winner's report shape is RULED**: on a field tied inside the
density drift (four candidates within 0.001 points while density moves 0.6) the
tool reports the TIE — "four candidates tied at ~55.65%, pick on other grounds" —
never a WINNER. Queued for the next errand window with `--exact-grid` preserving
every recorded run. No closed ruling is re-litigated: elevated NONE stands on the
marginal everywhere it was ruled, and the marginal is what the denser ladder
confirms small.

**4d — "converged" is corrected in place this fold** (prop.json's elevated
grounds): the repaired instrument's ladder still climbs at 40 rays/face
(53.920% → 54.849%), so the record's label overstated; **E14 Ruling 8's decision
STANDS** — it rests on the marginal gain, which stays small at every measured
density.

**4e — `character.json`'s `bg-max-pct 2.0` is WITHDRAWN to the expressed
suspension (100.0) this fold.** Its stated derivation — "bounded from this
subject's measured distribution" — was measured against the corner-median
reference E16-8 just retired, so the derivation no longer describes the quantity
the probe reports. *A condition whose stated derivation does not describe it was
never a threshold*; withdrawing is not choosing a new number. RE-ARM CONDITION:
derive from clean data when the polish arc's W3 re-make projects.

**4f — `ship.json`'s inert top-level `_not_on_route` duplicate is REMOVED this
fold.** Dead config an instrument does not read invites drift; its text survives
in git history and in this ruling's evidence trail; anchor: the ship sweep
identical (85/85, exit 0), verified below.

## Ruling 5 — findings BANKED route-wide (2026-08-08)

**5a — the retirement's fourth grounds** (E16-8): the corner median did not
merely mis-measure — it under-reported risk worst exactly where risk
concentrated, scoring the edge-on blade (yaw 270, the ribbon the stroke lane
existed for) as the safest of seven views at 6.23% against a true 19.87%.
Appended to the corner-median retirement's record.

**5b — discovered-not-listed, second instrument**: the handoffs table's
hardcoded list hid E15's two handoffs from the table AND the gate (29 → 31 rows);
the glob + inverse guard is now the form in both places it was needed.

**5c — the silent-sameness mechanism** (E16-4): under height-fit, horizontal
pixel scale is independent of frame width, so a wrong frame renders
log-identical output. Banked as the reason emit refuses rather than warns.

**5d — the shared-tree rules are ADOPTED as standing practice**: no in-repo DB
sha is an anchor (fresh scratch paths only); explicit `git add` paths only; the
DB stays out of mid-batch commits; and **in a shared working tree, do not use
the stash** (the executor's lesson, owned and adopted).

**5e — data is not a literal** (E16-1): an instrument that quotes the record
keeps the record's own characters and bends the console's error handling, never
the data. The AST-scoped repair that protected the parser regex and the
DB-feeding ellipsis is the pattern.

## Ruling 6 — E17 is AMENDED IN PLACE and unblocked (2026-08-08)

The E17 kickoff gains a dated amendment this fold: **T12** (the E16-5 repair per
Ruling 2, its pre-stated anchor and its test in one commit), **T13** (the det_a
repair per Ruling 3), **T14** (the CODE_ROWS row per 4a), **T15** (E16 and E17
arcs enter verify's count legs with their own greps — the E15 Ruling 8b pattern —
if session budget allows, else the next errand window), and the E16-11 port is
un-excluded (its anchor is `c284693`). The E16-5 test port excluded at dispatch
is now specified by Ruling 2b.

## Ruling 7 — the batch CLOSES; the roadmap advances (2026-08-08)

Ten repairs landed, one halt ruled, zero credits, every anchor either held or
honestly failed. With the MCP specs landed and ruled earlier today, **both
in-flight sessions of Ruling 35's step 1 are now landed and ruled.** Step 2's
order, revised by the Director's same-day word: **the E17 harness first**, then
the index-MCP build kickoff drafts **on the E17 ruling** — the build lands on a
repo that already has tests. The push carrying this ruling carries the batch's
last two commits (`c284693`, `abe62a9`).

## Standards compliance (this ruling)

| standard | score | evidence |
|---|---|---|
| PIN_PER_STEP | 3 | every ruling cites the report section, the commit, or this seat's own re-run; the E16-5 anchor is pre-stated from measured values before its repair executes |
| ANDON_AUTHORITY | 3 | the one failed anchor was halted, reported, and is ruled here rather than tuned past; the det_a race is eliminated by construction, not retried |
| NAMED_COMPENSATORS | 2 | every disposition is per-commit revertible; the two profile withdrawals name their re-arm conditions; no publish, no spend. Scored 2: the compensators are stated, none needed exercising |
| DECOMPOSE_BY_SECRETS | 3 | repairs routed by what owns them — instrument questions to tools, subject questions to profiles, class questions to the classification table, harness work to E17 |
| UNCERTAINTY_GATED_HUMANS | 2 | 4b/4c are queued with their forms ruled so no future session re-derives them; the Director's windows stay open on every disposition, one sentence each |
| EXTERNAL_VERIFIER | 3 | the acceptance evidence includes this seat's independent re-runs (sweep, claims, verify on the default console) and the batch's own anchors were recorded artifacts, not the tools' claims about themselves |
