# E69 — the same write-gate, every face: the garment-edge fail

**Advisor spec, 2026-08-19. One executor seat (Sonnet), background. Tree
`E:\AI\training\facet_E69\`. ZERO CLOUD. ZERO THRESHOLD CHANGES. Prep stays halted until
this sitting's numbers exist.**

## TWO FAILS, ONE FAMILY, TWO SEATS — the Director's framing, and it corrects the advisor's

The advisor's E68 summary ran the two together as one continuing problem. They are not one
story:

| | hair — **DONE** | garment edge — **OPEN, this arc** |
|---|---|---|
| colour on the map | amber → gone | magenta + blue, unchanged |
| count | 21,915 → **0** | **5,846 still flagged** |
| next | none | the same write-gate, **not head-band-only** |

**Amber** was mesh hair in front of twin wall: 21,915 pixels, removed by E68's withhold with
set-identity proof, now holes at 0.27% of the atlas stacked at the crown. That is the
correct outcome for *paint bigger than geometry*. **The 2% ratio still failing is the
moving-denominator law, not evidence the withhold missed. Do not put the yellow back. Do not
loosen 2%.**

**Magenta and blue** on the vest opening, collar, sleeve and fingers are the 5,193
unclassified plus 653 class-1 that E68 was **forbidden to touch** — median 306 px from the
head box. Same mechanism (mesh silhouette a little fat of the paint, so a texel looks up
wall or near-wall) at a different place: garment edge, not hair. Views 1 and 7 carry more of
it at the collar. **That is why the ANDON is still honest and prep is still halted.**

## The change — the same rule, unrestricted

Apply **E68's existing `--headband-bg-withhold` write-gate to every face**, not just the
head band. Geometry says figure, paint says wall → hole. **The background test remains
`project_twins`' own `dE_bg` at its own window** — no new constant, exactly as E68.
Implement as a widened scope on the existing flag (or a sibling flag), default off, with the
default path proven byte-identical to pre-E69.

## Forbidden, each for a stated reason

- **No colour key.** Corner-median has failed four times here; the backdrop spans
  L\* 48.14–81.12.
- **Do NOT import W3's edge-dist 7.0.** It is a spatial guess, and we already know the exact
  leftover texels — a measured set beats a guessed distance. (Same family as the greatsword's
  thin-extent, refused at E67.)
- **Do not retune `bg-max-pct` 2.0 or `bg-de` 10.** Assert both byte-unchanged in code and
  print them.
- Do not put the hair withhold back, do not remesh, do not regenerate, do not brush.

## Measurement — E68's own method, unchanged

- Flagged mass before/after, pooled and per view, with **set-identity** proof of what moved
  and what did not.
- **Hole cost measured E68's way**: marginal fraction, per view and pooled, and *where the
  new holes sit* — by region, so a hole at the collar reads differently from one at a fingertip.
- Per-view flagged percentage against the untouched 2.0% limit, reported whether or not it
  passes.

## The two outcomes, both pre-registered

- **If hole cost stays small and the leftover set collapses** — prep can *talk about* a first
  brush. Talking is the Director's; this seat does not open it.
- **If the silhouette is systematically fat** — that is a **mesh/paint registration
  question**, and it is still **not** a looser ANDON. Report the evidence for systematic
  fatness (is it uniform around the figure, or concentrated at particular structures?) and
  halt there.

## Gates

- **Gate A** — no cloud call.
- **Gate B** — `bg-de` and `bg-max-pct` byte-unchanged, asserted in code and printed.
- **Gate C** — default path byte-identical to pre-E69.

## ⚑ SHEET FOOTER — standing requirement, the Director's words

Every sheet this line produces carries these two sentences in its footer, verbatim:

> The warm rim light in the twins is still paint.
> The overlay dots are still the map.

They exist because a future reader will otherwise take the overlay for a rendering artifact,
or the paint's own rim for contamination.

## Standards compliance

1. **PIN_PER_STEP — 3.** Same mesh, ring, cameras and thresholds; one scope widens.
2. **ANDON_AUTHORITY — 2.** Gates halt; a failing limit is a reported result, never a tuning
   opportunity.
3. **NAMED_COMPENSATORS — 2.** Zero spend; default-off flag; repo edits by pathspec.
4. **DECOMPOSE_BY_SECRETS — 2.** Rule, bake and remap separable.
5. **UNCERTAINTY_GATED_HUMANS — 2.** Prep stays halted; the brush remains the Director's.
6. **EXTERNAL_VERIFIER — 3.** E67's unchanged classifier judges the repair, and it was never
   tuned for either side.
