# Experiments

Each experiment is a spec written by an advisor session, run by a separate executor
session, and judged by the Director. The separation is deliberate: the session that
designs an experiment does not get to grade its own results, and the session that runs
it does not get to decide what the results mean.

**A spec is written before the work. A report is written after. Conclusions come last,
from the advisor, only once the Director has seen the evidence.**

| id | question | status |
|---|---|---|
| [E01](E01-facial-structure-ceiling.md) | Where is the facial-structure ceiling — framing, generation resolution, generator, or reconstruction itself? Does any configuration produce a connected surface rather than shell soup? | **RULED** → [Gate 1 ruling](E01-ruling-gate1.md) |
| [E02](E02-texture-stage-on-sound-inputs.md) | Is the texture stage sound when its inputs are? Every texture result in this project was produced on a blob mesh with twins belonging to a different silhouette. | **RULED — answer is NO** → [Gate 1 ruling](E02-ruling-gate1.md). Director rejected with every input sound. Two twins + eight cameras paint only ~25% of the surface; the rest is interpolated across a 35,070-island atlas where neighbours are unrelated pieces of the model. Next target is the UV unwrap, not the brush. |
| [E05](E05-paint-more-surface.md) | Why is three quarters of the asset interpolated rather than painted? | **RULED** → [Gate 0](E05-gate0.md). Neither lever moved it. **49% of valid atlas texels are never visible from any of 46 exterior cameras** — half the surface the atlas pays for is not on the outside of the model. Native xatlas UVs adopted permanently |
| [E06](E06-cull-the-unseeable.md) | Does culling exterior-invisible faces before unwrapping fix coverage, atlas efficiency and the artifact mechanism together? | **RULED** → [report](E06-report.md), [Gate 1 ruling](E06-ruling-gate1.md). Culling works and the blade is fixed by the Director's eye. Interpolation fell **68%** (2,551,893 → 813,773 dilated texels), brush share 27% → **52.7%**. Two predictions falsified: density rose ~17% not 2×, because **charts fragment under culling rather than merging**. Atlas layout is now the binding constraint |
| [E07](E07-the-atlas-is-not-a-neighbourhood.md) | The loop writes colour from two places and neither knows where a texel's neighbours are *on the surface*. Does giving it one remove the defect class the Director named? | **RULED — CLOSED, neither arm adopted** → [Gate 1 ruling](E07-ruling-gate1.md). Director: *"the images don't look good"*, and on being asked which defect, **"all of it — the asset is not close"**. The finding is a measurement failure: **four of the five units this experiment graded on are 5×5 high-pass statistics** and the defect that decides acceptance is a *large region of the wrong material*, which is smooth inside itself and registers only its rim. L1 cut dilation source distance **70×**, took mean fallback 734 → 0 and speckle below A0 — and changed nothing to the eye. Structural read: **28.4% twins / 37.7% brush invention / 33.9% interpolation** — four experiments improved how the other 71.6% is filled, none reduced it. Next is a free geometry pass on the twin-projection ceiling at N = 2/4/6/8 views, **not** another fill arm. Evidence: [Gate 0](E07-gate0.md), [Gate 0.5](E07-gate0_5.md), [report](E07-report.md), [Amendments 1-2](E07-the-atlas-is-not-a-neighbourhood.md). Both premises held: **74.9%** of dilated texels took colour from another island, from a median **0.177 away on a figure 1.0 tall**; step ratio **5.500**. **L1** passes the restated gate (source distance **0.87 edges**, 0.004% beyond 20), mean fallback **734 → 0**, speckle **2.64/1.07/0.23 → 2.43/0.91/0.16**, 10.0% of turnaround figure pixels changed and concentrated on the back half — but its head-zoom pass condition is **not met** (3,372 vs <3,100; dilation 759 vs <400). **L2 re-anchored on the accepted set** moved the ratio 5.500 → **2.600** and blotch **−53.5%**, then **tripped the flattening ANDON at −10.66%** (limit −5%) with the denominator inflating 4q → 5q; GPU not authorised. Correction to the spec: **the gutter is not the mechanism** — only 32.5% of paths cross one, and the minimal `& valid` patch still leaves 53.3% cross-island while stranding 174,898 texels |
| [E08](E08-cover-the-figure-with-reference.md) | Can the styled reference cover the figure — and if it does, does the defect the Director rejected go with it? | **RULED — CLOSED, ACCEPTED at Gate 1** (2026-08-04, on the GLB at the Director's own zoom) → [E08-ruling-gate0.md](E08-ruling-gate0.md), 35 amendments. Measured provenance **68.8% reference / 4.2% brush / 27.0% dilation** against the rejected asset's 28.4 / 37.7 / 33.9. The first accepted asset. *The archaeology below is the arc's evidence trail as it accumulated:* [Gate 0](E08-gate0.md). **Half 1 passes**: 8 cameras reach **74.10%** of valid texels (halt was 60%), but A2's 80% is unreachable — 12 cameras + 2 elevated at the loosest threshold still gives 79.06%. The two SIDES add more surface than the four diagonals (267,176 vs 247,979), falsifying A1b. **Half 2's construction is impossible**: hold-one-out needs ≥2 cameras per texel and the two twins overlap in **exactly 0** — `facing_front = −Ny` and `facing_back = +Ny` cannot both clear 0.45. Viable from N=4, strong at N=8 (79.9% of reachable). Substituted **reference-agreement ΔE in CIE Lab**, which validates: null at **ΔE 0.72** where the asset carries the reference, **23.18** brush, **18.70** dilation, separating every region the Director named from the controls by **18–65×** — the blade's non-reference surface reads neutral steel (123,122,124) → flesh (147,97,63), median ΔE **32.71**. Severity tracks reference coverage: blade 62% non-reference → ΔE 23.17; tunic 1.2% → ΔE 0.49. ⚠ The acceptance stage discards **46.2%** of what the cameras physically reach, so 8 views likely buy ~40% reference coverage, not 74%  · **[Arm A](E08-armA.md) run**: the reorder is a **no-op** (comparative ownership is byte-identical to the absolute gate at three floors — the shipped loop already falls back to a worse-facing view), and the facing floor is not the lever either (0.45 → 0.05 buys +1.31 points). The cost is the other two tests: **EDGE +211,087**, **MASK +257,511** texels if removed. **The saved `*_mask.png` is missing 24% of the mesh silhouette** — 146,356 px true vs 111,602 used, IoU 0.76, loss *interior* (a stripe down the whole blade), from `figure_mask` keying a grey-on-grey clay render; registration ruled out at shift (0,0). And `project_twins`' "twin is fatter than the mesh (15.8% vs 9.9%)" compared the twin against **that same broken mask** — against the true silhouette it is **twin 17.43% vs mesh 19.01%, IoU 0.911**, so the mesh is fatter and the premise making the EDGE test safe is void  · **[Arm A2](E08-armA2.md) built**: `project_twins` now answers *is there surface here* from **geometry** (raycast silhouette) instead of a threshold on the clay render; `--mask-keyed` reproduces every prior arm byte-for-byte (sha `b12917a2c7c14c4b`). **Reference coverage 681,212 → 938,718 texels, 28.4% → 39.1% of valid, 53.8% → 74.2% of reachable**, with `lost 0` — strictly additive, no diffusion, no GPU. Failure mode checked: recovered texels sit at median ΔE 38.31 from the twin background, 0.18% within ΔE 10, *cleaner* than the already-trusted set (0.32%). The blade is still hole — now held back by the EDGE test, whose justification Arm A voided. **Coverage is not quality**: a stage-1 ΔE grade is vacuous by construction, so whether the rejected defect goes with it is unanswered and needs the loop  · **[Arm A3](E08-armA3.md) HALTED** on the andon Amendment 3 placed, which caught a real defect on its first live run. The invariant holds exactly (max e/R = 0.3333, 0 violations) and does what it was built for — the blade's 4-8px stratum goes from **77.6% removed to 33.5%**. But the background probe fired: **75.13%** of newly-admitted texels sit within ΔE 10 of the twin's background (median ΔE 4.9) against **0.11%** for the already-trusted set. Probe validated both ways first — it fires on a deliberately loose build (49.45%), and the twin's own paint is clear of background everywhere (blade median ΔE **24.80**), falsifying my hypothesis that it was flagging grey steel. **The mechanism inverts the story**: the twin's keyed mask is contaminated with background at **21.3% / 16.4%** in the 1-2 / 2-4px strata, falling to 0.1% at 32px+ — E01's keying failure, alive and *thin*. The absolute erosion was **accidentally correct**: it deleted 100% of those strata, removing the contaminated tendrils along with the blade. The invariant preserves thin structure proportionally, so it preserves both — they live at the same scale and half-width cannot tell a blade from a shadow tendril. No A3 atlas written  · **[Arm A4](E08-armA4.md) HALTED at its own precondition — a full negative result.** The colour discriminator was to be derived from the measured bimodality; **there is no bimodality.** ΔE-from-background over the twin figure rises **monotonically 5→30 with no antimode**, and the two views disagree on whether a weak one exists. The "gap between 10 and 25" came from comparing two summary statistics (region medians ~25 vs the contaminated median 4.9) — distant medians do not imply a gap in the density. Otsu is the wrong tool and its own output says so: cut **33.6**, class means 25.8/41.6 — dark paint vs light paint — rejecting 41,194px at **median depth 8px**, deep interior, because the contaminated class is 0.5% of the mask. And colour is not a proxy for boundary here: only **47%** of sub-ΔE-12 pixels lie within 2px of the edge. Restricting to thin strata does not rescue it — 16–28% of those pixels are legitimate paint above ΔE 25. The gate cost one measurement and no build. Measured support for the unspecified arm: the background is mid-grey and the subject contains mid-grey steel and leather, so a background outside the subject's gamut would *create* the separation rather than threshold a distribution that has none  · **[Background arm](E08-armBG.md) HALTED — the separation appears and the reference repaints.** Colour derived, not chosen: **rgb (0,0,255), min ΔE 123.31** to the subject gamut, where the current grey sits at **6.00** — below the ΔE 10 line, which is A4's failure in one number. Clay re-render byte-identical to the shipped views; `turn_render` gained `--bg` with the default unchanged. **Separation appears and both views agree**: min ΔE to gamut 0.00/0.20 (grey, literally inside it) → **54.15/61.05** (blue), and the render background beat an explicit "plain grey background" prompt term at denoise 0.92. Clay keying fixed at source as a side effect (9.9% → **18.1%** vs a 19.01% true silhouette). **But the subject repaints**: median ΔE **14.30/11.41** inside the exact silhouette, 69.9%/56.1% above ΔE 10 — and at material level on terms the prompt held byte-identical: gold knee plates **gone**, charcoal boots → **brown fur**, wine-red skirt → **green**. All three executor predictions falsified. Pre-registered reading applies: **the absolute erosion's cost at the blade is necessary, not accidental** — no cheaper route survived (A3 shape-blind, A4 no bimodality, background repaints the reference)  · **[BG2](E08-armBG2.md)**: control built from the exact mesh silhouette, so the contour term is **identical across arms (9,958 px both)** and total control divergence is **0.13–0.22%** of lit pixels. The repaint largely goes with it — median ΔE **14.30 → 8.19** (front) and **11.41 → 6.06** (back), and the **ΔE > 25 gross-material share collapses ~10×, 19.1%/23.2% → 2.3%/2.1%**. By eye the two arms now carry the same materials, and **BG2-grey matches BG1-*blue*, not BG1-grey** — which localises BG1's relocation to the control, as pre-registered. Separation survives (0.35/0.40 → **58.44/64.16**). Lands **between** Amendment 6's two branches: gross relocation gone, a tonal residual remains (38.3%/21.9% above ΔE 10). Reported, not resolved — the Director has not seen these twins. `figure_mask` is now retired from every path that governs anything  · **[Registration](E08-registration.md)**: measured against the exact silhouette (19.01%, bbox 849×388) — E01's own criterion. The corner-median key **failed loudly first** (painted 31–76%, bbox 751px in a 752px frame) because diffusion paints a lit gradient, not a flat field; replaced with a fitted quadratic background, which leaves the shipped twin's numbers unmoved. Clean grey-vs-grey isolation: **BG2-grey registers best on 5 of 6 cells** (IoU **0.9314 / 0.9222** vs shipped 0.9088 / 0.8900 and BG1-grey 0.9040 / 0.8638), painted fraction closest to truth (18.95 / 19.93 vs the shipped 17.38 / 17.01, both undersized), stable across three tolerances. **The extra control was noise**: BG1-grey carried 3.4× the contour (33,026 vs 9,958 px) and registers worse everywhere. Blue twins **not reportable** — IoU swings 0.61→0.92 on threshold alone, so no figure is quoted. Result splits: the geometry control clears the pre-registered bar; the blue background is undecidable on this axis  · **[A2 re-run](E08-armA2R.md)**: BG2-grey twins + fitted keying ship together. Reference coverage **28.4% → 39.1% → 43.0% of valid**, and **53.8% → 74.2% → 81.6% of reachable** (ceiling 52.66% of valid). Decomposed rather than asserted: the estimator alone moves **−0.1 pt** on the shipped twins (it reduces to a corner median on a flat field, so nothing prior loses comparability) — **the entire +4.0 pt is the better-registered twin**. Guards: `reachable` invariant held at 1,265,391 exactly; the bbox andon passed within 1.2% (843×385 / 859×386 vs 849×388). ⚠ Not strictly additive — 148,693 newly styled, **54,978 lost**, net +93,715 — and the newly-styled set sits at **2.03%** within ΔE 10 of background against 0.26–0.28% for the trusted set. Reported, not ruled on. All three predictions held  · ⚖ **[DIRECTOR'S CANON RULING](E08-director-canon-ruling.md)** — *"the image to the left is the character that I originally designed. I'll take the gold over the fur, because the character is on point."* **Identity dominates registration.** BG2-grey wins every stable registration measure and is visibly more legible, and it is **the wrong man**. **A2R is withdrawn as off-canon** — its +4.0 pts were bought with a different character. **The geometry-derived CONTROL is withdrawn as a twin-generation path**: same mesh, seed and prompt, only the ControlNet input changed, and the character changed with it. **A2 stands unchanged at 28.4% → 39.1%** — it never touched the twins. Amendment 4's original halt (*"an arm that improves keying by changing the reference has broken the thing it was protecting"*) was right and the reframing away from it was wrong; *"better registration is a better reference, no taste required"* is falsified. ⚠ **The provenance defect is now critical**: the canon twin cannot be regenerated — built identically it does not reproduce (IoU 0.9040 vs 0.9088) — so the asset the route exists to carry is a file we can copy and cannot recreate  · **[Armour test](E08-armour-test.md) — canon CAN be stated in the prompt.** One term inserted ("gold knee plates"), control image byte-matched at 20,973 px, nothing else changed: **the plates returned**, correctly placed, with the clean control's legibility kept. All three executor predictions falsified. The tension the canon ruling exposed largely dissolves — canon from the prompt, control free to be clean — and it splits Amendment 8 precisely: the surplus control was **noise for registration and signal for identity**. Canon pair frozen under `canon/` with an incomplete-by-record MANIFEST; `restylize_views` gains versioned per-view prompts and a provenance sidecar so this cannot recur. Open: the forearm bracer (same one-term fix, untried)  · **Bracer test — SPLIT**, and it is the first crack in the specification premise: with a byte-matched control, *"brown leather"* landed (fur cuff → smooth segmented leather) and **"gold-trimmed" did not**. Two one-term tests now stand at **one full, one split**. Not tuned — per Amendment 11, adding terms until the element appears would fit the spec to the outcome. `canon/MANIFEST.md` records the **role demotion**: the pair is a SPECIFICATION SOURCE, not the projection reference, so its irreproducibility stops being load-bearing — a specification reproduces where an artifact does not. **[Spec proposal](E08-canon-spec-proposal.md)** is up for ratification: 5 proposed additions read off the canon twin (belt medallion — which the prompt appears to misname as a *"gold necklace"* that is not there — sword crossguard, plate fur trim, green skirt layer, pauldron scrollwork), plus the proportions explicitly marked NOT canon  · **[N11 grammar test](E08-N11-grammar-test.md) — DROPPED, stopping rule fires.** Promoting the gold trim from a modifier to its own head noun ("a gold plate on each outer forearm") produced **no plate and no measurable change**: median ΔE **1.07** whole-figure, 1.15 on the forearm. The model did not respond to the phrase at all. Both the advisor's prediction and mine falsified. **Arm B waits**; the specification premise is weaker than Amendment 12 assumed. Live alternative, pre-registered before the roll and NOT tested here per the stopping rule: every element that **replaced** what occupied its surface landed (knee plates, leather bracers); every attempt to **add** a second element to an occupied surface dropped, in both grammatical forms |
| E09 | Chart fragmentation — the binding constraint on texel density. Culling removed 47% of faces but only 34% of charts. Remeshing is the untested candidate. | **deprioritised by E07's ruling** — density and softness are a different axis from wrong material in the right shape. Was E08; renumbered 2026-08-05 |
| E03 | How does the bust crop's facial geometry reach the full-figure mesh — head graft, or detail transfer? | unblocked (E02 ruled); not scheduled — E12 Ruling 2 measured a beast head with legible native structure, so the bust-crop question currently has no live subject |
| [E04](E04-the-galleon-through-the-route.md) | Does the route hold on a **non-character subject**? A galleon: landscape framing, no face to anchor density on, and thin rigging everywhere. | **RULED — CLOSED, ACCEPTED at Gate 1** (2026-08-05, "it looks good to me," on the five-column sheets) → [E04-ruling.md](E04-ruling.md), 29 rulings + the close (*the count read "28" until E15's verifier counted the record*). Mix **36.89 / 6.87 / 56.24** = **86.4% of the pre-registered 42.72% ceiling**; zero credits across every generation. The central hypothesis (*no shared-code edit needed*) was **falsified five times, and that was the payoff** — each falsification hardened the profile system. The second accepted asset. On-surface restatement of every headline: [E10-offsurface-ruling.md](E10-offsurface-ruling.md) |
| [E10](E10-environment-contact-layers.md) | Can an environment-contact effect (the Director's waterline) ship as **data, not geometry** — the GLB whole, the scene's water doing the hiding? | **RULED — CLOSED** (2026-08-05, "that looks about right," confirmed against a photograph of a real galleon) → [E10-ruling.md](E10-ruling.md), 12 rulings, four generations, zero credits. The law, measured across eight arms: **inpainting continues an asset; full-frame generation introduces new material; layers fill by masked projection** (−1.6 to −4.1 ΔL\* inpaint against +33.4 full-frame, one field changed). `waterline_z` rides in profile + manifest with four independent confirmations. Ruling 4's queued `pos.npy` dispatch ran post-close: **the off-surface 2.5% is painted, not padding** — every headline restated on-surface beside the as-recorded family → [E10-offsurface-ruling.md](E10-offsurface-ruling.md) |
| [E11](E11-dense-turnaround-export.md) | Do accepted assets become **training data** — a dense-turnaround export the sdlab lane validates without edits? | **RUN AND RULED** (2026-08-05) → [E11-report.md](E11-report.md), [E11-ruling.md](E11-ruling.md). All four hypotheses confirmed: the export is a pure function, the shared views reproduce the record byte-identically, indexed conversion is lossless per file, and **both dense trees validate through the lane with zero schema edits** (galleon 28-cam with the native owner channel; W3 26-cam, owner honestly absent). Ruled: the emit render generation trains; flat-only is the honest export; backgrounds are augmentation-side via exact silhouettes |
| [E12](E12-executor-kickoff.md) | Does the route hold its **third subject class** — a beast: wing-membrane SHEETS, filament horns and spines, a live head/allocation question, scale relief? | **RULED — CLOSED, ACCEPTED at Gate 1** (2026-08-07, "Fantastic!! This is really good!", on the GLB at the Director's own zoom) → [E12-ruling.md](E12-ruling.md), Rulings 1–30; [Gate 0 report](E12-gate0-report.md). **The third accepted asset, zero credits across the entire arc.** Sixteen handoffs from designation ("3 is the winner") to acceptance in three days: the register earned (ultra-realistic, NO LoRA — the style-register-is-subject-data ruling), the occupancy-complete fixture, the accepted pair reproducing pixel-exact, harmonization adopted, stage 1 at 87.5% of the pre-registered 50.46% ceiling, four no-LoRA strokes, mix **44.15 / 3.07 / 52.78 of valid = 87.49 / 2.86 / 9.65 of reachable**. Now **dataset asset #3** (dense export + live ingest, the first manifest born declaring subject/style/tone-transform/derivation under the lane's 1.3.0). The finalization arm is a register-LoRA retexture, parked on dataset maturity (Ruling 28a) |

| [E13](E13-detail-pass-spec.md) | Do crop-framed twins, projected first through crop cameras, deliver crisper detail-region texture than full-figure twins alone? | **RULED at E12 Ruling 24** — split verdict: the **capability is banked permanently** (crop-camera projection anchored at zero changed pixels, twice; the frame-bounds and width-fit fixes) while the **generation half is falsified for now** (crop paint drifts register at bust frames — frame-changes-register, third measured instance; reference-conditioning is the named future lever). The A2 finding — the 4096 atlas under-resolves the head at BOTH frames — made the per-subject head rect the primary head lever, specced and parked at the Director's timing |
| [E14](E14-executor-kickoff.md) | Does the route hold its **fourth subject class** — a prop: a near-2D blade at figure scale, hard facets, a helical wrap, steel's grey-on-grey? | **RULED — CLOSED, ACCEPTED at Gate 1** (2026-08-08, "Fantastic! I accept", on `longsword_hero.glb` at the Director's own zoom) → [E14-ruling.md](E14-ruling.md), Rulings 1–35. **The fourth accepted asset, zero credits designation to acceptance.** The drifted stone returned to garnet by arithmetic, not regeneration (the re-projection, Rulings 25–27); eight strokes at zero re-rolls; finalize replayed byte-for-byte; **dense export at zero gap notices → INGESTED as dataset asset #4** (114 records across five ingests; the lane pushed to main at the Director's word); **the polish arc chartered** (Ruling 35). *This row read "STAGED, gate 0 dispatched" until 2026-08-08 — the arc outran its row and the sweep cannot see prose staleness; the status-table duty is the advisor's* |
| [E15](E15-context-index-kickoff.md) | Can the record be indexed — SQLite+FTS5, generated, verify-gated — so sessions query instead of reading six hundred lines? | **RUN AND RULED** (2026-08-07) → [E15-report.md](E15-report.md), [E15-ruling.md](E15-ruling.md). All four verify legs pass at two seats' hands — byte-identity across interpreters, zero dangling over 1,079 rows, counts against independent greps, **14/14 seeded**. Index contents at acceptance: ruling rows 285 · law rows 69 · decision rows 217 · prose sections 1,457 over 159 files (*phrased as row counts deliberately — a bare arc-attributable count here would read as this arc's own tally and go falsely STALE; E15 handoff 2 flagged the hazard, and handoff 3 then measured the first fix firing on its own explanatory quotation — a sweep cannot tell a phrase used from a phrase mentioned*). The honest boundary: artifacts/phenomena are extraction not parsing; supersedes-links nearly empty; the seeded gate is **growth-sensitive** (measured live mid-session) and re-runs every fold. Found E04's true ruling count (29 vs the prose's 28) by counting the record itself |
| [E16](E16-errands-kickoff.md) | The errand batch — eleven queued repairs, each one commit + one anchored regression; the one behavior change as an opt-in flag. | **RUN AND RULED** (2026-08-08) → [E16-errands-report.md](E16-errands-report.md), [E16-ruling.md](E16-ruling.md). **Ten anchors HELD; E16-5's anchor FAILED honestly** (nothing changed) and is ruled: the honest condition alone, the repair to E17 with its anchor pre-stated from the measured table. Predictions 9 held / 1 split / 1 falsified — both misses the informative ones (the retired corner median under-reported risk worst on the highest-risk view; the "complete" handoff list hid two of E15's). Four dispatch corrections caught against source; one executor finding withdrawn in place under controlled re-test; the shared-tree rules adopted as standing; the twelfth errand (the det_a race, hit live by both seats independently) ruled into E17. Zero credits |
| [E17](E17-harness-kickoff.md) | The test harness — the record's run-once checks (E16's anchors, the selftest, the four-leg verify) become a persistent `tests/` suite + paths-gated CI, under the tests-ride-the-commit law. | **RUN AND RULED** (2026-08-08) → [E17-harness-report.md](E17-harness-report.md), [E17-ruling.md](E17-ruling.md). **32 tests, 32 passed at two seats' hands** (24 hermetic + 8 artifacts; *this row read "27 tests, 27 passed … 19 hermetic" until 2026-08-08 — that was Ruling 1's mid-arc state, and **Ruling 5 closed the arc at 32**; flagged by E19 §6a as want 1 and corrected here at the E19 ruling. The suite is 202 at the time of writing, but that is E18's and E20's doing and belongs in their rows, not this one*; the advisor's re-run reproduced the session's result exactly under the trellis2-env python — and the advisor's own wrong-interpreter first run is owned in the ruling, with the T18 pre-check ruled in so the trap fails in one line next time). The mid-flight E16 amendment executed as tool+test single commits with ruled anchors held; both first-run artifacts failures were the session's operand errors against a record that was right, diagnosed FROM the record, no anchor moved. The repo's first CI workflow rides it (ubuntu, paths-gated, pinned, hermetic set). Predictions 11 held / 2 falsified, both misses informative (pytest already present; the selftest is artifacts-tier — the hermetic fixture is commissioned to the build session). Zero credits |
| [E18](E18-index-mcp-kickoff.md) | Spec 1 becomes a running server, in facet — the record-index MCP (`record_query`/`record_get`/`record_build`/`record_verify`/`record_health`/`record_claims`) over stdio, the four-leg verify as its refusing health surface. | **RUN AND RULED** (2026-08-08) → [E18-index-mcp-report.md](E18-index-mcp-report.md), [E18-ruling.md](E18-ruling.md). **The record-index MCP is a running server** — six tools over stdio, built exactly as pinned, 60 tests riding the commits that built it (suite 32 → 92, green at two seats). **The health surface FIRED rather than being asserted**: leg 2 caught a hand-corrupted scratch DB, every read tool refused with the code and the one fix command, one `record_build` recovered, and the staleness banner fired on a real corpus move. **The dogfood found a defect the suite could not** — `verify`/`claims` each leaked a sqlite connection, harmless for every one-shot caller ever, fatal for the first long-lived one; fixed at the root, can-fail proved by injection. Twelve flags ruled (the DB now travels as a DB+certificate PAIR; `BAD_ARGUMENT` joins the codes; the pathspec-scoped commit is standing shared-copy practice). Predictions 10/5, misses informative. Zero credits |
| [E19](E19-treatment-kickoff.md) | The full treatment — facet becomes a presentable studio repo (shipcheck gate, logo, landing page, Starlight handbook, metadata, repo-knowledge entry, staged translations + v1.0.0 tag) **without rewriting its record**. | **RUN AND RULED** (2026-08-08, at the Director's word) — the protocol files are the spec; Law 1: the measured record is added to, never rewritten (claims sweep 0 STALE gates every content phase); Law 2: public surfaces lead-authored, no subagents; publish actions staged for the Director's word after the ruling, translations advisor-run first; halted at [E19-treatment-report.md](E19-treatment-report.md) and is **RULED** → [E19-ruling.md](E19-ruling.md), 7 rulings. **THE TREATMENT IS ACCEPTED**: shipcheck 3% → 85%, README 867 → 208 with the relocation audited mechanically (3 lines differ, all tagline), landing page + six-page Starlight handbook **LIVE** (Pages green at run `31266198261`), and the five `[mcp]` gates SKIP-ed against a serverless `main` **re-run on the landed server** rather than re-asserted — B4 fired live at the ruling's own hands. **The release is HELD on one repair**: the test count was stale on the live public front door — 32/24, true when E19 wrote it and falsified the same day by E18's 60 tests, invisible because the treatment surfaces sit **outside the claims sweep** (§6b, now demonstrated rather than theoretical). Corrected at ten sites *before* the translations, per the release-ordering law. Predictions 13 held / 1 split / 1 missed |
| [E20](E20-coverage-kickoff.md) | Unit coverage — the tool surface gets tests **below the replay level**: the parsers, the projection core, the write-head, the dilation lookup, the frame math, and a guards-can-fail audit closing Ruling 5c's class wholesale. | **DISPATCHED** (2026-08-08, at the Director's word — "32 tests is weak"; he is right, and this is the fix). No count target (the count is the Goodhart metric); the target is the load-bearing surface covered, per-tool, hermetic on synthetic fixtures. The assertion law: pins only anchored/accepted behavior — wrong-looking behavior halts into a finding for the ruling, never a pin. **GATE-HALTED before any unit was written** ([E20-gate-halt.md](E20-gate-halt.md) — the session fired its own sequencing gate, measured all three legs, and the conftest collision became record when E18's D1 modified the exact named file 65 seconds after one of its observations). The Director ruled: wait for E18's halt, no partial run, no worktree. Entry measurements banked ([Ruling 4](E20-ruling.md)): **8 of 33 tools reached by any test, all at whole-tool subprocess level** — the measured shape of "32 tests is weak". Fixtures authorship ruled (E18 authors the pattern, E20 extends — [Ruling 2](E20-ruling.md)); **one re-fire condition remains: E18's report exists**. **RUN AND RULED** (2026-08-08) → [E20-coverage-report.md](E20-coverage-report.md), [E20-ruling.md](E20-ruling.md) Rulings 5–12. **THE COVERAGE ARC IS ACCEPTED — suite 92 → 202** (110 tests in three files, tools reached 8 → 10, guard sites demonstrably fired 9 → 17, hermetic tier 47 s). **Its largest deliverable is a REFUSAL**: three of the six dispatched units *cannot exist* — `project_twins`, `texpass_finalize` and `mesh_stats` are scripts, not modules (`texpass_finalize` has **zero function definitions**), so the session produced the AST evidence, proposed three seams and declined to restructure accepted-asset tooling on its own authority. **All three seams TAKEN at Ruling 6**, ordered by risk, each one commit with its anchor re-run (T12 · the twin-projection anchor · T7's byte-identity). U6's scope ruled at **212 of 409 sites** — `superseded/` excluded permanently on principle (those tools are kept *because* they fail; a can-fail test there would make the repo maintain a contract on its own dead ends). Findings disposed: `github_slug` RETIRED (zero callers, unverifiable external claim), one comment corrected, the silent `tools/call` handed to E18's lane with a named hypothesis (a missing `notifications/initialized`). Watched predictions: **P12 FALSIFIED — the recorded E07 repair holds**; P17 hit to the decimal (2.11:1); **P5 still unsettled and now the repo's highest-value open question** (`fit_background` at frame-edge figures — the reason seam 1 outranks seam 2). F4 falsified Ruling 4a's own denominator (`tools/` holds **150** files, not 33) — corrected in place. Predictions 8 hit / 3 missed / 1 partial / 6 unsettled, and the miss pattern became a standing law: **a documented past bug is evidence an area is well-guarded, not evidence it is fragile** |

| [E21](E21-cli-contract-kickoff.md) | The installed CLI's operator contract — the three `[cli]`/`[mcp]` gate items that became applicable the moment the extraction turned on four new shipcheck tag families: the exit-code registry, the `--debug` gate over raw tracebacks, and logging levels. | **DISPATCHED** (2026-08-08, at the Director's word). Scope is RULED and narrow — **the two published console scripts only** (`facet-mcp`, `facet-index`); the 34 research instruments, `diagnostics/` and `superseded/` are out, because the `[cli]` tag applies to what facet *installs* and retrofitting accepted-asset tooling for a checkbox is not what the gate asks. Three calls are explicitly the advisor's, not the executor's: **what exit code a fired ANDON deserves** (a gate firing is the tool working, not a runtime error — E08 A32 binds and must not be weakened), what a failed `verify` returns (`claims` is already ruled to never fail, E15 Ruling 9b), and **where the quiet/measurement boundary sits** — levels may govern chatter, never a measurement or a refusal, because a `--quiet` that hides an ANDON is the same defect as a `--debug` that skips one. Targets **0.2.0**: it changes the behaviour of a CLI published the same day, and the four version declarations plus `bin/facet.js`'s tag pin all move together. Tests start at **T29**, every code carries a can-fail leg, and exit codes are asserted through a subprocess rather than read from source. **HALTED AT THE REPORT** (2026-08-08) → [E21-cli-contract-report.md](E21-cli-contract-report.md); **awaiting the ruling at `E21-ruling.md`**. Suite **218 → 248**; version **0.2.0** across all four declarations. Two of the three gate items closed with measured evidence — the registry (`0`/`1`/`2`, with `3` declared and pinned as never returned) and the traceback gate. **The pre-change surface was inverted at BOTH ends, not one**: user error exited 2 *and* runtime error exited 1 with a raw traceback, so **three distinct outcome classes shared exit 1**; the gate line had named only the argparse half. **The third item stayed open on purpose** — the dispatch reserved the quiet/measurement boundary for the advisor to rule *before* it ships, so the arc measured the census a ruling needs instead of guessing at one: of `verify`'s 35 print sites **zero are progress chatter**, and `record_mcp` *parses verify's stdout* to build the schema-versioned certificate, which makes the objection to a quiet `verify` **mechanical rather than philosophical**. Both ruled-to-the-advisor exit codes were left **unchanged rather than picked**, and are pinned only on what is ruled (a gate still fires and refuses; a failing verify still prints its evidence) — never on their integers. **The arc's largest finding is F2: the ANDON is a bare `assert`, so `PYTHONOPTIMIZE=1` — an environment variable an installed command carries — removes the gate entirely and the build exits 0** with a silently incomplete index. E08 Amendment 32's own failure mode landing on the repo's own gate, with the interpreter as the separator instead of a shell chain; reported, deliberately not fixed and deliberately not pinned, because pinning it would anchor a defect. Predictions **6 hit / 5 miss / 3 split** in the blind band, and **every clean miss is a quantity, none a behaviour** — plus one *lookup* that was wrong (the version bump took 7 edits, not the 5 a grep reported), which is the more chastening of the two |

| [E22](E22-gates-not-asserts-kickoff.md) | The repo's ANDONs are bare `assert`s, and `-O` / `PYTHONOPTIMIZE=1` deletes them. Does converting them to raises preserve every anchored behaviour exactly, and does the write path then refuse under `-O` as it does without it? | **DISPATCHED** (2026-08-08, at [E21 Ruling 2](E21-ruling.md)). Measured at the ruling seat: `tools/` carries **294 bare asserts across 72 files**, and of the gates carrying the `ANDON` token — **`texpass_iter` 8 as assert and 0 as raise** (the write-head at the centre of E08 Amendment 32), `texpass_finalize` 4/0, `project_twins` 15/1, `e11_manifest` 35/1, `e11_export_turnaround` 24/1 — with a control on the pinned interpreter showing the gate **silent and execution continuing past it** under both `-O` and the env var. A32 was earned when a shell chain walked past a fired ANDON and committed 47,020 texels; the repair put the check inside the tool, and **one environment variable removes 87 of those checks** — *strictly worse than the original defect*, because the shell chain at least let the ANDON print. Severity stated honestly: nobody sets `PYTHONOPTIMIZE` here and no artifact is claimed corrupted — **A32's test is separability, not probability**. Scope is 87 sites plus two developer guards; the **~207 non-ANDON asserts are OUT**. **Every conversion is a PURE MOVE** and an anchor that does not reproduce byte-for-byte **reverts** it rather than being adjusted (T7, T26's three fired ANDONs, the twin-projection anchor). Tests from **T30** assert each gate fires under a normal interpreter **and** under `-O` — the test the repair makes writable and the current construction makes impossible — and **no test pins the defect**. [E21 Ruling 4](E21-ruling.md)'s `4 = REFUSED` folds in. Halts at `E22-gates-report.md`. **RUN AND RULED** (2026-08-08) → [E22-gates-report.md](E22-gates-report.md), [E22-ruling.md](E22-ruling.md), 11 rulings. **THE ARC IS ACCEPTED**: 88 sites converted as a pure move — shown by whole-file AST equality against the prior commit, not by anyone's reading of the diff — all three anchors reproduced (T7 byte-identical *including its sidecar json*), suite **248 → 275** with the artifacts tier live and zero skips, CI green, and the compensator gate carried out further than it was written: **7,312 files sha256-manifested** across the whole recorded root before the first replay, re-checked three times, **0 changed**. The thesis was re-measured at the ruling seat as a before/after pair on one gate in four interpreter modes — **SILENT under `-O` and under `PYTHONOPTIMIZE=1` at the old tree, FIRING in all four at the new one.** ⚑ **THE SCOPE PREMISE WAS FALSE, AND IT WAS THE ADVISOR'S ERROR MADE TWICE.** The dispatch computed its excluded class as `294 − 87`, **subtracting a five-tool subtotal from a repo-wide total**, and gave the difference a name, a character ("developer sanity checks") and a reason to be left alone. Measured independently from git blobs: **294 asserts, 278 carrying the ANDON token, 16 not.** The excluded class is **16**; **191 declared gates had been misfiled into it by arithmetic**. Every per-tool cell E21 Ruling 2 published reproduces to the digit — only the two derived totals are wrong. **Ruling 3 RATIFIES the ANDON axis rather than overturning it**, and the evidence is that all 15 surviving non-ANDON asserts were read by hand: two of them sit *inside* converted write-path tools and each carries a comment **declining** andon status and citing E08 A3, so the token is a maintained taxonomy, not a marker someone forgot to type. Ruling 4 corrects the report's own **192/57 → 191/56** before it can scope the next arc — the same subtotal-as-total shape, **the second consecutive arc in which the scope number rather than the work was the defect** — and splits the remedy by risk: **57 route sites in 12 files → E23**, 132 diagnostics sites as a later mechanical arc, `superseded/`'s one site **never** (those tools are kept precisely so they fail the same way). `SystemExit` **not** unified, with the constraint recorded; `4 = REFUSED` does **not** extend to the 34 research tools; the `mcp_support` constant **adopted**; the certificate schema **not** bumped — a free version is no better than a free integer, and the real gap is that the field's domain was never written down. The arc's two new laws are now in CLAUDE.md: **a gate is never a bare `assert`** (E21 Ruling 2 wrote it and **no fold had carried it until this one**) and **check that the population is real before you predict its density** — P18 predicted 4, *halved* an untutored 8–12 on this repo's own calibration lesson, and measured **175**, which is the sharpest miss in the record |

| [E23](E23-route-gates-kickoff.md) | E22 left **191** ANDON gates that `python -O` still deletes. **57 of them are in route tools** — the twelve scripts that produced four accepted assets. Does the same pure move hold on files that **no test has ever executed**? | **DISPATCHED** (2026-08-08, at [E22 Ruling 4](E22-ruling.md)). **The scope is ENUMERATED in the dispatch, not derived** — two consecutive arcs had a scope-number defect and both came from arithmetic, so the twelve files are listed with their per-file counts (`bake_hero_prep` 15, `brush_cloud_step` 9, `subject_profile` 6, `e13_harmonize` 5, `bake_hero_fuse`/`bake_hero_pack`/`silhouette_masks` 4 each, `cull_unseen` 3, `export_asset_source`/`palette_gate`/`resample_atlas` 2 each, `restylize_views` 1) and the executor is told to verify rather than inherit them. Same pure-move bar as E22, and `AssertionError` stays the type **even though nothing forces it here** — measured: the repo's only `except AssertionError` is `facet_index.py:216` and none of the twelve is `facet_index`, so the type is kept because *the pure move is the one that changes nothing*, not because a handler demands it. Shapes measured at dispatch: **31 multiline, 0 `not`-conditions** (E22 had 3, so the negation rule is uniform), 0 sharing a line, 0 without a message. ⚠ **THE WHOLE DIFFICULTY: zero of the twelve are reached by any test.** E22's safety was T7/T10/T26; this arc has **no behavioural net**, so the AST proof carries the entire load. And the twelve are **two runtimes** — 10 files / 38 sites run under the pinned interpreter (`--help` exits 0 writing nothing, measured on all ten), while **`bake_hero_prep` and `bake_hero_pack` `import bpy`** and cannot run outside Blender, taking 19 sites with them. **11 of 12 are SCRIPTS that execute at import** and three have zero function definitions, so a test may never import them — E20's wall, and its refusal stands. Tests owed: `py_compile` on all twelve (hermetic, `bpy`-free, catches a bad splice — 12/12 clean today), the three-mode `--help` smoke on the ten, the structural law with a can-fail leg, and **fire what can be fired hermetically while reporting what cannot and why**. Compensators score **2 and are worse than E22's**: these tools bake, cull, resample and export into `E:\AI\training`, which **is not in git**, so the compensator is a restore-from-source that has never been rehearsed. Halts at `E23-route-gates-report.md`. **RUN AND RULED** (2026-08-08) → [E23-route-gates-report.md](E23-route-gates-report.md), [E23-ruling.md](E23-ruling.md), 13 rulings. **THE ARC IS ACCEPTED**: 57 sites converted as a pure move, **12/12 whole-file AST identical** to the rule applied at `48fa733`, 0 comment tokens changed, 57/57 per-site, **0 reverted**; suite **275 → 370**; trees manifested at 7,312 files across **four** measurements, 0/0/0 every time. **The arithmetic closes end to end for the first time in three arcs** — 88 + 57 = 145 AssertionError raises, 191 − 57 = **134** remaining (132 `diagnostics/` + 1 `superseded/` + 1 `verify/`) — re-measured at the ruling seat, as was the thesis itself: `subject_profile:60` fired through `silhouette_masks` is **SILENT under `-O` at `48fa733` and FIRES in all four modes at HEAD**. ⚑ **GATE 4 FIRED.** CI went red because `restylize_views` imports `cv2` at module level and CI's pinned install never had it — **no test had ever invoked any of the twelve**, so the dispatch's own premise produced its first casualty within an hour. The executor measured module-level imports across all twelve *before* repairing (`cv2` is the only real gap), followed the pattern `ci.yml`'s comment already prescribes for `mcp`, and **named the two coverage-removing alternatives and rejected them in writing**. **Ruling 2 ratifies the repair and writes the boundary the repo lacked**: a gate measuring the *result* halts; a gate measuring the *environment's ability to run the measurement* may be repaired, if it adds capability rather than removing coverage. Verified at the ruling seat that the pin never reached the published surface — `pyproject.toml` untouched, deps still `mcp>=2.0.0` alone. ⚑ **AND A FABRICATED CITATION**, the arc's most serious defect: a CI run id was committed with a `PASS` beside it *before CI had run*, and `gh run view` returns **404 — it never existed**. Self-caught and replaced with both real ids in the same commit; the ruling seat swept the whole class and every other citation resolves. New law: **a report may not contain a placeholder shaped like evidence**, and the advisor resolves every external citation at ruling time — the index's pointer leg cannot see a URL in prose. **F1: `brush_cloud_step:204` cannot fire**, shadowed by the lora-w gate above the only call to `preflight()`; confirmed by reading the control flow, **kept and annotated** rather than deleted, because it is a precondition on a function's contract and not on today's single call site. T31 found it only because its legs match each gate's **own message** instead of a bare non-zero — ratified as the house pattern. **Ruling 9 adopts T31's pin of the remaining 134 as the structural fix for the scope-number defect that beat two arcs**: a count under a test cannot drift silently. Two more laws land in CLAUDE.md (the gate boundary; the placeholder rule), plus the P4b unit lesson — 4 predicted, **20** measured, because the prediction reasoned about *files* while the instrument measures a *scope*, and **a tool that writes more can have fewer gates with a write in scope, since writing more is what makes you factor the writing out** |

| [E24](E24-installed-paths-kickoff.md) | `pip install facet-mcp` gives you two commands that **cannot find the record**. Can the root be resolved so a source checkout, a frozen binary and a wheel install all work — without breaking either path that works today? | **DISPATCHED** (2026-08-08, at the v0.3.0 release read-back). Targets **0.3.1**; a defect fix, no new capability. **Found by running a VERB on the published wheel instead of `--help`** — `facet_index.py` ships as a top-level py-module, so `REPO = dirname(dirname(__file__))` resolves to **`<venv>/Lib`**: `build` fails, `q` without `--db` fails, `--print-tools` prints a `db:` path that cannot exist, and `record_get` refuses with *no record corpus under `<venv>\Lib`*. **Not a v0.3.0 regression** — the published 0.2.0 wheel fails identically; v0.1.1 fixed the *frozen* branch and left the *wheel* branch behind. **The `npx` binary is unaffected and remains the supported path.** Scope is the **19 enumerated `REPO` consumers** (11 in `facet_index`, 8 in `record_mcp`), `facet_index`'s `--db` default, and `$FACET_INDEX_DB` support in `facet_index` — a second measured defect, since the env var is honoured by `facet-mcp` only while the README claimed *either*. **Three constraints are rulings, not preferences:** the env var selects WHICH DERIVED DB and never which corpus, so it may not be repurposed as the root · a resolver that cannot find a corpus **must REFUSE** (`4 = REFUSED`, E22/E23) and never guess a fallback · **neither working path may regress** — T28's five frozen tests pass unchanged or the change is wrong. The design hypothesis is offered to be *measured, not obeyed*: `FROZEN` is a **proxy** for "REPO is not a real root" and is blind to the wheel case, so test the property — *does this directory contain the record* — which `record_mcp.py:272` already does, and whose marker was measured at dispatch to be absent from the published wheel. ⚑ **THE MISSING TEST TIER IS THE POINT: T28 has five tests for the frozen branch and ZERO for the installed-wheel branch**, which is why this shipped in four consecutive releases. The new tier must **build a wheel, install it, and run a verb**, and must **fail on the broken tree before it passes on the fixed one**. `release.yml`'s step named *"Verify the wheel runs from a clean venv"* runs only `--help` and `--print-tools` — it must run what a user runs. Halts at `E24-installed-paths-report.md`. **RUN AND RULED** (2026-08-09) → [E24-installed-paths-report.md](E24-installed-paths-report.md), [E24-ruling.md](E24-ruling.md), 7 rulings. **THE ARC IS ACCEPTED.** Re-measured at the ruling seat **on a wheel built there**, not on the report's word: from a checkout `q` → 0 with correct rows, `claims` → 0, and the banner reads `db: E:\AI\facet\docs/index/facet.db` where it read `<venv>\Lib\…`; from an empty directory `q`/`claims`/`build` all exit **exactly `4`**, naming both candidates and both markers, while `--print-tools` still exits 0 so the path that worked is unregressed. Suite **384**, T28's five **byte-identical and passing**, scope exactly four files, CI green with both scanners. **The resolver is ratified including what was left out**: it asks the property — *does this directory contain the record* — instead of `FROZEN`, which named a runtime and was a proxy; two markers because one is not a marker (the `CLAUDE.md` count is **depth-dependent** — 14 at one level, 62 recursively, the report's 26 a third depth — but **exactly one directory carries both at every depth**, which is the half that decides); and **no walk-up from cwd**, ratified because adding one later is additive while removing one is a behaviour change. ⚑ **RULING 3 — THE TIER WAS SKIPPING IN CI**: 370 passed / **6 skipped**, five of them T32's wheel legs, because `build` was not installed. A check present on the rig and absent from the gate that fires every push **is the defect's own shape**; `build==1.5.0` is pinned in this fold under the pattern `ci.yml` already documents. ⚑ **RULING 4 — T28 PINS SOURCE TEXT** (`assert "os.getcwd() if FROZEN else REPO" in SRC`), which forced this design rather than a cleaner one, and beside it sits `assert … or True # documentation`, **an assertion that cannot fail**. New law: pin a structural property with an **AST walk**, never a source-string match — the repo already does the former in four places. ⚑ **RULING 5 IS THE ADVISOR'S OWN MISS**: `README.md:47` claimed the broken wheel "works for `q` and `claims`" — I measured `q` and generalised to `claims` without running it; E24 ran it and it exited 2. Fourth front-door drift in two days, second of them mine. P1 missed its band (17 predicted, **13** measured) and the reason is E23's law one level down — two "survivors" are line numbers of a *statement whose other line changed*, so the unit was ambiguous where the population was right |

| [E25](E25-diagnostics-gates-kickoff.md) | E22 converted 88 gates and E23 converted 57. **133 ANDON checks are still bare `assert`s** — the measurement instruments that produced the evidence for four accepted assets. Does the same pure move close the class? | **DISPATCHED** (2026-08-08, at [E22 Ruling 4](E22-ruling.md) and [E23 Ruling 9](E23-ruling.md)) — **RUNS IN PARALLEL WITH [E24](E24-installed-paths-kickoff.md)**, at the Director's word. Scope **enumerated**: 132 sites in `tools/diagnostics/` across 42 files + 1 in `tools/verify/` = **133 across 43**. `superseded/`'s one is **permanently out** and gate 6 exists to stop it being tidied away. Same pure-move bar; **`not`-conditions are 5 here where E23 had 0**, so the negation rule is *not* uniform and each must be checked. ⚑ **THE PARALLEL COORDINATION SECTION IS THE NEW PART AND IT BINDS**: disjointness was *measured* at dispatch — E24 owns `facet_index.py`/`record_mcp.py`/`test_t28`/`release.yml`, E25 owns `diagnostics/*`, `verify/*` and `test_t31`, and **no diagnostics or verify tool imports either of E24's files**, so there is no runtime coupling either. File-specific `git add` always; **`git fetch && git merge --ff-only` before every push, NOT `pull --rebase`** — corrected minutes after dispatch by trying it, because rebase refuses outright while another session has unstaged changes and `--autostash` would stash *their* work; and a `fold`-marked failure must be reported *with the fact that a second session was live* — [E23 Ruling 10](E23-ruling.md) said the concurrent writer can be another session, and now there genuinely is one. A defect found in E24's files is **reported, not fixed**. Test tier measured at dispatch rather than hoped for: `py_compile` on all 43, and **41 of the 43 give `--help` exit 0 writing nothing** in three interpreter modes — the two exclusions are `e12_head_render.py` (`import bpy`) and `e04_make_brush_prompts.py`, which **does file work before argparse** and is itself a finding the report must carry. **28 ANDONs in scope already raise `SystemExit` across 12 files** — [E22 Ruling 5](E22-ruling.md) rules they stay; report the collision, do not resolve it. And **T31's `REMAINING_ELSEWHERE = 134` must move to 1** — deliberately, in the commit that earns it, because that constant is E23 Ruling 9's structural fix for the scope-number defect that beat two arcs. Halts at `E25-diagnostics-gates-report.md`. **RUN AND RULED** (2026-08-09) → [E25-diagnostics-gates-report.md](E25-diagnostics-gates-report.md), [E25-ruling.md](E25-ruling.md), 10 rulings. **THE ARC IS ACCEPTED, AND THE CLASS CLOSES EXACTLY.** 133 sites across 43 files, pure move — re-proven at the ruling seat with its own instrument: **43/43 whole-file AST identical** to the negation rule applied at `1b60478`, 0 comment tokens changed, 0 reverts. **1 ANDON assert remains anywhere under `tools/`** — `superseded/texpass_thin_mask.py`, the site [E22 Ruling 4](E22-ruling.md) ruled never converted, and **T33 now pins it by name** so the permanent exclusion is enforced rather than remembered. `AssertionError` ANDON raises total **278 = 87 (E22) + 57 (E23) + 133 (E25) + 1 pre-existing**, against a pre-E22 census of 278 — **four arcs and the arithmetic closes on the first reading**, after E22's phantom 207 and E23's 192-that-was-191. ⚑ **GATE 4 IS *BLOCKED*, NOT FAILED, AND THE EXECUTOR REFUSED TO INVENT A VERDICT** — it wrote "see the CI line below" per [E23 Ruling 3](E23-ruling.md) and left it. Resolved at the ruling: E25's own run was **CANCELLED** by E26's push 5½ minutes later (`cancel-in-progress: true`, same ref, same workflow — a setting the studio's Actions rules *require* and which is correct for sequential work), and the next run containing the commit failed **25 of 25 in `test_t34_front_door_counts.py`**, zero in E25's files — reproduced on a pristine clone. **New law: a parallel arc cannot own a CI verdict for its own commit**; gate 4 is satisfied by the first *completed* run whose tree contains the commit, the report must name what else was in it, and a red run for another arc's reason is *blocked*, not failed. **⚑ Amendment 1, same session: gate 4 is now PASS** — E26 committed its surface fix (`5049707`) and hand-dispatched CI, which went **green: 639 passed, both scanners ✓**, and `59f9409` is an ancestor of it. The law resolved a gate within the hour of being written; the blocked verdict is left standing rather than rewritten, because the sequence — cancelled run, attributed red, qualifying green — is the useful part. **F1 is the arc's best finding**: the AST-proof instrument decoded the prior commit with the platform codec and reported 10 comment-changed files *before anything was converted* — unfixed it would have **reverted every file containing a non-ASCII character**, which is 36 of 43. Reproduced here to the digit, and the mechanism **refined**: a symmetric wrong codec compares equal and hides nothing — **the asymmetry was the bug**. Caught because a number that had to be 0 came back 10. **F2**: the first conversion passed 43/43 in a form the 145 existing sites do not use, and was **reverted wholesale rather than patched** — ratified, second arc running. P3 missed low (46 predicted, **17** measured) on a **reachability unit** — third arc in a row where the population was right and the unit was wrong. Also ruled: E22's unreconcilable **16.3 GB manifest figure is withdrawn** (two independent measurements now agree at 7,312 files / 17,072,807,610 bytes); `test_t05_claims_sweep` **is owed a `fold` marker**, routed to E26 because that file gained five E26 tests mid-arc; and a push publishing another arc's commits is **correct**, needing a look-before-you-push check rather than a prohibition |

| [E26](E26-front-door-counts-kickoff.md) | The repo's stated test count has drifted on its public surfaces **four times in two days** — once inside the very commit that fixed the previous drift — and every one was caught by a person reading the page. What should watch it? | **DISPATCHED** (2026-08-09, from [E20](E20-ruling.md)'s want 2 — **and reshaped by measuring it first**). ⚑ **WANT 2 AS WRITTEN WOULD NOT HAVE CAUGHT ANY OF THEM**, and that is measured, not argued: `claims()` scans `record_markdown()` (= `CLAUDE.md` + `README.md` + `docs/**.md`), so the presentation surfaces are indeed invisible — but its seven claim families are rulings/amendments/addenda/handoffs/experiment-span and **ZERO mention tests**, and `measurements()` reads **only the index**, so **a test count is not in the index and structurally cannot be**: the index is a pure function of the corpus markdown while the count is a property of `tests/`. A widening alone would report nothing new about the thing that broke. **The shape that works is already ratified** — [E23 Ruling 9](E23-ruling.md) put a live-moving quantity under a **test**. So the arc splits by **source of truth**: **Half A** (do first) is a hermetic test pinning every stated count on every surface — README ×2, SHIP_GATE, `site-config.ts`, two handbook pages, and the seven translations (digits only) — against what `pytest --collect-only` actually collects, with a stated rule for **not** firing on a *historical* count, since `CHANGELOG`'s released entries and `SHIP_GATE`'s lineage are correct forever. **Half B** is want 2 correctly scoped: widen the **sweep's own scan set**, and ⚠ **`record_markdown()` is RULED UNCHANGED** — it feeds the index as well as the sweep, and the candidates are **17 files / 2,768 lines against 254 / 62,161, seven of them non-English translations**, so folding them in would move prose rows, fts rows and **the seeded set's rankings that leg 4 gates on**. Risking legs 1/2/4 to improve a *report* is a bad trade. Gate 2 is E24's lesson carried: **Half A's test must FAIL on a deliberately stale surface** before it passes at HEAD. May run in parallel with E25; T-numbers are a shared namespace, take **T34**. Halts at `E26-front-door-counts-report.md`. **RUN AND RULED** (2026-08-09) → [E26-front-door-counts-report.md](E26-front-door-counts-report.md), [E26-ruling.md](E26-ruling.md), 7 rulings. **THE ARC IS ACCEPTED.** T34 (34 tests, three legs) pins every front-door count against `pytest --collect-only` in a subprocess; Half B widened the sweep's own scan set 256 → 266 files with `record_markdown()` **AST-identical** and all four legs passing. ⚑ **AND IT IS THE ONLY INSTRUMENT IN THIS REPO THAT HAS CAUGHT A LIVE DRIFT END TO END** — not planted, not a fixture mutation: it fired on `main`, in CI, run [`31294891661`](https://github.com/mcp-tool-shop-org/facet/actions/runs/31294891661), 25 assertions reading *"states full 423, the collector reports 648."* **The drift was this arc's own.** The executor measured on a worktree pinned to `1b60478` while E25 committed T33 **locally** into the shared copy — and **`git fetch && git merge --ff-only origin/main`, the advisor's own coordination rule, cannot see that**: it compares against `origin`, and a sibling session's *unpushed* commit leaves `origin/main` an ancestor of `HEAD`, so the guard truthfully reports *already up to date*. New law: **a worktree answers what your change produces, not what the tree you commit to contains** — re-measure any asserted quantity against the tree you are about to commit. But the structural fix had already landed and is this arc's own deliverable: **no coordination rule needed to catch it, because T34 did** — [E23 Ruling 9](E23-ruling.md) vindicated twice. The failing run stays in the record as a failure. **Ruling 3 sizes the routed matcher gap by planting all three notations on a swept surface**: `999 tests` is caught, **`999/991` and `384 → 999` SLIP** — live, not latent. Ruled **do not widen the matcher**: the first one was proximity-shaped and its own census returned 45 hits of which **15 were not test counts** (`RTX 5090`, four ISO dates, `limit=999`) while *missing* a real French site spelled *hermétiques*. The fix is a **writing convention declared in T34's docstring**, not a regex. ⚑ **Ruling 4 is the advisor's**: the dispatch's enumeration was arithmetically right (22 sites, exactly as predicted) and **structurally wrong** — `SHIP_GATE.md:61` is *one physical line* carrying four current-state clauses and eight historical pairs, so no file- or line-scoped rule can work and the executor had to invent the span model to satisfy a gate I specified on a line I had built that way myself. And **`docs/advisor-kickoff.md` was stale by three releases** — untouched since the suite was 248 — *the very document whose step 1 reads "RE-COUNT."* **Half B's yield is near zero and that is the answer**: three of the four named files carry no family claim at all and CHANGELOG's three arrive UNPARSEABLE, so **want 2 is CLOSED rather than carried** — what it reached for was done by Half A. P8 missed 8→34 (predicted what it would *write*; the instrument counts parametrized cases — **fourth consecutive arc where the population was right and the unit was wrong**) and P9 predicted a density without checking the population existed, *"the law the dispatch quoted at me, which I applied to P1 and not to P9"* |

| [E27](E27-measurement-mcp-kickoff.md) | Spec 2's eight job-shaped tools become a running server, **in facet**. Does the measurement surface hold as one instrument — the same numbers for the same mesh, from any caller? | **DISPATCHED** (2026-08-09, at the close of the gates arc). **The spec is [docs/specs/measurement-mcp-spec.md](../specs/measurement-mcp-spec.md) and it is the contract** — the dispatch does not restate it, it adds facet's overlays and what has changed underneath. **This is the last thing standing between here and the polish arc**: [E14 Ruling 35](E14-ruling.md) is the Director's word that the exemplars are polished *after* the four MCP tools are built and verified with tests, and **one of four is built** (the record index, E18, now shipped on two registries). Its first consumer is the best one available — four accepted exemplars measured **before and after** a polish pass by the same instrument, which is where instrument identity stops being a README claim. Six things changed under the spec since it was written and are enumerated for verification: the build bar is the studio's not a prototype's · **spec open question 3 is CLOSED** (`ai-eyes-mcp` is seven image-grading tools, this server measures geometry and texels — disjoint, measured at the ruling seat) · questions 1 and 2 are **the Director's and may not be decided** · every ANDON now raises · exit codes are ruled (`4` = REFUSED) · the wheel path resolver exists and must not be reinvented. **The bar is not the pure-move bar — this is a build**: job-shaped tools, **the server WRAPS and does not re-implement** (gate 3 makes a missing behaviour a *finding*), a refusal beats a wrong number, tests ride the commit. ⚠ **The fixture question is the arc's real work** — the recorded trees are not in git; synthetic first on E18's D2 pattern, anchors in the artifacts tier, and **measure which tools can be tested hermetically and report the split** rather than padding it. The spec's instrument laws are binding: name the denominator, a hue carries its chroma, a statistic of angles is circular, report the total *and* the largest connected component — and a tool that cannot honour one on some input **refuses on that input**. Halts at `E27-measurement-mcp-report.md`. **RUN AND RULED** (2026-08-09) → [E27-measurement-mcp-report.md](E27-measurement-mcp-report.md), [E27-ruling.md](E27-ruling.md), 9 rulings. **THE ARC IS ACCEPTED.** `tools/measure_mcp.py` serves the spec's eight names as `facet-measure` 0.1.0 — **four wrap** (`mesh_stats`, `reach_ceiling`, `texel_provenance`, `measure_report`), **four refuse** with structured errors naming what is missing and whose question unblocks them. Every payload carries the identity envelope (server version + instrument sha256 + params + config hash) and `measure_report` refuses a mismatched comparison **naming the differing field**. Gate 3 re-measured at the ruling seat: `git diff --name-status` over the arc's commits is **`A tools/measure_mcp.py` and nothing else** — no instrument re-implemented *or edited*, `facet_index.py`/`record_mcp.py` byte-untouched. Suite 648 → **684**, reproduced at the ruling seat (684 passed, 405.83 s, exit 0); the **artifacts anchor proves the served `reach_ceiling` reproduces E12's pre-registered ceiling digit for digit** (3,240,510 · 1,358,656 · 1,635,304 · 50.46%) — verified against the cited source and confirmed **not a silent skip** (`pytest -rs`: 6 passed, 0 skipped), which is [E24 Ruling 3](E24-ruling.md)'s lesson held. CI citation **resolved rather than trusted** ([`31299692852`](https://github.com/mcp-tool-shop-org/facet/actions/runs/31299692852), `a30fea2`, both scanners green) per E23's fabricated-citation law. ⚑ **RULING 2 IS THE ADVISOR'S CORRECTION AND IT CHANGES THE DIRECTOR'S QUESTION**: the report said open question 2 is forced *twice*; measured, it is forced **three times** — `e12_offsurface.py` takes **9 flags with a required `--prep` and no hardcoded subject**, and its docstring's first line is *"E10 Ruling 4's question, **any subject**"*. It exists **because** `e10_offsurface.py` is hardcoded to the ship, written by an earlier seat that refused to edit a shipped instrument — so excluding `e12_*` excludes the very file written to avoid the hazard the exclusion prevents. F2's remedy ("parameterise e10 or commission fresh") is void: **the instrument is already built**; the commission narrows to the *erode/margin* half, which exists in neither offsurface tool. One `grep -c add_argument` separated the report from this ruling — *naming a resource is not enumerating it*, the advisor's own recurring shape found in a report. **F1 reproduced independently**: `e14_topology.py` throws `IndexError` on tied extents (`wide = 3 - argmin - argmax`) on the unit cube — and ruled **provably non-perturbing to repair**, since the expression is arithmetically unchanged on unequal extents, so a tie fix is byte-identical on every recorded subject. The same run corroborated P5 for free — the tool prints `SHELLS (shared-vertex, = mesh_stats)` *before* the crash. **F3 confirmed and ruled documented-not-renamed**: `e13_anchor_check.py` is the spiral-law guard; the file keeps its name (cited in closed rulings), the spec keeps `anchor_check`, and the collision rides in the refusal text pinned by a test. **P1 is the fifth consecutive unit/population miss and the first of its shape** — the population was real and every member was real, but the prediction assumed a property none was checked for (*that each has an instrument to be hermetic about*); folded to CLAUDE.md. **P2's unit ambiguity was disclosed by the executor rather than resolved in its own favour** — the operational reading governs from here, and **the score does not move** because both readings were in band before the choice, so this is resolving an ambiguity rather than retuning a condition after seeing its result. F4's largest-connected-component gap is **commissioned to the instrument, not the wrapper** (computing it in the server would be the measurement arithmetic gate 3 forbids). `measure_mcp.py` **stays out of the wheel by default, not by deferral** — instrument identity is the product, and publishing a surface before the polish arc has exercised it would freeze a comparability boundary nobody has tested. Ruling 9: the parallel-seat hole ([E26 Ruling 2](E26-ruling.md)) ran live in **both** directions and both seats held — but **both saves were observational, not mechanical** (one seat watched a directory; the other was told by the mount's staleness banner), so no new gate is commissioned on an unmeasured design. `v0.3.1` shipped from the release seat **before** this arc landed and contains none of it (`git merge-base --is-ancestor v0.3.1 HEAD` confirms). ⚑ **AND THE RULING SEAT'S README AUDIT FOUND THE NEXT DRIFT, IN A CLASS NO INSTRUMENT WATCHES**: the front door still read *"134 gates in the remaining research tools are still asserts"* — the pre-E25 state, stale by 133, on the public page, **three arcs after the fact**, alongside "twenty-three experiments" when the record holds 26. T34 pins *test* counts; a **gate** count and an **arc** count are the same shape of live-moving quantity with nothing looking at them. Corrected in place, and it is [E20](E20-ruling.md)'s want 2 arriving a third time in a new class |

| [E28](E28-instrument-census-kickoff.md) | Which of `tools/diagnostics/`'s instruments belong on the measurement surface — **measured, not curated**? And do the three tools open question 2 released wrap without editing what they wrap? | **DISPATCHED** (2026-08-09, at the Director's rulings on **both** open questions). ⚖ **OQ1 — *"Commission the census first"*: the boundary is not set by curation and not set blind.** The spec estimated its own denominator at "~80 files"; measured, `tools/diagnostics/` holds **99 `.py` and 0 of any other extension**, so the arc opens by verifying its own population rather than inheriting it. The census is a **committed, re-runnable instrument** (`tools/instrument_census.py`) with its file count under a test — not a hand-read, because a hand-read is what produced "~80" ([E23 Ruling 9](E23-ruling.md)). Six mechanical axes (invocable · subject-bound · the question · cited in the corpus · anchored by a test · import-safe in three interpreter modes) plus one **labelled proposal** for the job-shape mapping, where *"no opinion" is an acceptable value* — a table of confident `none`s reads as a measurement. ⚖ **OQ2 — the `e12_*`/`e14_*` family is IN**, releasing `mesh_topology`, `thin_extent_curve` and `offsurface_rate` at once. ⚑ **AND THE DIRECTOR'S SECOND CLAUSE VOIDED A DOCTRINE FOUR ARCS HAD INHERITED**: *"not sure why that closed ruling exists."* **It does not** — searched across all 25 ruling documents, **no ruling in this repo forbids editing an instrument**. The sentence originates in one executor's docstring explaining its own choice (`e12_offsurface.py:8`) and two documents then cited it as binding at three sites, both in the words *"the record already refuses this move."* What **is** ruled is narrower and stays: corrections to a closed ruling are additive not rewrites ([E10-off Ruling 1](E10-offsurface-ruling.md), whose object is the ruling **document**), and *an instrument does not change under the session using it* ([E12 Ruling 6d/6e](E12-ruling.md), which **scheduled** two repairs rather than forbidding them) — one textual, one temporal, neither permanent. **The hazard the folklore stood in for is real and checkable: a cited number must still reproduce from the tool at HEAD**, which is a test, not a taboo. Corrected in place in the spec, the placement memo and CLAUDE.md; the one site that handled it correctly was the served tool's own refusal text, which routed the question to a ruling instead of assuming one. Scope: the census (halts) → the three wraps, gated on that halt → F1's tie repair as a precondition, under the proof obligation [E27 Ruling 3](E27-ruling.md) states (*byte-identical index selection on every distinct-extent mesh*, discharged by exhaustive comparison plus a randomized sweep, not by reading a diff) → **T34 gains a fourth leg**, because the experiment count on the public front door was found stale **again** at this dispatch (**27 rows and the index agree; the README said twenty-six at two sites**) — the third instance in 24 hours, and E27's own correction of it undershot by one *inside the fix*. `offsurface_rate` serves the **bake half only**; the erode/margin statistic exists in neither offsurface instrument and stays uncommissioned. Halts at `E28-instrument-census-report.md`. **TASKS 0 AND 1 RUN; HALTED AT THE MID-ARC GATE AWAITING THE RULING** (2026-08-09) → [E28-predictions.md](E28-predictions.md), [E28-instrument-census-report.md](E28-instrument-census-report.md), [`docs/instrument-census.md`](../instrument-census.md). Task 0's fourth leg **fired on two real stale surfaces** — `site-config.ts` read *"Twenty"* and `docs/advisor-kickoff.md` read *27* against a status table holding 28, **neither named by the dispatch**, which measured only README.md's two already-current sites; its own notation test then caught a defect on first run (French `vingt-six` failed to parse as a pair, fell through to `six`, and returned **6** — a confidently wrong reading, so a hyphenated pair is now all-or-nothing). Gate 1 verified three ways: **99 `.py`, 0 other, 0 subdirectories**. Gate 2 held — T41's can-fail legs passed *before* the first census number was read. Gate 6 held — 7,312 files / 17,072,807,610 bytes, **0/0/0** before and after. ⚑ **THE MEASUREMENT: 93 of 99 carry a flag surface and only 6 have a `__main__` guard**, so the pre-registered three-clause `invocable` measures **5** against a predicted 60 — the **sixth consecutive unit miss**, and the component the prediction was actually reasoning about (flags) measured 93, *above* the band in the other direction. The definition was **not** retuned. The directory's house style is a straight-line module-level script, which is why axis F declines to probe 88 files rather than executing arc scripts against recorded trees; the served MCP invokes instruments as **subprocesses, never imports**, so the missing guards do not block wrapping. ⚑ **THREE FINDINGS MOVE TASK 2's SCOPE, which is what the halt was for**: (1) **half the already-served surface is outside this census's population** — `mesh_stats` and `measure_report` are implemented in `tools/verify/`, which no census has measured, so a boundary ruled against these 99 alone would exclude two of the four tools that already work; (2) **`mesh_topology` has two candidate instruments** (`e14_topology.py` *and* `e12_nonmanifold.py`, plus `e14_atlas_anatomy.py` on the hollow/double-wall half) where the planned wrap names one; (3) **`anchor_check` has no implementation** and the five nearest all sit in the `ambiguous` cell — reported as a finding, not built, per the dispatch. Axis G is a **proposal**: 21 mapped, 58 `none`, 17 `ambiguous`, 3 `no opinion`, every one of the 17 hand-read against its fuller docstring. The EXTERNAL_VERIFIER remediation ran an **independent regex method** against the AST classifier over 396 cells and found **2 disagreements, both resolved in the census's favour** (a `#` comment and a *function* docstring are not binding literals). **Three self-references were caught and handled**: axis D read 76 before the census's own output existed and **99** after — excluded by construction, because a headline that flips to 100% on a second invocation is not a number; axis E's first implementation matched only `foo.py` where the **pre-registered** definition said *"basename or dotted module name"*, under-reporting by two, corrected to **46** (44 strict), **which makes P4's miss larger**; and that correction's own can-fail leg moved the strict count 44 → **45 by existing**, now rewritten with synthetic names and pinned by a test asserting T41 anchors nothing alone. ⚑ **AND A GATE FIRED ON THE CLOSING SUITE RUN AND IS REPORTED AS FIRED**: `test_t41_axis_d_is_idempotent_across_runs` failed (**1 failed / 734 passed**, 51 rows drifted) because writing the report — which names the files the census found — made **all 99 "cited"** where the committed census had 76. **The fourth self-reference, and the largest.** Axis D now excludes the documents that describe the census; the headline is unmoved at **76** and the contaminated read rides beside it as `cited_raw` at 99. The four alternatives are named and rejected in writing (F10) and the adopted one is the rule already in the instrument before the gate fired, so it passes the retune test: **the number does not move.** Only one of the four self-references was found by looking; three were found by a check that fired. Tasks 2 and 3 untouched. ⚖ **RULED IN THREE SITTINGS; THE ARC IS CLOSED** (2026-08-09) → [E28-ruling.md](E28-ruling.md), Rulings 1–26. Re-measured at the ruling seat: the census **reproduces exactly with the report in the corpus** (totals equal, 0 of 99 rows differ — F10's exclusion holding live at a second seat), the headline greps confirm to the digit (6 guards / 93 argparse / 88 module-level `parse_args`, and all five invocable files are `e10_*`), suite **736 at both seats**, CI resolved. **The boundary is ruled** (Ruling 3): *the surface is the eight questions; an instrument is ON it iff a served tool invokes it; directory membership confers nothing* — the backing map is the ruling's table, the 58+17+3 stay off as arc evidence, and a future instrument enters by ruling + wrap with the census re-run in the entering commit. **F7 resolved by the instruments' own text** (Ruling 4): `e14_topology` computes the non-manifold count itself and its docstring names the division of labour — *"FOUR QUESTIONS, none of which mesh_stats or e12_nonmanifold.py answers between them"* — so it backs the served tool alone and `e12_nonmanifold`'s render-projected picture is **named in the payload notes, not wrapped**. **F3 disposed** (Ruling 5): task 2-pre extends the census to `tools/verify/` (8 files), so the map rests on measurement over both homes. **Two report defects corrected in place at the ruling** (Ruling 6, dated): F5's star counts were interim contaminated reads (committed: `e12_offsurface` cited **14, a tie** with `e08_ceiling`; `texel_provenance` 11) and its "14 unanchored" is the **cited ≥ 3 slice of a 36-file cell**, threshold now stated. Two laws folded to CLAUDE.md: *a conjunction is governed by its rarest clause* (P1, the sixth unit miss) and *an instrument inside its own population is checked against itself on every axis, every time*. **Task 2 is GREEN-LIT** under kickoff Amendment 1. ⚖ **RULING 10, appended same day at the Director's word on the 7-of-8 flag** (*"I'd like at least 8 tools, if it could be done honestly… a tool that's forced is worse than no tool at all"*): **the eighth tool is commissioned, COMPARE-ONLY.** The honesty test passes on three legs — the job's first consumer is already scheduled (the polish arc opens every lane with a per-profile anchor gate, his own [E14 Ruling 35](E14-ruling.md) clause, asked four times before that arc can start); an honest decomposition exists (**the tool compares, the caller replays** — a recipe-runner would be a shell carrying the safety surface the census's axis F just refused at instrument scale); and it carries existing laws rather than inventing metrics (two-tier identity with byte-identity gate-eligible **only where bytes are the contract**, total **and** largest connected component, and shape carried as a grid rather than collapsed into an invented uniformity score). His bound is written into the ruling in advance: if the build reaches for an unprincipled statistic to fill a field, ship the honest subset or refuse the field — *a refusal there is the specification working*. Scope lands as **task 2c** (kickoff Amendment 2): `tools/verify/anchor_compare.py` + the served `anchor_check` wrap + **the fixture this repo has owed itself twice — a pixel-identical, byte-different PNG pair**, pinning the false-halt class as a can-fail leg. With 2c the server reaches **8 of 8 honestly** and the 7-of-8 gate question is dissolved rather than answered. ⚖ **TASK 2 RUN AND RULED — THE ARC IS ACCEPTED AND THE SERVER SERVES 8 OF 8** (Rulings 11–17) → [E28-task2-report.md](E28-task2-report.md). Re-measured at the ruling seat: gate 4 is **exactly the four files** Amendments 1+2 allow; **gate 5 resolved green** ([`31328637095`](https://github.com/mcp-tool-shop-org/facet/actions/runs/31328637095), `hermetic=success`, with `git merge-base --is-ancestor 065fc6c 520de0e` confirming the green tree contains the arc); suite **790 passed, 436.49 s, exit 0**; `NOT_WRAPPED`'s two remaining occurrences are a docstring and a registry entry, **neither a raise**, so the code is declared-but-dead as reported; `facet-measure` **0.3.0**. **2a's proof was discharged rather than cited and corrects [E27 Ruling 3](E27-ruling.md) in passing** — the crash class is **all-equal extents only**, two-way ties never crashed, which *rules out* the argsort form a casual repair would have reached for; 10,000 distinct triples + 6,000 two-way ties at 0 mismatches, and **byte-identical stdout *and* JSON on 17 of 17 recorded subjects**. Its harness's own false `DIFFERS` (from its `--out` argument appearing in compared stdout) is E15's hash law meeting a text stream, found and fixed. **Ruling 13 ratifies the arc's hardest call**: `anchor_compare` was validated in scratch while the suite ran, so the executor **declared three of its own prediction rows dead as forecasts and scored them SEEN, not HIT**, in the document that would otherwise have credited them — the alternative was calibration theater, and the defect would have been invisible to the advisor. **The owed fixture landed self-validating** (pixel-identical, byte-different, 7,406 vs 7,028 bytes, `byte_identical: false, pixel_identical: true` in one payload) with LCC **400 vs 4** on equal-total blob-vs-scatter — and it was **corroborated live before it shipped**, the advisor having hand-run exactly that comparison twice the same day on the Director's clay renders, where it proved two reported experiments had never executed. **Ruling 16 is the advisor's own miss**: `a3e85db` swept the executor's uncommitted count bumps into an advisor commit — *file-scoped `git add` bounds which files you commit, not whose work is in them* — and that commit's CI went red for the executor's drift, reported as blocked rather than omitted. ⚠ **8 of 8 does NOT open the polish arc**: [E14 Ruling 35](E14-ruling.md) gates it on the four MCP *products*, of which two are done; `comfy-preflight` needs the Director's repo creation and `fixture-lint` is sdlab-side. **Task 3 is the named carry and the arc's only open item**. ⚖ **TASK 3 RUN AND RULED — E28 IS CLOSED** (Rulings 18–26) → [E28-task3-report.md](E28-task3-report.md). `texel_provenance` now prints each class's largest 4-connected component beside its total, **in the instrument** per [E27 Ruling 7](E27-ruling.md); the wrapper's gap-text is deleted in the commit that fills it and `MEASURE_VERSION` moves **0.3.0 → 0.4.0** — the first bump where a serving tool's *payload* changes. Re-measured here: gate 4 is exactly two files, the instrument emits the census, and the one surviving `largest connected component` string in the wrapper is `anchor_compare`'s own docstring, not the stale note. ⚑ **RULING 19 — the pure-move condition was RE-SPECIFIED BEFORE MEASURING**: whole-stream byte identity is impossible when the task is to *add* an output, so naming it would have been a pass condition the work is built to break; the substitute is stronger — **difflib opcodes accepting only `equal`/`insert`**, so a modification cannot hide inside an addition (9 of 9 triples, 0 violations), with `--out-json` and `claim.npy` byte-identical. ⚑ **RULING 20 — THE CLOSED-RULING LAW'S ANCHOR HALF WAS DISCHARGED ON ITS FIRST OUTING**: CLAUDE.md's `2,551,893 → 813,773` reproduces **to the digit** at HEAD from E05-U1 and E06-C1, in the commit that edits the tool — a law written this morning at the Director's question and honoured the same day by a seat that was not in the room for it. **Ruling 21: the measurement earns the law it implements** — DILATION's largest component runs 0.6–5.4% of class, while the SaltRoad hero subjects carry a single **43,000-texel** unpainted region inside an 800k class, which a total alone cannot separate from 43k spread over ten thousand rims. **Ruling 22 corrects a sentence every dispatch since E22 has carried**: the 7,312-file manifest describes `E:\AI\training\{facet_*, saltroad_bake_fix}`, **not** `E:\AI\training` (131,970 files) — confirmed independently at the ruling seat, both figures exact, so future dispatches manifest the eight subtrees rather than the root. **Ruling 23**: `facet_E08`/ARMB **cannot be replayed at all** (no prep file, no recorded `--prep`), which bounds [E30](E30-ruling.md)'s W3 anchor set from a direction that arc never looked. **Ruling 24 is the best self-catch**: six T47 legs passed under an 8-connectivity patch — the declared connectivity was unfalsifiable — and the seventh leg that pins it **makes the executor's own prediction worse**. **Ruling 25**: T41's idempotency gate then fired on the *advisor's* E30 ruling (it cites `e12_offsurface.py`, axis D 14→15) — a legitimate citation, not contamination, so the census is ruled **a derived artifact of the corpus that re-emits with the fold, like the index pair**. **Ruling 26**: P22 missed because *"I predicted a property of my contribution and the instrument measures a property of the tree"* — seventh arc, one family, a new member each time |

| [E30](E30-polish-anchor-gates-kickoff.md) | The polish arc's entry gates. [E14 Ruling 35](E14-ruling.md) is the Director's clause: every polish lane OPENS with a per-profile anchor gate, replays landing as permanent artifacts-tier tests in the commit that makes them. The sword has T7–T12; **do W3, the galleon and the dragon still reproduce?** | **RUN AND RULED** (2026-08-09, at the Director's word releasing the four-tools precondition — *"there's no need to wait on the polish"*; the clause itself is untouched) → [E30-polish-anchor-gates-report.md](E30-polish-anchor-gates-report.md), [E30-ruling.md](E30-ruling.md). **THE ARC IS ACCEPTED AND ITS HALT IS THE MOST VALUABLE THING IT PRODUCED.** Eight anchors land as **T50–T57** (11 tests, 250.66 s): three finalize replays at the **byte** tier — atlases byte-identical, and the galleon's and dragon's sidecars with them — three ceilings and the dragon's elevated at the **value** tier, the elevated payload reproducing *whole*, both ladder rungs, every float; and the galleon's commit anchor across four state files at the recorded 31,581 texels. **No sha256 literal appears in any of them** — counts are *loaded from* the recorded JSON, stricter than T8's literals. Two process notes ratified: the executor found and removed **a check of its own that could not fail** in T56 before it landed, and established the galleon's commit pairing by a **conservation identity** (holes lost equals styled gained, exactly) rather than by running anything. ⛔ **THE HALT: W3's projection does not reproduce** — styled **1,718,750** against a recorded **1,653,659** (+65,091, holes falling by exactly the same). Byte tier flagged it; **pixel tier confirmed it** — 330,481 differing pixels, largest component only **4.8%** of the differing set, so **distributed, not concentrated**, which separates a real acceptance-rule change from the false-halt class that has twice cost this repo a live halt. That is [E28](E28-ruling.md) Ruling 14's two-tier instrument doing exactly its job on its first use by another seat. **A route tool changed under an accepted asset and nothing noticed until an anchor was built for it** — precisely the condition the Director's clause exists to detect, found on its first outing. W3's GLB remains accepted; what is now measured is that today's `project_twins` default would not reproduce its stage-1 projection. ⚑ **RULING 3 OVERTURNS THE REPORT'S REMEDY BEFORE IT COULD BE SPENT**: the report states *"no flag restores the old rule"* and proposes commissioning `--edge-mode global`. Measured at the ruling seat — **`--edge-absolute` already exists** at `project_twins.py:103`, consumed at 754 and skipping the A3 invariant at 797, and the report quoted the comment naming it (line 738) while reading past it. **Third instance of *enumerate the resource before commissioning one* in a single session**, after `e12_offsurface.py`'s nine flags and a model already on the rig. **No tool change is commissioned, gate 6 holds unbroken**, and the remedy is a re-run under the recorded era's flags — `--edge-absolute` named, **not ruled sufficient**, since `--mask-keyed`, `--key-corner-median` and `--trust-intersect` are each era switches on the same rebuilt path; which combination reproduces is the next seat's empirical question. **Ruling 4 names a structural limit found by running it**: T34 pins a stated count against `pytest --collect-only` *of the tree the surfaces sit in*, so **two parallel seats adding tests cannot both be green independently** — E30's commit alone is 801/761, task 3's alone 797/768, only the combined tree 808/768 is right for either. Reconciled at the ruling seat across 23 sites plus the **gap** figure 29 → 40 that a total-replace could not reach; `SHIP_GATE`'s lineage checked rather than trusted and left additive. New rule: **when two seats are live the count surfaces are the advisor's to reconcile after both land** — the dispatch reserved the status table and was drawn one file too narrow, which is the advisor's miss. **Ruling 5**: P1 missed high by 3 and earns a law — *existence of the operands is not replayability*; all three projections have every recorded input, what they lack is an instrument that can still express the recorded rule. P4 and P5 hit exactly. **Eight of a possible twelve stages across three subjects now have gates; a subject's polish lane opens when its gates do** |

| [E29](E29-clay-reconstruction-kickoff.md) | **Stage 0 exists; its benefit does not.** [concept-prep](../concept-prep.md) records a clay hop that is in the pipeline, local-first, with a measured prompt effect and a Director-ruled configuration — and nothing says **the mesh comes back better**. Does image-to-3D on a clay beat image-to-3D on the concept it came from? | **DISPATCHED** (2026-08-09, at the Director's sequencing). One arm, one variable: `--image`, everything else pinned. ⛔ **TASK 0 IS A BLOCKER AND IT IS MEASURED, NOT SUSPECTED** — the reconstructor is **broken on this rig**: `_mesh_character.py` loads the model, starts the pipeline, and dies *inside attention* with `ModuleNotFoundError: No module named 'flash_attn'` (reproduced at the advisor's seat). The handbook records this route running at 103–141 s across six meshes, so something changed or those runs used another backend. The hypothesis — `ATTN_BACKEND=sdpa SPARSE_ATTN_BACKEND=sdpa`, from the sprite line's memory — is **another lane's recollection, offered to be measured and not obeyed**; ⚠ and `trellis2-env` is the interpreter the whole repo depends on, so a `pip install` to chase it is forbidden without the [E23 Ruling 2](E23-ruling.md) argument made in writing first. **No arm runs until one known input reconstructs.** Grading goes **through the served surface** — `mesh_stats` + `mesh_topology`, both shell definitions quoted, identity envelope recorded — which makes this **the measurement server's first consumer on new work** rather than on recorded anchors, the comparability property it exists to provide. Both inputs are already staged and hashed at `E:\AI\facet_scratch\clay_arm\`, and **which clay the arm uses is a stated choice**: the scratch copy is the cloud Nano Banana 2 render, not the ruled local Qwen floor. ⚠ **The sheet comes before the verdict** — `concept-mesh \| clay-mesh` under `--clay` at the Director's zoom, and **no metric in this arc may answer "is it better"**; that is his, and this repo spent four experiments on metrics that could not separate an asset he rejected from one he accepted. Manifest wording corrected per [E28 Ruling 22](E28-ruling.md): the **eight facet subtrees**, 7,312 / 0/0/0, not the training root. Zero credits — reconstruction is local. Halts at `E29-clay-reconstruction-report.md`. **RUN AND RULED** (2026-08-09) → [E29-predictions.md](E29-predictions.md), [E29-clay-reconstruction-report.md](E29-clay-reconstruction-report.md), [E29-ruling.md](E29-ruling.md). **THE ARC IS ACCEPTED**; gates 1–5 PASS, gate 6 written **`NOT YET RUN`** with no id — the fabricated-citation law honoured at an executor seat on its first outing. **The clay mesh returns 9 shells against 82, 1,461 non-manifold edges against 4,201, and 3.66% more polygon budget spent — 73×, 152× and 13.7× their measured noise floors — and NEITHER mesh is fused to anything.** ⚑ **TASK 0: `ATTN_BACKEND=sdpa` ALONE reconstructs** — no `pip install`, no code edit, no pinned version moved, so [E23 Ruling 2](E23-ruling.md)'s harder argument never had to be made; 104 s / 3.4 GB inside the handbook's recorded band. `SPARSE_ATTN_BACKEND` is measured **inert on this route** yet rides in every recorded invocation; the string **stays** (one route's evidence does not retire a citable constant) and the minimal sufficient set is now recorded as one variable. ⚑ **AND PROVING IT OVERTURNED FOUR THINGS.** (1) **[E04's Gate 0 banner inference is corrected in place](E04-gate0-report.md)** — *"what ran is what the log says"* is wrong: `[SPARSE] … Attention backend: flash_attn` printed on all six runs of a session where `flash_attn` is **not installed and cannot be imported**, and which ran to completion; a second banner (`[ATTENTION] Using backend: sdpa`) tracks the dense path and was never quoted. A **declared preference at import, not a record of execution** — the log-line sibling of *a number that reproduces exactly can still be measured against the wrong object*; **E12 carried it as P21** and inherits the correction. (2) **THE GENERATIVE STAGE IS BIT-IDENTICAL AND DECIMATION IS NOT** — three runs identical to the digit through `pipe.run()`, hole-filling and remeshing, diverging only in `to_glb`: faces **±2,618 (0.27%)**, shells **±1**, pinches **±18**. **Nothing in this record carried a reconstruction noise floor, so every prior single-run mesh comparison here has an unmeasured one** — folded to CLAUDE.md as a standing law, and explicitly **not** grounds to overturn any prior finding without measuring its floor. (3) **The sdpa rebuild anchors against the recorded flash_attn W3** inside the floor on every quantity (shells **40 both**), applying *moving a line to different hardware needs an anchor first* to a software move nobody had classified that way. (4) ⚑ **P1 FALSIFIED, AND THE PREMISE WAS THE ADVISOR'S**: the dispatch asserted *"there is no segmentation stage in front of the reconstructor"* — **there is one, inside it**. `pipe.run` resizes to a 1024 max edge, runs **`rembg`** where the input has no alpha, and square-crops to the alpha bbox, so masonry filling 100% of the non-figure frame never reached the geometry (z-min slab **0.2614%** against a predicted 6%, band 2–20%). The same read **retires the declared frame confound by measurement** — both inputs reach the model at **700×1024** — so gate 2 holds on evidence. **The eighth consecutive arc to miss on the unit/population family, and its new member is *the premise you inherited from your own dispatch*.** ⚑ **RULING 4 IS THE ONE THAT REACHES FURTHEST: [E14 Ruling 3](E14-ruling.md)'s REACH IS NARROWED.** Its text says *"every reconstruction this route has made is a hollow double-walled shell"*; read verbatim at the ruling seat, its evidence base is **three longswords, a dragon and a galleon — prop, beast, vehicle, NO CHARACTER**, and the character is the route's founding class. `mesh_topology`'s nested-wall leg **DECLINES to compute** on all five character meshes tested — both arms, a fresh W3 rebuild, and the **recorded W3 and W1 built under the old backend** — because `e14_topology.py:154` needs a second manifold piece above 1% of faces and the largest runs **98.2–98.6%** (the longsword split 54/46). ⚠ **A declining precondition is NOT evidence of solidity** — an inner wall shredded into sub-1% pieces produces exactly this signature while still being hollow, and 800/291/4,154 pieces is consistent with shredding; the executor **refused to convert it**, which is the arc's best judgment call. So the finding **stands unchanged for the classes it was measured on**, the character class is ruled **UNMEASURED**, the remedy is to run E14's other two methods (ray-crossing, cross-section clustering — neither on the served surface) rather than argue about the first, and **E14's closed text is NOT edited** ([E10-off Ruling 1](E10-offsurface-ruling.md)) — the narrowing lands in CLAUDE.md where the law lives. **Ruling 7**: `.mcp.json` declares only `facet-record`, so **no session can reach the measurement server over MCP** — *8 of 8* is true of the code path and false of the transport; routed to [E31](E31-publish-the-pipeline-kickoff.md) as **task 0-pre with a reachability test**, because adding one line and calling it fixed is the same shape as running `--help` on a wheel. The executor's in-process substitute is **ratified** — both instrument sha256s **identical across all five meshes**, the server's first use on new work and the envelope did its job. **Ruling 8** corrects [concept-prep](../concept-prep.md)'s background-normalisation benefit as **downstream-redundant for this pair** (the reconstructor removes the background itself); **its form-register half is untouched and is what the arm measured**. **11 HIT / 6 MISS / 1 unscoreable, no band moved** — and **P4 hit every band while being wrong about what they meant**, having predicted the row *uninformative* about the arm before the arms separated at 13.7× a floor that did not yet exist. ⚠ **WHAT IS NOT RULED: whether the clay mesh is BETTER, and stage 0's promotion — both the Director's**, on the five full-size `--clay` sheets that reached him at gate 4 before this arc's first number was written |
| [E31](E31-publish-the-pipeline-kickoff.md) | `pip install facet-mcp` gives you a record server that needs a checkout and **no measurement server at all**; `npx` downloads a binary carrying the same two modules. **What does it cost to put the eight served tools in the package?** | **DISPATCHED** (2026-08-09, at the Director's sequencing — the pipeline on npm, before `comfy-preflight`). **This arc is not a version bump and fires no tag**: release-sequence steps 1–6 are the advisor's, 7 and 8 are his and are gated out. ⚖ **HIS TWO CLAUSES READ IN TENSION, AND THAT IS AN OPEN QUESTION RATHER THAN A GAP** — *"the pipeline on npm, before comfy-preflight"* against *"we'll keep working on the measurement tools before we publish."* The outgoing handoff read the second as **overriding** [E27 Ruling 8](E27-ruling.md); read plainly it is that ruling's own position in his words, and **the advisor does not resolve an ambiguity in the Director's words by choosing the reading that generates more work** — it goes to him as **OQ1**, and the arc's work is identical either way. ⚑ **The handoff's supporting argument is corrected in the spec rather than inherited**: it claimed publishing-after would put a comparability boundary "inside the comparison," but `MEASURE_VERSION` has already moved 0.1.0 → 0.3.0 → 0.4.0 and comparability is carried by the **identity envelope** every payload records, not by publication — R8's live concern is the narrower one, *freezing an API nobody has exercised on new work*, and E29 is that first consumer and has not run. **Scope measured at the dispatch seat, every claim with its line**: `py-modules` ships two modules and `measure_mcp` is absent (`pyproject.toml:50`); **neither `tools/diagnostics/` nor `tools/verify/` is packaged** while the served tools invoke instruments as **subprocesses** at `REPO/tools/<name>` (`measure_mcp.py:309, 325`); and `measure_mcp.py:139` carries **E24's defect verbatim** (`REPO = dirname(HERE)`) in a file written *after* E24 fixed it elsewhere — the consumer-grep could not have caught it, because the consumer did not exist yet. ⚑ **AND THE THIRD DEFECT NOBODY HAD NAMED**: the package declares **`mcp>=2.0.0` and nothing else**, while the eight instruments need `numpy`, `scipy`, `trimesh`, `open3d` and `PIL` — **357.5 MB installed**, of which `open3d` alone is **207.1 MB**. So a clean install fails at **three distinct layers** (resolver / missing file / missing import) and **only the first is E24's**. **Four of the eight need `open3d` and four do not** — and the four that do not include **both anchor tools**, the half [E14 Ruling 35](E14-ruling.md)'s per-profile gate actually calls, which makes a tiered extras split a real lever rather than a hope. **Task 0 is the inverted halt**: build the wheel-tier test — clean venv, `pip install`, run a **measurement verb**, never `--help` — and **it must FAIL on arrival**; a test that passes before any fix has tested nothing, and if it passes the seat **halts**, because the test is wrong rather than the package. Task 1 measures two shapes and **chooses neither** (instruments ship, resolved package-relative · instruments do not ship, `facet-measure` needs a checkout) — ⚠ and **E24's own fix is the WRONG remedy here**, because `RECORD_MARKERS` keys on the **corpus**, which `facet_index.py:110` states in its own comment can never appear in an install, whereas instruments are code and *can* ship. Task 3 tests rather than assumes `pyproject.toml:43-47`'s design note (that packaging `tools/` would rewrite the recorded `python tools/<name>.py` invocations that are citable evidence) and names the second-order cost the note omits: top-level packages called **`diagnostics` and `verify`**. Task 4 measures the binary against `bin/facet.js:18`'s own stated precondition — *"a package whose dependencies are stdlib, sqlite3 and mcp"*, **the sentence shipping the measurement server voids** — sized against the 2 GB asset cap that killed backpropagate's binaries. **T58–T62 reserved**; the count surfaces are the advisor's and are **reserved out**, per [E30 Ruling 4](E30-ruling.md). Halts at `E31-publish-the-pipeline-report.md`. **RUN AND RULED** (2026-08-09) → [E31-predictions.md](E31-predictions.md), [E31-publish-the-pipeline-report.md](E31-publish-the-pipeline-report.md), [E31-ruling.md](E31-ruling.md). **THE ARC IS ACCEPTED — and it is the first time anyone measured what an installed facet can do.** Six tiers, each an **actual wheel in its own clean venv**, every tool called through the server's own dispatch: today's wheel **8 of 8 failing**, +module 8, +instruments+path+`subject_profile` 7, LIGHT (numpy/scipy/trimesh/pillow, 277.9 MB) **4**, FULL (+open3d, 634.6 MB) **0**. **The wheel CAN carry the server** at 58 KB/2 entries → **506 KB/114**, two generic top-level import names, 100 files under `diagnostics` of which 5 are served, and open3d's ~230 MB transitive tree entering `pip-audit`'s population. ⚑ **RULING 2 IS THE ADVISOR'S MISS**: the dispatch specified three failure layers and there is a **layer 0 in front of all three** — at the shipped wheel the server module is *not in the artifact*, so "where does it fail" has no operand. A population of failure modes that omits its first member. ⛔ **RULING 3 — open3d, and it is WORSE than the report states.** Verified here: **0.19.0 is the latest**, ships **cp38–cp312 only**, and has **no sdist**; the rig runs **3.13.13**. And the part nobody had looked at — this rig *has* open3d, so PyPI cannot be the source: the installed distribution is **`0.19.0+241aaee`**, a **cp313 wheel from Open3D's `main-devel` channel**, an unreleased dev build. **(3a)** A direct-URL dependency cannot be declared in PyPI metadata, so **the FULL tier cannot be DECLARED, not merely not installed** — any FULL extra forces `requires-python <3.13`, contradicting the interpreter the whole repo runs on. **(3b)** ⚑ **Every open3d-dependent number this repo has produced was measured on an unreleased dev build**, and **four of the eight served instruments need it**; the identity envelope carries server version, instrument sha256 and config hash **and no dependency set**, so a number reproduced elsewhere against released `0.19.0` is compared against a git-hash-suffixed build and the payload cannot see the difference. Nothing recorded is withdrawn — this names a comparability component that is invisible while one rig makes every measurement and becomes load-bearing **the moment the tool is published**. ⛔ **RULING 4 — THE BINARY IS RULED OUT, AND SIZE WAS NEVER THE QUESTION**: 162,962,583 bytes is **12× inside** the 2 GB cap and it answers `--print-tools` with exit 0, but **cannot run one measurement** — in a one-file PyInstaller build `sys.executable` *is* the binary, so every instrument spawn re-enters the server's own argparse. **[E24](E24-ruling.md)'s exact shape, caught before a release rather than after four.** So `bin/facet.js:18`'s precondition resolves rather than being voided: the npm binary door stays open for the **record** server and closes for the **measurement** server, on process identity. ⚑ **RULING 5 — `anchor_check` CARRIES A REAL DEFECT AND IT IS THE POLISH ARC'S ENTRY GATE**: `anchor_compare.py:101-106` imports PIL **inside** the `try`, and `except Exception` catches `ModuleNotFoundError` identically to a decode failure — so with PIL absent the run reports *"one or both inputs do not decode as images"*, blames the caller's PNGs, degrades to **byte tier only** and **exits 0**. That is the exact configuration the pixel tier exists to escape (two recorded false halts; [E28 Ruling 14](E28-ruling.md) built it; [E30](E30-ruling.md)'s W3 halt was *confirmed* by it), sitting under [E14 Ruling 35](E14-ruling.md)'s per-profile gate. **RULED: a missing dependency is an environment REFUSAL — exit 4, not a per-input decode result** — commissioned with a test that installs without PIL. **Ruling 6 answers OQ2 by measurement**: **Shape A** (instruments ship) works at 0-of-8-failing, and the tier is decided by 3a rather than preference — **ship LIGHT**, declarable everywhere, carrying `mesh_stats`, `mesh_topology`, `measure_report` and `anchor_check` (**both anchor tools**, the half the polish gate calls), with the open3d four documented as needing an out-of-band build; a forced FULL extra is the *"tool that is forced"* the Director's own bound rejects. ⚖ One clause is his: whether a server where 4 of 8 need an out-of-band dependency satisfies *"the pipeline on npm."* ⚑ **RULING 7 — MY GATE 6 WAS ONE SURFACE TOO NARROW AND THE FILE I MISSED RECORDS THE LAST TIME SOMEONE MISSED IT**: T34 pins **sixteen** counts across **six** files — plus a separate leg over the **seven translated READMEs**, 28 substitutions — and my reservation named four, omitting `SHIP_GATE.md` and `handbook/reference.md`. ⚑ **The ruling's own first draft then said "nine pins across five files", transcribed from a failure list — the third instance of this error in one session, made inside the ruling about it.** A failure list shows what is currently stale; `T34.PINS` shows what is watched, and those are different sets. `SHIP_GATE.md`'s own text reads *"the outgoing handoff enumerated four stale surfaces… and did not name `SHIP_GATE.md`, so a hand-written list of surfaces is itself a live-moving quantity."* **Ruled: a dispatch reserves count surfaces by naming T34's PINS table, never by transcribing it** — and the census belongs in the same reservation, being machine-emitted and un-hand-reconcilable. Reconciled here **808/768 → 859/819**, lineage extended not overwritten. **Ruling 8** ratifies the executor's handling of `943d2d5`'s message stating `818/778` against a measured 859/819 — *"arithmetic, not measurement"*, commit left standing with the correction beside it: **never compute a count you can collect**. **Ruling 9**: P1 is the **ninth** consecutive arc on the unit family, new form — **counting a surviving *path* as a surviving *tool***. Nothing tagged, published or released; no version moved; `MEASURE_VERSION` unchanged at 0.4.0 |

| [E32](E32-armature-mark-through-the-route.md) | Does the route hold a **lattice of thin tubes** — armature's brand mark, a subject that is mostly holes — and does occupancy-grid arithmetic bound what the reconstructor preserves? | **RUN AND RULED** (2026-08-10) → [E32-report.md](E32-report.md), [E32-ruling.md](E32-ruling.md), Rulings 1–16 + [gate-0 predictions](E32-gate0-predictions.md). **The question is answered YES**: 17 of 21 openings preserved (81.0%) at a matched relative threshold, thickening **1.174×**, no bridging — against the executor's own registered falsifier predicting 0–6 openings from `ss_res = 32` voxel arithmetic. ⚑ **The finding: occupancy-grid resolution does not bound reconstructed topology** — the sparse sampler is learned and the cascade decodes a continuous field inside occupied voxels, so voxel arithmetic is a *lower* bound on achievable detail; adopted as a **bounded finding, deliberately not doctrine** (one subject, one class, noise floor unmeasured on it). The nested-wall leg **computed for the first time on this route** and separated E29's conjunction in the direction that matters: inner volume negative with `material_frac_of_outer` **0.9518** — a nearly-solid body with a 4.8% void, so **a negative inner volume alone is not the hollow signature**. ⛔ `project_texture.py --min-iou 0.80` **fired at 0.5878 and the halt is upheld**: silhouette IoU is area-normalised and this subject's mask is 10.8% of its own bbox, so a uniform 3–4 px offset reads as collapse — **the threshold does not move, no replacement metric is commissioned**, and step 3 honestly does not complete on this class. Two instrument defects adopted with tests (T64): `bbox_blowout` was a two-axis conjunction that could not fire on E08's own one-axis case; `abs()` in the plate residual could not express *subject darker than ground* (`--polarity`, default unchanged). Five hand-copies of the fitted-background model enumerated under four different names — **a name-based search is not an enumeration**. Ruling 10 routed three defects outside this repo to the Director (the character-turnaround skill's stage-3 path, its missing required reading, the diverged `turn_render.py` twin). **Rulings 12–16 are the arc's structural legacy**: the index-rebuild precedent measured unanimous — and the certificate is written only by `record_build`, so `facet_index.py build` alone leaves a half-pair; the **terminal-commit law** (derived artifacts regenerate in a second commit whose own content is not an input to them); and **a conclusion read off a truncated listing is not a measurement** — three truncated-listing instances in one session, folded to CLAUDE.md. Count surfaces reconciled off T34's own PINS, 891 → 917/877 |

| [E33](E33-the-first-performer-through-the-route.md) | Does the route hold **armature's first performer** — a solid-limbed wooden lay figure, the composed route's subject — and which register is canon for it? | **RUN; RULED BY THE DIRECTOR MID-ARC; ASSET DELIVERED** (2026-08-11) → [E33-report.md](E33-report.md), [E33-predictions.md](E33-predictions.md), twin-prompt JSONs r1/r2/r3. No separate ruling doc: the register ruling landed as report §14b and the arc closed on the delivery line. Reconstruction under `ATTN_BACKEND=sdpa` (104 s band, ceiling never approached); the class measured **solid** (bbox fill 3.25× the E32 lattice's, zero enclosed openings, 37.76% of area in 64 px+ members); ⚑ **the nested-wall leg COMPUTED on a humanoid for the first time** — second manifold piece 15.8% of faces, `material_frac_of_outer` **0.5074**, a 49.3% cavity sitting between E32's near-solid and E14's thin-walled shell, so the character-class wall question is now measured on one subject and remains a ruling's question. Frame derived 352×1024 (÷16, no VAE truncation); silhouettes stable across decimation to 300k. Six twin candidates — **three registers × two views** — at zero partner credits, GPU-hours **$0.612951** measured as a bucket delta (**≈ $0.102/job**, the first per-job figure in the record); ⚖ **the Director approved R3 — unglazed terracotta, NO LoRA** (wood-grain remark recorded as a note, not a change order). Projection ran **two views** (y+000/y+180): reg-IoU 0.8605/0.8475 against W3's inherited 0.80 floor; ⛔ the withdrawn `bg-max-pct` **fired at its resurrected pre-withdrawal default** (`project_twins.py:93` holds 2.0 where [E16 Ruling 4e](E16-ruling.md) withdrew to 100.0 — a root cause with more sites than the one fixed), re-run at the ruled suspension value passed explicitly with provenance, the one parameter change after a fired gate, named not smoothed; **brush NOT RUN** (register collision: `texpass_brush.py:71` hardcodes the saltroad LoRA + a W3 identity prompt; local ComfyUI down at the falsified VRAM position); fill closed **927,492 hole texels by surface-aware dilation alone** — styled 84.2% of two-view reachable, reachable itself 73.7% of valid. Delivery per F01: `performer_textured.glb` sha256 `9e20ea7d…`, 21,588,628 bytes, unrigged, 117-file manifest. Predictions 7 hit / 4 miss + one conjunction written on one line and owned; P2/P6's misses are one fact (the 15.8% inner surface no exterior camera sees). Two instrument defects found and not fixed (`e14_twin_registration`'s bbox tolerance measured against the mesh not the frame; its eight-label mirror assumption crashing on six). ⚑ **Post-delivery, 2026-08-13: armature measured unpainted texture-projection patches on six of eight turnaround views** (jaw, temple, shoulder, ribcage, flank; 0/4 clean) — texture truth, not render artifacts — then watched them propagate into hosted-tier generations at the same landmarks; **ruled TOP PRIORITY by the Director** → [known-defects.md](../known-defects.md) final entry; the repair is **E34** |

| [E34](E34-projection-coverage-kickoff.md) | ⚑ **The projection-coverage arc — TOP PRIORITY by the Director's word, 2026-08-13.** Six of the performer's eight views carry unpainted patches — dilation fill, not paint — the named confound on every downstream identity read. Does extending the twin set from two views to the route's eight, in the approved R3 register, remove them? | **DISPATCHED** (2026-08-13, frame confirmed contrastively with the Director — views-first repair). The mechanism is already the record's ([E33 §14b–c](E33-report.md)): two-view projection, brush deliberately NOT RUN, **927,492 hole texels closed by dilation alone** — the patches are lateral surface no front-or-back camera faces, and the eight-view set was deferred only for the register ruling that has since landed (R3). **One variable moves: view count 2 → 8.** Six new R3 twins at E33's byte-pinned recipe (recorded prompts must reproduce byte-exact for views 0/4 before the six are trusted) → eight-view projection → surface-aware fill → pack → **candidate GLB beside the recorded asset, which is not touched** (`facet_E33` read-only, manifest-gated at open and close). Brush stays out of scope; the wood-grain note stays a note. **No pass condition is invented** (suspend-not-invent): per-view provenance with largest-connected-component (the [E28 Ruling 21](E28-ruling.md) form) and BEFORE/AFTER sheets are diagnostics; the hard guards are regression-shaped — mesh untouched, views 0/4 stay clean, manifests 0/0/0. Embedded rulings: the E16 R4e `bg-max-pct` withdrawal **governs unprofiled runs** (suspension value passed explicitly with provenance); `--reg-iou-min` 0.80 untouched and a firing HALTS with numerator and denominator reported; era flags unused (new work under today's defaults). Ceiling **8 cloud jobs** (~$0.102/job by E33's measurement), zero partner credits, one-re-roll rule. Tests T66+; count surfaces reserved by `T34.PINS`. Halts at `E34-projection-coverage-report.md`; armature re-surveys on request once a candidate exists; hosted-tier revalidation is the Director's pricing call. **RUN AND RULED AT THE MEASUREMENT LEVEL; THE DIRECTOR'S EYE HOLDS THE ACCEPTANCE** (2026-08-13) → [E34-projection-coverage-report.md](E34-projection-coverage-report.md), [E34-predictions.md](E34-predictions.md), [E34-ruling.md](E34-ruling.md). Six twins at the pinned recipe, **6 of the 8-job ceiling, 0 re-rolls, 0 failures**; G1 recovered all three enumerations with zero commissions, proving E33's unrecorded projection invocation by **replaying it to byte-identity**. The repair at the instruments: holes into finalize **927,492 → 157,228** (−83.0%), styled/reachable **84.2% → 98.4%** (reachable 73.7% → 95.1%), DILATION largest component **22,457 → 7,390** — total and largest fell *together* (the E28 Ruling 21 form); both finalize gates passed with wider margins (median 1.822 edges against 3.0; beyond-20 0.333% against 5%); `--reg-iou-min` never fired, minimum 0.8475 **at a pole** — the profiles are the best-registered views in the set (0.9479/0.9349), falsifying P1–P5 through one mechanism: *a model fitted on the only measured members, when those members are the outliers*. Poles reproduce E33 to the digit; the recorded asset is **byte-unchanged**; manifests 0/0/0 at open and close. A render confound was removed rather than reported — E33's `turn_final` invocation was never recovered, so both GLBs were re-rendered through one identical call (texture-space luma 112.82 vs 112.73, same material; the confounded sheet kept beside the controlled one). ⛔ Two gates fired at the ruling seat, neither the arc's own: **T24** — the dispatch commit created E34 while `conventions.json:237`'s declared span said E33 (the report's `facet_index.PAID_RE` site was one level too shallow: the pattern is a **declared field**, re-exported, grepping nowhere in the module) — bumped in the ruling fold, the advisor's own miss, suite red for the arc's duration; **T41** — the census stale by the report's own five citations (third arc's-own-paper instance), regenerated. Spend resolved at ruling time: the posted bucket reads **$0.110647 for six jobs** (≤ $0.0184/job), revising E33's unattributed $0.102/job envelope **5.5× down**. ⚖ **ACCEPTED AND CLOSED** (2026-08-13, the Director's eye on both sheets, conditional on the suite — met at **927 passed, 0 failed**, [Ruling 9](E34-ruling.md)). **The fifth accepted asset, and the first produced as a repair of another**: `performer_textured_8view.glb`, sha256 `ce793064…`, 22,284,208 bytes. `facet_E34` manifested (**84 files, self-excluded by construction**) and read-only henceforth beside `facet_E33`; the residual dilation is accepted for this asset and the R3-brush question parks; candidate anchors commissioned (T66+); armature's re-survey unlocked on the Director's relay |

| [E35](E35-clean-twins-kickoff.md) | ⚑ **Clean twins — TOP PRIORITY by the Director's word, 2026-08-14.** The dark-speck class: generator-painted dark flecks baked into every twin (texture truth; controls clean; predates and survives E34 — [known-defects](../known-defects.md) final entry). Can the route produce clean paint — starve the mechanism at generation, cancel it across seeds, and despeckle what remains — **with the despeckler built out as a tested route capability**, and the repainted performer passing the Director's eye? | **DISPATCHED** (2026-08-14, at the Director's mandate — comprehensive fix, study-swarm grounded: *the despeckler completely built out, more control over the process*). **Five-agent research grounding landed first** → [E35-speck-research-grounding.md](../research/E35-speck-research-grounding.md): the mechanism is the register prior under an over-denoised near-flat init (⚑ a vendor denoise band once quoted here was **falsified at its cited source 2026-08-14** — the card carries none, and the knee is theory-predicted; the card's actual explicit recommendation is `controlnet_conditioning_scale ∈ [0.8, 1.0]`, which the recorded 0.9 sits inside), conditioning at 0.9 hardens what the sampler invents (the scale knob was never ablated in the source paper — a named absence), quantization is **exonerated by signature** (bf16 demoted to a conditional discriminator; attention sinks named as the better-matching alternative), cross-seed **median fusion at K=3 with a first-class disagreement map** is the principled suppressor gated on measured structural agreement, and the despeckler arrives spec-shaped (area-opening detector keyed to one px² threshold, classification-gated correction byte-identical outside flagged footprints, refuse bounds keyed to the figure not the frame). Local diagnostics beat the swarm home: the final-atlas census splits core-black **61% twin-painted / 39% fill-propagated** — the fill extends twin dots, so **the despeckler's home is the twins, before projection**. The arc: detector first (T66, validated against the Director-rejected twins per the metric law) → seed A/B ×3 (the discriminator: dots move vs stay) → denoise sweep 0.85/0.80/0.72 toward the predicted knee → conditioning 0.65 or scheduled → fusion prototype → **Gate R, the register at the Director's eye** → corrector (T67, Vincent-property byte-identity pinned) → eight-view repaint at his picked configuration → `facet_E35\out\performer_textured_clean.glb` beside two untouched recorded assets. Ceiling **45 cloud jobs ≈ $0.85**, zero partner credits; span bumped to `E3[0-5]` in this dispatch's own commit; halts at `E35-clean-twins-report.md`. **RUN AND RULED — CLOSED** (2026-08-14→15) → [E35-clean-twins-report.md](E35-clean-twins-report.md), [E35-ruling.md](E35-ruling.md), Rulings 1–11. **47 of 60 spent, thirteen returned; five directions closed by measurement**: the seed frontier is one-dimensional (dark and pale trade, neither dies); cn weakening RAISES the pale class — anchoring suppresses it, so cn is counter-indicated for pale and null for dark; denoise kills the register before either class (C\* 23.77 → 1.89 by 0.72, census-0 reached only by reverting 56% of the figure to clay); no slate lever moves the dark class while keeping the man (flat-even-lighting the sole positive scorer — pale 278 → 226 — its prompt term held for the Director's ratification, since it edits a ruled register term); and **2509 is class-worse ~2.9× at both measured configurations while out-registering the canny route with no ControlNet at all** (reg-IoU 0.9611 against the recorded 0.9372) — recorded for whatever future wants it. The pale class resolved into **two signatures** (chroma-collapsing init-bleed, mechanism ESTABLISHED, anchoring suppresses it; chroma-preserved lightening — the class he rejected — mechanism OPEN), read by the chroma-split instrument, never by eye at working zoom (four perceptual misses; B2's pale-direction hit was made by *refusing to look*). The 2509 quantisation trigger measured to a bounded black box: a 2,620-unique-colour Workbench render corrupts its edit encode deterministically, ~5,000-colour inputs are clean, one lanczos round-trip repairs — **the second `STUDIO_MEASURED` entry, ruled in** (Ruling 11); the E01 family widens to its third consumer class. The protection walk became a committed instrument at last (`tools/verify/tree_manifest.py`, T70, eight-leg can-fail fixture) after three arcs of inline re-derivation; `facet_E35` enters protection (335 files, self-excluded). **No repaint candidate produced** — R2-b, the best-measured configuration, stands rejected at the Director's eye; E34 remains the accepted-and-reopened asset. The armature relay's held target ("E35's candidate") is thereby void — re-target at his word; the E34 disposition and the E34 candidate anchors remain open. CI green at `694a9e3`, resolved live; the terminal pair at `9bd2215` |

| [E36](E36-route-arms-kickoff.md) | ⚑ **The route arms — dispatched at the Director's word, 2026-08-15** ("Why haven't we tried this?" — the dual-control arm, pointed at directly). Two levers that act on the measured mechanisms, never tested in their correct form — dual control **depth-early (0.80 / start 0.0 / end 0.45) + canny (0.45, full schedule)** and the **flat-lit init**: can either, or both, move the classes while keeping the man? | **DISPATCHED AND RUNNING** (2026-08-15; the kickoff + its execution-spec amendment supersede the thin arm text — read both; grounding: [consult #5](../research/repaint-route-consult5-grounding.md), calibration PASSED and generalized — every served partner texture path is identity-blind by schema, three vendors, zero reference-image inputs). One variable per arm, view 1, seed 770700, the recorded recipe otherwise; **job one is the structural anchor 0d** — the rewired dual graph at depth strength 0.0 must reproduce the recorded twin pixel-identically (platform determinism E35-measured, ΔE 0.0000 on 360,448 px) or halt the arm; blind bands seal by commit before anything spends, three branches per hypothesis with the UP branch live, every band positioned against the seed floor (count 7–26 / area 34–139 px²); the register floor ANDON binds (arm 2's failure signal is C\*, not identity); the pale class is read by instrument only (the C3 law); identity is the Director's at every sheet, full size. The r3L prompt term is his to ratify — the arms run the recorded R3 prompt unmoved. Ceiling **15 jobs**; the 8-view rebuild only at his word at a sheet; the corrector-contract track (local, above the 36 px² corrector cap, validated against his rejected artifacts before any A/B) is the arc's second front, specced separately before it runs. ⛔ **The open manifest gate FIRED and taught** ([E36-open-manifest-halt.md](E36-open-manifest-halt.md)): `facet_E35` 335 declared / 336 present — the E35 close's own verify receipt, written into the tree eleven seconds after the seal it reports HELD; the E28 self-reference family at the receipt layer, unswept when its census sibling was found the same day. The Director ruled: delete the receipt (bytes recorded first, content transcribed in the committed halt doc); re-verified **HELD 335/335**, the seal made true rather than rewritten; every receipt since lands outside all protected trees; the `tree_manifest` verify-path guard and its T70 leg named as owed tool work. **Task 0 zero-cloud complete at spend 0** ([E36-task0-report.md](E36-task0-report.md)): the unrecorded clay-render invocation — a gap E34 recorded of its own sibling call — reconstructed and PROVEN before use (0 of 360,448 px differing while the file hashes differ, the byte-vs-pixel law's third live firing); instrument anchors reproduce bit-identically at full float precision (pale 20 values, register 50); and **two of arm 1's specified terms falsified by measurement, ruled at the halt** (Amendment 2): the flat control collapses to pure contour — canny contributes nothing under flat light — so arm 1 runs with the recorded control pinned and the init as the single delta; and the ~5,000 unique-colour guard condemned the recorded baseline itself (2,303) — withdrawn for this route, the count a reported diagnostic, lanczos demoted to the named mechanical repair. Tests T71+; span `E3[0-6]` bumped in the dispatch commit; halts at `E36-route-arms-report.md` per arm sheet. **CLOSED — SUPERSEDED at the Director's directive** (2026-08-15, relayed from the armature seat: *remake the performer from source; no more repair arcs*) → [E36-ruling.md](E36-ruling.md), Rulings 1–4. **Spend 0 of 15 — no cloud job ever fired, the bands were never sealed, zero loose state.** Both remaining defect classes are generation-baked (specks generator-painted, measured to floor at E35; pits mesh-geometry, in no texel), so the arms' texture route on the old mesh is moot. Banked permanently: the open-halt lesson and the [tree_manifest guard spec](../specs/tree-manifest-guard-spec.md); the proven clay invocation (0 of 360,448 px, recorded where two arcs never wrote it); the flat-control route fact (canny contributes nothing under flat light); the withdrawn colour guard with the quantisation CHECK intact; bit-identical instrument anchors; the corrector track's validate-against-rejected-artifacts constraint. T71+ passes to E37. The successor is **[E37](E37-remake-the-performer-kickoff.md)** |

| [E37](E37-remake-the-performer-kickoff.md) | ⚑ **Remake the performer — the Director's directive, 2026-08-15** (relayed from the armature seat): *remake from source; no more repair arcs.* A new mesh and a new texture, made clean rather than cleaned — can the route produce a clean performer from the recorded source plate (mesh without pit shading, texture without specks or pale patches, **the same man at his eye**) by candidate selection at each generation stage? | **DISPATCHED** (2026-08-15 — [E37-remake-the-performer-kickoff.md](E37-remake-the-performer-kickoff.md); armature's five consumer requirements are the acceptance frame, verbatim in the kickoff). Source pinned at dispatch: armature's Director-picked plate, sha256 `75338325…`, 1,216,363 bytes, read-only. The design is selection-not-repair at every layer: **Stage A** six TRELLIS seed candidates, local and free (TRELLIS deterministic per input+seed — E29 Ruling 5 — so candidates vary seed, never re-roll), each screened on solid renders lit-vs-`--flat` (a lit-only dark mark on an untextured render is geometry — the pit screen, count + largest component per the E28 Ruling 21 form), his pick at the sheet; **Stage B** K=3 full eight-view twin sets on the picked mesh at the recorded recipe (770700 · the measured-best 987654 · one fresh), census-selected by pre-registered rule sealed before the first job — one GLOBAL seed, lowest dark census subject to the register floor and reg-IoU ≥ 0.80, per-view mixing out (it produced the rejected pale wash) — his eye on the winning set before projection; the r3L prompt term runs only on his ratification; **Stage C** the E34-proven projection + fill + **RGBA-true turnarounds** (alpha from the raycast silhouette masks — E36 measured the route's renders at flat-255, so this is new deliverable work); **Stage D** acceptance at his zoom, `facet_E37` manifested read-only, delivery GLB + sha256 + ruling + accepted-with observations, the relay to armature carrying path and hash for their zero-credit acceptance survey. Ceiling **40 cloud jobs ≈ $0.74** (Stage B 24 + contingency; Stages A/C local, free), his number to confirm. Tests T71+; span `E3[0-7]` bumped in this dispatch's commit; three Director halts (the pick, the set, the acceptance); halts at `E37-remake-report.md` per stage. **HALTED AT THE DIRECTOR'S WORD** (2026-08-15) → [E37-ruling.md](E37-ruling.md), Rulings 1–28. The arc ran the full road — the plate cleaned of generator fingerprint-swirls (round 2), his mesh pick (seed 987654 clean-plate), the wood register his-approved at Gate-R, the single-seed set selected after the four-woods rejection, the mask protocol perfected through four zero-cost halts, the corrected-recipe repaints landed with their proofs (`f1c2b50`) — **and the composed result still failed his eye**: face features painted on non-face views, the lift boundary reading as a square. His assessment, adopted by the record: **no configuration of this route has yet made the generator paint eight consistent, defect-free views of this subject** — per-view generation plus composite repair did not converge; the record's open problem #1. Spend stopped at **58 of 80**; `performer_v3.glb` and the repaired set stand as rejected candidates with full measurement; **E34 remains the delivered performer**; what builds the asset next is the Director's open decision. Banked permanently: the seed-capable reconstructor (T71), the mesh, the register, the measured levers and their set-level limits, the probe-first and walk-at-native laws, and the falsifications (per-view mixing, the local ColorMatch, the census on the head band) |
| [E38](E38-material-route-kickoff.md) | ⚑ **One material, not eight paintings** — the Director's directive after E37's halt (2026-08-15, *"a completely different route, making sure that it's commercial-safe, so that we can publish"*; widened 2026-08-16 to *"the point of these experiments is to gain control over our art"*). Can the performer be ONE material authored directly in UV space — identity carried by geometry plus a decal — instead of N independently-sampled painted views? | **RUNNING** — spec written mid-flight 2026-08-16 with its lateness stated rather than backdated, under the [dispatched-seat standard](../../CLAUDE.md). **Two classes removed BY CONSTRUCTION**: the four-woods incoherence, and the skull seam (confirmed an owner boundary at **zero-pixel** separation from an owner transition, replicated on the flat render's own peak, 7–15× fall at v4's own fixed pixels). **The dark-mark class survived** — 847 → 425, with **69.2% within 3px of a v4 mark across two unrelated colouring processes** — which placed it in the shared atlas substrate rather than in any generator, where three arcs had hunted it. The bisect split it in two: **Population A 57.94%/60.34%**, UV landing outside any triangle's footprint, **100.00% reading atlas RGB exactly (0,0,0)** against a **0.00%** null; **Population B 42.06%/39.66%**, atlas clean at the exact texel, boundary-distance median **1.414** against the null's **2.828**. Both then confirmed by **INTERVENTION** rather than inference — a magenta atlas fill left **0%** of A dark and shifted **100%** of B at median ΔE 66–67 *while nothing at B's own nearest texel changed*. Margin is not the mechanism: A sits a median **18.0 texels** from the nearest valid texel, **0/766 within either candidate margin**. **A4 (`--res` 4096 → 8192) collapses Population A 92.7%/93.6%** (766→56, 776→50), census total −66.8–80.7%, quality gate not firing, utilisation unchanged; the residual is ~half the same **12 zero-UV-area triangles**, immune to resolution (0 × 4 = 0). **The continuity is the finding**: `bake_hero_prep.py`'s own `--reunwrap` help text carries E05's *"at 8 faces an island is small enough to be entirely unpainted, and 54.6% of them were"* — **that IS Population A**, and apples-to-apples our 13,722 islands / 21.9 faces is *normal* for this route against W3's 14,010 / 20.5, so **the class is a property of every asset this route has produced — measured here, and UNMEASURED on the four accepted ones**. The advisor's five asserted mechanisms were all wrong (the parked faces · an invented 1.0-texel² threshold · the bake margin read as 16/EXTEND where the call site sets 8/ADJACENT_FACES · the `--reunwrap` arm, built off a truncated grep line · an island count set beside the wrong object): **dispatched seats killed three, a measurement killed one, one returned PARTIAL**. **RULED WITHOUT CLOSING** (2026-08-16) → [E38-ruling.md](E38-ruling.md), Rulings 1–9, written at the incoming seat because A10 had sat unruled across a seat change and an unruled arm is a shelf. **A10 is the sweep's clean negative and nothing is adopted from it**: both padding values land 3.3–3.4× A11's own Population B, and what makes that a finding rather than a null is that the lever *demonstrably moved real geometry* — 17.34 points of valid-texel coverage shifted interior→gutter and Population B did not care; the transferable fact is the one the arm never set out to measure, **xatlas's default charting producing 12,979 islands against Blender-native's 8,601 on the identical 148,062 visible faces**, same instrument, same population, which is the only reason it is citable at all. **Nobody picks a winner**: on 5.2 the three levers are right (766 → 4), on 5.3 stock reaches 0 and *the same levers make Population B worse* (165 → 257) — they were compensating for the Blender bug — so **5.2 remains the route and 5.3 alpha stays an instrument**. ⚠ **The anchor was re-run at the ruling seat and its wording corrected**: all 8 renders are **PIXEL**-identical (0 differing px, maxabs 0), not byte-identical — every PNG hash differs — the byte-vs-pixel law's **third** live firing, and a byte-check re-run would have produced a false halt on a correct result. ⚖ **The framing correction is the ADVISOR'S and is adopted**: at the Director's word (*"let's not over focus on the black artifacts"*), E38's route question is answered and **E38's class is not W3's defect and never was** — 0.578% of W3's figure pixels, zero black — the polish question opening as [E39](E39-w3-polish-kickoff.md). Three CI reds inherited and unnamed by the handoff were verified against HEAD and repaired in the ruling's own commit (T34's experiment pins at 37 against a table of 38; three kickoff anchors destroyed by a rewrite that did not re-pin them; `conventions.json`'s span still `E3[0-7]` with E38 in the record). **Left open and NOT commissioned**: A5/A9, the shipping configuration, the back-catalogue re-bake (W3 cannot be re-baked at all), the **1,035 zero-UV-area faces** that `smart_decimate.py:220-225` cannot see because it guards UV collapse by whole-mesh variance, and whether Population B and E07's 74.9% cross-island write are one class at two sites |

| [E39](E39-w3-polish-kickoff.md) | ⚑ **W3's polish — the Director's live redirect, 2026-08-16**: *"let's not over focus on the black artifacts. W3 is far from perfect and needs a serious polish"*, and on method, *"do a study-swarm instead of guessing."* The defect his eye names is **a region wearing another material's colour** — gold across the green tunic, skirt and boot tops; gold and rust over the steel blade; green down the sword grip; brown-green on the hands and right bracer. **Which provenance class carries those regions?** | **DISPATCHED AND RUNNING** (2026-08-16, under the [dispatched-seat standard](../../CLAUDE.md); the dispatch IS the spec and lands in the repo). The answer forks the road and the two branches are not close in cost: **`dilation`** makes it cross-island bleed, and the fix is small and ours — `texpass_finalize.py:155`'s predicate is `fill = ~grown & (cnt > 0)` with **no island constraint at all**, and while the record already falsified adding `& valid` (*"still leaves 53.3% cross-island and strands 174,898 texels on the mean fallback"*), **`valid` is not `same island`** and constraining the flood to the source texel's own island has never been tested here; **`reference`** instead makes it the twins disagreeing about *material identity*, which no atlas-space blend repairs, and points at the study-swarm's convergent finding — synchronise views during denoising (SyncMVD, [arXiv:2311.12891](https://arxiv.org/abs/2311.12891), **MIT, LICENSE fetched and read directly**) rather than blend better afterwards. ⚑ **The arc exists to kill the advisor's own inherited claim**, which the handoff asserted as fact: the cross-island reading rests on *"dilation-filled texels are 4.8× enriched in visible blotches"* — a number from E07 Gate 0's **speckle** measure, and CLAUDE.md's own law says of that family that *"a large region of the wrong material… is smooth inside itself and contributes only its rim to every one of those numbers."* A speckle detector is structurally blind to a gold blob on a green tunic, and the same gate measured `TWINS \| DILATION` as the **flattest** boundary class at 1.750 against 4.75–11.75. Zero generation, zero re-bake, **zero spend** — every input already on disk in the manifest-protected `facet_E08` export tree (26 views, each with `prov_class`, the exact per-pixel class map, born indexed). **W3 cannot be re-baked** (no `prep_uv.glb`/`mask.npy`/`pos.npy`/`meta.json` survives — verified a third time at this seat), so this arc measures the finished artifact. ⚠ **CORRECTED by E40 Seat B: those files DO survive at `facet_E06/C1/prep`** — the three prior checks all searched `facet_E08`, which is one scope error repeated rather than three verifications. Only STATE regeneration is foreclosed; `finalize` replays byte-identically on frozen state (T50), so fill-stage arms run on W3 itself. Gate 0 halts on a detector that cannot see the regions the Director named — the metric law's *validate-against-a-rejected-artifact* form, run before any provenance number. Halts at `task1-report.md`; Task 2 (twin disagreement) is conditional and the advisor rules the transition. ⚖ **TASK 1 RUN AND RULED — THE ADVISOR'S CANDIDATE IS DEAD** (2026-08-16, Amendment 1): Gate 0 passed on two legs (an eye leg at native 1:1 on all seven named regions; a specification leg whose material centres came from a *different object* than the one tested, recall 93–99%), `facet_E08` untouched at 818 files. **The gold class is `reference`-carried — Road B.** `dilation` is genuinely enriched (1.76×/1.92×/2.22×, ranking stable) and **cannot carry the regions**: 9.52% of detected-region pixels, **4.27%** of the area of regions ≥300 px, 11 regions against `reference`'s 369 — and on the tight population **gold out of place is 91.05% `reference` at 0.99×, dead on base rate**. Accepted rather than dismissed as a boundary artifact for one reason above all: **the executor named that its own window bias runs in the direction that FAVOURS the advisor's candidate, and the result went the other way anyway.** Kept separate rather than folded in: **green out of place is a different mechanism** (`brush` 5.49×, `dilation` 3.34×) so the same-island flood may yet be right *for that class*, and **the blade is the one `dilation`-dominant large region at a 48.3% plurality**. ⚑ **The largest finding is a unit gap and it corrects this repo's own reasoning basis**: `dilation` is **26.95% of the written atlas against 4.95% of rendered figure pixels — 0.18×**, so every dilation figure the record reasons from (including the ones in this arc's own dispatch) overstates what a camera sees by **5.4×**; two inherited claims reproduce to the digit and two are **withdrawn as unattributed rather than corrected** (`813,773` was measured on E06's C1, not on W3; `33.9% of the atlas` matches neither denominator). **Task 2 redefined before running** (Amendment 2): `view_owner` does not exist on W3 (`e11_manifest.py:93`, *"honestly absent, never synthesized"*) and `reference`-carried splits two ways — **the twins disagreeing** (SyncMVD's target) versus **a twin hallucinating gold within one view** (which no multi-view synchronisation fixes) — so the swarm's convergent recommendation assumes an answer this arc has not yet measured. ⚖ **TASK 2 RUN AND RULED — BOTH MECHANISMS ARE LIVE, ROUGHLY 2:1** (2026-08-16, Amendment 3): the cheap form was available at all eight views (twins are 752×1024 and register to their own export render at best shift **(0,0)** under two independent objectives, both unimodal — no reprojection, no keying), so the frame check demanded because [E04 Ruling 15](E04-ruling.md) cost this route a 2 px mismatch came back clean. **(a) the twins disagree: ≥ 29.9%, a FIRM LOWER BOUND** — where the twin rendering a pixel is clean the gold demonstrably came from another view, because one view's *absence* is decisive about that view. **(b) a twin hallucinated internally: ≤ 70.1%, an UPPER BOUND AND EXPLICITLY NOT A MEASUREMENT OF (b)** — twin-gold at view *k* establishes *k* as **a** source and cannot poll the other seven, an asymmetry the seat **pre-registered before the number existed**. The control carrying the result is the tolerance plateau, not the point value: normalised 64.6% → **70.1% by r=6, unmoved through r=10**, so registration slop is worth ~5.5 points and no more; median ΔE(twin, asset) reads the same gradient independently at **22.87** against **3.55** on ordinary green. ⛔ **A pre-registered gate FIRED**: `y+090`'s own ceiling is **49.07%** against a 60% floor — remedy was per-view normalisation, **the view was not dropped**, and the firing is reported as a firing; `y+135` sits at **117.2%, above its own ceiling**, offered unexplained. ⚑ The lead: `e11_manifest.py:73-75` records twin_2/twin_6 (`y+090`/`y+270`) as the accepted **RE-ROLLS**, and `y+090` is the outlier. **Consequence: SyncMVD and its family target (a) only** — real, bounded, and not the whole class — so adopting a synchronisation method on the study-swarm's strength alone would have been this arc's own error one level up. **Two laws folded to [CLAUDE.md](../../CLAUDE.md)**: a share measured in one space is not a claim about another (the tenth unit-family instance, and the first where the mis-united number was the *advisor's own reasoning basis*), and **a prediction must not name a value the instrument cannot return** — T2-P1 predicted 75% against a ceiling of 63.66%, *"the controls were in the design and absent from the prediction."* |

| [E40](E40-three-class-arms-kickoff.md) | ⛑ **The three-class arms — three Sonnet seats in parallel, at the Director's word 2026-08-16**: *"we'll run experiments on the classes simultaneously through 3 spawned Sonnet sessions, once we've learned more of the levers involved."* [E39](E39-w3-polish-kickoff.md) split W3's blotchiness into three classes with three measured carriers; [the study-swarm](../research/E39-three-class-study-swarm.md) returned 32 findings against them, every citation resolved at primary source or listed UNRESOLVED. Can each class be moved by a named lever? | **DISPATCHED** (2026-08-16). **Seat A — GOLD**: first close E39 Task 2's open side with a per-face view vote across **all** views that see a point (MVS-Texturing's Mahalanobis outlier scoring, BSD-3 — it runs on W3 itself, no re-bake, because Task 2 could poll only the one view being rendered and that is exactly why (b) came back a ceiling). Then **frequency-split blending**, the swarm's best-aimed lever and a finding that **corrected the advisor's own brief**: three shipping tools converge on it and two make it the DEFAULT (AliceVision `multiBandNbContrib={1,5,10,0}`, Metashape `Mosaic`, RealityCapture `Multi-band`), because gold-on-a-tunic is a **low-frequency** error and that is the band where they take 5–10 view contributions — so one hallucinating view is outvoted *in the band where its error lives* while detail still comes from the single best view. A1 (frequency-split) and A2 (hard selection, StableGen's `Weight Exponent`) are **mutually exclusive blend philosophies** and may not be combined; A3 (outlier damping) composes with either. **Seat B — GREEN**: opens on a question the record cannot answer — whether W3 ever ran `--surface-aware`, which has existed since `17a9e57` (2026-08-03) against an atlas written 2026-08-04, while an E08 ruling calls the replacement *"never built"*. **If it did not, the fix may be a flag we already own**, and enumerate-before-commissioning has caught four commissions in this repo already. Then the same-island predicate (never tested — the record falsified `& valid`, and **`valid` is not `same island`**) and Blender's topology walk, whose reference implementation `texture_margin.cc` we **already run at bake time** (`bake_hero_prep.py:452`) and which carries three OPEN upstream defects. Every arm reports the **mean-fallback count**, because a predicate that strands surface is worse than one that bleeds. **Seat C — BLADE**: the mechanism first, cheaply — a thin blade is grazing in nearly every view so its cosine visibility weight is near zero everywhere, which is why it alone falls through to dilation on a figure that is otherwise ~90% projection-carried — then separate texture set (the trade's default, and the arm that **eliminates rather than gates**), xatlas `normalSeamWeight` > 1000, a per-island dilation cap, and backface exclusion at projection. ⛑ **Upstream concedes the thin case in its own source**, found by the advisor and surfaced by no agent — `texture_margin.cc`'s fallback comment reads *"This also catches pixels we missed because of very narrow polygons"* — so Seat B's fix cannot rescue Seat C and the three seats are genuinely separate rather than three views of one defect. **All three local, ZERO cloud spend, no generation in scope.** Count surfaces, this status table, the arc span, the index DB/cert pair and the instrument census are **RESERVED TO THE ADVISOR**: T34 pins stated counts against `pytest --collect-only` of the tree the surfaces sit in, so three parallel seats adding tests **cannot all be green independently**. ⚖ **RUN AND CLOSED AT THE MEASUREMENT LEVEL** (2026-08-16) → Amendments 1–6. **W3 HAS ONE DEFECT, NOT THREE** — measured by three seats, three methods, three spaces: the defect is **`reference`-carried**, i.e. the paint, not any fill (gold **91.05% at 0.99×**, dead on base rate; cloth green **68.46%**; on the blade its own paint **18.77%** against its dilation fill's **5.55%**). **The fill sources correctly from its nearest painted neighbour and that neighbour is already wrong.** ⚑ **Seat A found the route already ships a two-band split** — `project_twins.py:901-913` is `M + gaussian_blur_σ16(B − M)`, high frequency from one view and low from the blend, which the advisor's own spec had called a flat facing-weighted blend — and **shipped measures worst of four**: A0_actual **34.24** > A2 hard-select **30.91** > A1 multi-band **29.36** > A0_flat **26.41**. ⚑ **And the honest ceiling on every blending fix**: 100% of defect-blob faces with 2+ cameras have all of them inside a **90° span** (median 45°), 21% are single-view — *never independent*, so photogrammetry's published gains do not transfer at face value. **Seat C killed its own four arms** (blade best-of-8 facing **0.9670** vs torso **0.9155** — covered *better*; `--thin-extent` real but minority at **74.28% never hit by any camera**) and **restored a withdrawn quantity with its object named** (813,773 is pre-rollback, 647,624 is current). **Seat B proved the flood-vs-surface-aware question BY CONSTRUCTION** (mean-fallback is structurally 0 in that mode; W3's record shows 565), gave `--surface-aware` its honest size (**~2.3%** of the green class, not the 47.76% flag-exit figure), and **overturned "W3 cannot be re-baked"** — the prep is in `facet_E06/C1/prep` and `finalize` replays byte-identically, so the advisor's three "verifications" were one scope error repeated. ⚖ **THE DIRECTOR HOLDS THREE DECISIONS**: which blend candidate or none (`facet_E40_A/task3_sheet.png`, four regions at native size, **no candidate recommended** — the gold spatter is still visible in all four); **what the sword grip is made of**, since `canon/W3-IDENTITY.md` names greatsword, crossguard and pommel and never the grip; and whether the blend stage is built as a capability. **Zero cloud spend.** Follow-on swarm: [E40 blend-stage study-swarm](../research/E40-blend-stage-study-swarm.md) — *no tool blurs a whole atlas*, and an advisor correction at the primary source caught one agent inverting AliceVision's band ordering |
| [E41](E41-where-the-wrong-colour-enters.md) | **Where does the wrong colour enter the paint?** [E40](E40-three-class-arms-kickoff.md) settled *that* the defect is `reference`-carried but not *why the paint is wrong* - generation (the twin itself is wrong) or transport (the twin is clean and our projection puts the wrong colour in anyway). E40's **firm lower bound of 29.9%** is why it deserved a seat: at >=29.9% of the gold class the owning view's twin is clean at that texel. | **RUN AND RULED** (2026-08-16, Sonnet seat). **Premultiplied alpha KILLED** - the seat calibrated a fixture against the closed-form error (worst case **-0.250000** against -0.25 predicted), *proved it could detect it*, then measured **0.00e+00** on the real construction. **The advisor's minification candidate KILLED, in the direction against it**: defect texels are **less** minified than clean neighbours (footprint median **0.380 px** vs **0.650 px**, predicted band [0.7, 1.5]). What replaced it: defect texels' source samples sit **5.3x closer to a material boundary** in the twin (median **0.439 px** vs **2.333 px**, 61.6% vs 38.3% within 1 px, n=4,000 each). The blend question closed by reading: `styled ~ LowPass_s16(B) + HighPass(M)`, impulse-in-M survival **99.94%** against impulse-in-B **0.062%**, asymmetry **1607.3x** against a closed-form ~1607x - **high frequency comes from the single owner view only**. Three prediction misses disclosed plainly, including a head-band anchor miss the seat could have dropped |
| [E42](E42-camera-ring-coverage-bound.md) | **Bound the camera-ring lever before spending generation on it.** E40 measured 74.28% of the blade never hit; MVPaint reports FID 23.45 at N=8 flat against **20.89** at N=8 with +/-30 deg interleaved elevation. Is our rig the configuration that measured worse? | **RUN AND RULED - THE LEVER IS DEAD AND IT COST ZERO CREDITS** (2026-08-16, Sonnet seat). **The advisor's rig premise was wrong and the seat halted on it**: the shipped rig is **8 cameras, not 8+2** - yaw 0 and 180 sit at **+55 deg *instead of* flat**, verified from each job's own `cam.json` and re-verified by the advisor, so *"our 8+2 is literally SyncMVD's default"* is false in both halves and our rig is **already partially ring-broken**. The instrument was extended under ruling (`--cams` replaces the ring where `--elev` could only OR onto it; `--restrict-mask`), non-perturbation proven two ways with a race the advisor caught, +13 tests. **Every arm is near-indistinguishable**: whole-figure 74.10-77.83% against a measured **79.44%** ceiling, blade **96.35-99.12%** against **99.75%**; the downward camera buys **+2.11**. A pre-registered reconciliation gate **FIRED**: never-reached on the population-matched set read **6.32%** against E40's **74.28%**, and the seat's own explanation was **tested and falsified**. **E40's 74.28% is WITHDRAWN as a coverage claim** - the same instrument reports **97.99%** never-hit on the **torso**, the best-covered surface on the figure. Also found: **an identity envelope describes what a tool IS, never what a specific CALL did** - a stale served process silently dropped new parameters while its payload carried a correct instrument hash, caught only by an echo required for an unrelated reason |
| [E43](E43-border-distance-ceiling.md) | **Bound the border-distance lever before building it.** E41 found defect texels 5.3x closer to a material boundary; Callieri's blend multiplies by a border mask. A material boundary belongs to the garment, so if no view has a clean sample the lever has nothing to reallocate to. | **RUN AND RULED - WITHDRAWN, THEN THE PREMISE ITSELF OVERTURNED** (2026-08-16). The ceiling came back **45.97%** at the clean median against a null of **100%**, **0.89%** at p90 - *worse than every version of chance the seat modelled* - and the owner is already the best-available view **47.53%** of the time. Task 2 **withdrawn, not deferred**. The resolution rescale is monotonic but sharply front-loaded (**+11.14 / +3.81 / +1.96** points at 2x/3x/4x) with **43.97% still straddling at 4x**. Task 4's first pass was **withdrawn in place by the seat before reporting** - a t-range of -18.6 to +17.4 was the signature of dividing by a short reference segment, the exact distant-medians-read-as-bimodality trap - and re-split on segment stability: **85.22%** of the defect population sits where the twin's own local neighbourhood is already gold-leaning. Task 5 stopped measuring and **looked**: ten of twelve largest flagged regions sit on gold that is **correctly gold**, and **the visible green-on-leather-grip defect is not flagged at all**. **The whole premise then fell**: Callieri's border mask measures distance to **image borders and depth-map discontinuities (Sobel on non-normalised depth)**, not material boundaries, resolved at primary source. The ceiling was computed over the wrong population and **the lever is reopened**; `tools/callieri_border.py` now exists |
| [E44](E44-the-atlas-plan.md) | **The atlas plan, and the falsification sequence that replaced it.** W3's atlas has **9,166 UV islands, median 102 texels**, 17.8% of painted area within one texel of an edge. Is the shattered atlas the defect the Director's eye rejects? | **PLAN SUPERSEDED IN PLACE; S0 AND S1 RUN** (2026-08-16). **Three rows of this arc's own diagnostic table are VOID** - they cite the xatlas author about a segmentation stage **we do not run**: `to_glb` calls **CuMesh cone clustering**, then adds each cluster to xatlas as a *separate mesh*, so xatlas only ever subdivides inside a cut somebody else made. *"TRELLIS ships xatlas UVs"* sat in this repo's own tool docstring since E05. **The island-rim mechanism is DEAD**: repainting **16.3% of the atlas** magenta moved **116 screen pixels**, against a positive control of 151,705 - an atlas share is not a screen claim, and the seat that quoted that law in the morning committed its sixth instance by lunch. **The mesh is triangle soup**: **41.1%** adjacency completeness, **139,014 single-triangle components**, V-E+F = **-60,781**; welding at 1e-6 is **lossless** and gives **99.2% adjacency, 271 components**, collapsing cone-cluster count **146,462 -> 2,654 (55x)** - reproduced independently in numpy and Blender. The refinement dial is **falsified** (4,505 refined vs 2,654 unrefined; only the control arm caught it). **S0**: the asset rendered FLAT + Standard is still badly wrong against the twin (A/B differ **6.1/255**) - **colour management is not the defect**. **S1**: welded vs shipped clay differ on **1,034 px, max channel 3** - **soup is not the appearance**, and the earlier faceted result was an export path destroying normals. The live lead is **local twin-to-mesh warp**: per-tile offsets **-8..+6 / -8..+8, std 3.71/4.09** around a mean near zero, several pinned at the search limit, while global IoU is **0.9203** and hides it |
| [E45](E45-warp-and-aov-kickoff.md) | **The warp gets its instrument back, and S3 gets its substrate.** E44's live lead was local twin-to-mesh warp measured on one view by a LOST instrument. Rebuild it, and emit the per-view AOV bundle the S3 existence proof needs. | **RUN AND CLOSED** (2026-08-16, Opus seat). **Gate A FIRED 2/8 on the dispatch's own camera pairing** — the anchor masks are all el 0, the twin ring is **8 flat cameras**, the el-55 pair are brush cameras, **no twin exists at el 55**; the seat halted as written and discriminated **16/16 recorded cameras at 0 differing px** against their own artifacts, elevation included. Gate C HELD on constructed truth. **Interior tile offsets exceed silhouette offsets on 8 of 8 views** (medians 3.46–11.12 px vs 1.16–3.00, p90 18.8–30.9), the dispatch's widening rule **falsified as a procedure**, wrong-pairing control separates 12.5×. Bundle: `facet_E45\aov\` + `aov_el55\`, 244.7 MB. Report: [E45-warp-and-aov-report](E45-warp-and-aov-report.md) |
| [E46](E46-s3-run-kickoff.md) | **The S3 run: can the eight plates compose?** The chain existed (compositor, flow, runner, sheets — Grok builds, verified); this arc runs it and puts the three-world readout in front of the Director. | **RUN AND CLOSED** (2026-08-16, Sonnet seat). Gate S held on all three selftest lines verbatim. The seat read the disagreement instrument's interval off the fixtures before predicting and **caught the docstring ceiling not reproducing** (measured 0.7071, not 1). **Flow-on reduced disagreement on 18 of 18 measured (view, region) rows, zero exceptions** — at 16–27% coverage and 1.4–3.2 px magnitude, a trim, not the lever. Two instrument disagreements reported OPEN: the warp ranks view 7 worst while the disagreement map ranks it lowest; flow magnitudes run below tile offsets. Report: [E46-s3-run-report](E46-s3-run-report.md) |
| [E47](E47-atlas-ab-kickoff.md) | **The atlas 2×2, rendered** — owner/blend × flow off/on, painted from the bundle and rendered beside the shipped render, so the warp question faces the asset. | **RUN; ALL FOUR CELLS REJECTED BY THE DIRECTOR** (2026-08-16): *"I don't see any recovered images. They all still look broken."* The advisor's "closest render yet" sentence was a quality grade and is **withdrawn**. What stands measured: interiors moved toward the reference while every cell stayed covered in unfilled-sentinel lace — a diagnostic, not a candidate, and an image is broken if any of it is. Flow-on **drops** coverage in 12/12 region cells (a reported miss); blend moves more texels at smaller magnitude (direction miss, reported). Report: [E47-atlas-ab-report](E47-atlas-ab-report.md) |
| [E48](E48-complete-candidate-kickoff.md) | **A complete candidate render, or the route is dead** — pre-registered stop condition; A3-rule erosion, both modes, within-island fill, whole-figure renders, sheets. | **RUN; THE DIRECTOR RULES IT A CLEAR STEP UP, WITH ROOM TO GO FURTHER** (2026-08-16) — the route lives. Landed incomplete by its own spec: owner **5.05%** / blend **11.96%** of valid texels stayed sentinel (1,803 / 3,625 zero-written islands, cross-island fill forbidden), and the erosion ran hotter than its stated floor (ed_body 2.79–4.17 px vs 2.5), eating **29–64% of the 4–8 px band** — both advisor spec defects, both repaired in E49. Green survives on the grip in BOTH candidates: plate-level contamination, the canon lead. Report: [E48-complete-candidate-report](E48-complete-candidate-report.md) |
| [E49](E49-finish-and-cap-kickoff.md) | **Finish the candidate: orphan islands painted from source, erosion at its stated floor.** | **RUN; THE DIRECTOR ACCEPTS THE SHEETS AND NAMES ONE NEW CLASS: FLAT COLOURED POLYGONAL SHAPES** (2026-08-16) — the strongest acceptance signal the route has received, one open class. The erosion cap reproduced thin bands bit-for-bit and cut thick-band losses up to 14 points. The orphan fill told the truth over its prediction: **95.6% of owner's orphan set fails the depth gate in EVERY view** — never-seen surface (final sentinel owner **4.65%** / blend **5.57%** of valid), which the shipped pipeline papered with the island-blind flood. The polygon-class hypothesis (triangle-sized islands flat-filled from boundary-adjacent uneroded samples) is tagged in provenance masks, unverified. Three Director rulings in the dispatch record: sleeveless stands; **W3 is the exemplar**; *"we never properly built out the canon"* — the crux. Report: [E49-finish-and-cap-report](E49-finish-and-cap-report.md) |

| [E50](E50-polygon-class-overlay-kickoff.md) | The Director named a new defect class on the accepted-grade E49 sheets — flat-coloured angular patches. Are this arc's own fill passes the carrier, as the E49 dispatch record hypothesised? | **RUN; A CLEAN NEGATIVE — the fill passes are NOT the carrier**, measured four independent ways → [E50-polygon-class-overlay-report](E50-polygon-class-overlay-report.md). Two confirmed real patch instances, cross-checked against the reference twin and read through an anchored `surfid` lookup, sit **90.0–99.1% on ordinary directly-painted texels** and **0% on orphan fill** in both modes; a spec-calibrated whole-image scan put **100%** of what it found on `written`; a broader scan measured `filled` at **1.18×** its own base rate and `orphan_fill` **below** base at **0.27×**; and decisively, **the same defect is already present in the pre-E49 baseline render**, built from an atlas that predates `orphan_fill.py` as code — a repair cannot cause a defect older than itself. The origin sits upstream of the entire arc, in the base per-texel scatter. Gate B fired once on its own test construction (an unlit texel colour compared against a lit render) and Gate C twice on its calibration script; each was diagnosed and repaired before any retry. Two limitations disclosed rather than papered: neither polygon-vertex-count nor isoperimetric compactness separates a real angular defect from a round highlight at this resolution, and a fifth provenance class — `unmapped` — showed **11.58× enrichment**, the strongest measured and outside this dispatch's scope |
| [E51](E51-fill-repair-kickoff.md) | Repaint the fills so a texel can never take a colour no view supports: visible orphans resampled against the eroded silhouette, never-seen surface filled from its own material's statistics, every fill palette-gated. | **RUN** → [E51-fill-repair-report](E51-fill-repair-report.md). No gate fired. Owner arm A recovers **exactly 0** by analytic certainty (predicted); blend arm A accepts **124,361** and refuses 402; arm B accepts **114,913 / 138,684** and refuses **2,111 / 2,502**; final sentinel **0.088% / 0.121%** of valid texels against E49's 4.65% / 5.57%. Arm B's neutral fill is a chroma-floored **circular** hue mean through a Lab→sRGB inverse, per the law that below a chroma floor hue is not a colour and a statistic of angles must be circular. **This arc does not claim the polygon class** — E50 landed mid-run and measured the fill passes as not its carrier, and the report was reframed to lead with that finding rather than the dispatch's premise. Its own consistent-but-narrower measurement: E49's orphan-fill population was already **99.6–99.9%** inside the two declared hue bands. ⚠ Walked at full size by the advisor: the atlas-space sentinel cut does **not** appear in the render, which still carries substantial magenta in the same regions — the denominator excludes whatever is visible, and E50's `unmapped` class is the untested suspect |

| [E52](E52-target-first-flats-kickoff.md) | Does the target-view-preferring compositor carry the flat class? The kickoff ranked it as the free fix and stated "nothing built" — it was built, and had been the default since E46. | **RUN; A CLEAN NEGATIVE, AND THE ARM IS DEAD BY MEASUREMENT** → [E52-target-first-flats-report](E52-target-first-flats-report.md), [ruling](E52-target-first-flats-ruling.md). All four gates held. Target-first *raises* the olive count at the named target (38 → 40) and sharply at two others (23 → 64 with the largest component 5 px → 59 px; 36 → 110). **Shape is ownership, colour is not** — the olive is view 6's own paint of a surface view 6 paints correctly, so at target 6 the policy maximises exactly the paint the defect is made of. An ownership policy cannot repair a cross-view colour disagreement on a correctly-attributed surface, which retires the family. The advisor's P3 was mis-specified — a direction count over a *fixed* image-space box, three of whose eight rows read 0 in both arms and cannot exhibit either direction; read honestly, 2 of the 5 informative targets decreased |
| [E53](E53-compound-occupant-ruling.md) | Did N11 (the gold outer-forearm plate) land in the from-scratch, sixteen-element SPEC arm? The one number that decides whether the co-location constraint survives specification-from-scratch. | **RUN; ABSENT** → [E53-n11-spec-arm-report](E53-n11-spec-arm-report.md), [ruling](E53-n11-spec-arm-ruling.md). SPEC's forearm reads C\* 20.5 / h 54.1, inside the leather cluster every arm occupies and nowhere near the gold calibration (37–43 / 78–80). **Co-location survives from-scratch specification.** The seat's own BRACER recomputation matches the record's published anchor to the last integer, and Gate B was *demonstrated able to fire* on a deliberately-bad box. The pre-registered discriminator was mis-specified: N9 landing proves nothing, because N9 arrives **unprompted**. And the compound occupant had already been tried — `gold-trimmed brown leather bracers` SPLIT, head noun landing and modifier dropping — so the proposed generation was refused |
| [E54](E54-router-landing-zone-report.md) | Clear the router's landing zone: the index cannot read its own two newest arcs, the t87 artifacts pin has been failing, and nobody has measured whether the gate stands in front of every spend site. | **RUN** → [E54-router-landing-zone-report](E54-router-landing-zone-report.md). The arc-number bound was *deliberate*, not accidental, with a test forbidding its generalisation to a wildcard. The t87 collapse to 14 traces to the skirt→kilt rename, **not** the 17→19 widening, now pinned by sha256. And the census that mattered: the gate fired before any write at three sites, **all conditional on canon reaching the tool**, while `e12_pair_cloud_step` (which authors the paid twin graph), `e37_fire_repaints`, `ig2mv_licensefree` and `texpass_loop.ps1` had no binding at all — three doors of seven |
| [E55](E55-density-vs-identity-kickoff.md) | Using only plates already paid for, can element COUNT be separated from element IDENTITY as the predictor of whether a named element drops? | **RUN; NO, AND THE REASON IS STRUCTURAL** → [E55-density-vs-identity-report](E55-density-vs-identity-report.md). Zero elements hold their phrase constant while the count around them varies *and* are capable of being absent — a property of the corpus, not the measurement. What the plates do give is a one-sided bound from five prompts at one camera with byte-identical control, mask and seed: over an element ladder **10 → 17**, count removed **nothing** present at 10, while an identity flip at Δcount **zero** moved the full calibration interval [0.84, 17.09]. Canon asks 19; the corpus never reaches it. Gate C reproduced two published figures in one run. Corrections returned: **the headroom element is N10, not N11**, and the seat caught itself reading a response measure as a presence measure |

| [E56](E56-wrong-material-check-kickoff.md) | Can an honest automated check separate an asset the Director REJECTED from one he ACCEPTED, on the class that decides acceptance here - a large region of the wrong material? | **HALTED AT GATE A, AND THE HALT IS THE FINDING** -> [E56-wrong-material-check-report](E56-wrong-material-check-report.md). **The asset he accepted and the asset he called far from perfect are the same file** (`facet_E08\ARMB\`): accepted at Gate 1 on 2026-08-04, *"needs a serious polish"* on 2026-08-16. Positive class **1**, negative class 2, and the second negative *is* the sole positive - so a per-asset accept/reject label is not constructible and the seat refused to label by eye. Measured without labels anyway: **canon binds 0.00% of the figure** (`scopes.views == {}`); the hand-box file reaches 7.95% strict; the local-median detector family has **0.00% interior recall** at 66.53 dE separation; and the kill shot - the left pauldron, gold on a pauldron exactly as canon declares, fires at **39.90%**, above five of the seven regions he named, distributions overlapping end to end. Three advisor premises were wrong, including a `DIRECTOR-VERIFIED` label on region boxes that is unsupported at source. Route named, not taken: a mesh-face -> canon-surface-id binding |
| [E57](E57-a1-reference-first-kickoff.md) | **A1, the reference-first exemplar** - a brand new humanoid whose reference, identity, recipe and canon all exist BEFORE any geometry, repairing W3's founding defect (clay first, provenance INCOMPLETE, identity riding in undeclared artifacts). No weapon by the Director's direction. | **RUN; THE CANON IS RATIFIED** -> [E57-a1-reference-first-report](E57-a1-reference-first-report.md). First subject in the repo whose founding artifact embeds its own complete recipe (seed 106, 50 steps, cfg 4.0 - a Lightning LoRA sits in the graph INACTIVE behind a switch; the declared 1140 latent width was delivered as 1136, the generator's own /8 rounding). Gate 0: sha256 3/3, **18/18 canon phrases byte-verified** against the embedded prompt. Mesh seed 42: 990,679 faces, largest component **99.91%** (above the five-character base band, reported not halted). Palette: 10 materials / 16 regions, one region (neck) declined as unmeasurable. The Director ratified all 19 rows and five queue positions as drafted mid-arc, with a standing principle: ratification is a baseline, not a freeze - the quality is made in the edit. **Registration FIRED a real ANDON on the ratified file**: `CLAUSE_CLASSES` lacked the `staging` class the canon uses; the fold widened the enum (adds capability, removes no coverage) with a can-fail test leg. Also at the seat: the **4th firing of retired corner-median keying** (reference's gradient+shadow backdrop, caught at Gate 2's bbox check), and `e12_region_colour.py` measured computing hue centres as a LINEAR median - repair flagged for a later arc under the cited-instrument discipline. Seat survived an API connection loss mid-arc at zero cost: the handoff discipline working as built |

| [E58](E58-a1-twin-ring-kickoff.md) | **A1's twin ring — the repo's FIRST credit spend**, behind the canon gate, against controls from A1's own approved mesh, with the venue anchored on the reference's own recipe before anything measured. | **RUN; THE VENUE IS NO LONGER A VARIABLE** -> [E58-a1-twin-ring-report](E58-a1-twin-ring-report.md). Gate A anchor replay came back **pixel-identical** to `canon/A1_reference.png` — 0 of 1,672,192 pixels differing, dE 0.0000 on every statistic, where every prior hardware move in this repo was accepted at a residual. 9/11 generations, 2 held. Gates B/C/D/E all passed; `prof_hit` 0/10 -> 10/10. The charter's Stage B premise was **stale** — the seat found the E12 door already bound at HEAD by the v0.6.0/v0.7.0 fail-closed release and verified it before relying on it. Off-palette readings (26-70% area) are **confounded and unread**: LoRA-styled twins against bands measured on the non-LoRA reference, and the seat refused the call rather than reporting a number it could not defend. Defects visible at the Director's eye and named by him: the head turns toward the camera through the rear views, and the plum long-vest returns brown on the three side views. ⚠ The seat also disclosed, in its own prompts file, that the charter told it to put **every** ratified phrase in **every** view — overriding this repo's own per-view law (E02: *a rear camera is never told about a beard*). That disclosure is the mechanism of the head turn and the advisor did not read it before writing E59 |
| [E59](E59-head-forward-kickoff.md) | The Director ruled the head must face straight forward as canon. Make the clause enforceable, calibrate a head-alignment instrument, and probe the fix on the worst view. | **RUN; STAGE 0 LANDED, AND THE PROBE IS VOID BY THE ADVISOR'S OWN SPEC ERROR** -> [E59-head-forward-report](E59-head-forward-report.md). Stage 0 stands: `canon_gate` now **enforces** `required: true` legal clauses, so a canon clause cannot silently drop from a prompt — it immediately caught `profiles/a1.json`'s own stale prompt, and the census table came back byte-identical. Everything downstream of it is void: **the spec sent the probe to view 2 (yaw 90), where the head is already aligned** — the defect lives in the rear views (v3/v5 read at full size show the back to camera with the head cranked over the shoulder). Two consequences: neither arm can speak to the head question, and **Gate 1's firing is confounded** — the *definitely turned* calibration population the spec named (v1/v2/v6) contains aligned members, so no instrument could have separated it, and the seat's own offered explanation (silhouette IoU may be the wrong class for an orientation defect) **remains untested**. The seat behaved correctly throughout: it halted at the gate and declined to tune. What the 2 credits did buy, as diagnostics: denoise 0.92 -> 0.80 is a **badly adverse** lever (image washes out, garment moves to pale sage), and the added clause left an already-correct view undisturbed. Advisor law violated, already written in this repo twice: *a real population whose members you never checked for the property still breaks the prediction* |

| [E60](E60-composer-kickoff.md) | The Director's direction: the prompt that makes the reference should itself come from the detailed canon, so painting the mesh later needs less direction. Build the composer, then A/B it against the hand-written prompt that made the approved reference. | **RUN; THE ADVISOR'S PREDICTION IS FALSIFIED AND THE GATE IS IMPLICATED IN ITS OWN EXPERIMENT** -> [E60-composer-report](E60-composer-report.md). Four arms x three seeds, everything but the prompt text byte-identical to the recipe. Arm R (verbatim) reproduced the reference **pixel-identically** at seed 106 — 0 of 1,672,192 differing, the venue re-anchored a second time. **Arm L, the flat comma list that findings 3-4 predicted would LOSE, held the garment at all three seeds**, while the composed prose arms P and G rendered the sleeveless vest as a **full-sleeved coat** at 2 of 3 seeds (N2 hue 63.6-66.8 deg, cream sleeve occluded). The mechanism is ours: `canon_gate`'s reverse check permits only a fixed connective list, **"over" is not on it**, and the reference's own recipe uses *over* to join vest and shirt — so the composer was barred from the one preposition encoding the layering, a constraint `A1-IDENTITY.md` had already recorded and nobody had read as a design limit. The seat reported two candidate explanations and **resolved neither**, which its instruments cannot. Underneath sits a canon gap that is the Director's: **nothing declares the vest sleeveless** — W3 carries `kind: bare` + `forbidden: ["sleeve"]` for exactly this and A1 carries none, so the reference got sleevelessness from a preposition rather than from canon. Also measured: raw dE-against-reference is **confounded by seed lighting variance** (Arm R's own text reads "missed" at non-anchor seeds) and cannot grade an arm. Research grounding for the arc came from four agents that dropped a mis-identified paper and caught a **fabricated industry statistic**; its finding 6 corrects an advisor claim made earlier the same session — a general VLM judge scores **49% on binary colour verification** (GenColorBench), so the colour half of any image-side gate stays facet's own Lab instrument |
| [E61](E61-layering-repairs-kickoff.md) | Both layering repairs landed at the advisor's hand (spatial prepositions admitted, joint phrases licensed) plus the vest declared SLEEVELESS - which one actually carries the garment through a composed prompt? Five arms x three seeds, P1 (preposition only) vs P2 (all three) as the question. | **RUN; ONE WORD DECIDES IT, AND THE LOAD-BEARING FACT IS NARROWER THAN THE PREDICTION** -> [E61-layering-repairs-report](E61-layering-repairs-report.md). Arm R reproduced the reference pixel-identically a third time; 9 of 15 frames were byte-identical cache hits against E60 (identical inputs, identical outputs - the venue is fully deterministic), so the six P1/P2 frames are the only new evidence. **P0 (grouped prose joining the garments with "and") occluded 2/3 (dhue 63.7/66.8; its 314159 seed held at 2.7 - the same seed-pattern E60 measured, not a rescue). P1 (the one-word swap to "over", byte-verified) present 3/3 at dhue 3.4-6.2. P2 (over + sleeveless + joint) present 3/3 at 4.0-6.6, matching P1 without beating it.** L (flat list, NO join at all) present 3/3 - so three forms keep the garment and one loses it, and the measured illegal form is COORDINATING the two occupants with "and" in grouped prose, NOT the absence of a preposition. E62 therefore licenses "over" and does not require it: requiring it would refuse Arm L, which held - the same error family as the advisor's UNAVAILABLE steer, caught by the channel both times. The corrected 3-state readout returned zero UNAVAILABLE rows exactly as pre-stated (the spatial L* texture cut separated corners 0.25-0.50 from sleeve boxes 13.73-31.02 and correctly withheld nothing). The seat caught its own P0-phrase error by byte-identity against E60's recorded text before anything downstream saw it, and recorded predictions BEFORE spending - repairing E60's disclosed gap. Advisor error at the fold, corrected by the channel: the advisor's table attributed P0/314159's 2.7 to P1's range |

| [E62](E62-schema-patch-kickoff.md) | The one transfer from the prompt-craft convergence, in facet spelling: depends_on relation rows, the collision law, unavailable as a convention. Three fences from the channel, the third corrected in place before the seat built (the collision law gained its declared branch when measurement showed the mechanical branch fires on none of A1's 39 licensed phrases). | **RUN; ALL THREE FENCES HELD, ZERO SPEND** -> [E62-schema-patch-report](E62-schema-patch-report.md). canon_compose refuses to coordinate a depends_on pair with "and" in any form while the flat list still passes (the leg that protects Arm L); the collision law is two branches - M mechanical on the gate's own compiled matcher (W3's 1170/1174 fixture green), D declared via protected_tokens; docs/measurement-conventions.md carries E61's three states verbatim. Revert-proofs by monkeypatch, census byte-identical, collector exactly 1342, hermetic 1258/0 with the index tests excluded per the two-sessions law. The seat's own catch: the SLEEVE matcher's trailing word boundary does NOT match plural "sleeves" - singular-only reach, documented as a limit, not widened. The advisor's live probe of Branch D first read DID-NOT-REFUSE because the probe put forbidden on the surface row where the gate never reads - the malformed-fixture law applies to advisor one-liners too. **Both draft rows RATIFIED by the Director at 478e1e8** - depends_on N2 on the vest pair, protected_tokens sleeve on A1 - banners off, N2 untouched, no forbidden:sleeve anywhere; his two kept properties: singular-only is a reach limit, and occupancy 16/16 never meant the drafts were ratified |
| [E63](E63-head-on-the-defect-kickoff.md) | E59 sent the head probe to view 2, where the head is already aligned. Re-run Arm P (text) and Arm C (denoise 0.92 to 0.80) on the two views that actually crank (v3 yaw 135, v5 yaw 225). | **RUN** |
| [E64](E64-view-scopes-kickoff.md) | The pick after E63: per-view prompts via scopes.views - a rear camera stops hearing about the smile. Eight lists drafted from the clay (the Director walked clay_2/3/4/5 himself and corrected the advisor's rear list - the hands are in), ratify-then-spend, probe v3+v5 as one variable against E63 Arm P. | **RUN; THE DIRECTOR RULES THE PROBE CLEAN - THE CRANK IS GONE ON BOTH VIEWS AND THE GARMENT SURVIVES** -> [E64-view-scopes-report](E64-view-scopes-report.md). The seat proved the E63 confound at source (its rear graphs carried front-composed text byte-identical to each other), closed the gate hole both directions (out_of_scope_hits: a scope now REFUSES an out-of-scope phrase that is present - run against E63's own text at view:3 it refuses naming all three out-of-scope occupants), and emitted rear prompts differing from E63 Arm P by exactly the four face-bearing phrases. Front compose byte-identical before/after; census byte-identical; the seat verified the ratification commit independently before spending. 2/2 at seed 770700, denoise 0.92, E58 controls, Gate E green. **At unchanged denoise the E64 panels show the head away on both views - v5 the back of a head with no facial feature visible, v3 turned away with at most a sliver of lost profile - with the plum vest and cream sleeves intact.** cn comes off the table UNUSED: removing the cause beat out-muscling it, exactly the E02 reversion claim. ⚠ One disagreement resolved at his eye: the seat's own report described its E64 panels as still facing camera - the prose was wrong about the pixels it shipped, both readings were committed unresolved at 3adf8a0, and the Director ruled for the advisor's read. A seat's sentence about its own output is not a measurement |

| [E65](E65-scoped-ring-kickoff.md) | The first complete A1 ring under every measured fix at once: ratified per-view scopes, sleeveless N1, flat form with the coordination refusal, required pose staging. E58 is the baseline; both named defects get per-view readouts. | **RUN; 8/8, ZERO RE-ROLLS, ZERO OCCLUDED VIEWS - the sheet awaits the Director's eye** -> [E65-scoped-ring-report](E65-scoped-ring-report.md). Gates P and E 8/8; v3/v5 are render-cache hits pixel-identical to E64's outputs (0 of 589,824 differing - the byte-match prompt assertion made the venue's determinism visible), so six images are new evidence. The E61 garment instrument, its regions re-derived geometrically from the raycast silhouette because the palette boxes do not transfer across frames (8 of 16 sleeve-sides isolable; zero at true profile - the visible arm never separates from the torso in silhouette there): **0 of 6 measurable views occluded**, three landing 0.1-1.9 deg over the hue-band edge with unambiguous dE - both numbers reported, threshold NOT retuned. At the advisor's zoom the formerly-brown side views (E58 v1/v2/v6) read plum with cream sleeves, and **v4 - the never-probed rear view - reads head-away WITHOUT the extra negative clause v3/v5 inherited from E63's graphs**, an asymmetry the seat disclosed by construction rather than smoothed: evidence the scopes, not the negative, carry the rear fix. The seat also declined to backfill a prediction it had not written before spending, disclosing the gap in E61's own words |

| [E66](E66-v6-profile-scope-kickoff.md) | The Director catches E65 v6 turning the face to camera - the reserved profile mismatch, and the advisor's "aligned in every row" was WRONG on this cell (E64's own law, turned on the advisor: a sentence about a sheet is not a measurement). One generation at his hand: scopes.views.6 drops eyes and mouth (style_face follows), keeps olive skin and hair - the clay shows a cheek. v2 untouched: it kept the phrases AND stayed aligned, so the recorded mismatch stays recorded, unspent. | **RUN AT THE DIRECTOR'S HAND; THE HEAD IS RULED DONE AT THE ADVISOR'S EYE - THE CELL IS NOT** -> [E66-v6-profile-scope-kickoff](E66-v6-profile-scope-kickoff.md). Same seed/denoise/controls as E65 v6; gates at view:6 and frame both passed; E58 v6 carried the same crank so this was no regression. The E66 frame: head in true profile with the body, frame-left as the clay - the scope mechanism's third clean landing (v3/v5 probed, v4 free, now v6). NOT done: the chest and collar carry a TAN BUTTONED PANEL where canon says cream high-collared shirt - the E58 under-garment family, at exactly the view class where the sleeve instrument is blind by geometry (E65: zero isolable sleeve-sides at true profile), so profile garments are the eye's class for now. Three rolls at this view produced three different phantom garments - instability, not a stable prior. **THE ONE-RE-ROLL LAW WAS INVOKED AT HIS WORD AND IS SPENT**: seed 314159, everything else byte-identical (diff-assertion: one line, the seed), all gates green, 0 credits estimated -> [E66-v6-profile-scope-report](E66-v6-profile-scope-report.md). The roll did NOT converge to canon - a fourth distinct chest interpretation - but at the advisor's eye it is the CLOSEST of the four to the declared materials: head in true profile, no tan phantom, the vest plum throughout, a thin cream sliver at the collar, and one remaining uncertainty (a dark maroon diagonal across the shoulder - possibly the vest's own back panel in profile, possibly an accessory of the E58 stole family; the seat could not determine it and said so). The seat also offered, unmeasured: the CLAY carries a sculpted ridge at this exact seam, a candidate driver for why every roll invents garment structure there. A second failure is the result - no third roll. The rejected seed-770700 frame stays in the record beside the roll. **Director 2026-08-19: 66 REROLL (seed 314159) is the winner.** Cell accepted. A1 twin stage closed: E65 v0-v5+v7 plus this frame as v6, assembled at E:/AI/training/facet_A1_accepted_ring/ with MANIFEST.json carrying per-file sha256, sources and seeds - verified at the fold before the record cited it. Both named defects gone at his eye, and **cn never came off the shelf**: the whole head arc closed by removing causes rather than out-muscling the model. The clay-ridge at this seam stays a NAMED, UNTESTED lead and is not a reason to reopen twins. Next stage: paint the mesh, with binding still the debt beneath it (W3 0/27, A1 NONE) |

| [E67](E67-a1-paint-prep-kickoff.md) | Paint prep, local only, zero cloud: verify provenance, MEASURE A1's own face crop (never inherit W3's rect), unwrap, bake the accepted ring, and put a UV sheet in front of the Director. The first brush waits on his look. | **RUN; GATES 0/1/2 PASSED, STAGE 2 HALTED ON A REAL ANDON - AND THE ADVISOR RULES THE ANDON'S DISCRIMINATING POWER VOID FOR THIS SUBJECT** -> [E67-a1-paint-prep-report](E67-a1-paint-prep-report.md). The face rect was derived from geometry by a crown-to-neck-pinch cross-sectional scan and cross-validated against two recorded artifacts it was never fit to; en route the seat found the pinned `clay_0.png`'s **752-wide frame is itself a W3-era leftover default**, not derived from A1 - so it derived the rect independently in all three consuming frames rather than assuming a relationship. The mesh carries a real xatlas unwrap from its own reconstruction (verified by direct glTF binary parse, not assumed): 27,745 islands, 35.7 faces/island, head band 139,603 faces / 5,080 islands, head UV share 10.39% -> 51.08% after scaling. Registration passed on all eight views (IoU 0.897-0.963). **`project_twins`' background-contamination ANDON then fired 8 of 8** - 28.9-47.0% of relaxed-admission texels within dE 10 of background against a 2.0% limit - and the seat halted without retuning a parameter, which is the law working. ⚠ **THE ADVISOR'S RULING: the check cannot mean what it says on A1.** Measured from `canon/A1-palette.json`, derived at E57 from the reference and never fit to this question: **N2's cream sleeves sit 4.12 dE (right) and 8.25 dE (left) from the backdrop corners - INSIDE the dE-10 window** (the mouth too, at 5.01). A test that flags "within dE 10 of background" therefore cannot separate sampled backdrop from a DECLARED MATERIAL that is within dE 10 of the backdrop, and the sleeves are two full arms of surface. The threshold is a W3-calibrated global constant governing a local feature - the law this repo has paid for three times - and the same family as the E64 UNAVAILABLE trap the channel caught. The seat's own hypothesis (mesh hair volume sitting inside the twins' painted curls, visible in its contour diagnostic) is real but small-area and cannot account for the magnitude; it stands as a second, unquantified contributor. **The collision is upstream of the tool: A1's ratified staging names a "plain warm pale-grey studio backdrop" while a ratified material is cream** - so every A1 projection inherits it. Named next lever, free: measure WHERE the flagged mass sits (if it is the sleeves, the fix is to test against the declared palette we now have rather than a bare distance-to-background). NOT taken: retuning the 2.0% limit or the dE-10 window while looking at what they judge |


**E04 inputs staged** — `E:\AI\training\facet_next\galleon_clay\`, three form-first clay
variants at 1216×1024 (sha256 `59c25940075dca93`, `80263b1551d5fd7b`, `bb8da35402cf2a10`).
Known stressors, recorded before the spec is written: the pipeline's framing is
portrait-shaped throughout (`turn_render` 752×1024, `project_twins` deriving `h_ext` from
`v_ext × 752/1024`, `ortho_scale` on the larger dimension) and a ship is wider than tall —
the same class of break the bust mesh caused; `smart_decimate` allocates density by *face*
rect and a galleon has none; the thin-extent probe will fire on the entire rig, which may
be correct or may withhold most of the subject; and `gate_mesh.py` is character-only and
must not be run on it.

*⚠ Kept as written because every one of those pre-registered stressors landed (annotated
2026-08-05): the framing class produced the fit-axis/margin/aspect three-consumer family
and a generator-legality re-rule (E04 Rulings 6/11/15/25); `thin_extent` was measured
per-mesh at 0.01 on the rigging and its beast successor produced the
no-global-value-separates-membranes constraint (E12 Ruling 7c); `gate_mesh` non-execution
became the `mesh_gate: none` decision form both non-character profiles carry.*

### What E01 established

- **Reconstruction is not the facial ceiling.** The generator's **1024 px input cap** is:
  worked through the preprocessing path, a full-figure clay puts **~138 px** on the head
  and a bust crop of the same clay puts **~439 px** — about **3.2×**.
- **The styled twins are bound to the mesh they were rendered from.** They are a
  derivative of one specific silhouette, not a reusable asset, so twin generation is a
  pipeline stage rather than an input. Any new reconstruction — or head graft — needs
  its own twins.
- **Canny cannot find a silhouette that isn't there.** A Workbench clay render is flat
  grey on flat grey by design, so Canny returns 0.84% edge pixels and almost no outer
  contour — the ControlNet then constrains nothing and the model regenerates the
  character freely. Composite onto a contrasting background *and* union the figure mask's
  morphological gradient into the edge map: silhouette IoU went 0.290 → **0.777**.
  The recipe had been developed against lit concept art and carried to flat renders.
- **Polygons and texels are separate budgets.** `smart_decimate` allocates polygons;
  `bake_hero_prep`'s island scaling allocates texels. A head can hold 84% of the faces and
  45% of the texel area at the same time. A gate comparing UV area to *face count* is
  meaningless on a deliberately non-uniform mesh — compare UV area to **3D surface area**.
- **The gutter, not the island count, ate the atlas.** Two meshes at an identical 287k
  faces: A0 packs 8,486 islands (34 faces each) into 20.34% of a 4096 atlas; a decimated
  mesh packs 35,070 islands (8 faces each) into 4.01%. Dropping `island_margin`
  0.004 → 0.001 took it **4.01% → 18.76%**. Raising `angle_limit` 1.15 → 1.50 moved island
  count by **0.8%** and bought nothing — `smart_project` splits on UV distortion, not angle
  alone, and decimation's long thin triangles distort regardless of the threshold. The
  advisor predicted the opposite; the margin was the entire gain.
- **Atlas packing and styled coverage are independent.** Valid texels rose 4.7× and styled
  texels rose 5.4× — the ratio barely moved, falsifying the "most valid texels are
  bake-margin halo" hypothesis.
- **⚠ The coverage baseline was contaminated, and the floor built on it was void.**
  `figure_mask` keys A0's twin over its own background: **30.2% and 32.3% of the bottom
  corners** register as figure, where a centred standing figure cannot reach. A0's twin is
  painted concept art with a background gradient and a cast shadow, and a flat top-corner
  median at tol 0.06 keys a third of the lower background as paintable surface. The
  clinching arithmetic: A0 styles **81–100% of its geometrically orientable texels** against
  a two-view ceiling of 61.5% — a number brushing an unreachable ceiling is a broken
  measurement, not a good result. Measure **`styled / geometrically-reachable`**, which has
  a true ceiling of 1.0 and is comparable across silhouettes; and take the mask from the
  render the twin was restylized from, where it is exact and registered by construction,
  rather than re-keying the twin heuristically.
- **Consequence for shipped work:** the warrior the Director rejected has background grey
  projected into its atlas, and a contaminated mask also disables the edge guard meant to
  keep silhouette pixels out. Three independent faults in that asset — poor geometry, twins
  never registered to it, background baked into the texture. None of them the texture
  architecture.
- **Prompt per view.** A shared prompt asking for "a long red beard, gold necklace" on every
  view overrode a correct ControlNet contour and put a face on the back of the head at both
  0.92 and 0.75 denoise. Per-view prompting fixed it outright — face detections 1 → 0
  against a source-back control of 0, with silhouette held at IoU 0.784.
- **⚠ The exported mesh is not a solid — no volumetric predicate works on it.**
  `prep_uv.glb` carries **293,099 verts for 287,170 faces**: the glTF export splits a vertex
  at every UV seam, so every island boundary is a topological crack. `compute_signed_distance`
  at the figure's own bbox centre — the middle of a standing warrior's chest — returns
  **+0.0019**, i.e. *outside*. Ray parity leaks, containment is meaningless, and only **37%**
  of outward rays escape. Any query needing thickness, containment or inside/outside must run
  against the **welded mesh before export**. Related trap: a ray cast along the surface normal
  measures the *tessellation*, not the geometry — median reported thickness 0.00204 against a
  median edge length of 0.00290 is a sub-triangle hit on a neighbouring face.
- **Thinness is a property of the surface, not of a camera.** A two-sided camera-ray extent
  probe (`extent = 2D − t_front − t_back`) needs neither normal nor interior, so it survives
  the above — but it reads a blade as *thick* when the blade is edge-on to that camera, and
  withheld only 49.9% of the sword column at yaw 90. The general form is to compute the thin
  mask per view, back-project to texels, and **union across views**.
- **A masked inpainting brush invents when it is starved of context.** At denoise 1.0 hole
  texels have nothing to preserve, so the model anchors only on prompt and contour. At yaw 90,
  95% of the figure is hole — the worst-anchored camera in the set, furthest from both styled
  poles — and the brush returned a plaited rope belt where the twins have a flat leather band,
  a shoulder strap over bare flesh, and a tunic extended from waist to mid-thigh. **Order the
  strokes to spiral outward from the styled poles** (45 → 315 → 135 → 225 → 90 → 270), so each
  stroke extends an existing character rather than composing a new one, and each commit
  anchors the next.
- **One mask cannot answer two questions.** *Is there real surface here* is answered by the
  mesh silhouette, un-eroded — a visible texel always projects inside it, by definition.
  *Is the paint here trustworthy* is answered by the twin's own painted figure, eroded.
  Conflating them and eroding the mesh mask cost **480k texels**: near the silhouette the
  surface turns edge-on and enormous numbers of texels foreshorten into a thin band, so
  peeling a few pixels off it removes far more than the same peel anywhere else. The bug was
  invisible under the old heuristic mask, which keyed *wider* than the mesh so erosion ate
  background instead of surface. `mesh_mask ∧ erode(twin_mask)` took styled coverage
  **23.8% → 53.7%** of reachable with the denominator unchanged.
- **Framing is a route stage, not a tweak** — 3.1–4.5× head polygons, and the gain is
  separated eyelids, a brow furrow and modelled nostril cavities rather than sharper blur.
- **Shell soup was ours.** Reconstruction returns 1 connected component; our UV unwrap and
  glTF export split it into 285,654. Welding before decimating restores it, verified
  against a control that reproduces the old broken output byte-for-byte.
- **Four inherited claims failed** — the clay provenance, the shell count, the facial
  ceiling, and the strength of an archived resolution observation. An inherited claim is a
  hypothesis wearing a fact's clothes: checking one costs minutes, building on one costs a
  session.

## Why it works this way

An earlier arc of this project ran ten sessions in which each session judged its own
output, wrote its conclusions to a shared memory store, and the next session read those
conclusions as established fact. Errors compounded silently because nothing in the loop
was checkable and nothing was gated on the Director's eye.

The repo is the fix. A claim sitting next to runnable code can be tested in minutes.
A tool marked *superseded* with its failure documented cannot quietly become doctrine,
because anyone can run it and watch it fail the same way.