# Experiments

Each experiment is a spec written by an advisor session, run by a separate executor
session, and judged by the Director. The separation is deliberate: the session that
designs an experiment does not get to grade its own results, and the session that runs
it does not get to decide what the results mean.

**A spec is written before the work. A report is written after. Conclusions come last,
from the advisor, only once the Director has seen the evidence.**

| id | question | status |
|---|---|---|
| [E01](E01-facial-structure-ceiling.md) | Where is the facial-structure ceiling — framing, generation resolution, generator, or reconstruction itself? Does any configuration produce a connected surface rather than shell soup? | **RULED** → [Gate 1 ruling](E01-ruling-gate1.md) |
| [E02](E02-texture-stage-on-sound-inputs.md) | Is the texture stage sound when its inputs are? Every texture result in this project was produced on a blob mesh with twins belonging to a different silhouette. | **RULED — answer is NO** → [Gate 1 ruling](E02-ruling-gate1.md). Director rejected with every input sound. Two twins + eight cameras paint only ~25% of the surface; the rest is interpolated across a 35,070-island atlas where neighbours are unrelated pieces of the model. Next target is the UV unwrap, not the brush. |
| [E05](E05-paint-more-surface.md) | Why is three quarters of the asset interpolated rather than painted? | **RULED** → [Gate 0](E05-gate0.md). Neither lever moved it. **49% of valid atlas texels are never visible from any of 46 exterior cameras** — half the surface the atlas pays for is not on the outside of the model. Native xatlas UVs adopted permanently |
| [E06](E06-cull-the-unseeable.md) | Does culling exterior-invisible faces before unwrapping fix coverage, atlas efficiency and the artifact mechanism together? | **RULED** → [report](E06-report.md), [Gate 1 ruling](E06-ruling-gate1.md). Culling works and the blade is fixed by the Director's eye. Interpolation fell **68%** (2,551,893 → 813,773 dilated texels), brush share 27% → **52.7%**. Two predictions falsified: density rose ~17% not 2×, because **charts fragment under culling rather than merging**. Atlas layout is now the binding constraint |
| [E07](E07-the-atlas-is-not-a-neighbourhood.md) | The loop writes colour from two places and neither knows where a texel's neighbours are *on the surface*. Does giving it one remove the defect class the Director named? | **RULED — CLOSED, neither arm adopted** → [Gate 1 ruling](E07-ruling-gate1.md). Director: *"the images don't look good"*, and on being asked which defect, **"all of it — the asset is not close"**. The finding is a measurement failure: **four of the five units this experiment graded on are 5×5 high-pass statistics** and the defect that decides acceptance is a *large region of the wrong material*, which is smooth inside itself and registers only its rim. L1 cut dilation source distance **70×**, took mean fallback 734 → 0 and speckle below A0 — and changed nothing to the eye. Structural read: **28.4% twins / 37.7% brush invention / 33.9% interpolation** — four experiments improved how the other 71.6% is filled, none reduced it. Next is a free geometry pass on the twin-projection ceiling at N = 2/4/6/8 views, **not** another fill arm. Evidence: [Gate 0](E07-gate0.md), [Gate 0.5](E07-gate0_5.md), [report](E07-report.md), [Amendments 1-2](E07-the-atlas-is-not-a-neighbourhood.md). Both premises held: **74.9%** of dilated texels took colour from another island, from a median **0.177 away on a figure 1.0 tall**; step ratio **5.500**. **L1** passes the restated gate (source distance **0.87 edges**, 0.004% beyond 20), mean fallback **734 → 0**, speckle **2.64/1.07/0.23 → 2.43/0.91/0.16**, 10.0% of turnaround figure pixels changed and concentrated on the back half — but its head-zoom pass condition is **not met** (3,372 vs <3,100; dilation 759 vs <400). **L2 re-anchored on the accepted set** moved the ratio 5.500 → **2.600** and blotch **−53.5%**, then **tripped the flattening ANDON at −10.66%** (limit −5%) with the denominator inflating 4q → 5q; GPU not authorised. Correction to the spec: **the gutter is not the mechanism** — only 32.5% of paths cross one, and the minimal `& valid` patch still leaves 53.3% cross-island while stranding 174,898 texels |
| [E08](E08-cover-the-figure-with-reference.md) | Can the styled reference cover the figure — and if it does, does the defect the Director rejected go with it? | **RULED — CLOSED, ACCEPTED at Gate 1** (2026-08-04, on the GLB at the Director's own zoom) → [E08-ruling-gate0.md](E08-ruling-gate0.md), 35 amendments. Measured provenance **68.8% reference / 4.2% brush / 27.0% dilation** against the rejected asset's 28.4 / 37.7 / 33.9. The first accepted asset. *The archaeology below is the arc's evidence trail as it accumulated:* [Gate 0](E08-gate0.md). **Half 1 passes**: 8 cameras reach **74.10%** of valid texels (halt was 60%), but A2's 80% is unreachable — 12 cameras + 2 elevated at the loosest threshold still gives 79.06%. The two SIDES add more surface than the four diagonals (267,176 vs 247,979), falsifying A1b. **Half 2's construction is impossible**: hold-one-out needs ≥2 cameras per texel and the two twins overlap in **exactly 0** — `facing_front = −Ny` and `facing_back = +Ny` cannot both clear 0.45. Viable from N=4, strong at N=8 (79.9% of reachable). Substituted **reference-agreement ΔE in CIE Lab**, which validates: null at **ΔE 0.72** where the asset carries the reference, **23.18** brush, **18.70** dilation, separating every region the Director named from the controls by **18–65×** — the blade's non-reference surface reads neutral steel (123,122,124) → flesh (147,97,63), median ΔE **32.71**. Severity tracks reference coverage: blade 62% non-reference → ΔE 23.17; tunic 1.2% → ΔE 0.49. ⚠ The acceptance stage discards **46.2%** of what the cameras physically reach, so 8 views likely buy ~40% reference coverage, not 74%  · **[Arm A](E08-armA.md) run**: the reorder is a **no-op** (comparative ownership is byte-identical to the absolute gate at three floors — the shipped loop already falls back to a worse-facing view), and the facing floor is not the lever either (0.45 → 0.05 buys +1.31 points). The cost is the other two tests: **EDGE +211,087**, **MASK +257,511** texels if removed. **The saved `*_mask.png` is missing 24% of the mesh silhouette** — 146,356 px true vs 111,602 used, IoU 0.76, loss *interior* (a stripe down the whole blade), from `figure_mask` keying a grey-on-grey clay render; registration ruled out at shift (0,0). And `project_twins`' "twin is fatter than the mesh (15.8% vs 9.9%)" compared the twin against **that same broken mask** — against the true silhouette it is **twin 17.43% vs mesh 19.01%, IoU 0.911**, so the mesh is fatter and the premise making the EDGE test safe is void  · **[Arm A2](E08-armA2.md) built**: `project_twins` now answers *is there surface here* from **geometry** (raycast silhouette) instead of a threshold on the clay render; `--mask-keyed` reproduces every prior arm byte-for-byte (sha `b12917a2c7c14c4b`). **Reference coverage 681,212 → 938,718 texels, 28.4% → 39.1% of valid, 53.8% → 74.2% of reachable**, with `lost 0` — strictly additive, no diffusion, no GPU. Failure mode checked: recovered texels sit at median ΔE 38.31 from the twin background, 0.18% within ΔE 10, *cleaner* than the already-trusted set (0.32%). The blade is still hole — now held back by the EDGE test, whose justification Arm A voided. **Coverage is not quality**: a stage-1 ΔE grade is vacuous by construction, so whether the rejected defect goes with it is unanswered and needs the loop  · **[Arm A3](E08-armA3.md) HALTED** on the andon Amendment 3 placed, which caught a real defect on its first live run. The invariant holds exactly (max e/R = 0.3333, 0 violations) and does what it was built for — the blade's 4-8px stratum goes from **77.6% removed to 33.5%**. But the background probe fired: **75.13%** of newly-admitted texels sit within ΔE 10 of the twin's background (median ΔE 4.9) against **0.11%** for the already-trusted set. Probe validated both ways first — it fires on a deliberately loose build (49.45%), and the twin's own paint is clear of background everywhere (blade median ΔE **24.80**), falsifying my hypothesis that it was flagging grey steel. **The mechanism inverts the story**: the twin's keyed mask is contaminated with background at **21.3% / 16.4%** in the 1-2 / 2-4px strata, falling to 0.1% at 32px+ — E01's keying failure, alive and *thin*. The absolute erosion was **accidentally correct**: it deleted 100% of those strata, removing the contaminated tendrils along with the blade. The invariant preserves thin structure proportionally, so it preserves both — they live at the same scale and half-width cannot tell a blade from a shadow tendril. No A3 atlas written  · **[Arm A4](E08-armA4.md) HALTED at its own precondition — a full negative result.** The colour discriminator was to be derived from the measured bimodality; **there is no bimodality.** ΔE-from-background over the twin figure rises **monotonically 5→30 with no antimode**, and the two views disagree on whether a weak one exists. The "gap between 10 and 25" came from comparing two summary statistics (region medians ~25 vs the contaminated median 4.9) — distant medians do not imply a gap in the density. Otsu is the wrong tool and its own output says so: cut **33.6**, class means 25.8/41.6 — dark paint vs light paint — rejecting 41,194px at **median depth 8px**, deep interior, because the contaminated class is 0.5% of the mask. And colour is not a proxy for boundary here: only **47%** of sub-ΔE-12 pixels lie within 2px of the edge. Restricting to thin strata does not rescue it — 16–28% of those pixels are legitimate paint above ΔE 25. The gate cost one measurement and no build. Measured support for the unspecified arm: the background is mid-grey and the subject contains mid-grey steel and leather, so a background outside the subject's gamut would *create* the separation rather than threshold a distribution that has none  · **[Background arm](E08-armBG.md) HALTED — the separation appears and the reference repaints.** Colour derived, not chosen: **rgb (0,0,255), min ΔE 123.31** to the subject gamut, where the current grey sits at **6.00** — below the ΔE 10 line, which is A4's failure in one number. Clay re-render byte-identical to the shipped views; `turn_render` gained `--bg` with the default unchanged. **Separation appears and both views agree**: min ΔE to gamut 0.00/0.20 (grey, literally inside it) → **54.15/61.05** (blue), and the render background beat an explicit "plain grey background" prompt term at denoise 0.92. Clay keying fixed at source as a side effect (9.9% → **18.1%** vs a 19.01% true silhouette). **But the subject repaints**: median ΔE **14.30/11.41** inside the exact silhouette, 69.9%/56.1% above ΔE 10 — and at material level on terms the prompt held byte-identical: gold knee plates **gone**, charcoal boots → **brown fur**, wine-red skirt → **green**. All three executor predictions falsified. Pre-registered reading applies: **the absolute erosion's cost at the blade is necessary, not accidental** — no cheaper route survived (A3 shape-blind, A4 no bimodality, background repaints the reference)  · **[BG2](E08-armBG2.md)**: control built from the exact mesh silhouette, so the contour term is **identical across arms (9,958 px both)** and total control divergence is **0.13–0.22%** of lit pixels. The repaint largely goes with it — median ΔE **14.30 → 8.19** (front) and **11.41 → 6.06** (back), and the **ΔE > 25 gross-material share collapses ~10×, 19.1%/23.2% → 2.3%/2.1%**. By eye the two arms now carry the same materials, and **BG2-grey matches BG1-*blue*, not BG1-grey** — which localises BG1's relocation to the control, as pre-registered. Separation survives (0.35/0.40 → **58.44/64.16**). Lands **between** Amendment 6's two branches: gross relocation gone, a tonal residual remains (38.3%/21.9% above ΔE 10). Reported, not resolved — the Director has not seen these twins. `figure_mask` is now retired from every path that governs anything  · **[Registration](E08-registration.md)**: measured against the exact silhouette (19.01%, bbox 849×388) — E01's own criterion. The corner-median key **failed loudly first** (painted 31–76%, bbox 751px in a 752px frame) because diffusion paints a lit gradient, not a flat field; replaced with a fitted quadratic background, which leaves the shipped twin's numbers unmoved. Clean grey-vs-grey isolation: **BG2-grey registers best on 5 of 6 cells** (IoU **0.9314 / 0.9222** vs shipped 0.9088 / 0.8900 and BG1-grey 0.9040 / 0.8638), painted fraction closest to truth (18.95 / 19.93 vs the shipped 17.38 / 17.01, both undersized), stable across three tolerances. **The extra control was noise**: BG1-grey carried 3.4× the contour (33,026 vs 9,958 px) and registers worse everywhere. Blue twins **not reportable** — IoU swings 0.61→0.92 on threshold alone, so no figure is quoted. Result splits: the geometry control clears the pre-registered bar; the blue background is undecidable on this axis  · **[A2 re-run](E08-armA2R.md)**: BG2-grey twins + fitted keying ship together. Reference coverage **28.4% → 39.1% → 43.0% of valid**, and **53.8% → 74.2% → 81.6% of reachable** (ceiling 52.66% of valid). Decomposed rather than asserted: the estimator alone moves **−0.1 pt** on the shipped twins (it reduces to a corner median on a flat field, so nothing prior loses comparability) — **the entire +4.0 pt is the better-registered twin**. Guards: `reachable` invariant held at 1,265,391 exactly; the bbox andon passed within 1.2% (843×385 / 859×386 vs 849×388). ⚠ Not strictly additive — 148,693 newly styled, **54,978 lost**, net +93,715 — and the newly-styled set sits at **2.03%** within ΔE 10 of background against 0.26–0.28% for the trusted set. Reported, not ruled on. All three predictions held  · ⚖ **[DIRECTOR'S CANON RULING](E08-director-canon-ruling.md)** — *"the image to the left is the character that I originally designed. I'll take the gold over the fur, because the character is on point."* **Identity dominates registration.** BG2-grey wins every stable registration measure and is visibly more legible, and it is **the wrong man**. **A2R is withdrawn as off-canon** — its +4.0 pts were bought with a different character. **The geometry-derived CONTROL is withdrawn as a twin-generation path**: same mesh, seed and prompt, only the ControlNet input changed, and the character changed with it. **A2 stands unchanged at 28.4% → 39.1%** — it never touched the twins. Amendment 4's original halt (*"an arm that improves keying by changing the reference has broken the thing it was protecting"*) was right and the reframing away from it was wrong; *"better registration is a better reference, no taste required"* is falsified. ⚠ **The provenance defect is now critical**: the canon twin cannot be regenerated — built identically it does not reproduce (IoU 0.9040 vs 0.9088) — so the asset the route exists to carry is a file we can copy and cannot recreate  · **[Armour test](E08-armour-test.md) — canon CAN be stated in the prompt.** One term inserted ("gold knee plates"), control image byte-matched at 20,973 px, nothing else changed: **the plates returned**, correctly placed, with the clean control's legibility kept. All three executor predictions falsified. The tension the canon ruling exposed largely dissolves — canon from the prompt, control free to be clean — and it splits Amendment 8 precisely: the surplus control was **noise for registration and signal for identity**. Canon pair frozen under `canon/` with an incomplete-by-record MANIFEST; `restylize_views` gains versioned per-view prompts and a provenance sidecar so this cannot recur. Open: the forearm bracer (same one-term fix, untried)  · **Bracer test — SPLIT**, and it is the first crack in the specification premise: with a byte-matched control, *"brown leather"* landed (fur cuff → smooth segmented leather) and **"gold-trimmed" did not**. Two one-term tests now stand at **one full, one split**. Not tuned — per Amendment 11, adding terms until the element appears would fit the spec to the outcome. `canon/MANIFEST.md` records the **role demotion**: the pair is a SPECIFICATION SOURCE, not the projection reference, so its irreproducibility stops being load-bearing — a specification reproduces where an artifact does not. **[Spec proposal](E08-canon-spec-proposal.md)** is up for ratification: 5 proposed additions read off the canon twin (belt medallion — which the prompt appears to misname as a *"gold necklace"* that is not there — sword crossguard, plate fur trim, green skirt layer, pauldron scrollwork), plus the proportions explicitly marked NOT canon  · **[N11 grammar test](E08-N11-grammar-test.md) — DROPPED, stopping rule fires.** Promoting the gold trim from a modifier to its own head noun ("a gold plate on each outer forearm") produced **no plate and no measurable change**: median ΔE **1.07** whole-figure, 1.15 on the forearm. The model did not respond to the phrase at all. Both the advisor's prediction and mine falsified. **Arm B waits**; the specification premise is weaker than Amendment 12 assumed. Live alternative, pre-registered before the roll and NOT tested here per the stopping rule: every element that **replaced** what occupied its surface landed (knee plates, leather bracers); every attempt to **add** a second element to an occupied surface dropped, in both grammatical forms |
| E09 | Chart fragmentation — the binding constraint on texel density. Culling removed 47% of faces but only 34% of charts. Remeshing is the untested candidate. | **deprioritised by E07's ruling** — density and softness are a different axis from wrong material in the right shape. Was E08; renumbered 2026-08-05 |
| E03 | How does the bust crop's facial geometry reach the full-figure mesh — head graft, or detail transfer? | unblocked (E02 ruled); not scheduled — E12 Ruling 2 measured a beast head with legible native structure, so the bust-crop question currently has no live subject |
| [E04](E04-the-galleon-through-the-route.md) | Does the route hold on a **non-character subject**? A galleon: landscape framing, no face to anchor density on, and thin rigging everywhere. | **RULED — CLOSED, ACCEPTED at Gate 1** (2026-08-05, "it looks good to me," on the five-column sheets) → [E04-ruling.md](E04-ruling.md), 29 rulings + the close (*the count read "28" until E15's verifier counted the record*). Mix **36.89 / 6.87 / 56.24** = **86.4% of the pre-registered 42.72% ceiling**; zero credits across every generation. The central hypothesis (*no shared-code edit needed*) was **falsified five times, and that was the payoff** — each falsification hardened the profile system. The second accepted asset. On-surface restatement of every headline: [E10-offsurface-ruling.md](E10-offsurface-ruling.md) |
| [E10](E10-environment-contact-layers.md) | Can an environment-contact effect (the Director's waterline) ship as **data, not geometry** — the GLB whole, the scene's water doing the hiding? | **RULED — CLOSED** (2026-08-05, "that looks about right," confirmed against a photograph of a real galleon) → [E10-ruling.md](E10-ruling.md), 12 rulings, four generations, zero credits. The law, measured across eight arms: **inpainting continues an asset; full-frame generation introduces new material; layers fill by masked projection** (−1.6 to −4.1 ΔL\* inpaint against +33.4 full-frame, one field changed). `waterline_z` rides in profile + manifest with four independent confirmations. Ruling 4's queued `pos.npy` dispatch ran post-close: **the off-surface 2.5% is painted, not padding** — every headline restated on-surface beside the as-recorded family → [E10-offsurface-ruling.md](E10-offsurface-ruling.md) |
| [E11](E11-dense-turnaround-export.md) | Do accepted assets become **training data** — a dense-turnaround export the sdlab lane validates without edits? | **RUN AND RULED** (2026-08-05) → [E11-report.md](E11-report.md), [E11-ruling.md](E11-ruling.md). All four hypotheses confirmed: the export is a pure function, the shared views reproduce the record byte-identically, indexed conversion is lossless per file, and **both dense trees validate through the lane with zero schema edits** (galleon 28-cam with the native owner channel; W3 26-cam, owner honestly absent). Ruled: the emit render generation trains; flat-only is the honest export; backgrounds are augmentation-side via exact silhouettes |
| [E12](E12-executor-kickoff.md) | Does the route hold its **third subject class** — a beast: wing-membrane SHEETS, filament horns and spines, a live head/allocation question, scale relief? | **RULED — CLOSED, ACCEPTED at Gate 1** (2026-08-07, "Fantastic!! This is really good!", on the GLB at the Director's own zoom) → [E12-ruling.md](E12-ruling.md), Rulings 1–30; [Gate 0 report](E12-gate0-report.md). **The third accepted asset, zero credits across the entire arc.** Sixteen handoffs from designation ("3 is the winner") to acceptance in three days: the register earned (ultra-realistic, NO LoRA — the style-register-is-subject-data ruling), the occupancy-complete fixture, the accepted pair reproducing pixel-exact, harmonization adopted, stage 1 at 87.5% of the pre-registered 50.46% ceiling, four no-LoRA strokes, mix **44.15 / 3.07 / 52.78 of valid = 87.49 / 2.86 / 9.65 of reachable**. Now **dataset asset #3** (dense export + live ingest, the first manifest born declaring subject/style/tone-transform/derivation under the lane's 1.3.0). The finalization arm is a register-LoRA retexture, parked on dataset maturity (Ruling 28a) |

| [E13](E13-detail-pass-spec.md) | Do crop-framed twins, projected first through crop cameras, deliver crisper detail-region texture than full-figure twins alone? | **RULED at E12 Ruling 24** — split verdict: the **capability is banked permanently** (crop-camera projection anchored at zero changed pixels, twice; the frame-bounds and width-fit fixes) while the **generation half is falsified for now** (crop paint drifts register at bust frames — frame-changes-register, third measured instance; reference-conditioning is the named future lever). The A2 finding — the 4096 atlas under-resolves the head at BOTH frames — made the per-subject head rect the primary head lever, specced and parked at the Director's timing |
| [E14](E14-executor-kickoff.md) | Does the route hold its **fourth subject class** — a prop: a near-2D blade at figure scale, hard facets, a helical wrap, steel's grey-on-grey? | **RULED — CLOSED, ACCEPTED at Gate 1** (2026-08-08, "Fantastic! I accept", on `longsword_hero.glb` at the Director's own zoom) → [E14-ruling.md](E14-ruling.md), Rulings 1–35. **The fourth accepted asset, zero credits designation to acceptance.** The drifted stone returned to garnet by arithmetic, not regeneration (the re-projection, Rulings 25–27); eight strokes at zero re-rolls; finalize replayed byte-for-byte; **dense export at zero gap notices → INGESTED as dataset asset #4** (114 records across five ingests; the lane pushed to main at the Director's word); **the polish arc chartered** (Ruling 35). *This row read "STAGED, gate 0 dispatched" until 2026-08-08 — the arc outran its row and the sweep cannot see prose staleness; the status-table duty is the advisor's* |
| [E15](E15-context-index-kickoff.md) | Can the record be indexed — SQLite+FTS5, generated, verify-gated — so sessions query instead of reading six hundred lines? | **RUN AND RULED** (2026-08-07) → [E15-report.md](E15-report.md), [E15-ruling.md](E15-ruling.md). All four verify legs pass at two seats' hands — byte-identity across interpreters, zero dangling over 1,079 rows, counts against independent greps, **14/14 seeded**. Index contents at acceptance: ruling rows 285 · law rows 69 · decision rows 217 · prose sections 1,457 over 159 files (*phrased as row counts deliberately — a bare arc-attributable count here would read as this arc's own tally and go falsely STALE; E15 handoff 2 flagged the hazard, and handoff 3 then measured the first fix firing on its own explanatory quotation — a sweep cannot tell a phrase used from a phrase mentioned*). The honest boundary: artifacts/phenomena are extraction not parsing; supersedes-links nearly empty; the seeded gate is **growth-sensitive** (measured live mid-session) and re-runs every fold. Found E04's true ruling count (29 vs the prose's 28) by counting the record itself |
| [E16](E16-errands-kickoff.md) | The errand batch — eleven queued repairs, each one commit + one anchored regression; the one behavior change as an opt-in flag. | **RUN AND RULED** (2026-08-08) → [E16-errands-report.md](E16-errands-report.md), [E16-ruling.md](E16-ruling.md). **Ten anchors HELD; E16-5's anchor FAILED honestly** (nothing changed) and is ruled: the honest condition alone, the repair to E17 with its anchor pre-stated from the measured table. Predictions 9 held / 1 split / 1 falsified — both misses the informative ones (the retired corner median under-reported risk worst on the highest-risk view; the "complete" handoff list hid two of E15's). Four dispatch corrections caught against source; one executor finding withdrawn in place under controlled re-test; the shared-tree rules adopted as standing; the twelfth errand (the det_a race, hit live by both seats independently) ruled into E17. Zero credits |
| [E17](E17-harness-kickoff.md) | The test harness — the record's run-once checks (E16's anchors, the selftest, the four-leg verify) become a persistent `tests/` suite + paths-gated CI, under the tests-ride-the-commit law. | **RUN AND RULED** (2026-08-08) → [E17-harness-report.md](E17-harness-report.md), [E17-ruling.md](E17-ruling.md). **32 tests, 32 passed at two seats' hands** (24 hermetic + 8 artifacts; *this row read "27 tests, 27 passed … 19 hermetic" until 2026-08-08 — that was Ruling 1's mid-arc state, and **Ruling 5 closed the arc at 32**; flagged by E19 §6a as want 1 and corrected here at the E19 ruling. The suite is 202 at the time of writing, but that is E18's and E20's doing and belongs in their rows, not this one*; the advisor's re-run reproduced the session's result exactly under the trellis2-env python — and the advisor's own wrong-interpreter first run is owned in the ruling, with the T18 pre-check ruled in so the trap fails in one line next time). The mid-flight E16 amendment executed as tool+test single commits with ruled anchors held; both first-run artifacts failures were the session's operand errors against a record that was right, diagnosed FROM the record, no anchor moved. The repo's first CI workflow rides it (ubuntu, paths-gated, pinned, hermetic set). Predictions 11 held / 2 falsified, both misses informative (pytest already present; the selftest is artifacts-tier — the hermetic fixture is commissioned to the build session). Zero credits |
| [E18](E18-index-mcp-kickoff.md) | Spec 1 becomes a running server, in facet — the record-index MCP (`record_query`/`record_get`/`record_build`/`record_verify`/`record_health`/`record_claims`) over stdio, the four-leg verify as its refusing health surface. | **RUN AND RULED** (2026-08-08) → [E18-index-mcp-report.md](E18-index-mcp-report.md), [E18-ruling.md](E18-ruling.md). **The record-index MCP is a running server** — six tools over stdio, built exactly as pinned, 60 tests riding the commits that built it (suite 32 → 92, green at two seats). **The health surface FIRED rather than being asserted**: leg 2 caught a hand-corrupted scratch DB, every read tool refused with the code and the one fix command, one `record_build` recovered, and the staleness banner fired on a real corpus move. **The dogfood found a defect the suite could not** — `verify`/`claims` each leaked a sqlite connection, harmless for every one-shot caller ever, fatal for the first long-lived one; fixed at the root, can-fail proved by injection. Twelve flags ruled (the DB now travels as a DB+certificate PAIR; `BAD_ARGUMENT` joins the codes; the pathspec-scoped commit is standing shared-copy practice). Predictions 10/5, misses informative. Zero credits |
| [E19](E19-treatment-kickoff.md) | The full treatment — facet becomes a presentable studio repo (shipcheck gate, logo, landing page, Starlight handbook, metadata, repo-knowledge entry, staged translations + v1.0.0 tag) **without rewriting its record**. | **RUN AND RULED** (2026-08-08, at the Director's word) — the protocol files are the spec; Law 1: the measured record is added to, never rewritten (claims sweep 0 STALE gates every content phase); Law 2: public surfaces lead-authored, no subagents; publish actions staged for the Director's word after the ruling, translations advisor-run first; halted at [E19-treatment-report.md](E19-treatment-report.md) and is **RULED** → [E19-ruling.md](E19-ruling.md), 7 rulings. **THE TREATMENT IS ACCEPTED**: shipcheck 3% → 85%, README 867 → 208 with the relocation audited mechanically (3 lines differ, all tagline), landing page + six-page Starlight handbook **LIVE** (Pages green at run `31266198261`), and the five `[mcp]` gates SKIP-ed against a serverless `main` **re-run on the landed server** rather than re-asserted — B4 fired live at the ruling's own hands. **The release is HELD on one repair**: the test count was stale on the live public front door — 32/24, true when E19 wrote it and falsified the same day by E18's 60 tests, invisible because the treatment surfaces sit **outside the claims sweep** (§6b, now demonstrated rather than theoretical). Corrected at ten sites *before* the translations, per the release-ordering law. Predictions 13 held / 1 split / 1 missed |
| [E20](E20-coverage-kickoff.md) | Unit coverage — the tool surface gets tests **below the replay level**: the parsers, the projection core, the write-head, the dilation lookup, the frame math, and a guards-can-fail audit closing Ruling 5c's class wholesale. | **DISPATCHED** (2026-08-08, at the Director's word — "32 tests is weak"; he is right, and this is the fix). No count target (the count is the Goodhart metric); the target is the load-bearing surface covered, per-tool, hermetic on synthetic fixtures. The assertion law: pins only anchored/accepted behavior — wrong-looking behavior halts into a finding for the ruling, never a pin. **GATE-HALTED before any unit was written** ([E20-gate-halt.md](E20-gate-halt.md) — the session fired its own sequencing gate, measured all three legs, and the conftest collision became record when E18's D1 modified the exact named file 65 seconds after one of its observations). The Director ruled: wait for E18's halt, no partial run, no worktree. Entry measurements banked ([Ruling 4](E20-ruling.md)): **8 of 33 tools reached by any test, all at whole-tool subprocess level** — the measured shape of "32 tests is weak". Fixtures authorship ruled (E18 authors the pattern, E20 extends — [Ruling 2](E20-ruling.md)); **one re-fire condition remains: E18's report exists**. **RUN AND RULED** (2026-08-08) → [E20-coverage-report.md](E20-coverage-report.md), [E20-ruling.md](E20-ruling.md) Rulings 5–12. **THE COVERAGE ARC IS ACCEPTED — suite 92 → 202** (110 tests in three files, tools reached 8 → 10, guard sites demonstrably fired 9 → 17, hermetic tier 47 s). **Its largest deliverable is a REFUSAL**: three of the six dispatched units *cannot exist* — `project_twins`, `texpass_finalize` and `mesh_stats` are scripts, not modules (`texpass_finalize` has **zero function definitions**), so the session produced the AST evidence, proposed three seams and declined to restructure accepted-asset tooling on its own authority. **All three seams TAKEN at Ruling 6**, ordered by risk, each one commit with its anchor re-run (T12 · the twin-projection anchor · T7's byte-identity). U6's scope ruled at **212 of 409 sites** — `superseded/` excluded permanently on principle (those tools are kept *because* they fail; a can-fail test there would make the repo maintain a contract on its own dead ends). Findings disposed: `github_slug` RETIRED (zero callers, unverifiable external claim), one comment corrected, the silent `tools/call` handed to E18's lane with a named hypothesis (a missing `notifications/initialized`). Watched predictions: **P12 FALSIFIED — the recorded E07 repair holds**; P17 hit to the decimal (2.11:1); **P5 still unsettled and now the repo's highest-value open question** (`fit_background` at frame-edge figures — the reason seam 1 outranks seam 2). F4 falsified Ruling 4a's own denominator (`tools/` holds **150** files, not 33) — corrected in place. Predictions 8 hit / 3 missed / 1 partial / 6 unsettled, and the miss pattern became a standing law: **a documented past bug is evidence an area is well-guarded, not evidence it is fragile** |

| [E21](E21-cli-contract-kickoff.md) | The installed CLI's operator contract — the three `[cli]`/`[mcp]` gate items that became applicable the moment the extraction turned on four new shipcheck tag families: the exit-code registry, the `--debug` gate over raw tracebacks, and logging levels. | **DISPATCHED** (2026-08-08, at the Director's word). Scope is RULED and narrow — **the two published console scripts only** (`facet-mcp`, `facet-index`); the 34 research instruments, `diagnostics/` and `superseded/` are out, because the `[cli]` tag applies to what facet *installs* and retrofitting accepted-asset tooling for a checkbox is not what the gate asks. Three calls are explicitly the advisor's, not the executor's: **what exit code a fired ANDON deserves** (a gate firing is the tool working, not a runtime error — E08 A32 binds and must not be weakened), what a failed `verify` returns (`claims` is already ruled to never fail, E15 Ruling 9b), and **where the quiet/measurement boundary sits** — levels may govern chatter, never a measurement or a refusal, because a `--quiet` that hides an ANDON is the same defect as a `--debug` that skips one. Targets **0.2.0**: it changes the behaviour of a CLI published the same day, and the four version declarations plus `bin/facet.js`'s tag pin all move together. Tests start at **T29**, every code carries a can-fail leg, and exit codes are asserted through a subprocess rather than read from source. **HALTED AT THE REPORT** (2026-08-08) → [E21-cli-contract-report.md](E21-cli-contract-report.md); **awaiting the ruling at `E21-ruling.md`**. Suite **218 → 248**; version **0.2.0** across all four declarations. Two of the three gate items closed with measured evidence — the registry (`0`/`1`/`2`, with `3` declared and pinned as never returned) and the traceback gate. **The pre-change surface was inverted at BOTH ends, not one**: user error exited 2 *and* runtime error exited 1 with a raw traceback, so **three distinct outcome classes shared exit 1**; the gate line had named only the argparse half. **The third item stayed open on purpose** — the dispatch reserved the quiet/measurement boundary for the advisor to rule *before* it ships, so the arc measured the census a ruling needs instead of guessing at one: of `verify`'s 35 print sites **zero are progress chatter**, and `record_mcp` *parses verify's stdout* to build the schema-versioned certificate, which makes the objection to a quiet `verify` **mechanical rather than philosophical**. Both ruled-to-the-advisor exit codes were left **unchanged rather than picked**, and are pinned only on what is ruled (a gate still fires and refuses; a failing verify still prints its evidence) — never on their integers. **The arc's largest finding is F2: the ANDON is a bare `assert`, so `PYTHONOPTIMIZE=1` — an environment variable an installed command carries — removes the gate entirely and the build exits 0** with a silently incomplete index. E08 Amendment 32's own failure mode landing on the repo's own gate, with the interpreter as the separator instead of a shell chain; reported, deliberately not fixed and deliberately not pinned, because pinning it would anchor a defect. Predictions **6 hit / 5 miss / 3 split** in the blind band, and **every clean miss is a quantity, none a behaviour** — plus one *lookup* that was wrong (the version bump took 7 edits, not the 5 a grep reported), which is the more chastening of the two |

| [E22](E22-gates-not-asserts-kickoff.md) | The repo's ANDONs are bare `assert`s, and `-O` / `PYTHONOPTIMIZE=1` deletes them. Does converting them to raises preserve every anchored behaviour exactly, and does the write path then refuse under `-O` as it does without it? | **DISPATCHED** (2026-08-08, at [E21 Ruling 2](E21-ruling.md)). Measured at the ruling seat: `tools/` carries **294 bare asserts across 72 files**, and of the gates carrying the `ANDON` token — **`texpass_iter` 8 as assert and 0 as raise** (the write-head at the centre of E08 Amendment 32), `texpass_finalize` 4/0, `project_twins` 15/1, `e11_manifest` 35/1, `e11_export_turnaround` 24/1 — with a control on the pinned interpreter showing the gate **silent and execution continuing past it** under both `-O` and the env var. A32 was earned when a shell chain walked past a fired ANDON and committed 47,020 texels; the repair put the check inside the tool, and **one environment variable removes 87 of those checks** — *strictly worse than the original defect*, because the shell chain at least let the ANDON print. Severity stated honestly: nobody sets `PYTHONOPTIMIZE` here and no artifact is claimed corrupted — **A32's test is separability, not probability**. Scope is 87 sites plus two developer guards; the **~207 non-ANDON asserts are OUT**. **Every conversion is a PURE MOVE** and an anchor that does not reproduce byte-for-byte **reverts** it rather than being adjusted (T7, T26's three fired ANDONs, the twin-projection anchor). Tests from **T30** assert each gate fires under a normal interpreter **and** under `-O` — the test the repair makes writable and the current construction makes impossible — and **no test pins the defect**. [E21 Ruling 4](E21-ruling.md)'s `4 = REFUSED` folds in. Halts at `E22-gates-report.md`. **RUN AND RULED** (2026-08-08) → [E22-gates-report.md](E22-gates-report.md), [E22-ruling.md](E22-ruling.md), 11 rulings. **THE ARC IS ACCEPTED**: 88 sites converted as a pure move — shown by whole-file AST equality against the prior commit, not by anyone's reading of the diff — all three anchors reproduced (T7 byte-identical *including its sidecar json*), suite **248 → 275** with the artifacts tier live and zero skips, CI green, and the compensator gate carried out further than it was written: **7,312 files sha256-manifested** across the whole recorded root before the first replay, re-checked three times, **0 changed**. The thesis was re-measured at the ruling seat as a before/after pair on one gate in four interpreter modes — **SILENT under `-O` and under `PYTHONOPTIMIZE=1` at the old tree, FIRING in all four at the new one.** ⚑ **THE SCOPE PREMISE WAS FALSE, AND IT WAS THE ADVISOR'S ERROR MADE TWICE.** The dispatch computed its excluded class as `294 − 87`, **subtracting a five-tool subtotal from a repo-wide total**, and gave the difference a name, a character ("developer sanity checks") and a reason to be left alone. Measured independently from git blobs: **294 asserts, 278 carrying the ANDON token, 16 not.** The excluded class is **16**; **191 declared gates had been misfiled into it by arithmetic**. Every per-tool cell E21 Ruling 2 published reproduces to the digit — only the two derived totals are wrong. **Ruling 3 RATIFIES the ANDON axis rather than overturning it**, and the evidence is that all 15 surviving non-ANDON asserts were read by hand: two of them sit *inside* converted write-path tools and each carries a comment **declining** andon status and citing E08 A3, so the token is a maintained taxonomy, not a marker someone forgot to type. Ruling 4 corrects the report's own **192/57 → 191/56** before it can scope the next arc — the same subtotal-as-total shape, **the second consecutive arc in which the scope number rather than the work was the defect** — and splits the remedy by risk: **57 route sites in 12 files → E23**, 132 diagnostics sites as a later mechanical arc, `superseded/`'s one site **never** (those tools are kept precisely so they fail the same way). `SystemExit` **not** unified, with the constraint recorded; `4 = REFUSED` does **not** extend to the 34 research tools; the `mcp_support` constant **adopted**; the certificate schema **not** bumped — a free version is no better than a free integer, and the real gap is that the field's domain was never written down. The arc's two new laws are now in CLAUDE.md: **a gate is never a bare `assert`** (E21 Ruling 2 wrote it and **no fold had carried it until this one**) and **check that the population is real before you predict its density** — P18 predicted 4, *halved* an untutored 8–12 on this repo's own calibration lesson, and measured **175**, which is the sharpest miss in the record |

| [E23](E23-route-gates-kickoff.md) | E22 left **191** ANDON gates that `python -O` still deletes. **57 of them are in route tools** — the twelve scripts that produced four accepted assets. Does the same pure move hold on files that **no test has ever executed**? | **DISPATCHED** (2026-08-08, at [E22 Ruling 4](E22-ruling.md)). **The scope is ENUMERATED in the dispatch, not derived** — two consecutive arcs had a scope-number defect and both came from arithmetic, so the twelve files are listed with their per-file counts (`bake_hero_prep` 15, `brush_cloud_step` 9, `subject_profile` 6, `e13_harmonize` 5, `bake_hero_fuse`/`bake_hero_pack`/`silhouette_masks` 4 each, `cull_unseen` 3, `export_asset_source`/`palette_gate`/`resample_atlas` 2 each, `restylize_views` 1) and the executor is told to verify rather than inherit them. Same pure-move bar as E22, and `AssertionError` stays the type **even though nothing forces it here** — measured: the repo's only `except AssertionError` is `facet_index.py:216` and none of the twelve is `facet_index`, so the type is kept because *the pure move is the one that changes nothing*, not because a handler demands it. Shapes measured at dispatch: **31 multiline, 0 `not`-conditions** (E22 had 3, so the negation rule is uniform), 0 sharing a line, 0 without a message. ⚠ **THE WHOLE DIFFICULTY: zero of the twelve are reached by any test.** E22's safety was T7/T10/T26; this arc has **no behavioural net**, so the AST proof carries the entire load. And the twelve are **two runtimes** — 10 files / 38 sites run under the pinned interpreter (`--help` exits 0 writing nothing, measured on all ten), while **`bake_hero_prep` and `bake_hero_pack` `import bpy`** and cannot run outside Blender, taking 19 sites with them. **11 of 12 are SCRIPTS that execute at import** and three have zero function definitions, so a test may never import them — E20's wall, and its refusal stands. Tests owed: `py_compile` on all twelve (hermetic, `bpy`-free, catches a bad splice — 12/12 clean today), the three-mode `--help` smoke on the ten, the structural law with a can-fail leg, and **fire what can be fired hermetically while reporting what cannot and why**. Compensators score **2 and are worse than E22's**: these tools bake, cull, resample and export into `E:\AI\training`, which **is not in git**, so the compensator is a restore-from-source that has never been rehearsed. Halts at `E23-route-gates-report.md`. **RUN AND RULED** (2026-08-08) → [E23-route-gates-report.md](E23-route-gates-report.md), [E23-ruling.md](E23-ruling.md), 13 rulings. **THE ARC IS ACCEPTED**: 57 sites converted as a pure move, **12/12 whole-file AST identical** to the rule applied at `48fa733`, 0 comment tokens changed, 57/57 per-site, **0 reverted**; suite **275 → 370**; trees manifested at 7,312 files across **four** measurements, 0/0/0 every time. **The arithmetic closes end to end for the first time in three arcs** — 88 + 57 = 145 AssertionError raises, 191 − 57 = **134** remaining (132 `diagnostics/` + 1 `superseded/` + 1 `verify/`) — re-measured at the ruling seat, as was the thesis itself: `subject_profile:60` fired through `silhouette_masks` is **SILENT under `-O` at `48fa733` and FIRES in all four modes at HEAD**. ⚑ **GATE 4 FIRED.** CI went red because `restylize_views` imports `cv2` at module level and CI's pinned install never had it — **no test had ever invoked any of the twelve**, so the dispatch's own premise produced its first casualty within an hour. The executor measured module-level imports across all twelve *before* repairing (`cv2` is the only real gap), followed the pattern `ci.yml`'s comment already prescribes for `mcp`, and **named the two coverage-removing alternatives and rejected them in writing**. **Ruling 2 ratifies the repair and writes the boundary the repo lacked**: a gate measuring the *result* halts; a gate measuring the *environment's ability to run the measurement* may be repaired, if it adds capability rather than removing coverage. Verified at the ruling seat that the pin never reached the published surface — `pyproject.toml` untouched, deps still `mcp>=2.0.0` alone. ⚑ **AND A FABRICATED CITATION**, the arc's most serious defect: a CI run id was committed with a `PASS` beside it *before CI had run*, and `gh run view` returns **404 — it never existed**. Self-caught and replaced with both real ids in the same commit; the ruling seat swept the whole class and every other citation resolves. New law: **a report may not contain a placeholder shaped like evidence**, and the advisor resolves every external citation at ruling time — the index's pointer leg cannot see a URL in prose. **F1: `brush_cloud_step:204` cannot fire**, shadowed by the lora-w gate above the only call to `preflight()`; confirmed by reading the control flow, **kept and annotated** rather than deleted, because it is a precondition on a function's contract and not on today's single call site. T31 found it only because its legs match each gate's **own message** instead of a bare non-zero — ratified as the house pattern. **Ruling 9 adopts T31's pin of the remaining 134 as the structural fix for the scope-number defect that beat two arcs**: a count under a test cannot drift silently. Two more laws land in CLAUDE.md (the gate boundary; the placeholder rule), plus the P4b unit lesson — 4 predicted, **20** measured, because the prediction reasoned about *files* while the instrument measures a *scope*, and **a tool that writes more can have fewer gates with a write in scope, since writing more is what makes you factor the writing out** |

| [E24](E24-installed-paths-kickoff.md) | `pip install facet-mcp` gives you two commands that **cannot find the record**. Can the root be resolved so a source checkout, a frozen binary and a wheel install all work — without breaking either path that works today? | **DISPATCHED** (2026-08-08, at the v0.3.0 release read-back). Targets **0.3.1**; a defect fix, no new capability. **Found by running a VERB on the published wheel instead of `--help`** — `facet_index.py` ships as a top-level py-module, so `REPO = dirname(dirname(__file__))` resolves to **`<venv>/Lib`**: `build` fails, `q` without `--db` fails, `--print-tools` prints a `db:` path that cannot exist, and `record_get` refuses with *no record corpus under `<venv>\Lib`*. **Not a v0.3.0 regression** — the published 0.2.0 wheel fails identically; v0.1.1 fixed the *frozen* branch and left the *wheel* branch behind. **The `npx` binary is unaffected and remains the supported path.** Scope is the **19 enumerated `REPO` consumers** (11 in `facet_index`, 8 in `record_mcp`), `facet_index`'s `--db` default, and `$FACET_INDEX_DB` support in `facet_index` — a second measured defect, since the env var is honoured by `facet-mcp` only while the README claimed *either*. **Three constraints are rulings, not preferences:** the env var selects WHICH DERIVED DB and never which corpus, so it may not be repurposed as the root · a resolver that cannot find a corpus **must REFUSE** (`4 = REFUSED`, E22/E23) and never guess a fallback · **neither working path may regress** — T28's five frozen tests pass unchanged or the change is wrong. The design hypothesis is offered to be *measured, not obeyed*: `FROZEN` is a **proxy** for "REPO is not a real root" and is blind to the wheel case, so test the property — *does this directory contain the record* — which `record_mcp.py:272` already does, and whose marker was measured at dispatch to be absent from the published wheel. ⚑ **THE MISSING TEST TIER IS THE POINT: T28 has five tests for the frozen branch and ZERO for the installed-wheel branch**, which is why this shipped in four consecutive releases. The new tier must **build a wheel, install it, and run a verb**, and must **fail on the broken tree before it passes on the fixed one**. `release.yml`'s step named *"Verify the wheel runs from a clean venv"* runs only `--help` and `--print-tools` — it must run what a user runs. Halts at `E24-installed-paths-report.md`. **RUN AND RULED** (2026-08-09) → [E24-installed-paths-report.md](E24-installed-paths-report.md), [E24-ruling.md](E24-ruling.md), 7 rulings. **THE ARC IS ACCEPTED.** Re-measured at the ruling seat **on a wheel built there**, not on the report's word: from a checkout `q` → 0 with correct rows, `claims` → 0, and the banner reads `db: E:\AI\facet\docs/index/facet.db` where it read `<venv>\Lib\…`; from an empty directory `q`/`claims`/`build` all exit **exactly `4`**, naming both candidates and both markers, while `--print-tools` still exits 0 so the path that worked is unregressed. Suite **384**, T28's five **byte-identical and passing**, scope exactly four files, CI green with both scanners. **The resolver is ratified including what was left out**: it asks the property — *does this directory contain the record* — instead of `FROZEN`, which named a runtime and was a proxy; two markers because one is not a marker (the `CLAUDE.md` count is **depth-dependent** — 14 at one level, 62 recursively, the report's 26 a third depth — but **exactly one directory carries both at every depth**, which is the half that decides); and **no walk-up from cwd**, ratified because adding one later is additive while removing one is a behaviour change. ⚑ **RULING 3 — THE TIER WAS SKIPPING IN CI**: 370 passed / **6 skipped**, five of them T32's wheel legs, because `build` was not installed. A check present on the rig and absent from the gate that fires every push **is the defect's own shape**; `build==1.5.0` is pinned in this fold under the pattern `ci.yml` already documents. ⚑ **RULING 4 — T28 PINS SOURCE TEXT** (`assert "os.getcwd() if FROZEN else REPO" in SRC`), which forced this design rather than a cleaner one, and beside it sits `assert … or True # documentation`, **an assertion that cannot fail**. New law: pin a structural property with an **AST walk**, never a source-string match — the repo already does the former in four places. ⚑ **RULING 5 IS THE ADVISOR'S OWN MISS**: `README.md:47` claimed the broken wheel "works for `q` and `claims`" — I measured `q` and generalised to `claims` without running it; E24 ran it and it exited 2. Fourth front-door drift in two days, second of them mine. P1 missed its band (17 predicted, **13** measured) and the reason is E23's law one level down — two "survivors" are line numbers of a *statement whose other line changed*, so the unit was ambiguous where the population was right |

| [E25](E25-diagnostics-gates-kickoff.md) | E22 converted 88 gates and E23 converted 57. **133 ANDON checks are still bare `assert`s** — the measurement instruments that produced the evidence for four accepted assets. Does the same pure move close the class? | **DISPATCHED** (2026-08-08, at [E22 Ruling 4](E22-ruling.md) and [E23 Ruling 9](E23-ruling.md)) — **RUNS IN PARALLEL WITH [E24](E24-installed-paths-kickoff.md)**, at the Director's word. Scope **enumerated**: 132 sites in `tools/diagnostics/` across 42 files + 1 in `tools/verify/` = **133 across 43**. `superseded/`'s one is **permanently out** and gate 6 exists to stop it being tidied away. Same pure-move bar; **`not`-conditions are 5 here where E23 had 0**, so the negation rule is *not* uniform and each must be checked. ⚑ **THE PARALLEL COORDINATION SECTION IS THE NEW PART AND IT BINDS**: disjointness was *measured* at dispatch — E24 owns `facet_index.py`/`record_mcp.py`/`test_t28`/`release.yml`, E25 owns `diagnostics/*`, `verify/*` and `test_t31`, and **no diagnostics or verify tool imports either of E24's files**, so there is no runtime coupling either. File-specific `git add` always; **`git fetch && git merge --ff-only` before every push, NOT `pull --rebase`** — corrected minutes after dispatch by trying it, because rebase refuses outright while another session has unstaged changes and `--autostash` would stash *their* work; and a `fold`-marked failure must be reported *with the fact that a second session was live* — [E23 Ruling 10](E23-ruling.md) said the concurrent writer can be another session, and now there genuinely is one. A defect found in E24's files is **reported, not fixed**. Test tier measured at dispatch rather than hoped for: `py_compile` on all 43, and **41 of the 43 give `--help` exit 0 writing nothing** in three interpreter modes — the two exclusions are `e12_head_render.py` (`import bpy`) and `e04_make_brush_prompts.py`, which **does file work before argparse** and is itself a finding the report must carry. **28 ANDONs in scope already raise `SystemExit` across 12 files** — [E22 Ruling 5](E22-ruling.md) rules they stay; report the collision, do not resolve it. And **T31's `REMAINING_ELSEWHERE = 134` must move to 1** — deliberately, in the commit that earns it, because that constant is E23 Ruling 9's structural fix for the scope-number defect that beat two arcs. Halts at `E25-diagnostics-gates-report.md`. **RUN AND RULED** (2026-08-09) → [E25-diagnostics-gates-report.md](E25-diagnostics-gates-report.md), [E25-ruling.md](E25-ruling.md), 10 rulings. **THE ARC IS ACCEPTED, AND THE CLASS CLOSES EXACTLY.** 133 sites across 43 files, pure move — re-proven at the ruling seat with its own instrument: **43/43 whole-file AST identical** to the negation rule applied at `1b60478`, 0 comment tokens changed, 0 reverts. **1 ANDON assert remains anywhere under `tools/`** — `superseded/texpass_thin_mask.py`, the site [E22 Ruling 4](E22-ruling.md) ruled never converted, and **T33 now pins it by name** so the permanent exclusion is enforced rather than remembered. `AssertionError` ANDON raises total **278 = 87 (E22) + 57 (E23) + 133 (E25) + 1 pre-existing**, against a pre-E22 census of 278 — **four arcs and the arithmetic closes on the first reading**, after E22's phantom 207 and E23's 192-that-was-191. ⚑ **GATE 4 IS *BLOCKED*, NOT FAILED, AND THE EXECUTOR REFUSED TO INVENT A VERDICT** — it wrote "see the CI line below" per [E23 Ruling 3](E23-ruling.md) and left it. Resolved at the ruling: E25's own run was **CANCELLED** by E26's push 5½ minutes later (`cancel-in-progress: true`, same ref, same workflow — a setting the studio's Actions rules *require* and which is correct for sequential work), and the next run containing the commit failed **25 of 25 in `test_t34_front_door_counts.py`**, zero in E25's files — reproduced on a pristine clone. **New law: a parallel arc cannot own a CI verdict for its own commit**; gate 4 is satisfied by the first *completed* run whose tree contains the commit, the report must name what else was in it, and a red run for another arc's reason is *blocked*, not failed. **⚑ Amendment 1, same session: gate 4 is now PASS** — E26 committed its surface fix (`5049707`) and hand-dispatched CI, which went **green: 639 passed, both scanners ✓**, and `59f9409` is an ancestor of it. The law resolved a gate within the hour of being written; the blocked verdict is left standing rather than rewritten, because the sequence — cancelled run, attributed red, qualifying green — is the useful part. **F1 is the arc's best finding**: the AST-proof instrument decoded the prior commit with the platform codec and reported 10 comment-changed files *before anything was converted* — unfixed it would have **reverted every file containing a non-ASCII character**, which is 36 of 43. Reproduced here to the digit, and the mechanism **refined**: a symmetric wrong codec compares equal and hides nothing — **the asymmetry was the bug**. Caught because a number that had to be 0 came back 10. **F2**: the first conversion passed 43/43 in a form the 145 existing sites do not use, and was **reverted wholesale rather than patched** — ratified, second arc running. P3 missed low (46 predicted, **17** measured) on a **reachability unit** — third arc in a row where the population was right and the unit was wrong. Also ruled: E22's unreconcilable **16.3 GB manifest figure is withdrawn** (two independent measurements now agree at 7,312 files / 17,072,807,610 bytes); `test_t05_claims_sweep` **is owed a `fold` marker**, routed to E26 because that file gained five E26 tests mid-arc; and a push publishing another arc's commits is **correct**, needing a look-before-you-push check rather than a prohibition |

| [E26](E26-front-door-counts-kickoff.md) | The repo's stated test count has drifted on its public surfaces **four times in two days** — once inside the very commit that fixed the previous drift — and every one was caught by a person reading the page. What should watch it? | **DISPATCHED** (2026-08-09, from [E20](E20-ruling.md)'s want 2 — **and reshaped by measuring it first**). ⚑ **WANT 2 AS WRITTEN WOULD NOT HAVE CAUGHT ANY OF THEM**, and that is measured, not argued: `claims()` scans `record_markdown()` (= `CLAUDE.md` + `README.md` + `docs/**.md`), so the presentation surfaces are indeed invisible — but its seven claim families are rulings/amendments/addenda/handoffs/experiment-span and **ZERO mention tests**, and `measurements()` reads **only the index**, so **a test count is not in the index and structurally cannot be**: the index is a pure function of the corpus markdown while the count is a property of `tests/`. A widening alone would report nothing new about the thing that broke. **The shape that works is already ratified** — [E23 Ruling 9](E23-ruling.md) put a live-moving quantity under a **test**. So the arc splits by **source of truth**: **Half A** (do first) is a hermetic test pinning every stated count on every surface — README ×2, SHIP_GATE, `site-config.ts`, two handbook pages, and the seven translations (digits only) — against what `pytest --collect-only` actually collects, with a stated rule for **not** firing on a *historical* count, since `CHANGELOG`'s released entries and `SHIP_GATE`'s lineage are correct forever. **Half B** is want 2 correctly scoped: widen the **sweep's own scan set**, and ⚠ **`record_markdown()` is RULED UNCHANGED** — it feeds the index as well as the sweep, and the candidates are **17 files / 2,768 lines against 254 / 62,161, seven of them non-English translations**, so folding them in would move prose rows, fts rows and **the seeded set's rankings that leg 4 gates on**. Risking legs 1/2/4 to improve a *report* is a bad trade. Gate 2 is E24's lesson carried: **Half A's test must FAIL on a deliberately stale surface** before it passes at HEAD. May run in parallel with E25; T-numbers are a shared namespace, take **T34**. Halts at `E26-front-door-counts-report.md`. **RUN AND RULED** (2026-08-09) → [E26-front-door-counts-report.md](E26-front-door-counts-report.md), [E26-ruling.md](E26-ruling.md), 7 rulings. **THE ARC IS ACCEPTED.** T34 (34 tests, three legs) pins every front-door count against `pytest --collect-only` in a subprocess; Half B widened the sweep's own scan set 256 → 266 files with `record_markdown()` **AST-identical** and all four legs passing. ⚑ **AND IT IS THE ONLY INSTRUMENT IN THIS REPO THAT HAS CAUGHT A LIVE DRIFT END TO END** — not planted, not a fixture mutation: it fired on `main`, in CI, run [`31294891661`](https://github.com/mcp-tool-shop-org/facet/actions/runs/31294891661), 25 assertions reading *"states full 423, the collector reports 648."* **The drift was this arc's own.** The executor measured on a worktree pinned to `1b60478` while E25 committed T33 **locally** into the shared copy — and **`git fetch && git merge --ff-only origin/main`, the advisor's own coordination rule, cannot see that**: it compares against `origin`, and a sibling session's *unpushed* commit leaves `origin/main` an ancestor of `HEAD`, so the guard truthfully reports *already up to date*. New law: **a worktree answers what your change produces, not what the tree you commit to contains** — re-measure any asserted quantity against the tree you are about to commit. But the structural fix had already landed and is this arc's own deliverable: **no coordination rule needed to catch it, because T34 did** — [E23 Ruling 9](E23-ruling.md) vindicated twice. The failing run stays in the record as a failure. **Ruling 3 sizes the routed matcher gap by planting all three notations on a swept surface**: `999 tests` is caught, **`999/991` and `384 → 999` SLIP** — live, not latent. Ruled **do not widen the matcher**: the first one was proximity-shaped and its own census returned 45 hits of which **15 were not test counts** (`RTX 5090`, four ISO dates, `limit=999`) while *missing* a real French site spelled *hermétiques*. The fix is a **writing convention declared in T34's docstring**, not a regex. ⚑ **Ruling 4 is the advisor's**: the dispatch's enumeration was arithmetically right (22 sites, exactly as predicted) and **structurally wrong** — `SHIP_GATE.md:61` is *one physical line* carrying four current-state clauses and eight historical pairs, so no file- or line-scoped rule can work and the executor had to invent the span model to satisfy a gate I specified on a line I had built that way myself. And **`docs/advisor-kickoff.md` was stale by three releases** — untouched since the suite was 248 — *the very document whose step 1 reads "RE-COUNT."* **Half B's yield is near zero and that is the answer**: three of the four named files carry no family claim at all and CHANGELOG's three arrive UNPARSEABLE, so **want 2 is CLOSED rather than carried** — what it reached for was done by Half A. P8 missed 8→34 (predicted what it would *write*; the instrument counts parametrized cases — **fourth consecutive arc where the population was right and the unit was wrong**) and P9 predicted a density without checking the population existed, *"the law the dispatch quoted at me, which I applied to P1 and not to P9"* |

| [E27](E27-measurement-mcp-kickoff.md) | Spec 2's eight job-shaped tools become a running server, **in facet**. Does the measurement surface hold as one instrument — the same numbers for the same mesh, from any caller? | **DISPATCHED** (2026-08-09, at the close of the gates arc). **The spec is [docs/specs/measurement-mcp-spec.md](../specs/measurement-mcp-spec.md) and it is the contract** — the dispatch does not restate it, it adds facet's overlays and what has changed underneath. **This is the last thing standing between here and the polish arc**: [E14 Ruling 35](E14-ruling.md) is the Director's word that the exemplars are polished *after* the four MCP tools are built and verified with tests, and **one of four is built** (the record index, E18, now shipped on two registries). Its first consumer is the best one available — four accepted exemplars measured **before and after** a polish pass by the same instrument, which is where instrument identity stops being a README claim. Six things changed under the spec since it was written and are enumerated for verification: the build bar is the studio's not a prototype's · **spec open question 3 is CLOSED** (`ai-eyes-mcp` is seven image-grading tools, this server measures geometry and texels — disjoint, measured at the ruling seat) · questions 1 and 2 are **the Director's and may not be decided** · every ANDON now raises · exit codes are ruled (`4` = REFUSED) · the wheel path resolver exists and must not be reinvented. **The bar is not the pure-move bar — this is a build**: job-shaped tools, **the server WRAPS and does not re-implement** (gate 3 makes a missing behaviour a *finding*), a refusal beats a wrong number, tests ride the commit. ⚠ **The fixture question is the arc's real work** — the recorded trees are not in git; synthetic first on E18's D2 pattern, anchors in the artifacts tier, and **measure which tools can be tested hermetically and report the split** rather than padding it. The spec's instrument laws are binding: name the denominator, a hue carries its chroma, a statistic of angles is circular, report the total *and* the largest connected component — and a tool that cannot honour one on some input **refuses on that input**. Halts at `E27-measurement-mcp-report.md`. **RUN AND RULED** (2026-08-09) → [E27-measurement-mcp-report.md](E27-measurement-mcp-report.md), [E27-ruling.md](E27-ruling.md), 9 rulings. **THE ARC IS ACCEPTED.** `tools/measure_mcp.py` serves the spec's eight names as `facet-measure` 0.1.0 — **four wrap** (`mesh_stats`, `reach_ceiling`, `texel_provenance`, `measure_report`), **four refuse** with structured errors naming what is missing and whose question unblocks them. Every payload carries the identity envelope (server version + instrument sha256 + params + config hash) and `measure_report` refuses a mismatched comparison **naming the differing field**. Gate 3 re-measured at the ruling seat: `git diff --name-status` over the arc's commits is **`A tools/measure_mcp.py` and nothing else** — no instrument re-implemented *or edited*, `facet_index.py`/`record_mcp.py` byte-untouched. Suite 648 → **684**, reproduced at the ruling seat (684 passed, 405.83 s, exit 0); the **artifacts anchor proves the served `reach_ceiling` reproduces E12's pre-registered ceiling digit for digit** (3,240,510 · 1,358,656 · 1,635,304 · 50.46%) — verified against the cited source and confirmed **not a silent skip** (`pytest -rs`: 6 passed, 0 skipped), which is [E24 Ruling 3](E24-ruling.md)'s lesson held. CI citation **resolved rather than trusted** ([`31299692852`](https://github.com/mcp-tool-shop-org/facet/actions/runs/31299692852), `a30fea2`, both scanners green) per E23's fabricated-citation law. ⚑ **RULING 2 IS THE ADVISOR'S CORRECTION AND IT CHANGES THE DIRECTOR'S QUESTION**: the report said open question 2 is forced *twice*; measured, it is forced **three times** — `e12_offsurface.py` takes **9 flags with a required `--prep` and no hardcoded subject**, and its docstring's first line is *"E10 Ruling 4's question, **any subject**"*. It exists **because** `e10_offsurface.py` is hardcoded to the ship, written by an earlier seat that refused to edit a shipped instrument — so excluding `e12_*` excludes the very file written to avoid the hazard the exclusion prevents. F2's remedy ("parameterise e10 or commission fresh") is void: **the instrument is already built**; the commission narrows to the *erode/margin* half, which exists in neither offsurface tool. One `grep -c add_argument` separated the report from this ruling — *naming a resource is not enumerating it*, the advisor's own recurring shape found in a report. **F1 reproduced independently**: `e14_topology.py` throws `IndexError` on tied extents (`wide = 3 - argmin - argmax`) on the unit cube — and ruled **provably non-perturbing to repair**, since the expression is arithmetically unchanged on unequal extents, so a tie fix is byte-identical on every recorded subject. The same run corroborated P5 for free — the tool prints `SHELLS (shared-vertex, = mesh_stats)` *before* the crash. **F3 confirmed and ruled documented-not-renamed**: `e13_anchor_check.py` is the spiral-law guard; the file keeps its name (cited in closed rulings), the spec keeps `anchor_check`, and the collision rides in the refusal text pinned by a test. **P1 is the fifth consecutive unit/population miss and the first of its shape** — the population was real and every member was real, but the prediction assumed a property none was checked for (*that each has an instrument to be hermetic about*); folded to CLAUDE.md. **P2's unit ambiguity was disclosed by the executor rather than resolved in its own favour** — the operational reading governs from here, and **the score does not move** because both readings were in band before the choice, so this is resolving an ambiguity rather than retuning a condition after seeing its result. F4's largest-connected-component gap is **commissioned to the instrument, not the wrapper** (computing it in the server would be the measurement arithmetic gate 3 forbids). `measure_mcp.py` **stays out of the wheel by default, not by deferral** — instrument identity is the product, and publishing a surface before the polish arc has exercised it would freeze a comparability boundary nobody has tested. Ruling 9: the parallel-seat hole ([E26 Ruling 2](E26-ruling.md)) ran live in **both** directions and both seats held — but **both saves were observational, not mechanical** (one seat watched a directory; the other was told by the mount's staleness banner), so no new gate is commissioned on an unmeasured design. `v0.3.1` shipped from the release seat **before** this arc landed and contains none of it (`git merge-base --is-ancestor v0.3.1 HEAD` confirms). ⚑ **AND THE RULING SEAT'S README AUDIT FOUND THE NEXT DRIFT, IN A CLASS NO INSTRUMENT WATCHES**: the front door still read *"134 gates in the remaining research tools are still asserts"* — the pre-E25 state, stale by 133, on the public page, **three arcs after the fact**, alongside "twenty-three experiments" when the record holds 26. T34 pins *test* counts; a **gate** count and an **arc** count are the same shape of live-moving quantity with nothing looking at them. Corrected in place, and it is [E20](E20-ruling.md)'s want 2 arriving a third time in a new class |

| [E28](E28-instrument-census-kickoff.md) | Which of `tools/diagnostics/`'s instruments belong on the measurement surface — **measured, not curated**? And do the three tools open question 2 released wrap without editing what they wrap? | **DISPATCHED** (2026-08-09, at the Director's rulings on **both** open questions). ⚖ **OQ1 — *"Commission the census first"*: the boundary is not set by curation and not set blind.** The spec estimated its own denominator at "~80 files"; measured, `tools/diagnostics/` holds **99 `.py` and 0 of any other extension**, so the arc opens by verifying its own population rather than inheriting it. The census is a **committed, re-runnable instrument** (`tools/instrument_census.py`) with its file count under a test — not a hand-read, because a hand-read is what produced "~80" ([E23 Ruling 9](E23-ruling.md)). Six mechanical axes (invocable · subject-bound · the question · cited in the corpus · anchored by a test · import-safe in three interpreter modes) plus one **labelled proposal** for the job-shape mapping, where *"no opinion" is an acceptable value* — a table of confident `none`s reads as a measurement. ⚖ **OQ2 — the `e12_*`/`e14_*` family is IN**, releasing `mesh_topology`, `thin_extent_curve` and `offsurface_rate` at once. ⚑ **AND THE DIRECTOR'S SECOND CLAUSE VOIDED A DOCTRINE FOUR ARCS HAD INHERITED**: *"not sure why that closed ruling exists."* **It does not** — searched across all 25 ruling documents, **no ruling in this repo forbids editing an instrument**. The sentence originates in one executor's docstring explaining its own choice (`e12_offsurface.py:8`) and two documents then cited it as binding at three sites, both in the words *"the record already refuses this move."* What **is** ruled is narrower and stays: corrections to a closed ruling are additive not rewrites ([E10-off Ruling 1](E10-offsurface-ruling.md), whose object is the ruling **document**), and *an instrument does not change under the session using it* ([E12 Ruling 6d/6e](E12-ruling.md), which **scheduled** two repairs rather than forbidding them) — one textual, one temporal, neither permanent. **The hazard the folklore stood in for is real and checkable: a cited number must still reproduce from the tool at HEAD**, which is a test, not a taboo. Corrected in place in the spec, the placement memo and CLAUDE.md; the one site that handled it correctly was the served tool's own refusal text, which routed the question to a ruling instead of assuming one. Scope: the census (halts) → the three wraps, gated on that halt → F1's tie repair as a precondition, under the proof obligation [E27 Ruling 3](E27-ruling.md) states (*byte-identical index selection on every distinct-extent mesh*, discharged by exhaustive comparison plus a randomized sweep, not by reading a diff) → **T34 gains a fourth leg**, because the experiment count on the public front door was found stale **again** at this dispatch (**27 rows and the index agree; the README said twenty-six at two sites**) — the third instance in 24 hours, and E27's own correction of it undershot by one *inside the fix*. `offsurface_rate` serves the **bake half only**; the erode/margin statistic exists in neither offsurface instrument and stays uncommissioned. Halts at `E28-instrument-census-report.md`. **TASKS 0 AND 1 RUN; HALTED AT THE MID-ARC GATE AWAITING THE RULING** (2026-08-09) → [E28-predictions.md](E28-predictions.md), [E28-instrument-census-report.md](E28-instrument-census-report.md), [`docs/instrument-census.md`](../instrument-census.md). Task 0's fourth leg **fired on two real stale surfaces** — `site-config.ts` read *"Twenty"* and `docs/advisor-kickoff.md` read *27* against a status table holding 28, **neither named by the dispatch**, which measured only README.md's two already-current sites; its own notation test then caught a defect on first run (French `vingt-six` failed to parse as a pair, fell through to `six`, and returned **6** — a confidently wrong reading, so a hyphenated pair is now all-or-nothing). Gate 1 verified three ways: **99 `.py`, 0 other, 0 subdirectories**. Gate 2 held — T41's can-fail legs passed *before* the first census number was read. Gate 6 held — 7,312 files / 17,072,807,610 bytes, **0/0/0** before and after. ⚑ **THE MEASUREMENT: 93 of 99 carry a flag surface and only 6 have a `__main__` guard**, so the pre-registered three-clause `invocable` measures **5** against a predicted 60 — the **sixth consecutive unit miss**, and the component the prediction was actually reasoning about (flags) measured 93, *above* the band in the other direction. The definition was **not** retuned. The directory's house style is a straight-line module-level script, which is why axis F declines to probe 88 files rather than executing arc scripts against recorded trees; the served MCP invokes instruments as **subprocesses, never imports**, so the missing guards do not block wrapping. ⚑ **THREE FINDINGS MOVE TASK 2's SCOPE, which is what the halt was for**: (1) **half the already-served surface is outside this census's population** — `mesh_stats` and `measure_report` are implemented in `tools/verify/`, which no census has measured, so a boundary ruled against these 99 alone would exclude two of the four tools that already work; (2) **`mesh_topology` has two candidate instruments** (`e14_topology.py` *and* `e12_nonmanifold.py`, plus `e14_atlas_anatomy.py` on the hollow/double-wall half) where the planned wrap names one; (3) **`anchor_check` has no implementation** and the five nearest all sit in the `ambiguous` cell — reported as a finding, not built, per the dispatch. Axis G is a **proposal**: 21 mapped, 58 `none`, 17 `ambiguous`, 3 `no opinion`, every one of the 17 hand-read against its fuller docstring. The EXTERNAL_VERIFIER remediation ran an **independent regex method** against the AST classifier over 396 cells and found **2 disagreements, both resolved in the census's favour** (a `#` comment and a *function* docstring are not binding literals). **Three self-references were caught and handled**: axis D read 76 before the census's own output existed and **99** after — excluded by construction, because a headline that flips to 100% on a second invocation is not a number; axis E's first implementation matched only `foo.py` where the **pre-registered** definition said *"basename or dotted module name"*, under-reporting by two, corrected to **46** (44 strict), **which makes P4's miss larger**; and that correction's own can-fail leg moved the strict count 44 → **45 by existing**, now rewritten with synthetic names and pinned by a test asserting T41 anchors nothing alone. ⚑ **AND A GATE FIRED ON THE CLOSING SUITE RUN AND IS REPORTED AS FIRED**: `test_t41_axis_d_is_idempotent_across_runs` failed (**1 failed / 734 passed**, 51 rows drifted) because writing the report — which names the files the census found — made **all 99 "cited"** where the committed census had 76. **The fourth self-reference, and the largest.** Axis D now excludes the documents that describe the census; the headline is unmoved at **76** and the contaminated read rides beside it as `cited_raw` at 99. The four alternatives are named and rejected in writing (F10) and the adopted one is the rule already in the instrument before the gate fired, so it passes the retune test: **the number does not move.** Only one of the four self-references was found by looking; three were found by a check that fired. Tasks 2 and 3 untouched. ⚖ **THE CENSUS IS RULED** (2026-08-09, mid-arc) → [E28-ruling.md](E28-ruling.md), Rulings 1–9. Re-measured at the ruling seat: the census **reproduces exactly with the report in the corpus** (totals equal, 0 of 99 rows differ — F10's exclusion holding live at a second seat), the headline greps confirm to the digit (6 guards / 93 argparse / 88 module-level `parse_args`, and all five invocable files are `e10_*`), suite **736 at both seats**, CI resolved. **The boundary is ruled** (Ruling 3): *the surface is the eight questions; an instrument is ON it iff a served tool invokes it; directory membership confers nothing* — the backing map is the ruling's table, the 58+17+3 stay off as arc evidence, and a future instrument enters by ruling + wrap with the census re-run in the entering commit. **F7 resolved by the instruments' own text** (Ruling 4): `e14_topology` computes the non-manifold count itself and its docstring names the division of labour — *"FOUR QUESTIONS, none of which mesh_stats or e12_nonmanifold.py answers between them"* — so it backs the served tool alone and `e12_nonmanifold`'s render-projected picture is **named in the payload notes, not wrapped**. **F3 disposed** (Ruling 5): task 2-pre extends the census to `tools/verify/` (8 files), so the map rests on measurement over both homes. **Two report defects corrected in place at the ruling** (Ruling 6, dated): F5's star counts were interim contaminated reads (committed: `e12_offsurface` cited **14, a tie** with `e08_ceiling`; `texel_provenance` 11) and its "14 unanchored" is the **cited ≥ 3 slice of a 36-file cell**, threshold now stated. Two laws folded to CLAUDE.md: *a conjunction is governed by its rarest clause* (P1, the sixth unit miss) and *an instrument inside its own population is checked against itself on every axis, every time*. **Task 2 is GREEN-LIT** under kickoff Amendment 1 |

**E04 inputs staged** — `E:\AI\training\facet_next\galleon_clay\`, three form-first clay
variants at 1216×1024 (sha256 `59c25940075dca93`, `80263b1551d5fd7b`, `bb8da35402cf2a10`).
Known stressors, recorded before the spec is written: the pipeline's framing is
portrait-shaped throughout (`turn_render` 752×1024, `project_twins` deriving `h_ext` from
`v_ext × 752/1024`, `ortho_scale` on the larger dimension) and a ship is wider than tall —
the same class of break the bust mesh caused; `smart_decimate` allocates density by *face*
rect and a galleon has none; the thin-extent probe will fire on the entire rig, which may
be correct or may withhold most of the subject; and `gate_mesh.py` is character-only and
must not be run on it.

*⚠ Kept as written because every one of those pre-registered stressors landed (annotated
2026-08-05): the framing class produced the fit-axis/margin/aspect three-consumer family
and a generator-legality re-rule (E04 Rulings 6/11/15/25); `thin_extent` was measured
per-mesh at 0.01 on the rigging and its beast successor produced the
no-global-value-separates-membranes constraint (E12 Ruling 7c); `gate_mesh` non-execution
became the `mesh_gate: none` decision form both non-character profiles carry.*

### What E01 established

- **Reconstruction is not the facial ceiling.** The generator's **1024 px input cap** is:
  worked through the preprocessing path, a full-figure clay puts **~138 px** on the head
  and a bust crop of the same clay puts **~439 px** — about **3.2×**.
- **The styled twins are bound to the mesh they were rendered from.** They are a
  derivative of one specific silhouette, not a reusable asset, so twin generation is a
  pipeline stage rather than an input. Any new reconstruction — or head graft — needs
  its own twins.
- **Canny cannot find a silhouette that isn't there.** A Workbench clay render is flat
  grey on flat grey by design, so Canny returns 0.84% edge pixels and almost no outer
  contour — the ControlNet then constrains nothing and the model regenerates the
  character freely. Composite onto a contrasting background *and* union the figure mask's
  morphological gradient into the edge map: silhouette IoU went 0.290 → **0.777**.
  The recipe had been developed against lit concept art and carried to flat renders.
- **Polygons and texels are separate budgets.** `smart_decimate` allocates polygons;
  `bake_hero_prep`'s island scaling allocates texels. A head can hold 84% of the faces and
  45% of the texel area at the same time. A gate comparing UV area to *face count* is
  meaningless on a deliberately non-uniform mesh — compare UV area to **3D surface area**.
- **The gutter, not the island count, ate the atlas.** Two meshes at an identical 287k
  faces: A0 packs 8,486 islands (34 faces each) into 20.34% of a 4096 atlas; a decimated
  mesh packs 35,070 islands (8 faces each) into 4.01%. Dropping `island_margin`
  0.004 → 0.001 took it **4.01% → 18.76%**. Raising `angle_limit` 1.15 → 1.50 moved island
  count by **0.8%** and bought nothing — `smart_project` splits on UV distortion, not angle
  alone, and decimation's long thin triangles distort regardless of the threshold. The
  advisor predicted the opposite; the margin was the entire gain.
- **Atlas packing and styled coverage are independent.** Valid texels rose 4.7× and styled
  texels rose 5.4× — the ratio barely moved, falsifying the "most valid texels are
  bake-margin halo" hypothesis.
- **⚠ The coverage baseline was contaminated, and the floor built on it was void.**
  `figure_mask` keys A0's twin over its own background: **30.2% and 32.3% of the bottom
  corners** register as figure, where a centred standing figure cannot reach. A0's twin is
  painted concept art with a background gradient and a cast shadow, and a flat top-corner
  median at tol 0.06 keys a third of the lower background as paintable surface. The
  clinching arithmetic: A0 styles **81–100% of its geometrically orientable texels** against
  a two-view ceiling of 61.5% — a number brushing an unreachable ceiling is a broken
  measurement, not a good result. Measure **`styled / geometrically-reachable`**, which has
  a true ceiling of 1.0 and is comparable across silhouettes; and take the mask from the
  render the twin was restylized from, where it is exact and registered by construction,
  rather than re-keying the twin heuristically.
- **Consequence for shipped work:** the warrior the Director rejected has background grey
  projected into its atlas, and a contaminated mask also disables the edge guard meant to
  keep silhouette pixels out. Three independent faults in that asset — poor geometry, twins
  never registered to it, background baked into the texture. None of them the texture
  architecture.
- **Prompt per view.** A shared prompt asking for "a long red beard, gold necklace" on every
  view overrode a correct ControlNet contour and put a face on the back of the head at both
  0.92 and 0.75 denoise. Per-view prompting fixed it outright — face detections 1 → 0
  against a source-back control of 0, with silhouette held at IoU 0.784.
- **⚠ The exported mesh is not a solid — no volumetric predicate works on it.**
  `prep_uv.glb` carries **293,099 verts for 287,170 faces**: the glTF export splits a vertex
  at every UV seam, so every island boundary is a topological crack. `compute_signed_distance`
  at the figure's own bbox centre — the middle of a standing warrior's chest — returns
  **+0.0019**, i.e. *outside*. Ray parity leaks, containment is meaningless, and only **37%**
  of outward rays escape. Any query needing thickness, containment or inside/outside must run
  against the **welded mesh before export**. Related trap: a ray cast along the surface normal
  measures the *tessellation*, not the geometry — median reported thickness 0.00204 against a
  median edge length of 0.00290 is a sub-triangle hit on a neighbouring face.
- **Thinness is a property of the surface, not of a camera.** A two-sided camera-ray extent
  probe (`extent = 2D − t_front − t_back`) needs neither normal nor interior, so it survives
  the above — but it reads a blade as *thick* when the blade is edge-on to that camera, and
  withheld only 49.9% of the sword column at yaw 90. The general form is to compute the thin
  mask per view, back-project to texels, and **union across views**.
- **A masked inpainting brush invents when it is starved of context.** At denoise 1.0 hole
  texels have nothing to preserve, so the model anchors only on prompt and contour. At yaw 90,
  95% of the figure is hole — the worst-anchored camera in the set, furthest from both styled
  poles — and the brush returned a plaited rope belt where the twins have a flat leather band,
  a shoulder strap over bare flesh, and a tunic extended from waist to mid-thigh. **Order the
  strokes to spiral outward from the styled poles** (45 → 315 → 135 → 225 → 90 → 270), so each
  stroke extends an existing character rather than composing a new one, and each commit
  anchors the next.
- **One mask cannot answer two questions.** *Is there real surface here* is answered by the
  mesh silhouette, un-eroded — a visible texel always projects inside it, by definition.
  *Is the paint here trustworthy* is answered by the twin's own painted figure, eroded.
  Conflating them and eroding the mesh mask cost **480k texels**: near the silhouette the
  surface turns edge-on and enormous numbers of texels foreshorten into a thin band, so
  peeling a few pixels off it removes far more than the same peel anywhere else. The bug was
  invisible under the old heuristic mask, which keyed *wider* than the mesh so erosion ate
  background instead of surface. `mesh_mask ∧ erode(twin_mask)` took styled coverage
  **23.8% → 53.7%** of reachable with the denominator unchanged.
- **Framing is a route stage, not a tweak** — 3.1–4.5× head polygons, and the gain is
  separated eyelids, a brow furrow and modelled nostril cavities rather than sharper blur.
- **Shell soup was ours.** Reconstruction returns 1 connected component; our UV unwrap and
  glTF export split it into 285,654. Welding before decimating restores it, verified
  against a control that reproduces the old broken output byte-for-byte.
- **Four inherited claims failed** — the clay provenance, the shell count, the facial
  ceiling, and the strength of an archived resolution observation. An inherited claim is a
  hypothesis wearing a fact's clothes: checking one costs minutes, building on one costs a
  session.

## Why it works this way

An earlier arc of this project ran ten sessions in which each session judged its own
output, wrote its conclusions to a shared memory store, and the next session read those
conclusions as established fact. Errors compounded silently because nothing in the loop
was checkable and nothing was gated on the Director's eye.

The repo is the fix. A claim sitting next to runnable code can be tested in minutes.
A tool marked *superseded* with its failure documented cannot quietly become doctrine,
because anyone can run it and watch it fail the same way.
