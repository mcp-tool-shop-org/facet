# E17 — advisor ruling on the test harness (2026-08-08)

Evidence — what this seat OPENED: the report in full
([E17-harness-report.md](E17-harness-report.md), halted at `3bde219`), the blind
predictions (`eca32b0`, committed before any tool was read), the eight commits'
stats and messages, the errand session's two measured T12/T13 flags (relayed by the
Director), and **the full suite run at this seat's own hands, both tiers, under the
repo's own interpreter: 27 passed in 97.90 s** — with this seat's own first-run
failure disclosed at Ruling 2, because a ruling that hides its own first run is the
thing this repo exists to prevent.

## Ruling 1 — the harness is ACCEPTED: 27/27 at two seats' hands, every port anchored to the record (2026-08-08)

**1a — the suite is RATIFIED as delivered.** Nineteen hermetic and eight artifacts
tests, every one mapping to a named source anchor; the two inverse-guard tests
prove their guards CAN fail (truncated-list injections, E16-9's own method — the
kickoff's calibration demand met, not asserted); T3's pre-load proof is the right
construction (refusal on nonexistent inputs discriminates the guard from a
FileNotFoundError); T5 exercises the claims sweep's load-bearing semantics on
synthetic fixtures including the STALE routing; every skip prints its reason,
demonstrated against a nonexistent assets root.

**1b — the amendment executed exactly as ruled**: T12's anchor held on the
pre-stated table to the digit (the sword alone warns at 1.902512; the proxy's
galleon/beast warnings gone, which Ruling 2 named correct); T13's per-process temp
names land with the ruled collision case RUNNABLE — two simultaneous verifies on
one DB, byte-identity both, zero temps stranded — the errand session's sham-test
hazard (pre-created files pass without exercising anything) dodged by
construction; T14's classification moved the four sweeps' decided counts exactly
as Ruling 4a predicted (85 → 84, value 66 → 65, `_per_invocation` untouched) with
the T16 pin moving IN THE SAME COMMIT — the tests-ride-the-commit law observed
doing its job. T15's half-scope is RATIFIED as the honest half: an E17 leg before
this file existed would grep a file that is not there — the check-that-cannot-fail
class, correctly refused.

**1c — the two first-run failures are RATIFIED as reported**: both were the
session's own operands against a record that was right (T11's abandoned stroke-1-A
job; T8's invocation inferred from a pre-repair caption — the exact lie E16-6
repaired, preserved in a recorded artifact), both diagnosed FROM the record, no
anchor value moved at any point. The T11 diagnosis banked a sharpening: E16-10's
byte-identity was replay-vs-replay; **this suite anchors replay-vs-record, and
that is the standing stronger form** (flag 6 disposed: recorded, no action).

**1d — predictions 11 held / 2 falsified**, both misses informative: pytest was
already present (zero environment changes), and T2 is NOT hermetic — the law
commit's "re-runnable from a fresh clone" was half right, corrected at flag 4
below. The forced encoding matrix (T1b) is ratified as load-bearing: the rig's
ambient `PYTHONIOENCODING` would have made an inherit-the-default leg test utf-8
twice; the harness's own decode defect was found and fixed before any port was
judged, disclosed in full.

## Ruling 2 — this seat's own verification miss, owned in place (2026-08-08)

The ruling's first suite run returned **7 failed / 20 passed** — and the failures
were this seat's, not the suite's. The seven were exactly the tests whose tools
import `open3d` at module level; this seat's shell resolved bare `python` to an
interpreter without it, against the repo's own environment law (the trellis2-env
python, named in CLAUDE.md). The first hypothesis (the ambient-encoding delta;
this seat had removed `PYTHONIOENCODING` for E16's default-console verify) was
stated, tested, and **FALSIFIED** — same seven under forced utf-8; the
ModuleNotFoundError in T3's own failure text named the true mechanism. Under
`E:\AI-Models\trellis2-env\Scripts\python.exe`: **27/27, reproducing the
session's result exactly.** Check what the operands are — the law's third firing
today, this time on the ruling seat.

**The finding it surfaces is real and ruled in as T18**: a wrong-interpreter run
reads as seven alarming FAILs with misleading tracebacks instead of one clear
refusal. The harness gains an interpreter pre-check — `conftest` asserts the
child interpreter's capability once, loudly, naming the environment law and the
missing module — so the next wrong-env run fails in one line that says what to
fix. Assigned to the E17 session's final leg with T15b.

## Ruling 3 — the seven flags disposed (2026-08-08)

**3a — flag 1: the CI paths gate is WIDENED to include `pytest.ini`**, this
fold's own edit: the file configures the suite (strict markers ride in it), so a
pytest.ini-only change must trigger CI.

**3b — flag 2: no `pull_request` trigger, with the reason recorded**: both seats
commit direct to main by this repo's shared-copy design; a PR trigger would never
fire here. If the repo ever adopts PR flow, the trigger rides that change.

**3c — flag 3: the 3.12/3.13 divergence is ACCEPTED as named** (open3d wheel
availability at the pin, commented in the workflow). CI-green verification
happens at this ruling's push; the result is recorded in the session reply and
stands as this flag's closure.

**3d — flag 4: the hermetic-selftest fixture is COMMISSIONED**: a synthetic
minimal in-repo state (tiny mesh, tiny atlas, tiny prep) so the write-head's
losslessness runs hermetic in CI; assigned to the index-MCP build session's
kickoff, not improvised here. The artifacts-tier T2 keeps anchoring the REAL
recorded state — the fixture adds a hermetic twin, it replaces nothing. The
`eccc827` commit message's "re-runnable from a fresh clone" overclaim is
corrected by this ruling's record (CLAUDE.md's own law text — "persist" — stands
as written).

**3e — flag 5: T15's E17 half is UNBLOCKED by this file's existence** and
assigned to the E17 session as its final leg (T15b): E17's ruling greps enter
verify's count legs with the sequence bound, T1's assertions extend, anchor =
verify PASSED with the new legs green.

**3f — flag 7: the doubled apostrophes are ACCEPTED as-is**: pushed history is
not rewritten for cosmetics; the report records the cause.

## Ruling 4 — the roadmap advances: Ruling 35's step 2 OPENS (2026-08-08)

The repo now has what the Director asked for on the day he asked: a persistent
suite, green at two seats' hands, wired to the repo's first CI, with the
tests-ride-the-commit law enforced by a harness instead of a promise — and the
suite's tests are the record's own anchors, so what it protects is what the
Director already accepted. **The index-MCP build kickoff is the advisor's next
deliverable** (in facet, per his ruled placement; spec 1 as landed; the polish
arc as first consumer; the T2 fixture commission riding it). The E17 session
holds for T15b + T18 on the Director's relay, then its halt is final. Zero
credits, this arc as every arc today.

## Standards compliance (this ruling)

| standard | score | evidence |
|---|---|---|
| PIN_PER_STEP | 3 | the acceptance evidence is this seat's own 27/27 under the named interpreter, beside the session's; every disposition cites its flag, commit, or test node |
| ANDON_AUTHORITY | 3 | the seat's own 7-fail first run halted the ruling until diagnosed; the falsified hypothesis is recorded beside the true mechanism; T18 converts the trap into a one-line refusal |
| NAMED_COMPENSATORS | 2 | every fold edit is per-commit revertible; the CI widening is one path line; no publish, no spend. Scored 2: stated, none exercised |
| DECOMPOSE_BY_SECRETS | 3 | dispositions routed by owner — workflow config to this fold, tool legs to the session that owns the harness, the fixture commission to the build session, cosmetics to nobody |
| UNCERTAINTY_GATED_HUMANS | 2 | the fixture and PR-trigger questions carry their reasons and stay one-sentence overrulable; nothing was decided that the Director's word had reserved |
| EXTERNAL_VERIFIER | 3 | the suite was re-run at a different seat under the pinned interpreter and reproduced exactly; CI adds a third, clean-room runner at this push; the misses on BOTH seats today were caught by instruments, not assertions |
