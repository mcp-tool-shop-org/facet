# E63 report — the head probe, on the views that actually turn

Charter: [E63-head-on-the-defect-kickoff.md](E63-head-on-the-defect-kickoff.md)
Tree: `E:\AI\training\facet_E63\`
Spend: 4/4. Rank nothing.

## What was submitted

E58 `graph_3.json` / `graph_5.json` loaded verbatim. Named deltas only.

Arm P positive text is today's `canon_compose` flat form (gate-legal, required=20).
That is **not** an E58 splice: the E58 prompt is now refused (N1 sleeveless +
required staging). Disclosed in the charter before submit. Negative text gained
the CJK-comma complement. Denoise stayed 0.92.

Arm C: Arm P's text, denoise 0.92 → 0.80.

dry_run PASS (same LoRA catalog advisory as E58/E59). estimate_credits: 0
metered API nodes (GPU-seconds). Batch 4/4 ready, 0 failed. Gate E: all eight
panels 576×1024.

## What is visible (not a ruling)

Opened at full size: the four gens, the two E58 defect frames, the two controls,
and `stage3/E63_director_sheet.png`.

**View 3 (yaw 135).** Control is a rear-quarter silhouette, head with the body.
E58 defect: face toward camera over the left shoulder, smile visible. Arm P:
same class — face still toward camera over the shoulder. Arm C: face still
toward camera; garment gone to white/sage wash (same adverse class as E59
Arm C on the wrong view).

**View 5 (yaw 225).** Control is rear-facing, head with the body. E58 defect:
face over the right shoulder. Arm P: face still over the shoulder. Arm C:
head is more rear-aligned with the control; the whole figure is a near-white
clay with almost no material.

## Predictions vs what the pixels show

Advisor: *Arm P will not clear the crank.* Both P frames still show the over-
the-shoulder face. *Arm C is the arm that can win, if either does.* C on v5
is the only panel whose head sits nearer the control's rear pose, and it does
so by washing the image. C on v3 still cranks and washes.

No numeric instrument. E59 Gate 1 stays unused.

## Paths

- Sheet: `E:\AI\training\facet_E63\stage3\E63_director_sheet.png`
- Gens: `E:\AI\training\facet_E63\gen\a1_arm{P,C}_v{3,5}.png`
- Graphs: `E:\AI\training\facet_E63\stage0\graph_{P,C}_v{3,5}.json`
- Spend: `E:\AI\training\facet_E63\stage0\spend_record.md`

Charter next lever if the head is still cranked under Arm C: raise `cn`.
Do not raise it in this report.
