# E15 — advisor ruling on P1 (2026-08-07)

Evidence: [E15-report.md](E15-report.md) (`e8d88a7`), the blind predictions
(`763de62`, registered before the builder existed), and **this seat's own hands on
the deliverable**: `build` + `verify` re-run on the record as it stands tonight —
two rulings and three folds larger than the corpus the executor built against —
returning byte-identity, zero dangling pointers over 1,079 rows, counts matching
the verifier's independent greps, and **14/14 on the seeded set, exit 0**; plus
two live `q` calls at this seat (Ruling 7 below records what they showed).

## Ruling 1 — P1 is ACCEPTED, and the database is DEVELOPED

All four verify legs pass at two seats' hands. The re-run at this seat doubled as
the live growth-sensitivity test: the corpus grew by E12 Rulings 29–30 and three
advisor folds between the executor's build and this one, and the gate held —
§7d's requirement demonstrated rather than asserted. Determinism came back
stronger than specified (byte-identity across two interpreters; the
pre-registered `.dump` fallback never needed). **The Director's sequencing
condition — "the database is developed" — is met. E14 unlocks.**

## Ruling 2 — the defect class is NAMED AS A LAW

**A status is read from a convention's position, never matched in narrative
prose.** Both §7 defects were one class: case-insensitive verdict matching marked
three rulings ACCEPTED that accept nothing (the record SHOUTS its verdicts;
lower-case "accepted" is an adjective), and whole-`why` scanning read three
DECIDED profile values as UNDECIDED — including thin-extent, whose `why` narrates
*"the arc's one deliberately undecided value"* about a value Ruling 25c ruled at
0.005. **An index reporting a decided value as undecided is a second authority
contradicting the record — the one thing the design exists to prevent** — and
both fixes conform the parser to the record's own conventions (capitals carry
verdicts; the head of `why` carries the class label) rather than special-casing
documents. This is the index's own instance of *prose is not a registry*, and it
is recorded because the next parser of this corpus will meet the same trap.

## Ruling 3 — the gate changes are RATIFIED as strengthening, not retuning

Three changes to the seeded gate happened mid-flight and every one made the gate
stricter: **(a)** the 12→14 split — two dual-target questions split into their
component holdings so one rank cannot hide which of two answers was found;
**(b)** the loose file-level targets pinned to the rows that carry their values,
after the gate was caught passing on E04 Ruling 24 — a coincidental word-match
carrying none of the digits (*a gate that cannot tell the right row from a
coincidence is not testing anything*) — with the completion **enumerated from the
record by grepping for the digits, never from what ranked**; **(c)** the
dispatched sequence bounds left exactly where written, with rulings above the
bound surfaced on a separate completeness line instead of a widened condition.
No question was deleted or re-phrased to pass. The mechanism selection (§6) is
ratified on the same ground: every variant scored on the fixed key, two
falsified with the reason, the winner adopted at 14/14 — and the Ruling-14 case
study (bm25 cannot separate an acceptance from three rulings that mention an
accepted artifact; the exact phrase can) joins the record as the reason the
phrase stage exists.

## Ruling 4 — P3, the standing ritual, DECIDED

1. **Every advisor fold ends `build` + `verify`.** The seeded gate re-runs every
   time — it is measured growth-sensitive, and a gate trusted from the fold
   before is not a gate.
2. **The DB file commits at SESSION boundaries** — the close or relief fold —
   not at every fold. The executor's measured cost decides this: ~5.9 MB of
   binary, byte-different per build, deltas poorly; per-fold committing at
   tonight's cadence would write tens of megabytes of history per day for a
   file any session can regenerate in seconds. Staleness is bounded at one
   session; freshness is one deterministic, verify-gated `build` away. The
   spec's in-repo ruling STANDS — this is its operating cadence, and the
   Director's window is open on the trade.
3. **Kickoffs gain one line**: `python tools/facet_index.py build` after
   `git pull`, so a fresh session queries the tip, not the last commit.

Transcribed into [context-architecture.md](../context-architecture.md) this fold.

## Ruling 5 — the record findings DISPOSED

**E04's ruling count is corrected at every site** — three, one more than the
report named, found by consumer-grep: the main README, the handbook, and the
experiments table now read 29, each carrying the correction's provenance in
place. **The experiments status table is brought current** (E12's row stood at
"Rulings 1–8"; E13/E14/E15 had no rows) — the standing status-table instruction
owed this regardless of E15. The rest of §4 stands as recorded properties of the
record, deliberately unedited: the one-off `18g-CLOSED` closure convention (now
its own row kind), the deep granularity asymmetry across arcs, CLAUDE.md's
blank-line-free lists, the backslash paths. The parser reads the record as it
is; the record is not rewritten to suit the parser.

## Ruling 6 — §5 is P2's worklist, and P2 stays QUEUED

The honest boundary is the next pass's job list: `supersedes` links (2 + 1 —
the largest schema-vs-delivery gap; the record corrects by narrative),
artifact statuses (427 of 451 absent), phenomena minted through six different
verbs, and the self-reference (the E15 kickoff outranking answers for its own
question labels — P2's slimming reduces it). P2 remains a separate advisor-led
pass at the Director's timing, measured in lines shed, exactly as dispatched.

## Ruling 7 — one observed property, recorded without a gate

At this seat's own `q` calls: content phrasings return their targets at rank 1
(`dark desaturated crevice fill` → Ruling 27d, first slot); conversational
phrasings (`what did we rule about canny`) surface the evidence documents —
the canny report and its predictions — above the ruling. That is the FTS
boundary §5 predicts, recorded here so a loose phrasing's result is never read
as the index failing. The index serves the record's own vocabulary best,
which is the correct bias for a legal record.

## Ruling 8 — handoff 2 ruled: 0 STALE banked, the discovery defect is the finding, its fix is handoff 3 (2026-08-07)

Evidence: the handoff-2 report (`6a46621`), its blind predictions (`3730add`),
and the verb run at this seat — the same 18 rows, 0 STALE, exit 0.

**8a — the halt is ACCEPTED; both healthy-zero predictions confirmed.** The
range-vs-cardinal distinction is ratified as the sweep's load-bearing semantics
(a range asserts a MAX, a cardinal a COUNT — E12 numbers its handoffs to 16 and
carries 15; conflating the two manufactures a stale row out of a correct
document). The steward's self-caught detector defect — *a range is a count-claim
only if it starts at 1* — took 37 unparseable rows to 4 with every survivor
real, and the AMBIGUOUS handling of `+ the close` is right: picking a reading
would invent a claim to check.

**8b — THE DISCOVERY DEFECT IS THE REAL FINDING, and its fix is RULED.**
[E15-ruling.md](E15-ruling.md) carries this arc's rulings and not one is in the
index: `NUMBERED_RULING_FILES` is hardcoded, written before this document
existed, and the verify gate greps the same list — so it tests that no listed
file loses a ruling and **cannot test that a file is missing from the list**.
The failure-mode law, landing on the instrument built to enforce it. The fix,
for handoff 3 and nothing else: **a sorted glob over the ruling-document naming
convention** (equally deterministic, cannot silently omit), the **discovered
file list printed in verify's own output** (the glob's misses become visible
rather than assumed — the same hazard one level up, closed by printing), and
**E15 added to the count leg** with its own grep, sequence check and
completeness line.

**8c — the `285 rulings` hazard is CLOSED PROSE-SIDE, this fold.** The
experiments-table row quoted the index's own total inside E15's row in the
`N rulings` phrasing family — a false STALE waiting for 8b to land, flagged by
the steward before it could fire. Reworded out of the family; the sweep's
inability to distinguish attribution is a recorded boundary, not something to
engineer around.

**8d — the "Amendment" overload is RECORDED as a record property.** E08's
numbered series and per-spec appended amendments share one noun; no family
parses the latter, correctly — a family would silently mis-attribute spec
amendments to E08's series. Unparseable-with-reason is the honest state.

**8e — banked**: two defined families with no live site stay defined and
printed · flag counts stay refused in code as another instrument's numbers ·
`docs/profiles-design.md` is the one unclassified file, printed as such · the
E14 lane's untracked diagnostic noted by the steward is that lane's business,
untouched.

## Standards compliance (this ruling)

| standard | score | evidence |
|---|---|---|
| PIN_PER_STEP | 3 | every ruling cites the report section, commit, or this seat's own run; the re-run's corpus delta is named (two rulings, three folds) |
| ANDON_AUTHORITY | 3 | the acceptance rests on the verifier's own exit code at a second seat; the growth-sensitivity requirement is ruled into the standing ritual as a per-fold gate |
| NAMED_COMPENSATORS | 2 | the prose corrections are annotated in place with provenance, never silent; the DB cadence decision names its trade and the Director's window; undo is reverting one fold |
| DECOMPOSE_BY_SECRETS | 3 | the parser/schema/verifier separation is preserved by Ruling 5's refusal to rewrite the record for the parser; P2's worklist is routed to its own pass |
| UNCERTAINTY_GATED_HUMANS | 2 | the DB-cadence trade and P2's timing carry the Director's named window; the self-reference property is surfaced, not engineered around |
| EXTERNAL_VERIFIER | 3 | the acceptance evidence is the verifier run at a DIFFERENT seat on a GROWN corpus — the strongest available form; the seeded key remains authored independently of the builder |
