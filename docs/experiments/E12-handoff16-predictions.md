# E12 handoff 16 — blind predictions, registered before the export runs

**Executor session, 2026-08-07 ~01:50.** Registered before the exporter, the manifest
tool or the lane validator is invoked on anything. The dispatch's four named items are
P1–P4; P5–P10 are mine, registered because a number nobody predicted teaches nothing.

## Blind status — exactly what this seat has already seen

Disclosed per the handoff-5/6 form, because "blind" is a claim and a claim needs its
scope.

**Seen before writing this file** (all of it while surveying *inputs*, none of it a
measured outcome of this dispatch):

- `tools/e11_manifest.py` and `tools/e11_export_turnaround.py` in full (Task 0 requires
  reading both before invoking either).
- `git log` on `e11_manifest.py`: **`renders_are` was added in `18bdcdf`, AFTER the
  galleon manifest was emitted in `20f2a0a`** — the E11 Ruling 3 errand, deliberately
  parked for "the next manifest emission." This bears directly on P2 and is the reason
  P2 is registered the way it is.
- The committed galleon manifest's top-level keys and sha256
  (`396637d2030e7f01…`, 21,208 bytes, 28 renders, `renders_are` **absent**).
- `profiles/beast.json`'s `tools["cull_unseen.py"]["production"]["value"]` — **so P1 is
  NOT BLIND** and is registered as an arithmetic expectation, not a guess.
- The dragon provenance atlas's four unique colours and their texel counts.
- The `E13_stage1/` and `E13_stroke/run/` directory listings — **so P4 is PARTLY not
  blind**: I have seen that `A0_stage1_owner.npy` exists and that no owner *display
  atlas* (the galleon's `out/owner_atlas.png` analogue) appears anywhere in the dragon's
  trees. What I have not done is confirm what the exporter can therefore carry.
- `run_log.jsonl`, `finalize.json`, the E11 report + ruling, E12 Rulings 27–28.

**Not seen:** the lane's 1.3.0 schema code, its validator, THE DRAGON SHAPE test, the
harmonization `operands.json` contents, `E12_gate0/frame_00003.json`'s yaw table, any
emit output for the dragon, and any manifest regenerated by any version of the tool.

---

## P1 — the camera count the exporter derives · **NOT BLIND**

**26.** Read from the profile, not guessed: `cull_unseen.py.production` lists 24 yaws at
15 deg plus `0,55` and `180,55`. The galleon's 28 came from a ship-specific superset
with four elevated; the beast's elevated ruling was NONE (Ruling 7), so the list is the
code default. The X1 mechanism (read the profile at runtime, transcribe nothing) is what
I intend to use, so the tool's answer should be exactly the profile's list length.

*What would falsify it:* the tool deriving anything other than 26 — which would mean the
profile string parses to a different set than it reads as, and that is a halt.

## P2 — the capability anchor · **the dispatch's stated baseline is already wrong, and I predict it fails as written**

The dispatch says the galleon manifest regenerated "with the new code and no new
declarations … must come back byte-identical to the committed one." **I predict this is
false before I write a line of code**, because `renders_are` entered the tool in
`18bdcdf` after the galleon tree was emitted, on Ruling 3's explicit instruction not to
churn the validated trees for a wording field.

So I register two anchors and predict both:

- **P2a — HEAD tool vs the committed galleon manifest: DIFFERS, by exactly one added
  top-level key (`renders_are`) and nothing else.** Predicted diff: one key inserted
  between `acceptance` and `palette`; all 28 render entries, all channel declarations,
  every other value identical.
- **P2b — my modified tool vs the HEAD tool, both with no new declarations:
  BYTE-IDENTICAL, 0 bytes differing.** This is the anchor whose purpose the dispatch
  states ("the capability that changes nothing when unused"), stated against the only
  baseline that can carry it. If P2b fails, that is the halt the dispatch means.

*What would falsify P2a:* any second difference. A second difference is a halt — it
would mean the tool drifted somewhere nobody recorded.

## P3 — the lane's validator on the dragon manifest · **blind**

**Zero gap notices, first run, and schema-valid at 1.3.0.** Grounds: addenda 4 says the
lane built THE DRAGON SHAPE acceptance test from these exact requirements, so a clean
pass is the expected outcome and not a suspicious one (the dispatch's calibration says
so too). Confidence is high on *shape* and lower on *plumbing*: my named risk is not the
contract but my own field spelling — a key named `subject_name` where the lane wants
`subject`, or a `record` string where it wants an object.

*Registered sub-prediction:* if anything fires, I predict it fires on
`asset.tone_transform.reference` (the only field whose value must resolve against
another part of my own manifest) rather than on the three flat declaration blocks.

## P4 — the owner channel · **partly not blind**

**Split, and the split is the finding.** I predict the dragon carries the *numeric*
owner channel exactly as the galleon does — `view_owner.npy` in texture space, per-view
`owner_id_*.npy` slices, and `loss_mask_*.png` — because all three derive from the
stage-1 owner sidecar, which exists (`A0_stage1_owner.npy`).

I predict the *display* channel is **absent**: the galleon's `owner_view` render is emit
run over `out/owner_atlas.png`, and no such display atlas exists for the dragon. I
predict I will carry the numeric channel and honestly omit `owner_view` rather than
synthesize an atlas — a third configuration (galleon: both; W3: neither; dragon:
numeric only), and one I predict the manifest can state without a schema problem.

## P5 — the export's purity spot check · **blind**

**Byte-identical.** One camera re-emitted from a fresh state should reproduce its first
emit exactly (X-H1 was confirmed on two subjects; the dragon runs the same emit path).
*Works-perfectly test:* this check returns "identical" when the export is pure AND when
I accidentally compare a file to itself — so the comparison must be between two
independently produced files in different directories, and I will say which.

## P6 — reference share across the 26 views · **blind**

**Eye-level yaws 60–85% reference; the two elevated (0,55 / 180,55) markedly lower,
25–55%.** Reasoning: the mix is 87.49% reference *of the reachable set*, but reachable
is 50.46% of valid, and the elevated pair looks down on the wing tops and back — the
surface Ruling 27e says the animal hides from every eye-level camera, hence dilation
territory. The galleon's analogue fell 82–92% → 61–68% on its elevated four; I predict
the dragon's drop is **larger** than the ship's, because a ship's deck is partly visible
from eye level and a dragon's wing tops are not.

## P7 — view 1 = yaw 45 · **blind**

**Confirmed.** Addenda 4 asserts it; the dispatch orders re-verification against
`frame_00003.json` rather than memory. I predict the frame JSON agrees. *If it does not,
I halt* — a tone-transform reference pointing at the wrong camera is a false provenance
claim, and inventing a fix while looking at the mismatch is the move that is always
wrong.

## P8 — the harmonization operands · **blind**

**`harmonize/operands.json` is the file addenda 4 means, and it covers 8 views.** Named
risk registered in advance: a second file `operands_v3r.json` sits beside it (view 3's
770701 cure, per Ruling 23f's "v9 x6 + view 3's cure + view 4-A"). I predict the
declaration needs **both** files, or an explicit statement of which one the ruled input
set's transform used — and that this is the dispatch's one genuinely under-specified
point.

## P9 — the tree's size and the durability line · **blind**

26 views x (asset + prov renders + silhouette + prov_class + owner_id npy + loss_mask +
admission json) plus the copied mesh/atlas/channels. At a 1792x1024 frame an int8
`owner_id` slice is ~1.8 MB, so the render-space npy total is ~48 MB, not the multi-GB I
first reached for. **Predicted total 1.0–2.5 GB**, dominated by the copied
`dragon_final.png` (10.9 MB), `prep_uv.glb` (35 MB), the two 16.8 MB texture-space npy
channels, and 26 x ~2 MB of PNG renders. Registered because a tree the lane will hold
sha-pointers into is a durability commitment, and its size is part of what that
commitment costs.

## P10 — what I expect to spend · **blind**

**Zero credits** (nothing generates), **no GPU** (emit and every raycast are open3d
CPU — E11's report established this), and the watchdog therefore not exercised. Wall
clock: the galleon ran 28 cameras x 3 channels; the dragon is 26 x 2 channels on a
1792x1024 frame, so I predict **15–45 minutes** for the export leg.

---

## Registered halts (so no outcome needs improvisation)

1. **P2a shows a second difference** -> halt with the diff.
2. **P2b is not byte-identical** -> halt; the capability is not behaviour-preserving.
3. **The frame JSON disagrees with view 1 = yaw 45** -> halt; do not re-derive.
4. **Any lane gap notice** -> halt with the notice list; do not adapt the manifest to
   quiet it (the dispatch's explicit instruction).
5. **Any write attempted against `E13_stroke/run/` or the accepted asset** -> halt; the
   inputs are read-only by Ruling 28.
