# E65 — the scoped ring: eight views with everything the record has paid for

**Advisor spec, 2026-08-18. One executor seat (Sonnet), background. Tree
`E:\AI\training\facet_E65\`. Spend: 8 generations + at most 2 spec-violation re-rolls
(the one-re-roll law, new seed, rejected artifact stays in the record).**

## What this is

The first complete A1 twin ring generated with every measured fix in place at once:
**per-view scoped prompts** from the eight RATIFIED lists (E64 — the Director ruled the
probe clean: crank gone on both rear views, garment intact), **the sleeveless N1 phrase**
(E60/E61), **flat form, never coordinating the depends_on pair with "and"** (E61), and
**head-forward + pose staging required everywhere** (E59/E63). E58's ring is the baseline
it is measured against — the ring that carried both named defects.

## Pins

Seed **770700** every view (comparability with E58/E63/E64 cells). Denoise 0.92, the E58
controls byte-identical, 576×1024, Gate E per view (delivered == requested). Prompts
composed per view from the ratified scopes — v3/v5's are byte-identical to E64's probed
prompts by construction; assert that in code before submitting (a drift there is a halt,
not a note). Every prompt passes the gate at its own `view:N` scope before submission.

## Measurement — the two named defects, per view, calibrated instruments only

1. **Head state per view** — the E63/E64 sheet convention (head crop at the Director's
   zoom). No instrument ranks it; the sheet carries it to his eye. The seat DESCRIBES
   panel content factually at most — and E64's lesson is in force: **a seat's sentence
   about its own output is not a measurement**; write what is uncertain as uncertain.
2. **Garment per view** — the E61 sleeve/hue instrument with both calibration
   populations, three states (UNAVAILABLE / AVAILABLE+present / AVAILABLE+occluded),
   spatial-only preflight, no VLM. This gives the **plum-going-brown side-view defect**
   (E58: v1/v2/v6) its first per-view measurement under the repaired composer — it was
   never diagnosed, only named. N2 hue per view against `canon/A1-palette.json`
   (chroma floor, circular hue). Report numbers; rank nothing.

## Gates

- Gate P: every composed prompt passes at its view scope; v3/v5 byte-match E64's.
- Gate E: frame per view.
- Spec-violation: a material not in ratified canon → one re-roll, new seed, both kept.

## The sheet

8 rows: control | E58 baseline | E65 scoped, head crop + sleeve crop per row, prompts
printed per view (they now differ by view — show the deltas). Full-size PNGs on disk.

## Out of scope, named

cn (off the table unused — E64); painting/projection (the next stage, not this arc);
profile-list edits (the Director's kept property: N9 on profiles is a recorded mismatch,
not rewritten for unspent views — the profiles generate AS RATIFIED); W3; any canon edit.

## Standards compliance

1. **PIN_PER_STEP — 3.** One seed, pinned params, byte-match assertion against E64's
   probed prompts, controls carried from E58.
2. **ANDON_AUTHORITY — 2.** Gates P/E halt; prompt drift is a halt.
3. **NAMED_COMPENSATORS — 2.** 8+2 ceiling; no undo on spend; everything else by pathspec.
4. **DECOMPOSE_BY_SECRETS — 2.** Compose / generate / measure / sheet as separable stages.
5. **UNCERTAINTY_GATED_HUMANS — 2.** The sheet is the Director's; the arc ranks nothing.
6. **EXTERNAL_VERIFIER — 3.** Deterministic gate + the calibrated E61 instrument; the
   seat's prose explicitly demoted below both.
